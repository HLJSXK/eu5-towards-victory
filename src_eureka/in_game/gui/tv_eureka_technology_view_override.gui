# Intentionally empty.
#
# The complete vanilla technology_lateralview.gui is copied and patched by
# scripts_eureka/patch_gui_progress.py. An empty window declaration here would
# override the vanilla window and remove its contents, so this file must not
# declare a second technology_lateral_view window.
