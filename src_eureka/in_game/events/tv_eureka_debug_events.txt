# Towards Victory — Eureka Prototype: Debug Verification Events
# Simple confirmation popups to verify each on_action hook actually fires.
# TEMPORARY — remove once hooks are confirmed working in-game.

namespace = tv_eureka_debug

# ── tv_eureka_debug.1 — Peace treaty location transfer hook fired ──────────────
tv_eureka_debug.1 = {
	type = country_event
	title = tv_eureka_debug.1.t
	desc = tv_eureka_debug.1.d
	outcome = neutral

	option = {
		name = tv_eureka_debug.1.a	# "Understood"
	}
}

# ── tv_eureka_debug.2 — Occupation hook fired ──────────────────────────────────
tv_eureka_debug.2 = {
	type = country_event
	title = tv_eureka_debug.2.t
	desc = tv_eureka_debug.2.d
	outcome = neutral

	option = {
		name = tv_eureka_debug.2.a	# "Understood"
	}
}

# ── tv_eureka_debug.3 — Annexation hook fired ──────────────────────────────────
tv_eureka_debug.3 = {
	type = country_event
	title = tv_eureka_debug.3.t
	desc = tv_eureka_debug.3.d
	outcome = neutral

	option = {
		name = tv_eureka_debug.3.a	# "Understood"
	}
}

# ── tv_eureka_debug.4 — Boost actually activated (condition check passed) ─────
tv_eureka_debug.4 = {
	type = country_event
	title = tv_eureka_debug.4.t
	desc = tv_eureka_debug.4.d
	outcome = positive

	option = {
		name = tv_eureka_debug.4.a	# "Understood"
	}
}
