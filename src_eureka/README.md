# Eureka

Empty framework for Eureka standalone submod.

## Structure

- `in_game/` - In-game content
- `main_menu/` - Main menu GUI files
- `localization/` - Localization files
- `descriptor.mod` - Mod metadata

## Build

Deployed via `build.bat` step 8 to `%GAME_MODS%\eureka\`.
