estate_loan_penalty = {
	global_max_control = -0.05
	tax_income_efficiency = -0.05
}

# Applied if the Crown refuses to repay an Estate Loan.
# Blocks new Estate Loans and makes regular borrowing more expensive.
estate_credit_ruined = {
	# Vanilla uses this modifier when calculating loan interest.
	# We approximate "x1.5" by adding +3% yearly interest (assuming 6% base).
	bank_interest = 0.03
}
