press_persian_claims = {
	type = situation
	show_message = no
	potential = {
		scope:actor = {
			any_primary_or_accepted_culture ?= { has_culture_group = culture_group:iranian_group }
		}
	}

	allow = {
		scope:actor = {
			at_war = no
		}
	}

	automation_tick = never
	automation_tick_frequency = 12
	ai_tick = monthly
	ai_tick_frequency = 3
	price = price:rto_press_claims_price

	cooldown = {
		type = press_claims_cooldown_key
		years = 1
	}

	select_trigger = {
		looking_for_a = situation
		interaction_source_list = {
			situation:rise_of_persia = {
				add_to_list = source
			}
		}
		target_flag = recipient
		name = "choose_situation"
		column = {
			data = name
		}
		visible = {
			situation:rise_of_persia = this
			situation_is_active = yes
		}
	}

	select_trigger = {
		looking_for_a = province
		target_flag = target
		name = "choose_province"
		none_available_msg_key = "no_provinces_available"
		column = { data = owner_flag }
		column = { data = name }
		column = { data = population }
		visible = {
			scope:actor = { 
				can_declare_war_on = root.owner  
			}
            OR = {
                region = region:persia_region
                area = area:jazira_area
                area = area:armenian_highlands_area
                area = area:eastern_khorasan_area
                area = area:western_khorasan_area
            	area = area:shirvan_area
                area = area:iraq_arabi_area
            }
			any_location_in_province = {
				is_neighbor_of = scope:actor
			}
			NOT = {
				owner = {
					OR = {
						this = scope:actor
						overlord ?= scope:actor
						has_mutual_scripted_relation = {
							type = relation_type:alliance
							target = scope:actor
						}
					}
				}
			}
		}
	}
	effect = { 
		if = {
			limit = {
				exists = scope:target
			}
			custom_tooltip = {
				text = rper_press_claims_explanation_tt
				scope:actor = {
					trigger_event_silently = rise_of_persia.100
				}
			}
		}
		else_if = {
			limit = {
				NOT = {
					exists = scope:target
				}
			}
			custom_tooltip = rper_press_claims_explanation_tt
		}
	}

	ai_will_do = {
		if = {
			limit = { 
				exists = scope:target
			}

			if = {
				limit = { 
					scope:target.owner = {
						is_subject = yes
						top_overlord ?= scope:actor
					}
				}

				add = {
					value = -1000
					desc = "our subject"
				}
			}

			if = {
				limit = { 
					scope:actor = { is_during_bankruptcy = yes }
				}
				subtract = {
					desc = "[bankruptcy|e]"
					value = 1000
				}
			}

			if = {
				limit = { 
					scope:actor = { num_loans > 0 }
				}
				subtract = {
					desc = "loans"
					value = 1000
				}
			}

			if = {
				limit = { 
					scope:actor = { stability < 0 }
				}
				subtract = {
					desc = "low stability"
					value = 1000
				}
			}

			if = {
				limit = {
					scope:actor = {
						has_truce_with = scope:target.owner
					}
				}
				add = {
					value = -1000
					desc = "truce"
				}
			}

			if = {
				limit = {
					scope:actor = {
						wants_to_subjugate = scope:target.owner
					}
				}
				add = {
					value = -1000
					desc = "wants to subjugate"
				}
			}

			if = {
				limit = { 
					scope:actor = { has_casus_belli_on = scope:target.owner }
				}
				add = {
					value = -1000
					desc = "We already have a cb on them"
				}
			}

			add = {
				desc = "RELATIVE_STRENGTH"
				value = "scope:actor.relative_defensive_alliance_strength(scope:target.owner)"
				multiply = 100
				subtract = 60
			}

			add = {
				desc = "we want their land"
				value = "scope:actor.conquer_desire(scope:target.owner)"
				divide = 5
			}

			if = {
				limit = { 
					scope:actor = { wants_casus_belli_with = scope:target.owner }
				}
				add = {
					desc = "wants casus belli on target owner"
					value = 5
				}
			}

			if = {
				limit = {
					scope:target.owner = {
						num_locations < 5
					}
				}
				divide = {
					value = 3
					desc = "very small enemy"
				}
			}
		}
		else = {
			add = {
				value = 100
				desc = BASE_VALUE
			}
		}
	}
}

# invade_persia_outsider = {

# }

claim_persian_mantle = {
	type = situation	
	potential = {
		scope:actor = {
			is_target_in_global_variable_list = {
				name = eligible_amirs_list
				target = this
			}
			NOT = { has_variable = claimed_persia }
		}
		OR = {
			situation:rise_of_persia.var:strongest_amir_variable = scope:actor
			situation:rise_of_persia.var:second_strongest_strongest_amir_variable = scope:actor
			situation:rise_of_persia.var:third_strongest_strongest_amir_variable = scope:actor
		}
	}

	automation_tick = never
	automation_tick_frequency = 12
	price = price:price_claim_persian_mantle
	allow = {
		scope:actor = {
			at_war = no
			any_owned_location = {
				OR = {
					this.region = region:persia_region
					this.area = area:jazira_area
					this.area = area:armenian_highlands_area
					this.area = area:eastern_khorasan_area
					this.area = area:western_khorasan_area
					this.area = area:shirvan_area
					this.area = area:iraq_arabi_area
				}
				count > 150
			}
		}
	}

	select_trigger = {
		looking_for_a = situation
		target_flag = recipient
		name = "choose_situation"
		column = {
			data = name
		}
		visible = {
			situation:rise_of_persia = this
			situation_is_active = yes
		}
	}

	effect = {
		scope:actor = {
			set_variable = claimed_persia
			add_prestige = 20
		}
	}

	ai_will_do = {
		add = 250	
	}
}

annex_persian_amir = {
	type = situation	
	potential = {
		scope:actor = {
			is_target_in_global_variable_list = {
				name = eligible_amirs_list
				target = this
			}
		}
		situation:rise_of_persia.var:strongest_amir_variable = scope:actor
	}

	automation_tick = never
	automation_tick_frequency = 12
	price = price:rot_reform_into_monarchy_price
	allow = {
		scope:actor = {
			at_war = no
			NOT = { has_country_modifier = rise_of_persia_integration_speed }
			any_subject = {
				OR = {
					capital.region = region:persia_region
					capital.area = area:jazira_area
					capital.area = area:armenian_highlands_area
					capital.area = area:eastern_khorasan_area
					capital.area = area:western_khorasan_area
					capital.area = area:shirvan_area
					capital.area = area:iraq_arabi_area
				}
			}
		}
	}

	select_trigger = {
		looking_for_a = situation
		target_flag = recipient
		name = "choose_situation"
		column = {
			data = name
		}
		visible = {
			situation:rise_of_persia = this
			situation_is_active = yes
		}
	}

	effect = {
		scope:actor = {
			add_country_modifier = {
				modifier = rise_of_persia_integration_speed
				years = 5
				mode = replace
			}
		}
	}

	ai_will_do = {
		add = 250	
	}
}

restore_persian_cities = {
	type = situation
	show_message = no
	potential = {
		scope:actor = {
			is_target_in_global_variable_list = {
				name = eligible_amirs_list
				target = this
			}
		}
	}

	allow = {
		scope:actor = {
			at_war = no
			is_during_bankruptcy = no
		}
	}

	automation_tick = never
	automation_tick_frequency = 12

	ai_tick = monthly
	ai_tick_frequency = 12

	price = price:price_restoring_persian_city

	
	cooldown = { 
		type = restore_persian_city_key 
		months = 12
	}
	
	select_trigger = {
		looking_for_a = situation
		interaction_source_list = {
			situation:rise_of_persia = {
				add_to_list = source
			}
		}
		target_flag = recipient
		name = "choose_situation"
		column = {
			data = name
		}
		visible = {
			situation:rise_of_persia = this
			situation_is_active = yes
		}
	}

	select_trigger = {
		looking_for_a = location
		interaction_source_list = {
			scope:actor = {
				every_owned_location = {
					limit = { 
						OR = {
							this = location:shiraz
							this = location:isfahan
							this = location:qom
							this = location:hamadan
							this = location:rey
							this = location:nishapur
							this = location:herat
							this = location:qazvin
							this = location:merv
							this = location:balkh
							this = location:tabriz
							this = location:kerman
							this = location:yazd
							this = location:mashhad
							this = location:kashan
							this = location:ardabil
							this = location:soltaniyeh
						}
						NOT = { location_rank = location_rank:city }
						OR = {
							can_become_rank = location_rank:city
							can_become_rank = location_rank:town
						}
					}
					add_to_list = source
				}
			}
		}
		target_flag = target
		name = "choose_location"
		none_available_msg_key = "no_locations_available"
		column = { data = name }
		column = { data = population }
		visible = {
		}
	}

	effect = {
		if = {
			limit = {
				exists = scope:target
			}
			scope:target = {
				if = {
					limit = { can_become_rank = location_rank:town }
					construct_location_rank = location_rank:town
				}
				else_if = {
					limit = { can_become_rank = location_rank:city }
					construct_location_rank = location_rank:city
				}
			}
		}	
	}

	ai_will_do = {
		add = 500
	}
}