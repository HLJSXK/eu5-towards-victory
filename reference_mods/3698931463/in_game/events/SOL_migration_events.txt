namespace = sol_migration

# Fires once for human players who load a save created before SOL mod v2.
# Triggered exclusively by SOL_migration_check in SOL_migration_on_actions.txt.
# No trigger block needed — all conditions are gated in the on_action.
sol_migration.1 = {
	type = country_event
	title = sol_migration.1.title
	desc = sol_migration.1.desc
	outcome = neutral

	option = {
		name = sol_migration.1.ok
	}
}
