global_living_standard_recalculate = {
	loc = global_living_standard_recalculate

	potential = {
		situation:global_living_standard = { situation_is_active = yes }
	}

	allow = {
		always = yes
	}

	requires_vote = {
		always = no
	}

	days = 1

	should_finalize_vote = {
		always = yes
	}

	select_trigger = {
		looking_for_a = situation
		interaction_source_list = {
			situation:global_living_standard = { add_to_list = source }
		}
		target_flag = recipient
		name = choose_situation
		column = { data = name }
		visible = { this = situation:global_living_standard }
		enabled = { always = yes }
	}

	# Last select_trigger is the vote target in resolution framework.
	select_trigger = {
		looking_for_a = situation
		source = recipient
		target_flag = vote
		name = select_vote
		column = { data = name }
		visible = { this = scope:recipient }
		enabled = { always = yes }
	}

	show_message = no

	effect = {
		scope:recipient = {
			every_country = {
				if = {
					limit = { total_population > 0 }
					gls_full_refresh_country = yes
				}
			}
		}
	}

	ai_will_select = {
		add = 0
	}
}
