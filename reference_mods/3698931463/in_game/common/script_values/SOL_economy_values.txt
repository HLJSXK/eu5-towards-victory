### ============================================================
### SECTION: Stability Cost Discount (Cultural Tradition)
### ============================================================
# Stability investment cost reduction from cultural tradition.
# Formula: 0.1 - 0.1 / (cultural_tradition / 500 + 1)

sol_cultural_tradition_stability_discount = {
    value = 0
    if = {
        limit = { exists = root.culture }
        value = sol_as_cts_coeff
        divide = {
            value = root.culture.cultural_tradition
            divide = 500
            add = 1
        }
        multiply = -1
        add = sol_as_cts_coeff
    }
}

### ============================================================
### SECTION: Standard of Living Display Values
### ============================================================
# Country and location cached SOL aggregates for GUI/map display.

gls_country_sol_all_display = {
	value = 0
	if = { limit = { has_variable = gls_country_sol_all } value = var:gls_country_sol_all }
}

gls_country_sol_all_deviation_display = {
	value = 0
	if = { limit = { has_variable = gls_country_sol_all } value = var:gls_country_sol_all subtract = 1 }
}

gls_country_location_avg_scale_display = {
	value = 0
	if = { limit = { has_variable = gls_country_location_avg_scale } value = var:gls_country_location_avg_scale }
}

gls_country_wealth_anchor_cached_display = {
	value = 0
	if = { limit = { has_variable = gls_country_wealth_anchor_cached } value = var:gls_country_wealth_anchor_cached }
}

gls_total_savings_display = {
	value = 0
	if = { limit = { has_variable = gls_total_savings } value = var:gls_total_savings }
}

gls_total_savings_target_display = {
	value = 0
	if = { limit = { has_variable = gls_total_savings_target } value = var:gls_total_savings_target }
}

gls_savings_pressure_adjustment_display = {
	value = 0
	if = { limit = { has_variable = gls_savings_pressure_adjustment } value = var:gls_savings_pressure_adjustment }
}

gls_total_income_display = {
	value = 0
	if = { limit = { has_variable = gls_total_income } value = var:gls_total_income }
}

gls_country_base_spending_display = {
	value = 0
	if = { limit = { has_variable = gls_country_base_spending } value = var:gls_country_base_spending }
}

gls_country_liquid_funds_display = {
	value = 0
	if = { limit = { has_variable = gls_country_liquid_funds } value = var:gls_country_liquid_funds }
}

gls_country_poverty_compensation_factor_display = {
	value = 1
	if = { limit = { has_variable = sol_country_poverty_compensation_factor } value = var:sol_country_poverty_compensation_factor }
}

gls_pop_nobles_display = {
	value = 0
	if = { limit = { has_variable = gls_pop_nobles } value = var:gls_pop_nobles multiply = 1000 }
}

gls_pop_clergy_display = {
	value = 0
	if = { limit = { has_variable = gls_pop_clergy } value = var:gls_pop_clergy multiply = 1000 }
}

gls_pop_burghers_display = {
	value = 0
	if = { limit = { has_variable = gls_pop_burghers } value = var:gls_pop_burghers multiply = 1000 }
}

gls_pop_commoners_display = {
	value = 0
	if = { limit = { has_variable = gls_pop_commoners } value = var:gls_pop_commoners multiply = 1000 }
}

gls_pop_tribesmen_display = {
	value = 0
	if = { limit = { has_variable = gls_pop_tribesmen } value = var:gls_pop_tribesmen multiply = 1000 }
}

gls_income_nobles_display = {
	value = 0
	if = { limit = { has_variable = gls_income_nobles } value = var:gls_income_nobles }
}

gls_income_clergy_display = {
	value = 0
	if = { limit = { has_variable = gls_income_clergy } value = var:gls_income_clergy }
}

gls_income_burghers_display = {
	value = 0
	if = { limit = { has_variable = gls_income_burghers } value = var:gls_income_burghers }
}

gls_income_commoners_display = {
	value = 0
	if = { limit = { has_variable = gls_income_commoners } value = var:gls_income_commoners }
}

gls_income_tribesmen_display = {
	value = 0
	if = { limit = { has_variable = gls_income_tribesmen } value = var:gls_income_tribesmen }
}

gls_building_maintenance_nobles_display = {
	value = 0
	if = { limit = { has_variable = gls_building_maintenance_nobles } value = var:gls_building_maintenance_nobles }
	multiply = -1
}

gls_building_maintenance_clergy_display = {
	value = 0
	if = { limit = { has_variable = gls_building_maintenance_clergy } value = var:gls_building_maintenance_clergy }
	multiply = -1
}

gls_building_maintenance_burghers_display = {
	value = 0
	if = { limit = { has_variable = gls_building_maintenance_burghers } value = var:gls_building_maintenance_burghers }
	multiply = -1
}

gls_building_maintenance_commoners_display = {
	value = 0
	if = { limit = { has_variable = gls_building_maintenance_commoners } value = var:gls_building_maintenance_commoners }
	multiply = -1
}

gls_building_maintenance_tribesmen_display = {
	value = 0
	if = { limit = { has_variable = gls_building_maintenance_tribesmen } value = var:gls_building_maintenance_tribesmen }
	multiply = -1
}

gls_noble_gdp_per_capita_display = {
	value = 0
	if = {
		limit = {
			has_variable = gls_income_nobles
			has_variable = gls_pop_nobles
			var:gls_pop_nobles > 0
		}
		value = var:gls_income_nobles
		divide = var:gls_pop_nobles
	}
}

gls_clergy_gdp_per_capita_display = {
	value = 0
	if = {
		limit = {
			has_variable = gls_income_clergy
			has_variable = gls_pop_clergy
			var:gls_pop_clergy > 0
		}
		value = var:gls_income_clergy
		divide = var:gls_pop_clergy
	}
}

gls_burghers_gdp_per_capita_display = {
	value = 0
	if = {
		limit = {
			has_variable = gls_income_burghers
			has_variable = gls_pop_burghers
			var:gls_pop_burghers > 0
		}
		value = var:gls_income_burghers
		divide = var:gls_pop_burghers
	}
}

gls_commoner_gdp_per_capita_display = {
	value = 0
	if = {
		limit = {
			has_variable = gls_income_commoners
			has_variable = gls_pop_commoners
			var:gls_pop_commoners > 0
		}
		value = var:gls_income_commoners
		divide = var:gls_pop_commoners
	}
}

gls_tribesmen_gdp_per_capita_display = {
	value = 0
	if = {
		limit = {
			has_variable = gls_income_tribesmen
			has_variable = gls_pop_tribesmen
			var:gls_pop_tribesmen > 0
		}
		value = var:gls_income_tribesmen
		divide = var:gls_pop_tribesmen
	}
}

gls_nobles_savings_display = {
	value = 0
	if = { limit = { has_variable = gls_nobles_gold } value = var:gls_nobles_gold }
}

gls_clergy_savings_display = {
	value = 0
	if = { limit = { has_variable = gls_clergy_gold } value = var:gls_clergy_gold }
}

gls_burghers_savings_display = {
	value = 0
	if = { limit = { has_variable = gls_burghers_gold } value = var:gls_burghers_gold }
}

gls_commoner_savings_display = {
	value = 0
	if = { limit = { has_variable = gls_commoner_gold } value = var:gls_commoner_gold }
}

gls_tribesmen_savings_display = {
	value = 0
	if = { limit = { has_variable = gls_tribesmen_gold } value = var:gls_tribesmen_gold }
}

gls_nobles_savings_target_display = {
	value = 0
	if = { limit = { has_variable = gls_nobles_target } value = var:gls_nobles_target }
}

gls_clergy_savings_target_display = {
	value = 0
	if = { limit = { has_variable = gls_clergy_target } value = var:gls_clergy_target }
}

gls_burghers_savings_target_display = {
	value = 0
	if = { limit = { has_variable = gls_burghers_target } value = var:gls_burghers_target }
}

gls_commoner_savings_target_display = {
	value = 0
	if = { limit = { has_variable = gls_commoner_target } value = var:gls_commoner_target }
}

gls_tribesmen_savings_target_display = {
	value = 0
	if = { limit = { has_variable = gls_tribesmen_target } value = var:gls_tribesmen_target }
}

gls_location_count_display = {
	value = 0
	if = { limit = { has_variable = gls_location_count } value = var:gls_location_count }
}

gls_location_actual_per_capita_spending_display = {
	value = 0
	if = { limit = { has_variable = gls_location_actual_per_capita_spending } value = var:gls_location_actual_per_capita_spending }
}

gls_location_color_factor = {
	value = 0
	if = { limit = { has_variable = gls_location_actual_per_capita_spending } value = var:gls_location_actual_per_capita_spending divide = 0.5 min = 0 max = 1 }
}
