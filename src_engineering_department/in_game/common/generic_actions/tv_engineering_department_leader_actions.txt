# @Generated by scripts/in_game/common/generic_actions/gen_tv_io_leader_actions.py
#   Data:    data/io_leaders.yaml + data/io_establishment.yaml
#   Regen:   conda run --no-capture-output -n eu5 python scripts/in_game/common/generic_actions/gen_tv_io_leader_actions.py
# Do not edit directly — modify the data file and re-run the generator.

# ═════════════════════════════════════════════════════════════════════════════
# Engineering Department (non-unique IO)
# ═════════════════════════════════════════════════════════════════════════════

tv_appoint_engineering_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ENGINEERING_DEPARTMENT_MEMBER_TT
			scope:actor = { has_variable = tv_engineering_department_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_engineering_leader"
		none_available_msg_key = "tv_no_engineering_leader_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			NOT = { is_artist = yes }
			custom_tooltip = {
				text = TV_CHARACTER_NOT_ALREADY_TV_IO_CHIEF_TT
				NOT = { has_variable = tv_io_chief_role }
			}
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				if = {
					limit = { has_variable = tv_great_engineer_char }
					var:tv_great_engineer_char ?= {
						remove_variable = tv_io_chief_role
						remove_character_modifier = tv_great_engineer_role_modifier
					}
				}
				set_variable = { name = tv_great_engineer_char value = scope:target }
				tv_engineering_on_great_engineer_appointed_effect = yes
			}
			scope:target = {
				set_variable = { name = tv_io_chief_role value = 1 }
				add_character_modifier = { modifier = tv_great_engineer_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}

tv_remove_engineering_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ENGINEERING_DEPARTMENT_MEMBER_TT
			scope:actor = { has_variable = tv_engineering_department_member }
		}
	}

	allow = { always = yes }

	effect = {
		scope:actor = {
			if = {
				limit = { has_variable = tv_great_engineer_char }
				var:tv_great_engineer_char ?= {
					remove_variable = tv_io_chief_role
					remove_character_modifier = tv_great_engineer_role_modifier
				}
			}
			tv_engineering_on_great_engineer_removed_effect = yes
			remove_variable = tv_great_engineer_char
		}
	}

	ai_will_do = { add = -100 }
}

tv_change_engineering_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ENGINEERING_DEPARTMENT_MEMBER_TT
			scope:actor = { has_variable = tv_engineering_department_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_engineering_leader"
		none_available_msg_key = "tv_no_engineering_leader_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			NOT = { is_artist = yes }
			custom_tooltip = {
				text = TV_CHARACTER_NOT_ALREADY_TV_IO_CHIEF_TT
				NOT = { has_variable = tv_io_chief_role }
			}
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		scope:actor = {
			if = {
				limit = { has_variable = tv_great_engineer_char }
				var:tv_great_engineer_char ?= {
					remove_variable = tv_io_chief_role
					remove_character_modifier = tv_great_engineer_role_modifier
				}
			}
		}
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_great_engineer_char value = scope:target }
				tv_engineering_on_great_engineer_appointed_effect = yes
			}
			scope:target = {
				set_variable = { name = tv_io_chief_role value = 1 }
				add_character_modifier = { modifier = tv_great_engineer_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}
