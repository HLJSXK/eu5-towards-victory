# EU5 Knowledge Brief

> **Auto-generated** — do not edit this file directly.  
> Edit `docs/knowledge/anti_patterns.yaml`, `valid_enums.yaml`, or `PROJECT_OVERVIEW.md`, then run `python scripts/gen_brief.py`.  
> Last updated: 2026-06-11

## Mod Identity

- **Mod Name:** Towards Victory (胜利条件)
- **Mod ID:** `eu5mp.towards_victory`
- **Version:** 0.1.0
- **Target:** EU5 `1.*.*`
- **Status:** In Development — All 6 Victory paths fully implemented: Conquest, Prosperity, Trade, Diplomatic, Cultural, and Scientific
- **Language:** English + Simplified Chinese

## Development Policy

Towards Victory is an unreleased v0.1.0 project. No saved-game compatibility, published
mod-version compatibility, or public API compatibility is required. Active code and data
should represent one current architecture. When a refactor improves clarity, performance,
generator ownership, or data flow, update all producers and consumers to the new shape and
delete stale branches rather than keeping migrations, aliases, duplicate paths, wrapper
entry points, or defensive fallbacks for old internal states. Existing helpers should be
reused only when they fit the current scope/lifecycle/data model; otherwise split, replace,
or remove them.

Existing mentions of compatibility launchers, legacy schema blocks, migrated old variables,
startup/migration repair, or fields retained for compatibility describe cleanup debt, not
contracts. When touching those areas, convert callers/data to the current shape and remove
the old path instead of preserving the compatibility layer.

## Summary

Current implementation update for the 2026-05-28 Trade League chart pass: the Trade League overview now includes a reusable rolling monthly trade-income chart on the overview tab, driven by the leader country's `monthly_trade_income` history and rendered as a horizontal row of vertical progress bars. Engineering Department wonder proposals now present three simultaneous Great Engineer options, support rewards were reduced, construction event localization is hand-maintained, the Engineering Department Organization/Logistics card now exposes native Production filters for Wonder Worksites and Wonder Material Workshops, and the 52 generic wonders now use one shared generated ritual system with timed burden/blessing rites, annex-building rites, and fixed-cost decoration rites. Unique-wonder rituals now use the same single-active state machine but can separate start mode from completion mode through listener-driven completion: monthly, ruler-death, pre-winning-war, and ending-war hooks can all feed the same normalized progress/completion pipeline. Wonder runtime attributes now have a generated global variable-map index keyed directly by canonical numeric wonder ids, ritual composite ids `wonder_id * 100 + style`, tooltip-safe per-part module and per-style final-building maps keyed directly by wonder id, country-scoped priority proposal candidate maps keyed by allocated candidate ids, and suitability row metadata including each mechanic id's row count; startup and save-load hooks directly rebuild the current maps and refresh country caches for low-risk attributes such as uniqueness, base/mechanic id, size, style count, display ids, ritual mode, ritual cost type, ritual listener/completion flags, helper/module/final `building_type` routes, and suitability row count. Construction module creation, helper-to-site synchronization, module materialization from unit variables, completed-module combination into helper buildings, and finalization all use the generated building-type maps; active-site construction/finalization paths refresh the saved resume/upgrade candidate rows for the current country/site/wonder, while Great Engineer proposal refresh validates only those saved rows instead of scanning all owned locations for expandable wonders. Engineering Department surveys now gradually reveal per-prototype suitability knowledge rows through country-scoped `tv_wonder_suitability_revealed` / `tv_wonder_suitability_actual` maps and location-scoped completed-survey maps while always showing the current wonder's location trigger conditions in the survey card; current display refresh, reveal progress, completed-survey copy, clear, and store now use map keys from the locked wonder/mechanic id instead of per-wonder state dispatch. IO chief appointments are mutually exclusive through `tv_io_chief_role`, and six per-IO CMF alerts now spell out which organization lacks its chief and open the matching organization panel on left click. Governor's House now gives Grand Generals loyalty, lets loyalty lower the power-balance equilibrium by the same amount, gives regional governors autonomous yearly tasks that pick the highest-weight regional priority among prosperity recovery, integration, culture conversion, religion conversion, recruitment, and order, adds laws for Grand General power plus Local Governor duty tendency, offers an overview auto-assign action plus adjacent automation gear toggle for filling empty governor seats with the highest-total-ability eligible characters, and grants a War Plan casus belli against the target when an offensive War Tent plan reaches 100 progress. The 2026-05-23 Wonder Construction rework uses IO-driven active-unit progress, size-based progress requirements, materialized module buildings for interrupted construction, priority resume/upgrade proposals, ceremony locking, and national/local completion celebrations. The 2026-05-22 Trade League pass added a generated action layer: the leader can create or manage persistent virtual demand, virtual production, and embargo actions for monopolized goods; monthly maintenance applies those effects only while the monopoly is active, so losing a monopoly suspends actions without deleting them. Engineering Department supports 52 universal wonders from `data/wonders.yaml`; shared proposal/site triggers, final buildings, final-building local effects, permanent base country modifiers excluding Great Power Score, generic ritual data, generated ceremony text, and split confirmation actions for free / `scaled_gold = 5` / 50-Prestige rituals are generated for all 52 from `data/wonder_mechanics.yaml`, covering prehistoric ritual, religious, infrastructure, frontier, military, civic, diplomatic, commercial, industrial, cultural, monument, and urban expansion themes, including the City Expansion Project, City Great Tower, Great Sculpture Group, Coastal Defense Cannon, Naval School, Colonial Trade Company, Great Assembly Hall, Mobile Fortress, Ancient City Ruins, and Summer Palace. Ending wonder debate sets `tv_wonder_stage = 2`, fires `tv_engineering_department.203`, and switches guidance to survey; finalization grants the current ruler +5 ADM/DIP/MIL and the current Great Engineer +10 ADM/DIP/MIL. After the builder confirms inauguration, all other countries with capitals receive a no-effect per-wonder world-news event describing the completed wonder. Organization/Logistics now has separate Organization and Logistics sections: each effective worksite level provides 1,000 base monthly construction speed before the Logistics Competence multiplier and consumes Tools plus Wonder Materials upkeep, while Wonder Material Workshops process lumber, masonry, glass, and marble into Wonder Materials and may also be built by standard AI/automation for importable supply. Survey competence now directly controls maximum wonder level, maximum worksite level, and monthly construction progress multiplier. The City Expansion Project can be surveyed only in cities or megalopolises, favors already built-up sites plus megalopolises, and gives its finalized local wonder +20 local maximum building levels per wonder level. Wonder construction has a generated 212-event monthly random deck with a 10% total monthly chance while `tv_wonder_stage = 3` and a construction site exists. Diplomatic Victory follows `Original_Design.md`: thresholds are 1 / 50 / 500 / 2,500 / 15,000; DVP comes from Diplomatic Support, alliance creation, and monthly alliance-tier maintenance, not from royal marriages or war wins; alliance cohesion uses base +0.1, leader diplomatic reputation x0.01, and +0.05 internal peace. Research subprocess A uses one monthly `random_list`: 90 no event, 5 existing gold-for-interest, and five 1-weight interest events trading 10 Prestige, 10% Clergy satisfaction, 10% Burghers satisfaction, 7 Stability, or 5 Legitimacy for progress; all six interest event types now select target-specific flavor text. If a selected research target is completed through normal technology research before concentrated research finishes it, a monthly pulse fires `tv_research.31`, notifies the player, and automatically resets the research cycle. The wonder-localization editor now runs as a root-level FastAPI web tool in `wonder_localization_editor_web/`, with `scripts/wonder_localization_editor.py` kept as a compatibility launcher. Wonder-owned localization now lives in `data/wonder_localization.yaml`, and the wonder-image helper reads prompts from `data/wonder_image_prompts.yaml` plus `data/unique_wonders.yaml` while `generate_wonder_image_config.json` stays runtime-only.

Governor's House subject-chain lifecycle: only independent countries may own a Governor's House. When an owner becomes a subject, its owned house is dissolved and owner-only Grand General, Governor, War Tent, and automation state is cleared; player countries see a confirmation popup while AI countries resolve silently. A top overlord's owned Governor's House automatically includes every subject below it as IO members without setting `tv_govhouse_member` on those subject members. When a former subject becomes independent, Conquest score is recalculated; countries that newly meet Conquest Milestone 1 use the normal milestone reward path, while countries that already had that milestone rebuild only the Governor's House.

Finalization routing update: Engineering Department wonder finalization now dispatches through generated one-event-per-wonder inauguration events and one-event-per-wonder world-news events in `tv_wonder_finalization_events.txt`, with the long locked-wonder `trigger_event_non_silently` branch maintained in the separate `tv_wonder_finalization_effects.txt` generated file. Wonder-type dispatch happens only before the inauguration or broadcast event is fired; each per-wonder inauguration event may branch internally on ceremony style for its description, but no event performs wonder-type dispatch. The visible inauguration and world-news events use static `image = "gfx/interface/icons/towards_victory/wonders/{wonder_image_name}.dds"` fields, while the shared hidden executor event carries no image. The inauguration option now directly runs the current wonder/style final-building construction effect first, then the tooltip-visible per-wonder reward effect, and only then a light hidden scheduler, `tv_wonder_trigger_finalization_hidden_event_effect`. The scheduler silently fires the shared hidden executor event `tv_engineering_department.6202`; that hidden event's `immediate` calls `tv_wonder_execute_finalization_hidden_event_effect` for cleanup, intermediate destruction, world-news broadcast, finalization-time expansion-candidate registration from current project variables, project-state clear, and the next concept start. This clears `tv_wonder_locked` before the new concept substate appears and avoids same-tick building-level reads after `construct_building instant = yes`. It also keeps the player-facing construction and reward lines visible in the option tooltip while keeping non-tooltip bookkeeping out of high-frequency event-option rendering and keeping all event IDs below EU5's `< 10000` numeric limit.

Towards Victory adds 6 generalized victory paths to EU5: Conquest, Prosperity, Trade, Diplomatic, Cultural, and Scientific. Each path has 5 milestone nodes that grant permanent buffs when reached. Progress is displayed via situation panels; milestones trigger popup events that notify the player and deliver the reward.

Engineering Department concept-stage update: Great Engineer proposal refresh now has an explicit `tv_wonder_stage = 0` substate marked by `tv_wonder_concept_in_progress`. Appointing a Great Engineer, refuting/bribing a proposal, or finishing an inaugurated wonder starts `tv_wonder_concept_progress` at 0 on the Engineering Department IO; monthly_change adds Great Engineer ADM x0.2 until it reaches 100, then `tv_engineering_department.30` lets the player acknowledge the completed concept. Only that event option runs proposal reroll work: appointment concepts rebuild/restore the feasible deck and roll three proposal slots, rejection/bribe concepts run the old rejection reroll, and post-finalization concepts roll the next proposals after finalization has already projected the current project variables into the priority candidate cache and cleared the finished project state. Finalization, abandonment, and support-collapse aborts all use the same variable-state priority candidate registration path before project variables are cleared: mode 1 is kept when intermediate/module state remains, mode 2 is kept when an existing or newly finalized final building still has scale-tier headroom, and stale rows are unregistered when no resume/upgrade state remains. Construction-stage module/materialization sync does not refresh the proposal priority cache. The Engineering Department GUI shows the concept text, `@adm!` icon, progress bar, and `xx/100` value while hiding proposal buttons, accept/refute/bribe actions, and wonder previews during this substate.

Trade League law update: `data/trade_league_laws.yaml` now drives generated `tv_trade_league_laws.txt`, adding five three-level vote-based laws: Customs Union, Maritime Convoys, Merchant Courts, Staple Monopolies, and Investment Code. Each level-1 policy trades roughly one benefit package for one cost, level 2 gives about 1.5x the same benefits for the same cost, and level 3 gives about 3x the same benefits for two distinct costs. AI policy vote bias uses base weights 200/100/0 for levels 1/2/3; levels 2/3 also scale from opinion of the leader (-50..+50 / -100..+100), leader diplomatic reputation (+2/+5 per point), and leader monthly trade income (+5/+10 per 100 income). Trade League law votes use the Grand Merchant law-change availability trigger and mutually block trade-chain, commercial-intelligence, and monopoly maintenance work.

The **Conquest Victory** path (征服胜利) is fully implemented: 5 triggers, 5 reward modifiers, 5 events, and localization in English and Simplified Chinese. Score = direct locations + 0.5 × subject-or-below locations; thresholds at 200 / 400 / 600 / 1200 / 2500.

The **Prosperity Victory** path (繁荣胜利) is fully implemented: 5 triggers, 5 reward modifiers, 5 events, and localization in English and Simplified Chinese. Score = (total_population + Σdev + country_tax_base) × 50 / (N + 50) × (1 + stability/100) × (gov_power/100), where N = num_owned_locations and gov_power = legitimacy/republican_tradition/devotion/tribal_cohesion/horde_unity by government type. The 50N/(N+50) effective scale term provides stronger diminishing returns on country size. Updated yearly; thresholds at 2,000 / 3,000 / 5,000 / 7,000 / 11,000 (provisional, calibrate after playtesting).

**Prosperity main mechanic:** Prosperity M1 creates the **Engineering Department IO** (`tv_engineering_department`), a non-unique founder-only IO with no parliament tab and no automatic leader election. Its appointed character leader is the **Great Engineer**, stored on the leader country as `tv_great_engineer_char` and managed by generated Appoint/Remove/Change leader actions. Wonder Construction implements **Conception, Debate, Survey, Organization, Logistics, Construction, Ceremony, and Finalization** for 52 universal wonder types, all managed through the same generated mechanics layer from `data/wonders.yaml` and `data/wonder_mechanics.yaml`: Sacred Mountain Complex through Summer Palace, spanning civil production, military defense, civic timekeeping, arts, diplomacy, law, commerce, finance, industry, maritime trade, civilizational memory, prehistoric ritual, cathedral, monastic, and urban building-cap expansion, civic tower, sculpture-group, coastal-artillery, and naval-school, colonial-company, assembly-hall, mobile-fortress, ancient-ruins, and summer-palace themes. Appointing a Great Engineer rebuilds the country's feasible proposal deck from owned terrain/city/port/infrastructure/mining conditions, then rolls or restores three simultaneous proposal slots on that engineer. The overview shows three wonder buttons, each labeled by size icon and wonder name; selecting one changes the Great Engineer's short pitch to that wonder's size-aware summary. Accepting locks the currently selected wonder and enters debate, while refuting or bribing refreshes all three proposal slots at once; rejected proposals are removed from the current deck, and if rejection empties the feasible deck the country immediately rebuilds the feasible list and rolls again. Debate uses the Engineering Department IO variable `tv_wonder_domestic_support` (0-200): estate demands for Nobles, Burghers, and Clergy can be accepted for +20 support through `tv_change_wonder_domestic_support_effect`, whose effect localization renders the IO variable change. Refuting or bribing estate demands does not exclude that agenda type. Demand types include the baseline privilege/tax/levy/policy agenda set plus resource-loss demands for 10 Prestige, 7 Stability, 5 Legitimacy, 5% loyalty from the other two estates, 2.5% inflation, one cabinet member, one artist, or one artwork; the cabinet/artist/artwork demands reroll matching slots after execution so two estates cannot keep pointing at a just-consumed scarce target. Monthly debate events can swing support by +/-20, and the debate can end at 100 support. After debate ends, support decays by 1 per month until the wonder is finalized; reaching 0 before finalization automatically declares construction complete if at least one wonder level is complete and the project can enter ceremony, otherwise it forces project collapse. Abandoning or forced collapse applies upper-estate satisfaction penalties, removes the Great Engineer, costs 20 Prestige, and resets wonder project state.

After debate, the player starts a location-select survey through `tv_wonder_start_survey`. Valid sites depend on the locked wonder type: Sacred Mountain uses hills/plateau/mountains; Triumphal Axis uses city/megalopolis; Great Port and Great Lighthouse use ports; Giant Necropolis uses hills or adjacency to city/town; Hydraulic Workshop uses land; Mining City now requires a mineral-producing raw material site (iron, copper, tin, lead, silver, gold, or marble). Fresh survey and construction candidate checks no longer exclude locations that already contain other TV wonder intermediate, module, or final buildings, but they do exclude the same wonder's existing module/helper/final buildings so resume and upgrade projects stay on the saved priority-candidate path. Priority resume/upgrade projects are valid when the saved site still matches the current wonder's base site rules and either has that wonder's module/helper building or has a final building below the location's stored scale tier. Survey progress is the Engineering Department IO variable `tv_wonder_survey_progress`; each monthly tick adds 0.1 times the Great Engineer's MIL and can fire one survey event from a single random roll. The country tracks the current survey site and three visible competence values: `tv_wonder_scale_competence`, `tv_wonder_logistics_competence`, and `tv_wonder_organization_competence`, initialized at 0 when a new survey begins. At survey start, hidden target competence values are precomputed from 25 base competence, each field's random 0-25 roll, the site preference bonus, and up to 10 from the Great Engineer's ADM; the visible competence values then rise by fixed monthly increments until completion. Monthly survey events can still raise or lower one visible competence by 5, or offer resource tradeoffs for +5. Site preference bonuses use: Sacred Mountain topography/vegetation (mountains +15, plateau +7.5, hills +0, forest +10, woods +5); Triumphal Axis development (0-100 maps linearly to 0-20) plus +5 for megalopolis; Great Port and Great Lighthouse harbor capacity (`harbor_suitability`, 0-100% maps linearly to 0-25, capped above 100%); Giant Necropolis hills +15, adjacent city +10 or adjacent town +5; Hydraulic Workshop total local building levels 0-100 maps linearly to 0-25; Mining City development 0-100 maps linearly to 0-20, plus +5 when the mineral site is a rural settlement. The survey card's Suitability Knowledge section always shows the current wonder's country-scoped build/location trigger through the normal trigger condition list; its suitability rows remain visible as placeholders until surveys reveal them, and revealed rows display actual-vs-maximum preference bonus as `?/+x` before the current site survey completes or `+y/+x` after completion. Each fresh completed survey increments the country-scoped `tv_wonder_suitability_revealed` variable map entry keyed by the wonder mechanic id up to that mechanic's row count, so unique wonders and their prototype share reveal progress. Actual suitability row values are stored in the country-scoped `tv_wonder_suitability_actual` map by `mechanic_id * 10 + row`, then mirrored into `tv_wonder_suitability_current_actual_<row>` for the selected wonder's GUI. Completed survey results are stored on the location in `tv_wonder_surveyed`, `tv_wonder_survey_scale_competence`, `tv_wonder_survey_logistics_competence`, `tv_wonder_survey_organization_competence`, `tv_wonder_survey_scale_tier`, and `tv_wonder_survey_suitability_actual` maps keyed by wonder id or suitability composite row id; selecting an already surveyed site restores the stored result instead of restarting progress and does not advance suitability knowledge. Scale competence maps to maximum wonder construction level using thresholds 20/40/60/80/100, with level 6 requiring 100% competence; Organization Competence maps linearly from 0-100% to a 0-20 Wonder Worksite level cap; Logistics Competence maps linearly from 0-100% to a 50%-150% monthly construction progress multiplier. The UI displays these three effects as concise three-line text blocks with the numeric value on the middle line. `tv_wonder_select_construction_site` confirms the completed survey site, grants 0/20/40 domestic support from the average result (<50 / 50-74 / >=75), and unlocks organization, logistics, and construction.

The generated site rules cover all generic wonders: University City excludes rural settlements and favors cities plus local literacy; Sky Dome Grand Temple requires state-religion population and favors monasteries, cathedrals, and higher state-religion share; Giant Tower Temple has no hard site limit and uses the same religious preference profile; River Extension requires a river site and favors development, bridges, and adjacent River Extensions; National Shipyard requires coast and favors harbor suitability plus development; Star Fortress City excludes rural settlements and favors mountains, plateau, forts, and cities; Great Wall excludes cities and favors mountains, plateau, forts, and rural settlements; Giant Armory excludes rural settlements and favors existing armory infrastructure, cities, and development; Library of Nation is capital-only and favors development, cities, and megalopolises.

`data/wonder_mechanics.yaml` is the shared mechanical data source for all 52 generic Engineering Department wonders: it drives final building local effects and maintenance, permanent base modifiers excluding Great Power Score, generic ritual data for the 52 non-unique wonders, generated ritual bridges for unique wonders, proposal deck bridges, site triggers, structured site preference scripts whose player-visible suitability rows are derived during generation, generated concepts/localization/GUI text, generated ceremony requirement/effect detail panels, finalization bridges, and the generated `tv_wonder_index_effects.txt` variable-map index including ritual mode/cost/listener/completion metadata plus helper/module/final `building_type` route maps. `buildings.*.final_local` is the shared authored local-effect map for final wonder buildings; it is not keyed by final branch, so the final-building generator applies the same local-effect package to all three generic final branches, and unique final buildings inherit that prototype package through their strict `base_effect_multiplier = 2`. All generic wonders use the same three-style ritual framework: style 1 starts a one-year national burden and later grants a weaker permanent blessing of the same modifier family, style 2 constructs a wonder-specific auxiliary annex building with unique local effects and +50% local estate power for the wonder's estate, and style 3 consumes one fixed decoration cost branch (one artwork, `scaled_gold = 5`, or 50 Prestige) for a large one-time reward. Display-only scripted effects feed the ceremony UI's original-style effect tooltip lists; style-2 annex rewards use generated location static modifiers in `src/main_menu/common/static_modifiers/` so the local modifier package can be shown through `add_location_modifier`. The Engineering Department GUI fragment now uses cached country variables (`tv_wonder_locked_*`, `tv_wonder_selected_ritual_*`) plus id-routed concepts/localization keys (`tv_wonder_display_<id>`, `tv_wonder_display_image_<id>`, `tv_wonder_display_<id>_ritual_<style>`, `TV_ENGINEERING_*_TEXT_<id>`) for preview images, proposal/locked text, ceremony buttons, active ritual tooltips, and per-mechanic suitability panels; direct per-wonder GUI expansion is retained only where mechanic-specific row content differs. Generic ritual confirmation is split across `tv_wonder_confirm_ceremony`, `tv_wonder_confirm_ceremony_scaled_gold`, and `tv_wonder_confirm_ceremony_prestige`; unique wonders still auto-lock style 1 on construction completion, but that single branch now also goes through the normalized `ritual` schema from `data/unique_wonders.yaml`, where `mode` is the start profile and completion can be driven by configured listeners plus `completion_trigger_script`, optional `snapshot_effect_script` / `progress_effect_script`, and branch-specific runtime variables.

`data/unique_wonders.yaml` defines the unique historical wonder layer (ids 101-201). Each entry maps to an existing generic wonder through `base_key`, inherits the generic size, site rules, derived suitability rows, buildings, and base country effect shape, then applies a 2x base-effect multiplier, a fixed `location`, unique concept/name/flavor/history-intro/image prompt, and one unique ritual definition through either the legacy `ceremony` block or the newer `ritual` block. The normalized unique `ritual` schema now supports `listeners`, `snapshot_effect_script`, `progress_effect_script`, `completion_trigger_script`, and `runtime_variables` in addition to the older start/completion hooks, so a single auto-locked ritual can complete on timer expiry, annex completion, war victory, ruler death, country building thresholds, or "N new buildings since the ritual began" baselines without requiring a multi-branch UI. Great Engineer proposal order is: unfinished resume/expand priority projects first, then feasible unique wonders before the generic random deck; proposal-slot removal still excludes already proposed unique wonders from the current reroll deck. Accepting a unique wonder applies +50 initial domestic support after IO progress reset. Unique survey target competence gains +100 Scale, +20 Logistics, and +20 Organization at survey start. When construction completes, unique wonders auto-select and lock ceremony style 1, so the GUI shows their single ritual text and Hold Ceremony action without the three style-choice buttons; that one branch can now still be authored as an immediate resolution, a timed ritual, an auxiliary ritual building flow, or a deferred event-driven ritual that waits on its configured listeners. `data/wonder_localization.yaml` is the shared source for generated wonder-mechanics localization text. The root-level `wonder_localization_editor_web/` FastAPI tool reads/writes that same localization source and exposes structured mechanics editing for site rules/preference templates, base country modifiers, generic/unique rituals, and final-building local effects; unique pages show inherited prototype site/suitability data and inherited+doubled prototype local effects read-only. The editor reads `data/wonder_editor_catalog.yaml` for precomputed vanilla country/local modifier options whose static modifiers are applied by vanilla events, plus Style 3 instant reward options generated from vanilla event effects. `scripts/wonder_localization_editor.py` remains the compatibility launcher, while hand-maintained wonder-concept and Engineering Department localization files stay as direct-edit side inputs for keys they still own.

Wonder construction currently relies on site-only `tv_wonder_labor_camp` support buildings plus the `tv_wonder_materials` market good. `tv_wonder_labor_camp` is localized as Wonder Worksite, unlocks after Organization/Logistics/Construction, employs 2,000 laborers per level, and each effective level provides 1,000 base monthly construction speed while consuming 10 `tools` and 20 `tv_wonder_materials`. `tv_wonder_materials` has no direct pop demand, uses `default_market_price = 5`, and market supply, worksite input demand, and stockpile represent the material reserve. The Organization/Logistics card exposes native Production filters for buildings that consume `tv_wonder_materials` (Wonder Worksites) and produce `tv_wonder_materials` (Wonder Material Workshops); standard AI/automation may build the workshop so imports are a viable supply route. Wonder Material Workshops convert 1.0 `lumber`, 0.7 `masonry`, 0.4 `glass`, and 0.2 `marble` into 1 `tv_wonder_materials`, giving about 13.6% base profit at default prices. Material depots and the old IO material-stockpile variable are removed. Monthly construction ticks recompute effective worksite levels, set the worksite cap from Organization Competence, and set `tv_wonder_monthly_construction_progress` directly from effective Wonder Worksite speed, so the active unit advances by effective worksite levels at 1,000 speed per level before the Logistics Competence multiplier. A separate construction random-event pulse starts as soon as a construction site is selected (`tv_wonder_stage = 3`), ignores current construction speed, and stops when construction is declared complete, the wonder is finalized, or project state is cleared by abandonment/collapse. That pulse uses one generated `random_list` with 90 no-event weight and a 10-weight nested deck of 212 eligible events: 168 token-exchange events plus 44 Great Engineer events gated by effective MIL thresholds 20/50/80. Construction event localization is now hand-maintained in EN/ZH instead of generated string concatenation; event options still re-check eligibility for stale popups and rely on real visible effects, while IO variable changes use `custom_description` plus effect localization. Each construction unit uses the IO variable `tv_wonder_construction_progress`; small/medium/large wonders require 100,000/200,000/300,000 progress per unit. Completing a unit materializes the corresponding local module building, grants +10 support, and may raise `tv_wonder_level` when all four parts catch up, granting another +10 support plus a level-up notification. Once `tv_wonder_level >= 1`, the player may declare construction complete, which destroys the site Wonder Worksites, locks further construction, awards +20 support, and unlocks ceremonies. All 52 generic wonders now share one generated three-style ritual structure instead of hand-authored per-wonder ceremony logic: style 1 starts a timed national ritual that applies a one-year negative country modifier and, on expiry, grants a weaker permanent blessing of the same modifier family; style 2 starts construction of a wonder-specific ritual annex building whose local modifier package is unique per wonder and whose building effect also adds +50% local estate power for the wonder's associated estate; style 3 is a fixed decoration branch that either destroys one artwork, charges `scaled_gold = 5`, or charges 50 Prestige and then grants the wonder's assigned large one-time reward immediately. Style-1 completion is checked by the monthly ritual pulse, style-2 completion comes from the annex building's `on_built`, and style-3 finalizes immediately after its cost and reward resolve. Unique wonders still expose only one auto-locked ritual branch in the current UI, but that branch now runs through the same normalized ritual execution layer and can therefore resolve as an immediate ritual, a timed ritual checked by the monthly pulse, or an auxiliary ritual building that finalizes on `on_built`; the shared confirmation routing also supports artwork, `scaled_gold = 5`, and 50-Prestige costs on unique rituals when configured. Finalization still triggers a visible inauguration event with unique flavor text per wonder/final building, constructs the matching event-only local wonder building at the selected site, raises that building to the current wonder level, applies the base wonder-level country modifier, applies generated unique ritual country modifiers where configured, grants extra support rewards scaled from remaining Domestic Support (0-200 support to 0-10 Stability, 0-10 Legitimacy, 0-20 Prestige), adds national and local wonder-completion celebration modifiers scaled by completed level, immediately adds +15% site prosperity per completed level, broadcasts a neutral no-effect world-news event with wonder-type-specific flavor to all other countries with capitals, clears temporary project variables and IO progress back to zero, and then stops support decay. Finalized wonder buildings have `max_levels = 6`, cannot be destroyed, hide normal player construction behind `country_potential = { always = no }` plus the "Wonders can only be built through the Engineering Department" allow tooltip, and carry only designed shared/base local effects; the old generic style-2 branch-specific local bonuses now live on the auxiliary annex buildings instead of the finished wonders. National/base/ritual effects are applied by permanent country modifiers instead of building country effects because wonders may not be in the capital. Support and module buildings can still carry their own local effects. When wonder construction terminates in any form, completed survey data, final buildings, leftover module buildings, and completed annex buildings persist. Future Great Engineer agendas prioritize half-built wonders, extra modules, and finalized wonders below their stored site cap; upgrades reuse the previous ceremony style and can claim completion rewards again. The custom IO panel hides the Members tab; its overview tab contains active conception and debate controls; its resolutions tab contains Survey plus Organization/Logistics; and its custom tab contains Construction plus Ceremony controls.

Engineering Department UI staging note: later-stage cards do not disappear while prerequisites are incomplete. Survey, Organization/Logistics, Construction, and Ceremony each show a locked informational card until the previous stage is complete. The header shows active IO construction progress when a unit is being built. The Construction/Ceremony tab includes a resume/upgrade hint, a unified active-unit progress bar, remaining-time text, and percent text. Ceremony style buttons behave as a toggle with the selected style shown in its pressed state; upgrade projects hide ceremony choices and show a locked-ceremony note. The detail panel shows only that branch's ritual requirement and wonder extra effect, and the bottom Hold Ceremony action dispatches from the selected ritual's cached cost type.

The **Trade Victory** path (贸易胜利) is fully implemented with a Trade League layer: 5 triggers, 5 reward modifiers, 5 events, and localization in English and Simplified Chinese. Score = monthly_trade_income (snapshot taken monthly by `tv_update_trade_score_effect`); thresholds at 100 / 200 / 400 / 1000 / 2000. M3 and M5 additionally require the country to be the strongest trader in at least 5 / 10 markets. **Trade League IO** (`tv_trade_league`): non-unique IO created by the Trade Victory M1 reward; the founder is added as a member, receives `tv_trade_league_member`, and remains the locked leader country (`leader_change_trigger_type = none`, `leader_change_method = none`). The appointed **Grand Merchant** is stored on the leader country as `tv_grand_merchant_char` and managed by generated Appoint/Remove/Change leader actions. The leader can invite countries through `tv_invite_to_trade_league` (base AI acceptance -200, opinion scaling 0.5, republic +20, merchant republic +30, <=5 locations +20, leader diplomatic reputation x2, leader monthly trade income / 10) and expel non-leader members through `tv_expel_trade_league_member`. The `tv_trade_league_member_status` special status gives non-leader members `global_merchant_capacity_modifier = -0.10`, while the leader gains `global_merchant_capacity_modifier = 0.02` per non-leader member through `leader_modifier`. `tv_trade_league_laws.txt` adds five generated three-level vote-based law groups, but the generated IO definition starts with no enacted laws; the policies are enacted later through votes.

Trade League monopoly tracking and actions are generated from `data/trade_league_goods.yaml`, which also defines the monopoly-tab display groups: Basic Goods, Food, Manufactured Goods, and Luxury Goods. Per good, the monopoly flag, total monopoly percentage, origin/node/consumer aggregate control percentages, top-3 market slot variables, and persistent virtual-demand, virtual-supply, and embargo state are stored on the Trade League `leader_country`, not in the IO type `variables = {}` block; initializing thousands of IO variables caused severe in-game stutter. The generated Trade League IO type also avoids an IO `monthly_effect`; `tv_trade_league_monthly_pulse` runs from `monthly_country_pulse`, lets each country check only the Trade League IOs it belongs to, and enters only IOs where `leader_country` is that country before calling the IO-scoped monopoly refresh. That maintenance only proceeds while the Grand Merchant exists, is alive, and passes the Grand Merchant availability trigger, then saves the IO as `scope:tv_trade_monopoly_io` and enters `leader_country` for all state writes. Trade Chain and Commercial Intelligence are placeholders, but `tv_trade_league_triggers.txt` already defines the Grand Merchant busy-state flags (`tv_trade_chain_active`, `tv_trade_commercial_intelligence_active`, and active `resolution:policy_vote` for law changes) and mutual-exclusion triggers so those future systems, Trade League law votes, and the monopoly report disable one another consistently. Monopoly tracking is split into annual global marker refresh and monthly IO refresh. Annual refresh ranks and stores market-center locations plus static slot metrics: origin markets by `produced_in_market:<good>`, node markets by summing market `every_export` routes with matching `goods` and `trade_volume`, and consumer markets by `goods_demand_in_market(goods:<good>)`; it also stores total export/import route denominators for the slots. Monthly IO refresh revisits only those marked markets, sums member-owned route numerators with market `every_export` for origin/node control and market `every_import` for consumer control, stores slot and aggregate percentages on the leader country, and marks `tv_trade_monopoly_<good>` when `tv_trade_monopoly_level_pct_<good> >= 100`. Each aggregate is the average of three slot controls, missing or zero-denominator slots count as 0, and the total monopoly percentage is the sum of the origin, node, and consumer aggregate controls, capped naturally at 300%. The generated projection effects copy the active category/page into 15 fixed display rows (`tv_trade_display_row_<n>_*`) and copy the selected good into generic detail variables such as `tv_trade_selected_good_scope`, `tv_trade_selected_monopoly_level_pct`, and `tv_trade_selected_origin_market_<rank>`. The UI reads only these projected variables through `InternationalOrganizationsView.GetInternationalOrganization.GetLeaderCountry.MakeScope.GetVariable(...)` and no longer displays world production, IO trade, `traded_in_market`, or `goods_supply_in_market` for the monopoly report.

The monopoly action layer generates category-select, previous/next page, and 15 fixed row-select actions plus one fixed selected-good action set: set virtual demand or virtual production through market + value `select_trigger`s, increase/decrease/cancel either virtual action, set an embargo through market + country `select_trigger`s, and cancel the embargo. Those fixed actions dispatch by `tv_trade_selected_good` into generated per-good script branches, so the GUI does not instantiate per-good buttons. Each point of virtual demand or production locks 5% monopoly level, while each embargo locks 30%. Virtual demand uses generated `goods_demand` entries and is applied as `add_temporary_demand` with `months = -1`; set/increase/decrease/cancel actions remove and reapply it as needed, monthly monopoly maintenance removes it when the monopoly is lost, and the Trade League monthly fallback removes all virtual demand while the Grand Merchant cannot maintain monopolies. Virtual production now follows the same persistent-action pattern experimentally: the leader country tracks `tv_trade_virtual_supply_applied_<good>`, applies `add_goods_supply` once when active, removes it by applying a negative `add_goods_supply` amount before changing/canceling/suspending, and reapplies it when maintenance conditions recover. Embargo applies a 2-month `merchant_power_severe_penalty` through `add_merchant_power`. Action variables persist on the leader country when the monopoly is lost, but effects are applied only while `tv_trade_monopoly_<good> >= 1`. Every generated action is also registered in `tv_trade_league_list.txt` and `messagetypes.txt`. The Trade League IO panel is generated: the header manages the Grand Merchant appointment/replacement/dismissal, the overview shows Grand Merchant status, member invite/expel controls, and a placeholder trade-chain card; the renamed Resolutions tab shows a commercial-intelligence placeholder; the custom tab shows category and page controls above 15 projected row slots, with each row retaining icon/name, origin/node/consumer control, reserved monopoly checkmark, and total monopoly percentage. Selecting a row refreshes a single detail card with a piechart, monopoly/used progress bars scaled to 300%, top-3 origin/node/consumer market tables, active action rows, and one leader-only fixed action card for monopolized goods. Detail rows show local demand, local production, total export/import, and IO export/import using the projected slot variables copied from the selected good; origin/node rows project `tv_trade_<prefix>_total_export_<rank>_<good>` and `tv_trade_<prefix>_io_export_<rank>_<good>`, while consumer rows project `tv_trade_consumer_total_import_<rank>_<good>` and `tv_trade_consumer_io_import_<rank>_<good>`.

The **Diplomatic Victory** path (外交胜利) is fully implemented: 5 triggers, 5 reward modifiers, 5 milestone events, and localization in English and Simplified Chinese. M1 requires 2 diplomatic supporters and 1 DVP, but its reward no longer creates the Diplomatic Alliance IO. The second accepted Diplomatic Support pledge triggers the visible flavor event `tv_diplo_alliance.2`; that event's option calls `tv_create_diplomatic_alliance_effect`, creates the founder's non-unique Diplomatic Alliance IO, enrolls all existing supporters, and ensures DVP is at least 1.

Diplomatic score = `tv_diplomatic_victory_points` from Diplomatic Support/alliance creation and monthly alliance-tier maintenance; royal marriages and war wins do not award DVP. Thresholds are 1 / 50 / 500 / 2,500 / 15,000. **Diplomatic Alliance IO** (`tv_diplomatic_alliance`): non-unique IO using the same character-leader-with-appointment pattern as the Arts Exhibition and Academy of Sciences IOs (`tv_diplomatic_alliance_leader_char` country variable; `leader_change_trigger_type = none`; leader titled Advocator). This IO has a custom `organization_panel` with HRE-style header (tier piechart at 220,60; cohesion piechart at 294,60; leader country flag), a parliament tab (`tv_alliance_assembly` parliament type, `uses_parliament_for_law_votes = yes`) for the 5-law system, and two IO-native variables: `tv_alliance_tier` (max 15, mirrored to the leader country for milestone triggers and maintained as the sum of current policy levels above 1) and `tv_alliance_cohesion` (max 100, `monthly_change` driven by base +0.1, `leader_country.modifier:diplomatic_reputation` × 0.01, and +0.05 internal peace).

Diplomatic Alliance laws note: the five generated law categories start with no initial policy on creation. Only the alliance leader can initiate policy votes, but reforms still resolve through the IO parliament. Policy changes are adjacent-only: no policy counts as level 0, and level N can be selected only from current level N-1 or N+1. AI vote weight scales by reform level: base 0/-100/-200/-300, opinion of the leader at 0/0.25/0.5/0.75 with tiered clamps, and leader diplomatic reputation x0/5/10/15. AI proposal/vote math may use `scope:recipient` because the documented policy AI scope gives recipient = IO, but generated blocks guard direct recipient reads with `exists = scope:recipient` for pre-evaluation safety; policy `allow` / `on_activate` / `on_deactivate` use the current IO root directly.

The **Cultural Victory** path (文化胜利) is fully implemented with an Arts Exhibition layer: 5 triggers, 5 reward modifiers, 5 events, and localization in English and Simplified Chinese. Score = `tv_cultural_influence_points` (CIP) accumulated via `on_work_of_art_created` (+3 CIP, scope `root.owner`) and `monthly_country_pulse` (+1 CIP per month); thresholds at 50 / 200 / 400 / 800 / 1600. M2–M5 also require `tv_arts_intl_influence` ≥ 25 / 50 / 75 / 100 respectively. **Arts Exhibition IO** (`tv_arts_exhibition`): non-unique IO; each country creates its own instance at M1 via `create_international_organization` with `set_leader_country = prev`. `tv_arts_intl_influence` (International Influence) is an **IO type variable** (`variables = { }` block) with `min = 0 / max = 100 / start = 0`, automatic `monthly_change` decay of −0.05/month (shown in the IO panel's standard `ios_information_header` piechart with `IOVariableTooltip` monthly breakdown). All effect sites modify it via `every_international_organizations_member_of = { limit = { international_organization_type = international_organization_type:tv_arts_exhibition } change_variable = { ... } }`. M2–M5 milestone triggers use `any_international_organizations_member_of = { ... var:tv_arts_intl_influence >= N }` from country scope. **IO leader = a court artist** (`leader_type = character`, manually appointed via Appoint/Remove/Change Leader actions in `tv_io_leader_actions.txt`; leader character stored on leader_country as `tv_arts_exhibition_leader_char`; IO `leader = { leader_country ?= { var:tv_arts_exhibition_leader_char ?= { add_to_list = leaders } } }` with `leader_change_trigger_type = none`). The appointed leader's `artist_skill × 0.002` is added to `tv_arts_intl_influence.monthly_change` (via `leader_country.var:tv_arts_exhibition_leader_char.artist_skill` chained access). Three further generic actions: "Send Artist Abroad" (200 ducats, 3-step selection: artist → country → location; 1–10 year tour with `yearly_country_pulse` timer and `on_character_death` hook), "Host Domestic Exhibition" (1000 ducats, +0.1 per owned artwork). The IO panel header shows the leader character automatically (standard vanilla character-leader IO header); no custom portrait slot is needed. **Standard touring events** (~8% per artist per month, skill-weighted): unremarkable (+0.1), great success (+0.5), terrible (−0.1), artist hired (+1.0 permanently), artistic inspiration (+10% artist_skill_level_gain for 10 years). Character modifier: `tv_artistic_inspiration_modifier`. **5 parallel monthly special events** (1/300 per artist per month each, once-per-tour guard `tv_tour_event_1_fired`–`tv_tour_event_5_fired`): 201 Cultural Diplomacy (bilateral +20 opinion `tv_cultural_diplomacy_opinion`, `yearly_decay = 6`), 202 Exhibition Success (5-year `tv_exhibition_success_location_modifier` on location: `local_migration_attraction +2`, `local_unrest −0.2`, `local_production_efficiency −0.2`), 203 Noble Patron (construct theater at exhibition location free, `cost_multiplier = 0 instant = yes`), 204 Artist Exchange (both touring artist and a local artist gain permanent `add_artist_skill = 0.05`), 205 Art Theft + Artist Death (destroy host artwork via `destroy_art`, −20 permanent opinion `tv_art_theft_opinion`, dispatch state cleaned up, `tv_on_exhibition` removed before `kill_character = this` to suppress double `.6` death event). **Probabilistic return** via `tv_end_artist_exhibition_effect`: 90% plain return event (.5); 2% × 5 special outcomes: .120 artwork brought home (`move_art + set_art_owner`), .121 inspiration creates art (`create_art`), .122 research progress (`add_research_progress = research_progress_mild_bonus`), .123 artist retires (`kill_character = this`), .124 gold donation (random 50–150 ducats). `scope:exhibition_target` is saved at the top of `tv_end_artist_exhibition_effect` for use in return event option effects. All tour-end paths (return effect, `on_character_death`, Artist Hired branch, event 205) clean up the 5 once-per-tour flag variables. **Dispatch constraints:** Each dispatched artist reduces the home country's `num_possible_artists` cap by 1 via a pre-defined country modifier (`tv_touring_penalty_N`, where N = current dispatch count; managed by `tv_update_tour_capacity_effect`). The home country tracks the count in `tv_dispatched_artists_count` (initialized to 0 in IO `on_creation`, updated on dispatch/return/death). A marker variable `tv_has_visiting_artist` is set on the target country when an artist arrives and cleared when they return or die; Step 2 of the dispatch selection filters out countries that already carry this marker so at most one artist from any IO member can tour each country at a time. The IO panel displays the current dispatched count via a `TV_ARTS_DISPATCHED_COUNT_LABEL` / `GetValue|0` widget.

**Current local exhibition mechanic:** The old one-click domestic exhibition has been replaced by a persistent local exhibition system. `tv_host_domestic_exhibition` requires at least 5 owned artworks, costs `scaled_gold = 3.5`, and uses a location select trigger. The chosen location receives the event-only `tv_local_arts_exhibition` building (nobles, employment 0.05, cultural category, heavy art-goods maintenance, `local_migration_attraction = 2`, `local_production_efficiency = -0.2`, `local_unrest = -0.2`), is stored as `tv_local_exhibition_primary_location`, and is added to the `tv_local_exhibition_locations` variable list. The IO variable `tv_local_exhibition_enthusiasm` starts at 80, caps at 200, decays by 8/month while active, and ends the exhibition at 0; the country tracks active state, active location list/count, committed exhibits, duration, and rating, with no post-exhibition cooldown. The player expands the exhibition with `tv_local_exhibition_expand` (`scaled_gold = 1.0`, no cooldown): the next exhibit tranche is committed using the old escalating requirements (5, then 10, then 20), the player selects an owned location adjacent to any active exhibition location, the special building is constructed there, the location is added to `tv_local_exhibition_locations`, and +10 Enthusiasm is awarded. Closing rewards are per-location CIP multiplied by `tv_local_exhibition_location_count`: per-location CIP = 0.5 × duration in months + tier bonus, with tier bonuses at ratings 1/2/3/4 = -3 / 0 / +3 / +6. Prestige rewards are not multiplied: tier 1 loses 10 Prestige, tier 3 adds +10 Prestige, and tier 4 adds +15 Prestige. Tier 4 still grants +0.25 development and +0.1 prosperity to the primary location, while the 1-year `tv_local_exhibition_triumph_location_modifier` is applied to every location in the closing `tv_local_exhibition_last_locations` snapshot. The Arts Exhibition panel's custom tab shows owned artworks, committed exhibits, active venue count, months open, rating, and a cold-to-hot enthusiasm progress bar. Active exhibitions also unlock royal visit, radical art, royal art sale, artist recruitment, foreign guest artist invitation, centerpiece designation, and live creation contest. Monthly exchange events each have a 1% chance and trade gold, Prestige, estate satisfaction, stability, legitimacy, host development, or host prosperity for +10 enthusiasm, with a decline option for -5 enthusiasm.

**Arts Exhibition laws:** The IO uses the vanilla IO Laws tab with eight generated law groups under `tv_arts_exhibition_laws`: Artistic Patronage, Artistic Organization, Official Artistic Purpose, Censorship and Artistic Freedom, Architecture and Urban Aesthetics, Knowledge Transmission Media, State Ritual Aesthetics, and Relationship Between Art and Science. All laws use `requires_vote = yes`, `has_parliament = no` hides the separate IO Parliament tab, and the IO starts with no initial policies. Each policy carries 1-2 positive and 1-2 negative country modifiers, with strength kept near a single Advance or estate privilege.

**Arts exchange mechanic:** The Arts Exhibition IO shows the arts exchange information and action cards in the resolutions tab, leaving the overview tab for touring controls and the vanilla Laws tab for legal policy votes. A country can start a year-long exchange with `tv_start_arts_exchange` (`scaled_gold = 2.0`) when it has an appointed Chief Artist and at least two living artists; there is no cooldown, but an active exchange blocks starting another. The country tracks `tv_arts_exchange_active`, elapsed/remaining month variables, and persistent participant lists (`tv_arts_exchange_participants`, `tv_arts_exchange_foreign_participants`), while each participant receives marker variables for cleanup and selection filtering. When the exchange starts, the country immediately preselects up to three additional domestic artists, stores them in `tv_arts_exchange_auto_invite_1/2/3`, shows them in event `tv_arts_exhibition.400`, and applies them from the event option. During the exchange the player can manually invite more domestic artists (`scaled_gold = 1.0`) or foreign artists (`scaled_gold = 1.5`, country selection then character selection); all invite selectors exclude artists currently touring abroad, local exhibition guest artists, and existing exchange participants. Monthly exchange outcomes fire through events with a 50% monthly roll; events 401, 402, 403, 404, and 410 have doubled internal weights, while the overall monthly roll stays 50%. Event 410 is the no-progress dispute outcome and costs 10 Prestige. `tv_arts_exhibition.420` announces the end and clears all exchange state.

Chief Artist work is mutually exclusive: Arts Exhibition law-change policy votes, Arts Exchange hosting, and Domestic Exhibition hosting each block the other two, with explicit busy-state tooltip text naming the active task.

**Arts influence clarification:** `tv_arts_intl_influence` decays by 0.1% of its current value per month and is still driven primarily by foreign artist tours and the appointed Chief Artist. Persistent local exhibitions award `tv_cultural_influence_points` at closure rather than directly adding International Influence.

The **Scientific Victory** path (科技胜利) is fully implemented with a unique Academy of Sciences building gate: 5 triggers, 5 reward modifiers, 5 events, and localization in English and Simplified Chinese. Score = `num_of_advances_researched` (snapshot taken monthly by tv_update_science_score_effect); thresholds at 100 / 150 / 200 / 300 / 400 (provisional, calibrate after playtesting). Rewards: M1 research speed, M2 institution spread/discipline, M3 institution spread/pop growth, M4 institution embrace cost/production, M5 production/discipline. **Unique mechanic:** Each milestone only fires after the corresponding **Academy of Sciences** building phase is manually constructed in the capital. The building chain (Phases I–V) is visible from game start; each phase requires the previous phase plus the corresponding score threshold (`tv_academy_of_sciences_level` variable). Construction conditions use `custom_tooltip` (score variable is not auto-parseable). **Academy of Sciences IO** (`tv_academy_of_sciences`): non-unique IO created by the Scientific Victory M1 reward after Phase I (`tv_academy_of_sciences_1`) is built; the same M1 popup both announces the Academy and grants the milestone reward. The reward sets `tv_academy_io_member = 1`, initializes research mechanism variables, and calls `create_international_organization`. **IO leader = an appointed non-artist character** (Chief Scientist), set manually via Appoint/Remove/Change Leader actions in `tv_io_leader_actions.txt`; leader character stored on leader_country as `tv_academy_leader_char`; IO `leader = { leader_country ?= { var:tv_academy_leader_char ?= { add_to_list = leaders } } }` with `leader_change_trigger_type = none`. The IO uses the vanilla IO Laws tab for `tv_science_factions` and `tv_academy_leadership`: the seven Academy laws have law icons but the IO starts with no initial policies; six laws can carry country_modifier gameplay effects after enactment, while `tv_academy_leader_gender_law` controls whether the Chief Scientist selector is male-only or unrestricted. All keep `requires_vote = yes` for IO policy votes; the Academy sets `has_parliament = no`, hiding the vanilla IO Parliament tab. **Research Mechanism (frontier-tech shortcut):** Four sub-processes complete one full cycle, which auto-researches one chosen Frontier advance via `research_advance = advance_type:X` (these advances remain normally researchable via the vanilla research tab — the Research Mechanism is an *extra* unlock route, not a gate). Sub-process A (Interest Collection): monthly random event (`tv_research.1`, ~10% base chance) costs dynamic gold via `change_gold_effect = { scale = -2 }` and adds +1 to `tv_research_interest`; completes at 5. Sub-process B (Prerequisites): institution-root targets require at least one owned location matching the corresponding institution's plausible spawn conditions with geography/region and existing-institution gates removed; non-institution-root targets still complete automatically. Sub-process C (Literacy Foundation): **IO variable** `tv_research_c_progress` accumulates `average_country_literacy × 0.05` per month; completes at 100 (~40 months at 50% literacy). When A/B/C are all complete, `tv_research.19` fires a one-time no-effect reminder that concentrated research can now begin. Sub-process D (Concentrated Research): **IO variable** `tv_research_d_progress` accumulates `effective ADM × 0.05` per month (base `leader_country.var:tv_academy_leader_char.adm` plus +20/+40/+60 from `tv_great_scientist_1/2/3`); completes at 100 based on effective ADM and then grants or upgrades the current Chief Scientist's Great Scientist trait. D halts when no leader appointed; while active it also applies `tv_concentrated_research_penalty` (−20% research speed), removed silently on completion or reset. **Target selection:** Player clicks "Request Research Topic" → strict-earliest-age random proposal (each candidate is excluded if `has_advance = vanilla_id` is true OR if any earlier-age Frontier advance is still unresearched) → event `tv_research.10` for confirmation. Abandoning a proposed or active target costs 10 Prestige and has no cooldown; abandon options are hidden, and the abandon action is disallowed, below 10 Prestige. **20 active Frontier advances:** 1=printing_press_advance(A3), 2=iron_working(A1), 4=gunpowder_advance(A2), 5=blast_furnace(A3), 6=scientific_experimentation(A4), 7=scientific_revolution_advance(A5), 8=manufactories_advance(A5), 9=industrialization_advance(A6), 10=vaccination_advance(A6), 11=renaissance_advance(A2), 12=banking_advance(A2), 13=professional_armies_advance(A2), 14=new_world_advance(A3), 15=pike_and_shot_advance(A3), 16=confessionalism_advance(A4), 17=global_trade_advance(A4), 18=artillery_institution_advance(A4), 19=military_revolution_advance(A5), 20=enlightenment_advance(A6), 21=levee_en_masse_advance(A6). Value 3 is retired from the old anatomy_advance(A2) target and is intentionally not reused. Each is listed in `data/locked_advances.yaml`; `tv_unlock_advance_<id>_effect` is generated by `scripts/gen_locked_advances.py` and called by `tv_research_concentrated_complete_effect` on cycle D completion. **Cycle phases:** 0=idle, 1=A/B/C running, 2=D concentrated research. State variables: `tv_research_phase`, `tv_research_target`, `tv_research_interest`, `tv_research_b_done`; display vars: `tv_rm_subprocess_a/b/c`, `tv_rm_subprocess_d_ready`. IO panel (`tv_academy_of_sciences.gui`) keeps the overview focused on target, Chief Scientist name (from appointed character), subprocess A/B/C/D status, current B requirements rendered with `RequirementsList` + `ShowTriggerConditions('tv_research_check_b_trigger', PlayerScope.Self)`, and three research action buttons. The header manages the Chief Scientist appointment/replacement/dismissal from the portrait area. The resolutions slot is renamed Philosophy and currently shows a debate UI shell: a Chief Scientist concept row, reserved issue-conception progress (`tv_academy_philosophy_concept_progress`) with empty fallback, six debate status circles, the current placeholder issue "The state should be governed by the capable", fixed Burghers/Nobles sides with a 50% two-color progress bar, three visible action slots backed by six hidden static category color variants each, two operation/effect detail cards, and a disabled execute button with no backend action. The victory situation panel keeps Scientific Victory to the shared progress row; Academy operational details live in the IO panel. **Medicine Tab:** A separate Medicine tab in the IO panel lets the player treat a court character with a bad health trait. The player clicks "Treat Character" (`tv_treat_character` generic action) to pick a target filtered by `tv_has_health_condition_trigger` (blind, one_eyed, maimed, disfigured, scarred, hunchback, pockmarked_trait, smallpox_trait, bubonic_plague_trait, sickly). Once a patient is selected, `tv_med_target_char` (country variable on leader_country) holds the character reference; only one active patient at a time. Monthly processing (in `monthly_country_pulse` effect block): if the Chief Scientist is alive, cache effective ADM into `tv_med_disp_adm` and base `dip/mil` into `tv_med_disp_dip/mil` for display, then `tv_med_progress += effective ADM × 0.1`; when progress ≥ 100, `tv_med_complete_treatment_effect` fires (`tv_medicine.3` popup, all bad health traits removed, state reset). Patient natural death triggers `tv_medicine.4` popup and state reset. Sub-action `tv_med_discovery_pulse` fires `tv_medicine.1` (~1%/month, fixed approximation of dip×0.2%): three options — Conservative (95%: +5 progress / 5%: patient dies), Risky (80%: +10 progress / 20%: patient dies), Wait (no effect). Sub-action `tv_med_accident_pulse` fires `tv_medicine.2` (~5%/month, fixed): notification only. Tab displays: current patient name (or "None"), progress bar (max 100, visible when patient active), monthly gain rate, discovery chance, accident chance. State variables: `tv_med_target_char`, `tv_med_progress`, `tv_med_disp_adm`, `tv_med_disp_dip`, `tv_med_disp_mil` — all country variables on leader_country, initialized when the Academy is created by the Scientific Victory M1 reward and reset by `tv_med_cleanup_effect`.

Academy Medicine also supports **Meditation**. The player clicks `tv_start_meditation` in the Medicine tab to select a living character with at least one negative non-health trait from the ruler, cabinet, or artist trait sets (`tv_has_meditation_negative_trait_trigger`); negative health traits remain treatment-only. Active meditation stores `tv_meditation_target_char` and `tv_meditation_progress` on the country. The target receives `tv_meditating_character_modifier` while meditation is active, blocking military leadership and applying −50% character cabinet efficiency; if the selected target is the current ruler, the country also receives `tv_meditating_ruler_country_modifier` for −0.2 monthly legitimacy. Monthly progress uses the same rate as medical treatment (`effective ADM × 0.1`) through `tv_change_meditation_progress_effect`, whose effect localization reports positive and negative progress changes. Reaching 100 fires `tv_medicine.20`, removes all matching negative ruler/cabinet/artist traits from the target, removes the temporary meditation modifiers, and clears meditation state. The player may stop meditation at any time through `tv_stop_meditation`; stopping only clears the meditation target/progress and temporary meditation modifiers, without cleansing traits. While meditation is active, `tv_meditation_monthly_pulse` has a 10% monthly chance to fire exactly one event from the shared meditation event list: calm (+5), restlessness (−5), high-DIP guidance (+10 if Chief Scientist DIP > 80), low-DIP counterguidance (−10 if DIP < 30), or inner turmoil (50/50 +5/−5). The Medicine tab shows a separate Meditation card with default guidance text, the selected character card, a progress bar, the monthly gain rate, and the fixed event chance.

Chief Scientist work is mutually exclusive: concentrated research, Academy law-change policy votes, medical treatment, and meditation each block the other long-running tasks, with explicit busy-state tooltip text naming the active task.

Current concentrated research behavior: D progress and completion require a living Chief Scientist with at least 20 effective ADM. The player can pause D with `tv_pause_concentrated_research`, preserving `tv_research_d_progress` while freeing the Chief Scientist for laws or medicine. While D is active, `tv_research_d_pulse` uses one shared `random_events` list for `tv_research.100`-`.129`; each currently eligible event has 1% monthly weight, and eligibility is gated by effective ADM tiers <20, 20-49, 50-79, and >=80.

Great Scientist traits (`tv_great_scientist_1/2/3`) are Chief Scientist-only character traits in `src/in_game/common/traits/tv_great_scientist_traits.txt`. They add +20 / +40 / +60 effective ADM only while the character is appointed as the Academy Chief Scientist; they are read by concentrated research progress, D-event tiering, and Medicine monthly progress. Completing concentrated research grants the current Chief Scientist level 1 or upgrades level 1→2→3; level 3 stays capped.

The mod is additive-only and uses the `tv_` namespace prefix throughout.

## Core Features

Current correction for this list: Diplomatic Victory Points now come from Diplomatic Support, the visible Diplomatic Alliance creation event, and monthly alliance-tier maintenance. Royal marriages and war wins no longer award DVP; M1 no longer creates the IO directly.

1. **6 Victory Situations** — One situation (`tv_victory_situation`) displays all 6 victory paths in a single panel with a top-level manual recovery button (`tv_reinitialize_mod`), a flavor text introduction (Section 1), per-path progress bars and milestone circles (Section 2), and a global leaderboard (Section 3).
2. **Milestone Events** — Popup country events per milestone. All 30 events implemented across 6 paths: Conquest (`tv.conquest.1`–`5`), Prosperity (`tv.prosperity.1`–`5`), Trade (`tv.trade.1`–`5`), Diplomatic (`tv.diplomatic.1`–`5`), Cultural (`tv.cultural.1`–`5`), and Scientific (`tv.science.1`–`5`).
3. **Permanent Rewards** — All 30 milestone rewards are permanent static modifiers (`days = -1`); no time-limited buffs. All 6 paths fully implemented.
4. **Diplomatic Victory Points** (`tv_diplomatic_victory_points`) — Initialized to 1 by Diplomatic Support/alliance creation, then accumulated through monthly Diplomatic Alliance tier maintenance. Royal marriages and war wins do not award DVP.
5. **Cultural Influence Points** (`tv_cultural_influence_points`) — Accumulated via `on_work_of_art_created` (+3 CIP to `root.owner`), `monthly_country_pulse` (+1 CIP/month), and persistent local exhibition rewards from the Arts Exhibition IO.
6. **Scientific Technology Score** (`tv_science_score`) — Monthly snapshot of `num_of_advances_researched`; thresholds 100 / 150 / 200 / 300 / 400.
7. **Academy of Sciences Building Chain** — Five capital buildings (`tv_academy_of_sciences_1`–`5`) gate Scientific Victory milestones. Each requires the previous phase built and the corresponding score threshold; `on_built` immediately triggers `tv_check_science_milestones_effect`. Building category: `cultural_category`; visible in all capitals from game start. Phase I no longer fires a separate Academy popup; Scientific Victory M1 creates the **Academy of Sciences IO** (`tv_academy_of_sciences`) inside its reward effect and shows a custom tooltip pointing to the Research Mechanism in the International Organization interface. Membership flag: `tv_academy_io_member` (country variable set to 1 on join). IO leader (Chief Scientist) = appointed non-artist character stored on leader_country as `tv_academy_leader_char`; managed via the Appoint/Remove/Change Leader actions in `tv_io_leader_actions.txt`. `leader_change_trigger_type = none`; `international_organization_chooses_new_leader` is deliberately NOT called — it triggers the vanilla leader-election process, violating the no-elections design. The Academy has a vanilla IO Laws tab but no Parliament tab; it starts with no initial law policies, while `tv_science_factions` and `tv_academy_leadership` law changes keep `requires_vote = yes`. `tv_academy_leader_gender_law` can remove the female-candidate filter under `tv_academy_leader_gender_unrestricted_policy` once that policy is enacted.
8. **Research Mechanism (Frontier-Tech Shortcut)** — Full four-subprocess cycle that auto-researches one of 20 active Frontier advances via `research_advance` on cycle completion. The advances remain normally researchable via the vanilla tech tree — the Research Mechanism is an *extra* unlock path, not a gate. Sub-process A: monthly random interest events (~10%; 5 funded successes needed; event namespace `tv_research`; each success costs dynamic gold via `change_gold_effect = { scale = -2 }`). Sub-process B: passive prerequisite check; institution-root targets require at least one owned location matching the corresponding institution's plausible spawn conditions with geography/region and existing-institution gates removed, while non-institution-root targets still complete automatically. Sub-process C: IO variable `tv_research_c_progress` auto-accumulates via `monthly_change`; completed by `monthly_effect`. Once A/B/C are all complete, `tv_research.19` pops once as a no-effect reminder that concentrated research can begin. Sub-process D: IO variable `tv_research_d_progress` auto-accumulates via the appointed leader's effective ADM (base ADM plus Great Scientist trait bonus), applies `tv_concentrated_research_penalty` (−20% research speed) while active, triggers unlock on completion, and grants/upgrades that Chief Scientist's Great Scientist trait. Three research generic actions: `tv_request_research_target`, `tv_abandon_research_target`, `tv_start_concentrated_research`; abandoning a proposed or active target costs 10 Prestige and has no cooldown, with abandon UI gated behind 10 Prestige. Lifecycle managed by scripted effects in `tv_research_subprocess_effects.txt`; conditions in `tv_research_subprocess_triggers.txt`; the Academy overview displays B's current conditions via `ShowTriggerConditions('tv_research_check_b_trigger', PlayerScope.Self)`. **20 active Frontier advances:** 1=printing_press(A3), 2=iron_working(A1), 4=gunpowder(A2), 5=blast_furnace(A3), 6=scientific_experimentation(A4), 7=scientific_revolution(A5), 8=manufactories(A5), 9=industrialization(A6), 10=vaccination(A6), 11=renaissance(A2), 12=banking(A2), 13=professional_armies(A2), 14=new_world(A3), 15=pike_and_shot(A3), 16=confessionalism(A4), 17=global_trade(A4), 18=artillery_institution(A4), 19=military_revolution(A5), 20=enlightenment(A6), 21=levee_en_masse(A6). Value 3 is retired from the old anatomy(A2) target and is intentionally not reused. Each is listed in `data/locked_advances.yaml` with `vanilla_advance_id` and `age` fields; the `tv_unlock_advance_<id>_effect` (set_variable + `research_advance = advance_type:<vanilla_id>`) is generated by `scripts/gen_locked_advances.py`. Target proposal uses strict-earliest-age priority: each candidate is excluded if `has_advance = vanilla_id` is true OR if any earlier-age Frontier advance is still unresearched.
9. **IO Leader Management** — Unified character-leader pattern across six TV character roles (Arts Exhibition, Diplomatic Alliance, Academy of Sciences, Governor's House Grand General, Engineering Department Great Engineer, Trade League Grand Merchant). Each IO uses `leader_type = character` + `leader_change_trigger_type = none` + `leader_change_method = none` + a `leader = { leader_country ?= { var:tv_<io>_leader_char ?= { add_to_list = leaders } } }` block, with Governor's House using `tv_conquest_general_char`, Engineering Department using `tv_great_engineer_char`, and Trade League using `tv_grand_merchant_char`. Eighteen generic actions (6 roles × 3 actions) in `tv_io_leader_actions.txt`: Appoint, Remove, Change. Appointing a chief sets `tv_io_chief_role` on the character, every selector rejects characters already carrying that marker, and remove/change/death cleanup clears it with the role modifier. Each action sets or clears the country variable only — `international_organization_chooses_new_leader` is deliberately NOT called (it triggers the vanilla leader-election process, violating the no-elections design). All TV IOs have `unique = no`; never use `international_organization:type_name` syntax for them — use `every_international_organizations_member_of = { limit = { international_organization_type = international_organization_type:<io_type> } ... }` instead. Arts Exhibition filters candidates by `is_artist = yes`; Academy, Engineering Department, and Trade League exclude artists; Diplomatic Alliance accepts any non-ruler/heir/consort alive adult character; Grand General additionally excludes female characters and artists. Six CMF alerts (`tv_missing_arts_exhibition_chief_alert`, `tv_missing_diplomatic_alliance_chief_alert`, `tv_missing_academy_chief_alert`, `tv_missing_engineering_department_chief_alert`, `tv_missing_trade_league_chief_alert`, `tv_missing_govhouse_chief_alert`) are checked monthly for human players and shown whenever the matching TV IO lacks a living appointed chief; their tooltips direct the player to open the matching organization panel and appoint the chief. **Character title prefixes (Arts Exhibition + Academy + Governor's House + Engineering Department + Trade League):** Appointing/removing leaders also adds/removes zero-stat character modifiers (`tv_chief_artist_role_modifier` / `tv_chief_scientist_role_modifier` / `tv_grand_general_role_modifier` / `tv_great_engineer_role_modifier` / `tv_grand_merchant_role_modifier`). These modifiers trigger title prefixes via generated `src/in_game/common/customizable_localization/character_title.txt` (copied from vanilla and patched by `scripts/in_game/common/customizable_localization/gen_character_title.py`) — the appointed character's name displays as "Chief Artist [Name]", "Chief Scientist [Name]", "Grand General [Name]", "Great Engineer [Name]", or "Grand Merchant [Name]" (首席艺术家/首席科学家/大将军/大工程师/大商人). Modifier definitions are in `src/in_game/common/static_modifiers/tv_io_role_modifiers.txt`. The Diplomatic Alliance leader has no title modifier.
10. **Governor's House IO** (`tv_govhouse`): non-unique IO created by the Conquest Victory M1 reward (200 locations). **Governor system:** characters appointed by the player to govern EU5 regions. Governor bonuses scale dynamically with character attributes, but the exact local modifier is selected by the three **Local Governor Functions** IO laws. Administrative Function options: `tv_governor_max_control` (`local_max_control`, adm × 0.001, default), `tv_governor_monthly_development` (`local_monthly_development`, adm × 0.00001), or `tv_governor_production_efficiency` (`local_production_efficiency`, adm × 0.001). Diplomatic Function options: `tv_governor_build_cost` (`local_build_buildings_cost`, −dip × 0.002, default), `tv_governor_pop_promotion_speed` (`local_pop_promotion_speed_modifier`, dip × 0.005), or `tv_governor_population_growth` (`local_population_growth`, dip × 0.00001). Military Function options: `tv_governor_unrest` (`local_unrest`, −mil × 0.002, default), `tv_governor_levy_size` (`local_army_levy_size_modifier`, mil × 0.002), or `tv_governor_fort_defense` (`local_defensive`, mil × 0.005). Every regional governor also applies `tv_governor_integration_speed` (`local_integration_speed`, ADM × 0.001) to owned locations in the governed region; the Governor's House overview card displays this as a separate left-side slot before the three law-selected function slots. Data model: `tv_govhouse_region_list` is a persistent country variable list of region scopes rebuilt from the country's currently owned locations at IO creation and every monthly governor refresh; `tv_govhouse_region_display_list` is a parallel country variable list of representative owned location scopes used by the GUI to display one row per owned region; `tv_governor_list` remains a persistent country variable list of appointed governor character scopes. Each governor character has `tv_is_governor`, `tv_governed_region` (region scope), `tv_governed_region_capital` (representative owned location retained for compatibility), and pre-computed function display variables (`tv_gov_admin_bonus_type/value`, `tv_gov_dip_bonus_type/value`, `tv_gov_mil_bonus_type/value`). The representative display location mirrors the active governor and copied function values via `tv_govhouse_display_governor_char` plus `tv_govhouse_display_{admin/dip/mil}_bonus_{type/value}`. Monthly_effect in the IO calls `tv_apply_governor_bonuses_effect`, which refreshes modifiers for alive governors, prunes dead/orphaned/lost-region assignments, and then rebuilds the region/display lists; legacy `tv_governed_area` assignments are migrated/cleaned during refresh. Three region-row generic actions: `tv_appoint_governor` (UI passes `scope:target_1` region, select_trigger chooses `scope:target` character), `tv_remove_governor` (UI passes region and the effect finds the assigned governor), and `tv_change_governor` (region parameter + character selection, replacing any existing assignment). The overview-level `tv_auto_assign_governors` action refreshes the region list, then repeatedly chooses one empty owned region and the highest `total_abilities` eligible character until every owned region is filled or no eligible character remains; all governor management actions require an alive Grand General in office. If the Grand General is dismissed or dies, existing governors remain appointed and continue refreshing, but no new governor can be appointed/changed/removed until a successor is appointed. **Grand General:** character leader stored as the independent country variable `tv_conquest_general_char`; managed via generated Appoint/Remove/Change IO leader actions and displayed in the IO header. Newly appointed Grand Generals initialize `tv_gg_loyalty` on the character if absent (nobles 0%, burghers/clergy 10%, crown 30%, other estates 20%); the value is mirrored to `tv_gg_loyalty_value`, does not drift automatically, and lowers the Governor's House power-balance equilibrium by the same amount. A monthly 5% loyalty pulse can fire one of three events: -5 loyalty, +5 loyalty, or a reward demand costing `scale = 1` gold for +5 loyalty with refusal causing -5 loyalty. `tv_govhouse_authority` IO variable (0-100, monthly decay × 0.0005, grows by Grand General's mil × 0.05). The Governor's House overview tab renders the display-location list: each row shows a region icon/name, the separate integration slot, right-aligned ADM/DIP/MIL function entries with formula tooltips, and the current governor as a standard `io_character_card`; Assign/Cancel/Change Governor buttons still pass the row's region scope. The dedicated Grand General tab shows the current variable-backed character, active training progress below the character card, authority value, Appoint/Change/Remove actions, and the existing Train Grand General action.

Governor automation note: `tv_enable_governor_automation` / `tv_disable_governor_automation` toggle the country variable `tv_govhouse_governor_automation_enabled` from the overview `button_checkbox_auto` checkbutton beside `tv_auto_assign_governors`. When enabled, governor death, manual removal, location ownership changes, governor country moves, a governor becoming ruler, and marriage/royal-marriage cases where a governor becomes a consort each run one guarded `tv_govhouse_auto_assign_if_enabled_effect` pass; that helper delegates to `tv_governor_auto_assign_effect` only while a living Grand General is available, so the automation does not rely on monthly polling.

Governor assignment country note: new Local Governor assignments store `tv_governor_country` on the character so event-driven vacancy cleanup can find the original appointing country even if the character's current owner changes.

Governor autonomous task note: `tv_governor_select_yearly_task_effect` sets one active task per regional governor. The six tasks are restore prosperity, integrate region, convert culture, convert religion, strengthen recruitment, and strengthen order; integration is one of these autonomous tasks, not a separate always-on governor modifier. The task values scale from ADM/DIP/MIL, are displayed through `tv_govhouse_display_task_type/value`, and are refreshed into region-wide location modifiers by `tv_governor_apply_modifiers_effect`.

Regional Governor random events: `tv_govhouse_governor_events_monthly_pulse` runs through the generated `monthly_country_pulse` registry and only triggers if at least one living governor has an owned governed region. It uses a single monthly country-level roll with 180 no-event weight plus 20 event weights, giving a 10% total monthly chance. Events `tv_govhouse.100`-`.119` each pick one random eligible governor in `immediate`, save the governor, governed region, and a representative owned location as event scopes, then apply one-year region-wide location modifiers and/or immediate or one-year country effects. The 20-event set covers two-choice good events, one-option good events, two-choice tradeoff events, two-choice bad events, and one-option bad events.

Governor's House laws: the IO uses the vanilla IO Laws tab. Six generated core law groups (`tv_govhouse_expedition_goal_law`, `tv_govhouse_governor_appointment_law`, `tv_govhouse_local_autonomy_law`, `tv_govhouse_religious_mission_law`, `tv_govhouse_frontier_military_law`, `tv_govhouse_imperial_integration_law`) each have four effect-bearing country policies. `tv_govhouse_grand_general_power_law` sits in the new `tv_govhouse_grand_general` category: Marching Orders gives newly appointed Grand Generals +10% initial loyalty, Garrison Orders makes governor task/function effects 20% stronger, and Supreme Command gives newly appointed Grand Generals -10% initial loyalty while doubling General Tactics and War Plan progress gain. Four generated Local Governor Function laws sit in `tv_govhouse_governor_functions`: the ADM/DIP/MIL function laws decide which scaled local modifiers governors apply, while `tv_govhouse_governor_duty_tendency_law` adds +0.30 task-selection weight to one of the six autonomous governor tasks. All eleven laws keep `requires_vote = yes`, `has_parliament = no` hides the separate IO Parliament tab, and the IO starts with no initial policies. There is no monthly default-law backfill.

Governor's House War Tent: requires an alive Grand General. The custom War Tent tab has three units: General Tactics, War Plan, and Main Actions. On war entry, `tv_govhouse_war_entry_effect` proposes one random tactic from the eleven-tactic pool and fires `tv_govhouse.40`; the player approves or rejects it. While continuously at war, the monthly pulse has a 5% chance to propose a replacement tactic, excluding the tactic already active. Approved tactics are applied as country-scoped static modifiers and are cleared when the country leaves all wars or loses its Grand General; this approximates general tactics because the army-wide combat modifiers did not apply reliably from the stored character path. Supreme Command doubles the applied tactic modifier size, including the Capital Raid location modifier. War plans are mutually exclusive: offensive plans select a target country and, while at war with that target, scale Siege Ability by progress x 0.2% and Discipline by progress x 0.1%; defensive plans need no target and, while in any defensive war, scale Fort Defense by progress x 0.2% and Military Tactics by progress x 0.005. On war entry, applicable offensive/defensive plan bonuses are activated through `tv_govhouse.41` / `tv_govhouse.42` events instead of being granted directly by the war-entry effect. Plan progress increases monthly by Grand General adm x 0.02 up to 100, doubled under Supreme Command; aborting/changing target resets it, and peace with an offensive plan target aborts that plan.

8. **Global Victory Leaderboard** — Monthly ranking of the top 5 countries by their best single-path progress (0–100 bar value). Per-country: `tv_best_path` (0–5), `tv_best_progress_pct`, `tv_best_milestone`, `tv_best_score` computed via `tv_update_best_path_effect` in `monthly_country_pulse`. Situation stores rank-1..5 country references (`tv_rank_1_country`–`tv_rank_5_country`) via `ordered_in_global_list` cascading-exclusion pattern. Displayed in Section 3 with flag, country name, leading-path circle icon, milestone count, and score text.

Governor's House eligibility/death note: new governors and Grand Generals exclude female characters and artists at the selection/action-effect entry points. There is no monthly legacy-eligibility cleanup, to keep the IO monthly path focused on active governor bonus refresh. `tv_govhouse_governor_death` is registered under `on_character_death`, fires `tv_govhouse.1`, removes the dead governor from `tv_governor_list`, clears their region modifiers, and rebuilds the display lists immediately.

Grand General training note: `tv_train_grand_general` applies the one-year `tv_grand_general_training_modifier` country modifier defined in `src/in_game/common/static_modifiers/tv_govhouse_modifiers.txt` (+10% `court_spending_cost_modifier`), tracks `tv_gg_training_months` on the Grand General via monthly pulse, mirrors it to the country-side GUI display variable `tv_gg_training_progress`, resolves at 12 months through `tv_training_resolve_effect`, and routes to skill gain, trait-path upgrade, injury, or death events before clearing state.

Grand General death notification note: natural Grand General deaths trigger `tv_govhouse.2` from `tv_govhouse_char_death`; training-caused deaths set `tv_gg_training_death_notified` before `kill_character = this` so the already-shown training death event is not duplicated.

Governor's House balance-of-power backlash note: `tv_govhouse_power_balance` is an IO variable in the 0..1 range and is checked monthly by `tv_govhouse_negative_monthly_pulse` (registered through `data/pulse_registry.yaml`). `tv_govhouse_power_equilibrium` recalculates monthly from a -20% base, the Grand General's share of combined ruler/general abilities, `tv_govhouse_power_consolidation`, and a subtraction equal to the mirrored Grand General loyalty value; `tv_govhouse_power_balance` then moves toward that equilibrium by up to 0.6% per month. Below 25% no backlash events fire. At 25%/50%/75% the pulse uses a single `random_list` roll with a 300 total weight so at most one backlash event can appear per month, and each eligible event has weight 1/300. Events `tv_govhouse.10`-`.13` are tier 1, `.20`-`.25` are tier 2, and `.30`-`.34` are tier 3. Every backlash event has exactly two options: accept the negative effect, or pay an alternative resource and call `tv_govhouse_reduce_power_balance_effect` to lower the balance by 5 percentage points. Tier 2 adds rare art/artist losses; tier 3 upgrades the local satisfaction loss to an area-wide effect and adds a cabinet crisis. The Governor's House GUI shows right-aligned fixed-width 25/50/75/100% markers and four warning rows below the Balance of Power bar for the <25, 25-49, 50-74, and 75-100 segments.

Grand General replacement cost note: generated Remove/Change Grand General actions call `tv_govhouse_pay_grand_general_replacement_cost_effect` before resetting `tv_govhouse_power_consolidation`; the cost is Government Power equal to `tv_govhouse_power_consolidation * 20`, so a fully consolidated Grand General costs 20 government power to remove or replace.

## IO Architecture Design Decisions

The character-led TV IOs (`tv_arts_exhibition`, `tv_diplomatic_alliance`, `tv_academy_of_sciences`, `tv_govhouse`, `tv_engineering_department`, `tv_trade_league`) share invariants that must never be broken.

### 1. leader_country = founding country, locked at creation

`on_creation = { set_leader_country = scope:actor }` runs once when the IO is created. `leader_country` is not reassigned by normal gameplay afterward; the only exception is the manual recovery action `tv_reinitialize_mod`, exposed at the top of the Victory Situation panel, which recreates missing victory-path IOs where possible and reassigns joined TV IO instances back to the current country. It controls IO permissions, `is_leader_of_international_organization` trigger, and `leader_country.modifier:` chained access in `monthly_change` scripted values.

### 2. No elections — leader_change_trigger_type = none is mandatory

All TV character-led IO type definitions keep `leader_change_trigger_type = none` and `leader_change_method = none`. These prevent the engine from reassigning `leader_country` automatically. `international_organization_chooses_new_leader` is **globally banned** from all TV IO-related code — this effect triggers the vanilla election process. Any call to it violates the no-elections design and risks silently reassigning `leader_country` to a different country.

### 3. Great person characters are independent country variables, not the vanilla ruler

The character displayed in the IO header is stored as a country variable on the `leader_country`:
- Arts Exhibition: `tv_arts_exhibition_leader_char`
- Diplomatic Alliance: `tv_diplomatic_alliance_leader_char`
- Academy of Sciences: `tv_academy_leader_char`
- Governor's House: `tv_conquest_general_char`
- Engineering Department: `tv_great_engineer_char`
- Trade League: `tv_grand_merchant_char`

These are set/cleared exclusively by the generated generic actions in `tv_io_leader_actions.txt`. The vanilla `leader_country.GetGovernment.GetRuler` and `appointed_leader.*` are different concepts and must not be used in TV IO calculations.

### 4. IO variable monthly_change uses chained character attribute access

All IO variable `monthly_change` scripted values that depend on the appointed great person must use `leader_country.var:tv_xxx_leader_char.attribute` chained access — never `appointed_leader.attribute`. Examples:
- CORRECT: `value = leader_country.var:tv_academy_leader_char.adm`
- FORBIDDEN: `value = appointed_leader.adm`

### 5. Header display uses blockoverride

All character-led IO panels use `blockoverride` on their header template to display the appointed character (read from the country variable via `GetPlayer.MakeScope.GetVariable(...).GetCharacter`). Never use the vanilla `GetLeaderCountry.GetGovernment.GetRulerOrRegent` accessor chain.

### 6. All TV IOs have unique = no

Each country pursuing the relevant victory path creates its own independent IO instance. The `international_organization:type_name` scope link is **invalid** for non-unique types. Always use `every_international_organizations_member_of = { limit = { international_organization_type = international_organization_type:<io_type> } ... }`.

## Directory Structure

```
src/
├── .metadata/metadata.json                mod ID, version, target game version
├── in_game/
│   ├── common/
│   │   ├── situations/                    towards_victory_situations.txt  [GENERATED]
│   │   ├── scripted_triggers/             towards_victory_triggers.txt  [GENERATED]
│   │   │                                  tv_research_subprocess_triggers.txt  [MANUAL — Research Mechanism conditions + Frontier-tech availability gate]
│   │   │                                  tv_medicine_triggers.txt  [MANUAL — tv_has_health_condition_trigger (10 bad health traits)]
│   │   │                                  tv_arts_exhibition_triggers.txt  [MANUAL — Chief Artist busy-state mutual exclusion triggers]
│   │   │                                  tv_trade_league_triggers.txt  [MANUAL — Grand Merchant busy-state mutual exclusion triggers]
│   │   ├── scripted_effects/              towards_victory_effects.txt  [GENERATED]
│   │   │                                  towards_victory_leaderboard.txt  [MANUAL — leaderboard effects]
│   │   │                                  tv_advance_unlock_effects.txt  [GENERATED — tv_unlock_advance_<id>_effect (set_variable + research_advance)]
│   │   │                                  tv_research_subprocess_effects.txt  [MANUAL — Research Mechanism lifecycle effects; strict-earliest-age target proposal]
│   │   │                                  tv_medicine_effects.txt  [MANUAL — Medicine treatment + meditation cleanup/progress/trait-removal effects]
│   │   │                                  tv_arts_exhibition_effects.txt  [MANUAL — artist tour helpers + local exhibition lifecycle/effects]
│   │   │                                  tv_trade_league_effects.txt  [GENERATED — Trade League creation plus rolling trade-income chart history, annual monopoly market markers, monthly route-control calculations, and temporary action effects]
│   │   │                                  tv_io_chief_alert_effects.txt  [MANUAL — per-IO CMF alert sync plus alert-click request tracking]
│   │   ├── effect_localization/           tv_arts_exhibition_effects.txt  [MANUAL — custom_description text mapping for local exhibition enthusiasm IO variable changes]
│   │   │                                  tv_engineering_department_effects.txt  [MANUAL — custom_description text mapping for Engineering Department Domestic Support IO variable changes]
│   │   ├── scripted_guis/                  tv_io_chief_alert_bridge.txt  [MANUAL — hidden bridge helper that clears alert-click IO open requests after the GUI opens the target IO]
│   │   ├── static_modifiers/              towards_victory_modifiers.txt  [GENERATED]
│   │                                  towards_victory_location_modifiers.txt  [MANUAL — governor location modifiers + tv_exhibition_success_location_modifier + tv_local_exhibition_triumph_location_modifier]
│   │                                  tv_research_modifiers.txt  [MANUAL — Concentrated Research penalty + D random-event cabinet modifiers]
│   │                                  tv_engineering_department_wonder_modifiers.txt  [MANUAL — temporary ceremony penalties and national completion celebration]
│   │                                  tv_engineering_department_wonder_mechanics_modifiers.txt  [GENERATED ? all 52 generic wonders' permanent base and ceremony country modifiers]
│   │   ├── building_types/                towards_victory_buildings.txt  [GENERATED — Academy of Sciences building chain tiers 1–5]
│   │   │                                  tv_arts_exhibition_buildings.txt  [MANUAL — event-only tv_local_arts_exhibition building]
│   │   │                                  tv_engineering_department_buildings.txt  [MANUAL — Engineering Department wonder support buildings only]
│   │   ├── goods/                         tv_wonder_goods.txt  [MANUAL — Wonder Materials produced good for Engineering Department construction supply]
│   │   ├── goods_demand/                  tv_trade_league_demands.txt  [GENERATED — virtual demand entries for Trade League monopoly actions]
│   │   ├── scripted_effects/              ...
│   │   │                                  tv_govhouse_effects.txt  [MANUAL — governor system, autonomous tasks, Grand General loyalty/training/power laws, replacement cost, War Tent, and balance-of-power backlash effects]
│   │   ├── generic_actions/               tv_govhouse_actions.txt  [MANUAL — tv_train_grand_general + governor assign/remove/change/auto-assign + War Tent actions]
│   │   │                                  tv_arts_exhibition_actions.txt  [MANUAL — foreign tours + persistent local exhibition actions]
│   │   │                                  tv_io_leader_actions.txt  [GENERATED — 18 IO leader Appoint/Remove/Change actions (6 roles × 3 actions)]
│   │   │                                  tv_trade_league_actions.txt  [GENERATED — expel member plus per-good monopoly selection, virtual demand/supply, and embargo actions]
│   │   │                                  tv_reinitialize_actions.txt  [MANUAL — Victory Situation recovery button; recreates missing IOs and resets TV IO leader_country to player]
│   │   ├── country_interactions/          tv_invite_to_trade_league.txt  [MANUAL — Trade League invitation country interaction]
│   │   ├── laws/                          tv_alliance_laws.txt  [GENERATED — 5 law categories × 4 policy levels for the Diplomatic Alliance IO]
│   │   │                                  tv_academy_laws.txt  [GENERATED — 7 Academy law categories: 6 effect-bearing under tv_science_factions + leader gender under tv_academy_leadership; changes keep IO policy votes]
│   │   │                                  tv_trade_league_laws.txt  [GENERATED — 5 three-level Trade League law groups with AI vote bias; IO starts lawless and changes keep IO policy votes]
│   │   │                                  tv_govhouse_laws.txt  [GENERATED — 6 core Governor's House law groups + Grand General Power + 4 Local Governor Function/Duty laws; changes keep IO policy votes]
│   │   │                                  tv_arts_exhibition_laws.txt  [GENERATED — 8 Arts Exhibition law groups with vote-based policies]
│   │   │                                  tv_research_actions.txt  [MANUAL — Research Mechanism and Academy Medicine IO panel actions]
│   │   ├── estate_privileges/             tv_engineering_department_privileges.txt  [MANUAL — Hydraulic Artisan Guild Burghers privilege used by the Hydraulic Workshop ceremony]
│   │   ├── generic_action_ai_lists/       tv_arts_exhibition_list.txt  [MANUAL]
│   │   │                                  tv_diplomatic_alliance_list.txt  [MANUAL]
│   │   │                                  tv_engineering_department_list.txt  [MANUAL]
│   │   │                                  tv_research_actions_list.txt  [MANUAL]
│   │   │                                  tv_trade_league_list.txt  [GENERATED — Trade League leader and monopoly actions]
│   │   ├── international_organizations/   tv_govhouse.txt  [MANUAL — Governor's House IO; vanilla Laws tab with vote-based policies; monthly_effect calls tv_apply_governor_bonuses_effect; Grand General leader via tv_conquest_general_char]
│   │   │                                  tv_diplomatic_alliance.txt  [MANUAL — character leader via tv_diplomatic_alliance_leader_char]
│   │   │                                  tv_arts_exhibition.txt  [MANUAL — character leader (artist), International Influence, Local Exhibition Enthusiasm, no initial laws]
│   │   │                                  tv_academy_of_sciences.txt  [MANUAL — character leader via tv_academy_leader_char; C/D IO variables; no Academy parliament; D uses effective ADM]
│   │   │                                  tv_engineering_department.txt  [MANUAL — character leader via tv_great_engineer_char; wonder construction state variables]
│   │   │                                  tv_trade_league.txt  [GENERATED — character leader via tv_grand_merchant_char; monopoly state lives on leader_country]
│   │   ├── international_organization_special_statuses/ tv_diplomatic_alliance.txt  [MANUAL — Diplomatic Alliance special status]
│   │   │                                  tv_trade_league.txt  [MANUAL — Trade League member capacity penalty and leader per-member bonus]
│   │   ├── parliament_types/              tv_alliance_parliament.txt  [MANUAL — tv_alliance_assembly IO parliament type]
│   │   │                                  tv_academy_parliament.txt  [MANUAL — dormant tv_academy_assembly type, not attached while Academy parliament is hidden]
│   │   └── on_action/                     towards_victory_yearly.txt  [GENERATED]
│   │                                      towards_victory_leaderboard.txt  [MANUAL — monthly_country_pulse hooks]
│   │                                      tv_research_on_action.txt  [MANUAL — B check, A and D random event pulses]
│   │                                      tv_medicine_on_action.txt  [MANUAL — tv_med_discovery_pulse (1%) and tv_med_accident_pulse (5%) sub-actions]
│   │                                      tv_trade_league_on_action.txt  [MANUAL - Trade League leader-country monthly monopoly maintenance pulse]
│   │                                      tv_io_chief_alert_on_action.txt  [MANUAL — monthly CMF alert sync and cmf_on_callback click handling]
│   │                                      tv_cmf_suppressions.txt  [GENERATED - unreachable CMF suppression block for intentional static warning noise]
│   ├── events/                            towards_victory_{conquest,prosperity,trade,diplomatic,cultural,science}_events.txt (one namespace per category — EU5 event IDs must be `<ns>.<int>` with exactly one dot)
│   │                                      tv_research_events.txt  [MANUAL — namespace tv_research; events .1/.10/.11/.19/.20/.30]
│   │                                      tv_research_d_events.txt  [GENERATED — namespace tv_research; D random events .100-.129]
│   │                                      tv_medicine_events.txt  [MANUAL — namespace tv_medicine; .1 discovery / .2 accident / .3 recovery / .4 patient died]
│   │                                      tv_arts_exhibition_events.txt  [MANUAL — artist tours + local exhibition start/end/monthly/action events]
│   │                                      tv_govhouse_events.txt  [MANUAL - Governor's House notifications plus balance-of-power backlash events]
│   │                                      tv_engineering_department_events.txt  [MANUAL — Great Engineer death, collapse, debate, and construction notifications]
│   └── gui/
│       │                                  location_window.gui  [GENERATED - vanilla location window copy with TV wonder badge overlay and tooltip]
│       ├── panels/situation/              tv_victory_situation.gui  [MANUAL]
│       └── panels/organization/           tv_govhouse.gui  [MANUAL — overview owned-region display list; dedicated Grand General tab with Balance of Power bar, segment warnings, variable-backed portrait/status/actions]
│                                          tv_diplomatic_alliance.gui  [MANUAL — custom HRE-style IO panel; Advocator header controls]
│                                          tv_arts_exhibition.gui  [MANUAL — touring overview controls, Chief Artist header controls, Arts Exchange resolutions tab, vanilla Laws tab, local exhibition tab, active exhibition action group]
│                                          tv_engineering_department.gui  [MANUAL — Great Engineer management plus staged debate, survey, organization/logistics, construction, and ceremony/finalization UI]
│                                          tv_io_chief_alert_bridge.gui  [MANUAL — hidden 0x0 CMF bridge widget that opens matching IO panels for alert-click requests]
│                                          tv_academy_of_sciences.gui  [GENERATED — Research Mechanism IO panel; Chief Scientist header controls, overview staged target/preparation/concentrated-research cards + research actions, Philosophy debate shell tab, Medicine tab treatment/meditation actions]
│                                          tv_trade_league.gui  [GENERATED — Trade League overview rolling chart, laws, intelligence, and monopoly tabs]
└── main_menu/common/
    └── game_concepts/                    tv_game_concepts.txt  [MANUAL — custom Victory/IO concepts]
└── main_menu/localization/
    ├── english/                           towards_victory_l_english.yml  [GENERATED]
    │                                      towards_victory_leaderboard_l_english.yml  [MANUAL]
    │                                      tv_diplomatic_alliance_l_english.yml  [MANUAL]
    │                                      tv_academy_of_sciences_l_english.yml  [MANUAL — Academy IO and law localization]
    │                                      tv_research_l_english.yml  [MANUAL — Research Mechanism and Academy Medicine keys + events]
    │                                      tv_arts_exhibition_l_english.yml  [MANUAL — Arts Exhibition IO, laws, tour, and local exhibition keys]
    │                                      tv_engineering_department_l_english.yml  [MANUAL — Engineering Department IO, Great Engineer, and wonder UI keys]
    │                                      tv_trade_league_l_english.yml  [MANUAL — Trade League IO, laws, Grand Merchant, invitation, and monopoly UI keys]
    │                                      tv_trade_league_monopoly_l_english.yml  [GENERATED — monopoly category, fixed action, and virtual demand localization]
    │                                      tv_game_concepts_l_english.yml  [MANUAL — custom game concept localization]
    └── simp_chinese/                      towards_victory_l_simp_chinese.yml  [GENERATED]
                                           towards_victory_leaderboard_l_simp_chinese.yml  [MANUAL]
                                           tv_diplomatic_alliance_l_simp_chinese.yml  [MANUAL]
                                           tv_academy_of_sciences_l_simp_chinese.yml  [MANUAL — Academy IO and law localization]
                                           tv_research_l_simp_chinese.yml  [MANUAL — Research Mechanism and Academy Medicine CN keys + events]
                                           tv_arts_exhibition_l_simp_chinese.yml  [MANUAL — Arts Exhibition IO, laws, tour, and local exhibition keys]
                                           tv_engineering_department_l_simp_chinese.yml  [MANUAL — Engineering Department IO, Great Engineer, and wonder UI keys]
                                           tv_trade_league_l_simp_chinese.yml  [MANUAL — Trade League IO, laws, Grand Merchant, invitation, and monopoly UI keys]
                                           tv_trade_league_monopoly_l_simp_chinese.yml  [GENERATED — monopoly category, fixed action, and virtual demand localization]
                                           tv_game_concepts_l_simp_chinese.yml  [MANUAL — custom game concept localization]
```

```
wonder_localization_editor_web/
├── __main__.py                           web launcher
├── server.py                             FastAPI app and static-file mount
├── service.py                            data loader, save flow, and regen bridge
└── static/                               web UI assets
```

Engineering Department wonder generated files: `src/in_game/common/scripted_triggers/tv_engineering_department_wonder_mechanics_triggers.txt`, `src/in_game/common/scripted_effects/tv_engineering_department_wonder_mechanics_effects.txt`, `src/in_game/common/building_types/tv_engineering_department_wonder_mechanics_buildings.txt`, and `src/in_game/common/static_modifiers/tv_engineering_department_wonder_mechanics_modifiers.txt` cover shared mechanics for all 52 generic wonders from `data/wonders.yaml` plus the 101 unique historical wonders from `data/unique_wonders.yaml`, with shared mechanical values in `data/wonder_mechanics.yaml`. `src/in_game/common/scripted_effects/tv_wonder_index_effects.txt` is the generated global index: existing numeric wonder ids remain the canonical runtime ids; wonder attribute variable-map keys are the canonical numeric wonder ids directly, including required construction progress; ritual branch ids reserve `wonder_id * 100 + style`; ritual mode/cost/listener/completion flags are cached from ritual-id maps; suitability row ids reserve `mechanic_id * 10 + row`; `tv_wonder_mechanic_id_to_suitability_row_count` stores reveal caps by mechanic id; and game-start/save-load hooks directly rebuild the current global maps without version-gated compatibility routers. `tv_wonder_module_effects.txt` uses captured helper/module/final `building_type` maps for locked-wonder module construction, completed-module combination, unit sync from buildings, helper application, module materialization, locked final-state restoration, and saved priority resume/upgrade candidates. Candidate rows are country-scoped maps keyed by `tv_wonder_priority_next_candidate_id`: `tv_wonder_priority_candidate_site_by_id`, `tv_wonder_priority_candidate_wonder_by_id`, and `tv_wonder_priority_candidate_mode_by_id`; lifecycle refreshes rewrite the row for the affected country/site/wonder, and Great Engineer proposal refresh validates only saved rows for ownership, base site rules, modules, or final-building scale headroom. Survey runtime state follows the same map schema: country reveal state uses `tv_wonder_suitability_revealed` by mechanic id, country actual rows use `tv_wonder_suitability_actual` by suitability row composite id, and location completed-survey caches use `tv_wonder_survey*` maps by wonder id or suitability row composite id. Final-building local effects are strict shared maps: generic final branches all receive the same `final_local` authored package, and unique final buildings inherit that prototype authored package at x2 through `base_effect_multiplier`. The generic side now includes generated timed ritual burden/blessing modifiers, annex ritual buildings, decoration reward effects, and split confirmation triggers/actions, while the unique side keeps one auto-locked ritual branch per wonder but now supports generated immediate/timed/auxiliary-building start modes, listener-driven completion checks, and optional confirmation/start/progress/completion scripts. The action/concept/localization/GUI generators (`tv_engineering_department_wonder_mechanics_actions.txt`, `tv_engineering_department_wonder_mechanics_concepts.txt`, `tv_engineering_department_wonder_mechanics_l_*.yml`, and `data/generated_fragments/tv_engineering_department_wonder_mechanics.gui`) cover all 52 generic wonders and ids 101-201 unique wonders through the same generated path; the concepts generator emits id-based `tv_wonder_display_<id>` name route concepts plus `tv_wonder_display_image_<id>` image route concepts, and the localization generator mirrors wonder concept names/descriptions plus `STATIC_MODIFIER_NAME_tv_wonder_display_<id>_level_<level>` aliases for dynamic GUI lookups. The GUI fragment still merges into `src/in_game/gui/panels/organization/tv_engineering_department.gui` through the merge script. `src/in_game/gui/location_window.gui` is generated from the vanilla location window and adds the TV location-scene wonder overlay: refresh effects project numeric id/level/ritual-style values into three compact slots and five tooltip slots, with both views filled by unique wonders first and generic wonders second. Unique wonders project onto their fixed historical locations at level 0 even before construction, while generic wonders project only after a final wonder building exists. Completion and upgrade paths refresh the current location display state, while the world refresh helper is reserved for startup and save-load initialization.

Governor's House War Tent file notes: `src/in_game/common/scripted_effects/tv_govhouse_effects.txt` contains the tactic, war-plan progress, modifier refresh, war-entry, war-end, monthly War Tent effects, and plan-activation event routing; `src/in_game/common/casus_belli/tv_govhouse_casus_belli.txt` defines the event-granted War Plan CB that uses the vanilla `superiority_rival` wargoal; `src/in_game/common/generic_actions/tv_govhouse_actions.txt` contains the offensive/defensive plan, abort, and change-target actions; `src/in_game/common/on_action/tv_govhouse_war_tent_on_action.txt` wires war declaration, war join, and war-ending hooks; `src/in_game/events/tv_govhouse_events.txt` contains the tactic approval event, offensive/defensive plan activation events, and the completed offensive-plan CB event; `src/in_game/gui/panels/organization/tv_govhouse.gui` now separates the overview, Grand General tab, and War Tent custom tab.

### Main Menu Graphics

`src/main_menu/common/modifier_icons/` contains TV modifier type icon mappings. `towards_victory_modifier_icons.txt` maps custom price, goods, and IO special-status modifier types to close vanilla `gfx/interface/icons/modifier_types/*.dds` assets, so the engine can resolve their UI icons without custom DDS files.

`src/main_menu/gfx/interface/icons/international_organizations/` contains the six TV IO `GetIcon` assets:
`tv_govhouse.dds`, `tv_diplomatic_alliance.dds`, `tv_arts_exhibition.dds`, `tv_academy_of_sciences.dds`, `tv_engineering_department.dds`, and `tv_trade_league.dds`.
`tv_trade_league.dds` reuses the Trade Victory `gfx/interface/icons/flat_icons/trade.dds` asset under the IO type key. These assets keep shared IO title/list/tooltip UI from falling back to the generic international organization icon.

`src/main_menu/gfx/interface/icons/laws/` contains seven Academy law icon assets copied from vanilla advance icons and six Governor's House law icon assets copied from related vanilla law icons for the original Governor's House policy groups.

`src/main_menu/gfx/interface/icons/towards_victory/wonders/` contains the Engineering Department wonder image assets. Besides per-wonder DDS files, the fixed event-stage asset set is `tv_wonder_construction.dds`, `tv_wonder_foundation.dds`, `tv_wonder_body.dds`, `tv_wonder_function.dds`, `tv_wonder_decoration.dds`, and `tv_wonder_complete.dds`. The Engineering Department overview tab displays the matching image for the current Great Engineer proposal before lock-in, then keeps displaying the locked target wonder during later construction stages. The illustration area is hidden only when there is no proposal and no locked target wonder. Location-window dynamic images resolve through `GetConceptTexture(Concatenate('tv_wonder_display_image_', id))` route concepts whose generated textures point explicitly at this `gfx/interface/icons/` directory.

`src/main_menu/gfx/interface/icons/trade_goods/` and `src/main_menu/gfx/interface/icons/trade_goods/illustrations/` currently hold the `tv_wonder_materials` trade-good icon pair. `src/main_menu/gfx/interface/icons/buildings/` contains the generated `tv_wonder_labor_camp.dds` Wonder Worksite building icon, named after the building type so vanilla `GetBuildingIcon` resolution applies it automatically. The repository now also includes a reusable DDS icon generation workflow in `scripts/generate_dds_icon.py` plus `generate_dds_icon_config.json`: it selects exactly one generation target (`trade_good_icon`, `trade_good_illustration`, or `building_icon`), can apply target-specific asset names and image prompt overrides, refines a short natural-language request into a target-aware production prompt for API targets, uploads that target's same-type style reference DDS/PNG files, then writes one configured DDS output with enforced dimensions, file-size limits, and optional mipmaps. It can also re-convert an existing generated PNG into the selected DDS target with `--convert-existing-png` without calling the image API, and target configs may enable `local_template` rendering for deterministic source-DDS transformations such as the Wonder Worksite icon based on vanilla `construction_center.dds`. `generate_wonder_image.py` plus `generate_wonder_image_config.json` is the batch wonder-image helper: it prepends the six fixed stage tasks from `fixed_wonder_stage_image_prompts`, then reads generic prompts from `data/wonder_image_prompts.yaml` and unique prompts from `data/unique_wonders.yaml`, writes PNG metadata under `data/generated_wonders/`, writes single-level DDS files under `src/main_menu/gfx/interface/icons/towards_victory/wonders/`, can rebuild existing wonder DDS files with `--convert-existing-assets` without calling the image API, and keeps the JSON config limited to API/runtime selection, per-task overrides, and overwrite defaults.

`unique_wonders_site/` is a root-level static, read-only atlas for the 52 generic Engineering Department wonders and 101 unique historical wonders. It reuses the map tiles and `locations_index.json` from `reference_mods/national_destinies_site/dist`, generates `dist/data/unique_wonders.json` from `data/unique_wonders.yaml`, `data/wonders.yaml`, `data/wonder_mechanics.yaml`, `data/wonder_localization.yaml`, and `data/index/{modifier,trigger}_localization.json`, and presents a left-side switch between Generic Wonders and Unique Wonders. Unique wonders render searchable map pins grouped by location; generic wonders have no map pins and open their right-side detail panel only from the left-side generic wonder list. Both modes show localized names/descriptions, localized construction-requirement rows parsed from site triggers, and localized modifier/effect rows matching the wonder editor's modifier/reward display style.

## AI Workflow Knowledge

AI workflow state is maintained as project source, not by manual human follow-up. `scripts/ai_context.py` builds the task-scoped context from changed or explicit files, generated-file metadata, `docs/knowledge/anti_patterns.yaml`, short domain risk cards in `docs/knowledge/risk_cards/`, and content markers for `variable_map` / `global_variable_map` / `local_variable_map` usage. Current registered risk cards: `events.md`, `generic_actions.md`, `gui.md`, `international_organizations.md`, and `on_action.md`; event files route to the events card because option tooltips can pre-evaluate option effect chains, including `hidden_effect`, while IO definitions, IO laws, and country interactions that find or mutate TV IOs route to the international-organization card because IO scopes are high risk. Files containing variable-map content route to the local Variable maps documentation and prioritize the `variable_map_scope_link_used_direct_rhs` anti-pattern because direct quoted map lookups are unsafe as RHS/effect parameters. Anti-pattern entries use `detectability` to distinguish safe regex lint (`lint`), parser/semantic rules (`needs_parser`), and AI-facing warnings (`advisory`). `scripts/validate.py` enforces safe lint rules, emits generic-action pre-evaluation and interaction-target chooser warnings, rejects visible generic-action calls to registered hidden-only cleanup helpers, emits IO policy AI-scope warnings/errors, fails duplicate main-menu localization keys within the same language pack, rejects dotted event references whose numeric event ID is `>= 10000`, checks that risk cards remain registered in `ai_context.py`, checks that each TV IO type has a matching `main_menu/gfx/interface/icons/international_organizations/<io_type>.dds` GetIcon asset, and checks that TV does not stack a second direct `effect` onto hardcoded vanilla on_action hooks such as `on_annex` or `on_winning_war`. `scripts/test_lint_rules.py` runs fixture tests from `tests/fixtures/anti_patterns/<rule_id>/` for selected lint regexes.

For Engineering Department wonders, remember the noncapital building rule: EU5 building `modifier` / `raw_modifier` are local, while `capital_country_modifier` only applies country effects if the building is in the capital. Since wonders can be outside the capital, final wonder buildings must contain only designed local effects, and all national/global effects must be applied through permanent country modifiers during finalization.

Wonder Construction random events are data-driven from `data/wonder_construction_events.yaml`. The generated outputs are `src/in_game/events/tv_wonder_construction_events.txt`, `src/in_game/common/scripted_effects/tv_wonder_construction_event_effects.txt`, `src/in_game/common/scripted_triggers/tv_wonder_construction_event_triggers.txt`, and the matching English/Simplified Chinese localization files. All generated construction-random events use the static `tv_wonder_construction.dds` event image. Hand-maintained Engineering Department wonder events also default to `tv_wonder_construction.dds`, except module synthesis uses `tv_wonder_complete.dds`, body/function/decoration module-complete split events use `tv_wonder_body.dds`, `tv_wonder_function.dds`, and `tv_wonder_decoration.dds`, and finalization/world-news events use the completed wonder's own DDS. Re-run all five `gen_tv_wonder_construction_events*` generators after changing token values, event categories, weights, eligibility rules, or flavor text.

## Script Reference

| Script | Input(s) | Output(s) | When to run |
|---|---|---|---|
| `scripts/validate.py --changed` | src/ mod files + data/generated_files.yaml + data/validation_baseline.yaml + vanilla-copy integrity checks + localization duplicate-key scan | Console/JSON report (exit 0/1); new unbaselined warnings fail validation | Before launching game |
| `scripts/ai_context.py --changed/--files` | git changed files or explicit paths + data/generated_files.yaml + risk cards + anti_patterns.yaml + variable-map content markers | Compact AI task context, required risk-card list, and required variable-map reads | At AI session start or before editing a task domain |
| `scripts/gen_brief.py` | anti_patterns.yaml + valid_enums.yaml + PROJECT_OVERVIEW.md | docs/knowledge/BRIEF.md | After editing any knowledge YAML |
| `scripts/test_lint_rules.py` | docs/knowledge/anti_patterns.yaml + tests/fixtures/anti_patterns/ | Fixture test report for anti-pattern lint regexes | After adding or changing `detectability: lint` rules |
| `scripts/gen_index.py` | reference_game_files + src/ | data/index/*.txt + data/index/modifier_localization.json + data/index/trigger_localization.json | Run by gen_brief.py automatically |
| `scripts/gen_wonder_editor_catalog.py` | reference_game_files/game/in_game/events + reference_game_files/game/main_menu/common/static_modifiers + reference_game_files/game/main_menu/common/modifier_type_definitions | data/wonder_editor_catalog.yaml | After updating vanilla reference files; refreshes wonder-editor country/local options from vanilla event-applied static modifiers and Style 3 instant reward options |
| `scripts/gen_scaffold.py --type X --name Y` | --type argument | Scaffold .txt/.yml file | When creating a new EU5 file |
| `scripts/generate_dds_icon.py` | generate_dds_icon_config.json + optional generate_dds_icon.local.json + selected target style DDS/PNG references or local template source DDS | data/generated_icons/*_<target>.png/json + one configured DDS target, single-level or mipmapped by target config | When creating or regenerating a trade-good icon, trade-good illustration, or building icon from a prompt and same-type style reference; when generating a configured local-template icon such as the Wonder Worksite; or when re-converting an existing generated PNG with `--convert-existing-png` |
| `generate_wonder_image.py` | generate_wonder_image_config.json + optional generate_wonder_image.local.json | data/generated_wonders/*.png/json + src/main_menu/gfx/interface/icons/towards_victory/wonders/*.dds as single-level DDS files | When creating or regenerating fixed stage images and Engineering Department wonder images from `data/wonder_image_prompts.yaml` + `data/unique_wonders.yaml`, or rebuilding existing wonder DDS files with `--convert-existing-assets` |
| `unique_wonders_site/scripts/build/build_site.py` | reference `national_destinies_site/dist` map assets + generated generic/unique wonder atlas data | unique_wonders_site/dist/data/locations_index.json + unique_wonders_site/dist/tiles/ + unique_wonders_site/dist/data/unique_wonders.json | After changing generic/unique wonder data, shared wonder mechanics/localization data, or reference map assets |
| `unique_wonders_site/scripts/build/build_wonders.py` | data/unique_wonders.yaml + data/wonders.yaml + data/wonder_mechanics.yaml + data/wonder_localization.yaml + data/index/{modifier,trigger}_localization.json + reference locations_index.json | unique_wonders_site/dist/data/unique_wonders.json | Usually called by build_site.py; run directly only when refreshing generic/unique atlas data without copying map assets |
| `scripts/gen_messagetypes.py` | reference_game_files vanilla messagetypes.txt + TV_ENTRIES block | src/main_menu/gui/messagetypes.txt | After adding a new generic action |
| `scripts/gen_victory.py` | data/victory_paths.yaml | 13 generated files (triggers, effects, modifiers, situations, yearly, 6× events, 2× loc) | After editing data/victory_paths.yaml |
| `scripts/in_game/common/generic_actions/gen_tv_io_leader_actions.py` | data/io_leaders.yaml | src/in_game/common/generic_actions/tv_io_leader_actions.txt | After adding/changing an IO or its leader actions |
| `scripts/in_game/common/international_organizations/gen_tv_trade_league.py` | data/trade_league_goods.yaml + data/trade_league_laws.yaml | src/in_game/common/international_organizations/tv_trade_league.txt | After changing the Trade League IO shell, lawless-start contract, or leader-country monopoly storage contract |
| `scripts/in_game/common/scripted_effects/gen_tv_trade_league_effects.py` | data/trade_league_goods.yaml | src/in_game/common/scripted_effects/tv_trade_league_effects.txt | After changing Trade League creation, rolling chart history, or monopoly update generation |
| `scripts/in_game/common/goods_demand/gen_tv_trade_league_demands.py` | data/trade_league_goods.yaml | src/in_game/common/goods_demand/tv_trade_league_demands.txt | After adding/removing tracked monopoly goods or changing virtual demand generation |
| `scripts/in_game/common/generic_actions/gen_tv_trade_league_actions.py` | data/trade_league_goods.yaml | src/in_game/common/generic_actions/tv_trade_league_actions.txt | After changing Trade League monopoly actions or tracked goods |
| `scripts/in_game/common/generic_action_ai_lists/gen_tv_trade_league_list.py` | data/trade_league_goods.yaml | src/in_game/common/generic_action_ai_lists/tv_trade_league_list.txt | After changing generated Trade League actions |
| `scripts/in_game/gui/panels/organization/gen_tv_trade_league_gui.py` | data/trade_league_goods.yaml | src/in_game/gui/panels/organization/tv_trade_league.gui | After changing Trade League panel layout, rolling chart slots, or tracked monopoly rows |
| `scripts/main_menu/localization/english/gen_tv_trade_league_monopoly_l_english.py` | data/trade_league_goods.yaml | src/main_menu/localization/english/tv_trade_league_monopoly_l_english.yml | After changing generated Trade League monopoly category, fixed action, or virtual demand localization |
| `scripts/main_menu/localization/simp_chinese/gen_tv_trade_league_monopoly_l_simp_chinese.py` | data/trade_league_goods.yaml | src/main_menu/localization/simp_chinese/tv_trade_league_monopoly_l_simp_chinese.yml | After changing generated Trade League monopoly category, fixed action, or virtual demand localization |
| `scripts/in_game/common/building_types/gen_towards_victory_buildings.py` | data/academy_buildings.yaml | src/in_game/common/building_types/towards_victory_buildings.txt | After changing Academy of Sciences building tiers |
| `scripts/in_game/common/building_types/gen_tv_wonder_module_buildings.py` | data/wonders.yaml + data/unique_wonders.yaml | src/in_game/common/building_types/tv_wonder_module_buildings.txt | After changing universal/unique wonder module/helper building definitions |
| `scripts/in_game/common/scripted_effects/gen_tv_wonder_module_effects.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/scripted_effects/tv_wonder_module_effects.txt | After changing universal/unique wonder list, module names, size progress, ceremony modifier naming, dynamic building-type map synchronization, or resume/expand building synchronization rules |
| `scripts/in_game/common/scripted_effects/gen_tv_wonder_index_effects.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/scripted_effects/tv_wonder_index_effects.txt | After changing wonder ids, map schema/version, low-risk indexed attributes, helper/module/final building-type route maps, ritual mode/cost/listener/completion metadata, or suitability row key rules |
| `scripts/in_game/common/scripted_triggers/gen_tv_engineering_department_wonder_mechanics_triggers.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/scripted_triggers/tv_engineering_department_wonder_mechanics_triggers.txt | After changing shared or unique wonder site rules, proposal gates, survey cache triggers, wonder-location occupancy checks, or priority resume/expand eligibility |
| `scripts/in_game/common/scripted_effects/gen_tv_engineering_department_wonder_mechanics_effects.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/scripted_effects/tv_engineering_department_wonder_mechanics_effects.txt | After changing shared/unique wonder proposal, survey, ritual, or bridge effects |
| `scripts/in_game/events/gen_tv_wonder_finalization_events.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/events/tv_wonder_finalization_events.txt | After changing the wonder list, finalization/world-news event routing, or finalization/world-news event descriptions/guards |
| `scripts/in_game/common/scripted_effects/gen_tv_wonder_finalization_effects.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/scripted_effects/tv_wonder_finalization_effects.txt | After changing finalization hidden-executor dispatch, per-wonder visible effects, per-style hidden effects, final building construction, or completion broadcast scopes |
| `scripts/in_game/common/building_types/gen_tv_engineering_department_wonder_mechanics_buildings.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/building_types/tv_engineering_department_wonder_mechanics_buildings.txt | After changing shared/unique wonder final building local effects or final-building maintenance |
| `scripts/in_game/common/static_modifiers/gen_tv_engineering_department_wonder_mechanics_modifiers.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/static_modifiers/tv_engineering_department_wonder_mechanics_modifiers.txt | After changing shared/unique wonder permanent base effects, id-based country display modifiers, or ceremony country effects |
| `scripts/main_menu/common/static_modifiers/gen_tv_engineering_department_wonder_ritual_auxiliary_location_modifiers.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/main_menu/common/static_modifiers/tv_engineering_department_wonder_ritual_auxiliary_location_modifiers.txt | After changing final-building local display modifiers, generic wonder style-2 ritual annex local effects, or their ceremony tooltip display modifiers |
| `scripts/in_game/common/generic_actions/gen_tv_engineering_department_wonder_mechanics_actions.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/in_game/common/generic_actions/tv_engineering_department_wonder_mechanics_actions.txt | After changing generated wonder ceremony confirm actions |
| `scripts/main_menu/common/game_concepts/gen_tv_engineering_department_wonder_mechanics_concepts.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | src/main_menu/common/game_concepts/tv_engineering_department_wonder_mechanics_concepts.txt | After changing generic/unique wonder effect concept text or id-based display/image route concept generation |
| `scripts/in_game/gui/panels/organization/gen_tv_engineering_department_wonder_mechanics_gui.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml | data/generated_fragments/tv_engineering_department_wonder_mechanics.gui | After changing generic/unique wonder GUI action blocks, survey-card suitability knowledge, ceremony cost dispatch, or ceremony requirement/effect detail cards |
| `scripts/in_game/gui/gen_location_window.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml + vanilla location_window.gui | src/in_game/gui/location_window.gui | After changing the location-scene fixed-slot wonder badge, tooltip layout, or dynamic id/name/image/effect route expressions |
| `scripts/in_game/gui/panels/organization/merge_tv_engineering_department_wonder_mechanics_gui.py` | data/generated_fragments/tv_engineering_department_wonder_mechanics.gui | src/in_game/gui/panels/organization/tv_engineering_department.gui | After regenerating the wonder mechanics GUI fragment |
| `scripts/main_menu/localization/english/gen_tv_engineering_department_wonder_mechanics_l_english.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml + data/wonder_localization.yaml | src/main_menu/localization/english/tv_engineering_department_wonder_mechanics_l_english.yml | After changing generic/unique wonder English localization, id-based route concept localization, or display modifier alias localization |
| `scripts/main_menu/localization/simp_chinese/gen_tv_engineering_department_wonder_mechanics_l_simp_chinese.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml + data/wonder_localization.yaml | src/main_menu/localization/simp_chinese/tv_engineering_department_wonder_mechanics_l_simp_chinese.yml | After changing generic/unique wonder Simplified Chinese localization, id-based route concept localization, or display modifier alias localization |
| `scripts/wonder_localization_editor.py` | data/wonders.yaml + data/wonder_mechanics.yaml + data/unique_wonders.yaml + data/wonder_localization.yaml + wonder concept / Engineering Department localization files | data/wonder_localization.yaml + hand-maintained wonder concept / Engineering Department localization files + structured wonder mechanics edits | Compatibility launcher for the root-level FastAPI wonder editor; edit wonder-owned names/flavor/history/proposal/ceremony/event text in `data/wonder_localization.yaml`, edit structured mechanics fields for site rules, base country modifiers, rituals, and final-building local effects, then regenerate the wonder-mechanics outputs |
| `scripts/in_game/gui/panels/organization/gen_tv_academy_of_sciences_gui.py` | data/locked_advances.yaml | src/in_game/gui/panels/organization/tv_academy_of_sciences.gui | After changing Academy panel layout or adding/removing Frontier advance targets |
| `scripts/in_game/common/laws/gen_tv_alliance_laws.py` | data/alliance_laws.yaml | src/in_game/common/laws/tv_alliance_laws.txt | After editing Diplomatic Alliance law categories or policies |
| `scripts/in_game/common/laws/gen_tv_academy_laws.py` | data/academy_laws.yaml | src/in_game/common/laws/tv_academy_laws.txt | After editing Academy of Sciences law choices |
| `scripts/in_game/common/laws/gen_tv_trade_league_laws.py` | data/trade_league_laws.yaml | src/in_game/common/laws/tv_trade_league_laws.txt | After editing Trade League law choices |
| `scripts/in_game/common/laws/gen_tv_govhouse_laws.py` | data/govhouse_laws.yaml | src/in_game/common/laws/tv_govhouse_laws.txt | After editing Governor's House law choices |
| `scripts/in_game/common/laws/gen_tv_arts_exhibition_laws.py` | data/arts_exhibition_laws.yaml | src/in_game/common/laws/tv_arts_exhibition_laws.txt | After editing Arts Exhibition law choices |
| `scripts/in_game/common/on_action/gen_tv_pulse_registry.py` | data/pulse_registry.yaml + vanilla pulse files | src/in_game/common/on_action/country_monthly.txt, country_yearly.txt, character_death_pulses.txt, ruler_death_pulses.txt | After adding/changing singleton pulse hooks |
| `scripts/in_game/common/on_action/gen_tv_cmf_suppressions.py` | data/cmf_warning_suppressions.yaml + data/trade_league_goods.yaml + data/locked_advances.yaml | src/in_game/common/on_action/tv_cmf_suppressions.txt | After adding/removing intentional CMF static-analysis warning suppressions |
| `scripts/in_game/events/gen_tv_research_d_events.py` | data/research_d_events.yaml | src/in_game/events/tv_research_d_events.txt | After changing Concentrated Research random event definitions |
| `scripts/in_game/events/gen_tv_wonder_construction_events.py` | data/wonder_construction_events.yaml | src/in_game/events/tv_wonder_construction_events.txt | After changing Wonder Construction random event definitions |
| `scripts/in_game/common/scripted_effects/gen_tv_wonder_construction_event_effects.py` | data/wonder_construction_events.yaml | src/in_game/common/scripted_effects/tv_wonder_construction_event_effects.txt | After changing Wonder Construction random event weights |
| `scripts/in_game/common/scripted_triggers/gen_tv_wonder_construction_event_triggers.py` | data/wonder_construction_events.yaml | src/in_game/common/scripted_triggers/tv_wonder_construction_event_triggers.txt | After changing Wonder Construction random event eligibility rules |
| `scripts/main_menu/localization/english/gen_tv_wonder_construction_events_l_english.py` | data/wonder_construction_events.yaml | src/main_menu/localization/english/tv_wonder_construction_events_l_english.yml | After changing Wonder Construction random event English localization data |
| `scripts/main_menu/localization/simp_chinese/gen_tv_wonder_construction_events_l_simp_chinese.py` | data/wonder_construction_events.yaml | src/main_menu/localization/simp_chinese/tv_wonder_construction_events_l_simp_chinese.yml | After changing Wonder Construction random event Simplified Chinese localization data |
| `scripts/in_game/common/customizable_localization/gen_character_title.py` | data/io_leaders.yaml + vanilla character_title.txt | src/in_game/common/customizable_localization/character_title.txt | After changing IO leader title modifiers |

Generated files that copy vanilla sources preserve the copied vanilla content verbatim except for explicit TV insertion points. The pulse registry generator copies full singleton pulse files for monthly/yearly/character-death hooks, while `on_ruler_death` is emitted as a delegate-only bridge that registers TV-owned callbacks through `on_actions = { ... }` rather than copying the vanilla `_hardcoded.txt` effect. Other TV extensions to hardcoded vanilla hooks follow the same delegate pattern so Jomini does not replace one direct `effect` block with another. The only intended changes are TV-owned insertions: pulse on_action names from `data/pulse_registry.yaml`, TV character title entries from `data/io_leaders.yaml`, and TV message type entries from `scripts/gen_messagetypes.py`. TV-authored `trigger`/`limit` condition blocks that use `has_variable` may wrap guards in `custom_tooltip`, but generated vanilla-copy files must not rewrite copied vanilla guards.

## Manual UI Notes

- `src/in_game/gui/location_window.gui` is generated from vanilla plus TV insertions. The location-scene wonder card uses fixed generated templates: three compact slots and five hover-tooltip slots, both filled by unique wonders first and generic wonders second, with one overflow line in the hover tooltip for any additional wonders beyond five. Slot state stores numeric ids, level, and ritual style only. Unique historical sites display their unique wonder at level 0 on startup before construction; level 0 tooltip rows show the name/image and `TV_LOCATION_WONDER_NO_EFFECT` instead of trying to route to a non-existent level-0 modifier or ritual effect. GUI names use dynamic clickable concept links built from id-based `tv_wonder_display_<id>` route concepts; images, national modifier effects, local modifier effects, and ritual effect lists are recovered through `tv_wonder_display_image_<id>`, `tv_wonder_display_<id>_level_<level>`, `tv_wonder_display_<id>_local_level_<level>`, and `tv_wonder_display_<id>_ritual_<style>_location_tooltip_effect` route names. The GUI does not use `GetFlagName` or runtime raw-DDS path concatenation.
- `src/in_game/gui/panels/organization/tv_arts_exhibition.gui` displays the Arts Exhibition touring action, current dispatched artist count, and all currently dispatched artists from `tv_dispatched_artists_list` as `io_character_card` artist rows in the overview tab. The header shows the variable-backed Chief Artist portrait/name when appointed; if vacant, the portrait area remains clickable to appoint a Chief Artist, and if occupied, two small Replace/Dismiss buttons sit in the portrait area's upper-right corner. A separate monthly International Influence contribution readout remains in the header. The resolutions tab is renamed to Arts Exchange and contains the exchange timer, participant list, and exchange action cards. The vanilla Laws tab is available for Arts Exhibition legal policy votes. Its custom tab tracks the persistent local exhibition: owned artworks, committed exhibits, months open, rating, enthusiasm progress bar, and local exhibition actions.
- `src/in_game/gui/panels/organization/tv_govhouse.gui` keeps governor region management in the overview, including an Auto-Assign Best Governors button and adjacent `button_checkbox_auto` automation checkbutton above the region list; shows each assigned regional governor's current autonomous task and its effect below the character card; leaves the vanilla Laws tab available for Governor's House legal policy votes; moves Grand General portrait/balance/training controls and active training progress to the resolutions tab; and uses the custom tab for War Tent tactics, war-plan status, and plan action buttons.
- `src/in_game/gui/panels/organization/tv_engineering_department.gui` hides the Members tab; the header shows the variable-backed Great Engineer portrait/name when appointed, keeps the vacant portrait area clickable for appointment, places Replace/Dismiss controls in the portrait area's upper-right corner, and includes a right-aligned Current Guidance card that changes its game-concept-linked instruction across appointment, proposal, debate, survey, site confirmation, construction, ceremony, and finalization states. The overview tab contains active Conception and Debate controls and displays the current proposal or locked target wonder's illustration below the proposal/action controls whenever one exists, including unique wonder images when their DDS assets exist; the illustration overlays a top location/type card and a bottom level-1 base-effect card with national and local modifier columns; Domestic Support is hidden until a wonder has been locked. The resolutions tab is Survey and Logistics: it contains locked-stage info cards before prerequisites, the active survey card, survey result readouts, and after site confirmation an Organization/Logistics card split into Organization and Logistics sections, each with explanatory text, an icon/progress row, and a native Production filter button for Wonder Worksites or Wonder Material Workshops; the survey card begins with Suitability Knowledge, showing the current wonder's location trigger conditions and placeholder/revealed suitability rows, with revealed rows displaying unknown/current actual bonus against the maximum bonus. The custom tab is Construction and Ceremony: it contains locked-stage info cards, construction-part buttons, active-part progress, the completion action, toggle-style ceremony selection for generic wonders, two selected-branch-only detail panels for ritual requirements and ritual effects, and a single bottom Hold Ceremony button that dispatches to `tv_wonder_confirm_ceremony`, `tv_wonder_confirm_ceremony_scaled_gold`, or `tv_wonder_confirm_ceremony_prestige` based on the cached selected ritual cost. Generic branches show their requirement and reward through `ShowScriptedEffectForScope` effect lists. Unique wonders skip style selection because their single ritual branch is auto-selected and locked at construction completion; until bespoke unique ritual contents are designed, their requirement panel displays None/无 and their effect panel displays the permanent country buff.
- `src/in_game/gui/panels/organization/tv_academy_of_sciences.gui` is generated from `data/locked_advances.yaml`. Its header shows the variable-backed Chief Scientist portrait/name when appointed, keeps the vacant portrait area clickable for appointment, and places Replace/Dismiss controls in the portrait area's upper-right corner. Its overview tab stages the Research Mechanism UI: with no target it shows only an instruction card; after target approval it shows a centered Research Target card plus A/B/C preparation progress and the B requirements list; once A/B/C are complete it replaces preparation progress with a completion info card and shows the Concentrated Research progress card below. The resolutions slot is renamed Philosophy and currently shows a debate UI shell: a Chief Scientist concept row, reserved issue-conception progress (`tv_academy_philosophy_concept_progress`) with empty fallback, six fixed debate status circles, the current placeholder issue "The state should be governed by the capable", fixed Burghers/Nobles sides with a 50% two-color progress bar, three visible action slots backed by six hidden static category color variants each, two operation/effect detail cards, and a disabled execute button with no backend action. The Medicine tab contains treatment and meditation progress/actions.
- `src/in_game/gui/panels/organization/tv_trade_league.gui` is generated from `data/trade_league_goods.yaml`. Its header shows the variable-backed Grand Merchant portrait/name when appointed, keeps the vacant portrait area clickable for appointment, and places Replace/Dismiss controls in the portrait area's upper-right corner. Its overview tab contains a reusable rolling trade-income chart for the leader country's `monthly_trade_income`, the Grand Merchant status card, Trade League invitation and expulsion buttons, and a placeholder Trade Chain card. The vanilla Laws tab is available as Trade Laws for the five three-level Trade League law groups. The Resolutions tab is Commercial Intelligence: once a Grand Merchant is appointed, all members can page through fixed projected rows for global markets, league total trade advantage, and network strength; selecting a market opens a detail card with the top three merchant-power countries, network strength, a 0-100 progress bar, and leader-only Build/Cancel Network controls. Intelligence network state is stored on the Trade League leader by market slot; the active market gains Grand Merchant DIP / 100 strength each month, inactive markets lose 1 strength, and each full strength point is synchronized as keyed permanent `tv_trade_intelligence_network` merchant power for current members in that market. The custom tab is Monopolies: it remains locked until a Grand Merchant is appointed, then shows Basic Goods/Food/Manufactured Goods/Luxury Goods filter buttons and previous/next page controls above 10 fixed projected row slots. Category, page, and row actions refresh the player's `tv_trade_display_row_<n>_*` and `tv_trade_selected_*` projection variables from the leader-owned monopoly state, so members can browse independently. Selecting a row opens one projected monopoly detail card and one leader-only fixed action card for virtual demand, virtual production, and embargo controls.

## AI Risk Cards

> Load the relevant card before editing files in that task domain.

| Domain | Card |
| --- | --- |
| events | `docs/knowledge/risk_cards/events.md` |
| generic_actions | `docs/knowledge/risk_cards/generic_actions.md` |
| gui | `docs/knowledge/risk_cards/gui.md` |
| international_organizations | `docs/knowledge/risk_cards/international_organizations.md` |
| on_action | `docs/knowledge/risk_cards/on_action.md` |

## Known Anti-Patterns

| Detectability | Category | Scope | Bad Form | Correct Form | Notes |
| --- | --- | --- | --- | --- | --- |
| advisory | workflow | any | Adding or preserving compatibility layers, legacy save migrations, alias variables, wrapper launchers, duplicate old/new paths, or defensive fallback branches for a previous internal implementation | Treat this unreleased v0.1.0 project as mutable. Update all callers, generators, data, and docs to the current architecture, delete stale branches/state, and prefer direct efficient refactors over compatibility shims. Only add compatibility when the user explicitly requests it for a named external consumer. | There are no shipped saves or public APIs to protect. Compatibility code creates hot-path cost, hides call-order bugs, and teaches future agents to preserve obsolete structures. |
| lint | modifier | location | auto_modifier with category = location (inside auto_modifiers/ file) | TRY_REPLACE entry in main_menu/common/static_modifiers/ with game_data.category = location | Location auto_modifiers are NON-FUNCTIONAL in EU5; bake effects into static_modifier values instead |
| lint | enum | location | location_rank:village | location_rank:rural_settlement | EU4 rank name; EU5 valid values are rural_settlement, town, city, megalopolis |
| lint | enum | location | location_rank:hamlet | location_rank:rural_settlement | EU4 rank name; EU5 valid values are rural_settlement, town, city, megalopolis |
| lint | enum | location | location_rank:metropolis | location_rank:megalopolis | EU4 rank name; EU5 valid values are rural_settlement, town, city, megalopolis |
| advisory | gui | any | Removing custom_tooltip from event options (wrong fix for dotted key confusion) | Keep custom_tooltip; verify key format via Step 2/3 — dotted suffix IS valid in event options | AI wrongly removed custom_tooltip thinking dotted key format was invalid. Never remove; only verify format. |
| advisory | event | any | Generating or referencing an event ID whose numeric part is 10000 or higher, such as tv_engineering_department.50011 | EU5 event IDs must be <namespace>.<integer> with the integer below 10000. Do high-cardinality dispatch before firing the event, or branch only over a small non-event-id dimension inside an already typed event. | The engine logs that five-digit numeric event IDs are invalid and can then report duplicate IDs as parser fallout. scripts/validate.py scans src/data/scripts for dotted event references whose numeric part is >= 10000. |
| lint | gui | any | text_single widget inside blockoverride "common_header_text" | blockoverride overrides a 'text' property value, not a widget container; do not add child widgets | Guessing block structure causes silent failures; always read cards.gui before using blockoverride |
| advisory | gui | any | Using GUI And(...) or Or(...) with three or more operands | Use And3(...) / Or3(...) for exactly three operands, or nest binary And(...) / Or(...) calls for larger expressions. | EU5 GUI And() and Or() are binary helpers, not variadic. And(a,b,c) fails conversion with 'Function And expected 2 arguments, got 3'. Pattern left empty because commas inside typed comparison calls make static regex detection noisy. |
| advisory | gui | any | Using percent position directly on icon widgets for fixed-position overlays | For icon overlays on a fixed-size parent, use pixel positions; if percent placement is needed, put the icon inside a positioned widget wrapper instead. | Percent position is confirmed on vanilla widget wrappers, but percent position directly on these icon overlays collapsed them to the left edge. Fixed pixel bins are reliable for fixed-width bars. |
| advisory | gui | any | Putting ignoreinvisible on a plain widget/uberwidget | Use ignoreinvisible on layout containers such as hbox/vbox/scrollwidget content, not on a plain image or wrapper widget. To hide an image area, control the parent widget's visible expression and let the actual image children use their own visible expressions. | A plain widget logs Property 'ignoreinvisible' not handled and Error setting properties for the uberwidget. The attribute is accepted on layout containers elsewhere in the same panel, but not on the generic image wrapper. |
| advisory | gui | any | Building a conditional background-image column inside a plain widget/button with only expanding layout policies and no explicit image bounds | Give the clickable wrapper and image column fixed bounds, and size each visible background child with fixed pixels that match the content area. Keep the existing card/hbox/vbox structure instead of relying on invisible siblings or layoutpolicy alone to size the preview. | Conditional background widgets can collapse to the parent's left edge when the visible branch has no explicit size. In the wonder location panel, the plain widget/button wrapper also had to fill the parent before the image column, and the inner preview widgets needed fixed pixel sizes instead of percentage bounds. |
| advisory | gui | location | Using location.owner inside a scripted effect rendered by ShowScriptedEffectForScope with LocationView.GetLocation.MakeScope.Self | When the GUI passes the location itself as the effect scope, enter the owning country with owner ?= { ... }. Reserve location.owner for contexts that expose a separate location event target, such as building on_built. | In location-window tooltips the scripted effect root is already the location. A dotted location.owner chain is parsed as an event-target link named location, which is invalid from a location root and can also leave COUNTRY.GetName unavailable for add_country_modifier tooltip localization. |
| advisory | gui | any | Putting $LOCALIZATION_KEY$ substitutions inside raw_text GUI fields | Use a localized text = "KEY" field and put inline icons or static text in the localization value, or split dynamic raw text and localized text into separate widgets. | raw_text renders the string literally; $KEY$ substitutions are not expanded, so the UI displays the key. |
| advisory | script | location | Treating location prosperity reads as 0..100 percent points, such as dividing an average prosperity value by 100 before using it as a weight | Use the raw signed fractional prosperity value. For recovery priorities, negate the average directly and clamp below 0. | Vanilla checks use fractional thresholds such as prosperity < 1 and prosperity < -0.1; an extra divide by 100 makes prosperity-derived weights too small to compete. |
| advisory | script | international_organization | Trade League monopoly control computed from produced/traded/supply market proxies or old global-vs-IO goods totals | Use annual top-3 origin/node/consumer market markers, then compute control from market every_export/every_import routes filtered by goods = goods:<good> and summed with trade_volume. | produced_in_market:<good> and goods_demand_in_market(goods:<good>) are valid only for the requested origin/consumer ranking inputs; traded_in_market and goods_supply_in_market are not route volume. Export control uses market every_export trade_volume; consumer control uses market every_import trade_volume. |
| advisory | script | international_organization | Declaring or reading generated per-good Trade League monopoly/action state as IO variables | Store generated monopoly/action variables on the Trade League leader country; the country monthly pulse should enter the matching leader-owned IO for membership checks, then enter leader_country for state writes, and GUI should read GetInternationalOrganization.GetLeaderCountry.MakeScope.GetVariable(...). | The IO type should remain a light shell with leader/member behavior. Global annual marker variables remain global; per-IO/per-good state lives on the leader country. |
| advisory | script | international_organization | Running generated per-good Trade League monopoly maintenance from the Trade League IO type's monthly_effect | Leave the Trade League IO type without monthly_effect. Register a named monthly_country_pulse hook, save the current country scope, iterate every_international_organizations_member_of filtered to tv_trade_league and leader_country ?= the saved country, then call the IO-scoped monopoly refresh from inside that IO. | For this generated high-cardinality system, even hidden IO monthly maintenance was too expensive. A country pulse turns the work into a cheap country membership/leader check before the heavy IO refresh runs. |
| advisory | script | international_organization | Refreshing Trade League commercial-intelligence merchant power by repeatedly adding a temporary months=2 modifier, or adding permanent merchant power without first clearing the keyed state. | For each monthly intelligence refresh, clear only the tv_trade_intelligence_network merchant-power key from previously tracked markets, then reapply months = -1 merchant power for current Trade League members only when the current floor strength is at least 1. Also clear that same key when the Grand Merchant becomes invalid. | Commercial-intelligence strength is leader-country state, but merchant power is market/country keyed state. Permanent application must be synchronized by key so inactive decay, cancellation, death, and membership changes cannot leave stale power, while monopoly embargo keys remain independent. |
| lint | script | international_organization | TV IO type starts with enacted laws or generated default policies | Keep TV IOs lawless at creation unless a design document explicitly says otherwise. For Trade League, do not emit a laws block with policy mappings in the IO type; laws are enacted later through policy votes. | AI generated default level-1 Trade League policies into tv_trade_league's IO definition. This violated the design that all TV IOs start with no laws. Vanilla setup shows an IO can explicitly start with laws = {  }. |
| advisory | script | international_organization | Defining a TV international organization without a matching io_opinion_<io_type> bias | Add src/in_game/common/biases/<io_type>_biases.txt with io_opinion_<io_type> = { value = <opinion> } and localize the same key. | EU5 applies automatic member opinions through io_opinion_<key>; missing the bias logs 'International Organization Type needs an opinion of other members'. |
| advisory | script | any | Passing a generic-action value selector or scoped variable directly as set_variable value, add_temporary_demand scale, or add_goods_supply amount | Wrap dynamic numeric reads in an explicit script-value block, e.g. value = { value = scope:target_1 }, value = { value = scope:target_1 divide = 2 }, scale = { value = scope:io.var:X }, or amount = { value = scope:io.var:X }. | Direct scoped numeric reads in these dynamic value slots can be interpreted as existence/truthy values and collapse to 1. The script-value block preserves the selected number and supports inline arithmetic. |
| advisory | script | any | Calling cleanup-only scripted effects from a visible generic action effect | Wrap cleanup/rebuild-only helper calls in hidden_effect = { ... }. scripts/validate.py enforces the registered high-risk helper list, including tv_governor_remove_effect. | Generic action effect tooltips can pre-evaluate cleanup helpers, causing variable/list cleanup chains to run through unsafe reads while merely hovering the button. Hidden output is required for these internal maintenance effects. |
| lint | modifier | country | trade_efficiency = <value> | Use trade_land_efficiency and trade_sea_efficiency separately (both category=country, percent) | trade_efficiency does not exist in EU5; split into trade_land_efficiency + trade_sea_efficiency |
| lint | localization | any | Smart/curly quotes (“”‘’) in localization YAML | Use straight ASCII double-quotes (") only; escape with \" inside values | PDX parser rejects non-ASCII quote characters; the failure is silent |
| needs_parser | localization | any | Defining the same localization key in more than one YAML file for the same language | Keep each localization key owned by exactly one file per language. When a generator takes over a key family, remove the stale manual copies instead of redefining them. | Main-menu localization YAML does not merge duplicate keys across files within one language pack. Startup logs a duplicate-key error and only one definition can win, so manual and generated files must have unambiguous ownership. |
| advisory | localization | international_organization | Custom IO law category localized only by raw category id (e.g. tv_science_factions) | Also define <CATEGORY_ID_UPPERCASE>_LAW_CATEGORY in main_menu localization, e.g. TV_SCIENCE_FACTIONS_LAW_CATEGORY | The vanilla IO Laws tab displays InternationalOrganizationLawCategory.GetName and looks up the uppercase *_LAW_CATEGORY key for law category headers; missing keys render literally. |
| advisory | gui | international_organization | Putting visible IO header controls such as action buttons in blockoverride "country_header_extra" on the standard organization header | Put compact IO header controls/readouts in ios_information_header_content_extra_1/2/3 with ios_header_content_extra_template, or override abilities_widget to include ios_information_header plus the controls. | In the standard organization_panel_default_header path, the visible lower header row is ios_information_header inside abilities_widget. country_header_extra is outside the character/header info row and can be absent or effectively hidden in UI bounds, so interactive controls may not render where expected. |
| lint | scope | location | add_province_modifier (EU4 effect) | add_location_modifier or add_static_modifier with location scope | EU4 province scope effects do not exist in EU5; use location-scoped equivalents |
| lint | scope | location | has_province_modifier (EU4 trigger) | has_location_modifier | EU4 province scope triggers do not exist in EU5 |
| lint | precision | any | float literal with 6+ decimal places (e.g. value = 0.123456789) | Round to at most 5 decimal places: value = 0.12346; EU5 engine reads no further | EU5 engine truncates all float literals at 5 decimal places; digits beyond the 5th are silently ignored |
| advisory | script | any | change_variable { divide = N } expecting float result when input < N (e.g. score ÷ large_threshold to get [0,1]) | Multiply by 100 (or desired precision) BEFORE dividing, then compare/cap at 100 instead of 1. For thresholds divisible by 100, use divide = (threshold÷100) in one step. | EU5 change_variable { divide } performs FLOOR integer division. E.g. 160÷2000=0, not 0.1. Use integer scale [0,100] with max=100 on GUI progressbar. Pattern left empty (no auto-detect) — all divide uses cannot be classified as bad without context. |
| advisory | gui | any | progressbar with dynamic GetVariable value binding and no explicit max | Always set max = 100 on the progressbar widget. Store values as integer [0,100] in scripted variables using integer-scale arithmetic. | Default progressbar max appears to be 1; GetVariable returning an integer 0-100 without matching max produces 0% for all values except 1 (which shows 100%). Pattern left empty — regex cannot reliably detect the missing-max + variable-value combination. |
| advisory | gui | any | Custom situation_panel leaves the shared default situation_subheader_content in place when no subheader content is needed | Add blockoverride "situation_subheader_content" {} near the top of the situation_panel block to suppress the default 45px empty row. | The shared situation_panel template includes a default subheader widget. Vanilla situation panels that do not use it override the block to empty; otherwise custom panels show unwanted blank space above their main content. |
| lint | gui | any | GetGoods('key'), GetGoodsFromKey, or MakeGoods('key') in GUI expressions to construct a Goods object from a string literal | Goods datacontext must come from a data model accessor chain (e.g. GoodsMarketEntry.GetGoods, Trade.GetGoods, GoodsItem.GetGoods in a foreach); there is no string-to-Goods lookup function in EU5 GUI | Exhaustive search of all reference_game_files *.gui files found zero occurrences of any string-to-Goods constructor; all goods access is typed accessor chains only |
| advisory | gui | any | Chaining .GetGoods from MakeScope.GetVariable(...) in GUI expressions | Use a real Goods datacontext from a typed data model/accessor, or generate static icon/name branches keyed by a saved numeric goods id. | Scope/numeric variables returned by MakeScope.GetVariable do not expose a .GetGoods accessor. Runtime error: Could not find data system function 'GetGoods'. |
| advisory | gui | international_organization | Chaining .GetInternationalOrganization from MakeScope.GetVariable(...) in GUI expressions | Call OpenInternationalOrganizationView only with a typed InternationalOrganization object from a verified view/datamodel chain, such as InternationalOrganization.Self or OrgItem.GetOrg.Self. | Variables saved from script scope are not promoted to GUI InternationalOrganization objects by .GetInternationalOrganization. Runtime error: Could not find data system function 'GetInternationalOrganization'. |
| lint | gui | building | Passing a building key string to SelectLocationToBuildDefault / SelectLocationToBuildWithLocation in GUI | Pass a BuildingType object from GUI data context, such as BuildingItem.GetBuildingType, LocationBuildingItem.GetBuilding.GetType, or BuildingType.Self. If no BuildingType object is available in the panel, route the button through a generic action with a location select_trigger. | The vanilla build-location GUI calls SelectLocationToBuildDefault with a typed BuildingType object. A literal string key can leave the click appearing successful only because a following PlayAudioEffect runs, while no build-location interface opens. |
| lint | gui | any | datamodel = "[GoodsView.GetGoods]" inside a ContextualTooltipType or tooltipwidget template to iterate all goods and filter by key | GoodsView is a view-model object only available in goods_overview.gui panel scope; it is not accessible from tooltip/ContextualTooltipType contexts. No equivalent all-goods datamodel exists for tooltip scope. | Exhaustive search of reference_game_files found GoodsView only in the goods_overview panel; tooltip templates have no equivalent all-goods iteration datamodel |
| lint | scope | any | multiply_global_variable = { name = X  value = 0.95 } to scale a global variable in-place | Use set_global_variable with hardcoded per-case values: set_global_variable = { name = X  value = computed_literal }. For era-decay patterns, hardcode the result per age (idempotent, no dedup guard needed). If truly dynamic, compute in a local_variable then use set_global_variable = { name = X  value = local_var:tmp }. | Exhaustive search of reference_game_files/ and reference_mods/ found no multiply_global_variable effect. Only set_global_variable and change_global_variable exist for global-scope numeric variables. |
| lint | script | any | remove_from_variable_list = { name = X target = Y } for persistent variable lists | remove_list_variable = { name = X target = Y } to remove a specific entry. add_to_variable_list = { name = X target = Y } is still correct for adding. Temporary lists (add_to_list) use a different API: every_in_list = { list = X } and remove_from_list = X. | EU5 uses remove_list_variable (not remove_from_variable_list) to remove a scope reference from a persistent variable list. Confirmed by govhouse implementation. |
| advisory | gui | any | datacontext = "[InternationalOrganizationsView.GetPlayer]" inside action_button_diamond in an organization panel | Remove datacontext entirely. Use Player.MakeScope.GetVariable(...).IsSet in visible expressions instead of Country.MakeScope.GetVariable(...).IsSet | Adding datacontext to action_button_diamond overrides the widget's internal binding, causing tooltip generation to fail silently (empty tooltip, button stays grayed regardless of action potential). Vanilla IO panels never use datacontext on action_button_diamond. Use Player (global GUI binding) instead of Country (datacontext-dependent) to access player-country variables. |
| needs_parser | gui | any | A GUI button widget without tooltip/tooltipwidget, including visual button templates such as button_checkbox_auto or disabled button_regular_diamond | Give every button-derived widget its own tooltip or tooltipwidget, even if another transparent action_button overlays it or the button is disabled. | The engine validates the button widget itself. A separate overlay action_button with title/description does not satisfy tooltip validation for the underlying button_checkbox_auto, and disabled placeholder buttons still need a tooltip. |
| lint | scripted_values | any | Writing a leading + sign on positive float literals, e.g. value = +0.025 or change_variable = { ... value = +0.003 } | Omit the + sign entirely: value = 0.025. The EU5/Jomini parser does not accept a leading + on numeric literals and will misread the value. | Triggered by Python f-string format spec +:  e.g. f'{delta:+.5f}' emits +0.00357 for positive deltas. Fix: use f'{delta:.5f}' so no leading sign is emitted for positive values. |
| lint | gui | any | GetPlayerCountry.MakeScope.GetVariable(...) or GetPlayerCountry in situation panel GUI expressions | Use Country.MakeScope.GetVariable('name').GetValue — Country is the implicit player country datacontext in situation panels. For situation-scope variables use SituationView.GetActiveSituation.GetSituation.MakeScope.GetVariable('name') | Exhaustive search of all situation panel .gui files found zero occurrences of GetPlayerCountry; western_schism.gui:43 confirms Country.MakeScope as the correct chain for per-country variables |
| advisory | gui | any | Using > >= < <= == operators directly in GUI visible expressions to compare variable values (e.g. visible = "[Country.MakeScope.GetVariable('x').GetValue >= 3]") | Use typed comparison functions: GreaterThanOrEqualTo_CFixedPoint(Country.MakeScope.GetVariable('name').GetValue, '(CFixedPoint)N.0') for integer milestone variables; EqualTo_float(FixedPointToFloat(...GetValue), '(float)N.0') for float comparisons | EU5 GUI expression language has no inline comparison operators; all comparisons require named typed functions (GreaterThanOrEqualTo_CFixedPoint, LessThan_float, EqualTo_int32, etc.) with explicit type-cast literals |
| advisory | gui | any | Using an icon widget or designing custom icon syntax without first checking if @icon_name! inline texticon syntax already covers the case | Check font_icons.gui for existing texticon definitions first; use @icon_name! in raw_text/text/localization if available; fall back to icon widget; only design from scratch as last resort | EU5 has 180+ pre-defined inline icons via the @xxx! texticon system; skipping this check causes unnecessary widget complexity and misses the preferred engine-native approach |
| lint | event | any | Event ID with more than one dot, e.g. tv.conquest.1 = { type = country_event ... } | Event ID must be exactly <namespace>.<integer> with one dot. Use one namespace per category (e.g. namespace = tv_conquest, IDs tv_conquest.1..5), or flatten to sequential integers under one namespace. Localization keys for title/desc/option name CAN still use dotted suffixes (tv_conquest.1.t). | EU5 jomini_eventmanager splits the event ID on the FIRST dot. tv.conquest.1 becomes namespace 'tv' + integer 'conquest.1' which fails to parse, the event is dropped, and surviving entries collide as duplicates. Engine error: 'Failed to split namespaced event ID, needs to be in the form of namespace.integer'. |
| needs_parser | gui | any | Setting parentanchor on a widget that is a direct child of an hbox or vbox (e.g. inside hbox = { widget = { parentanchor = vcenter ... } }) | Remove parentanchor from hbox/vbox children — the layout box arranges them automatically on the cross-axis. parentanchor remains valid on children of plain widget/window/container parents. | Pattern is empty because static detection requires parsing GUI parent structure. Audit any widget nested inside hbox = { ... } or vbox = { ... } and strip parentanchor. Children of progressbar/widget/window are unaffected. |
| lint | gui | any | Putting margin_top directly on a progressbar widget | Wrap the progressbar in a fixed-size widget/layout container, put vertical placement such as parentanchor/widgetanchor on the progressbar inside that wrapper, and leave margin_top off the progressbar itself. | EU5 progressbar does not handle margin_top. Text and container widgets may use margins, but progressbar vertical offset should be expressed through a parent wrapper. |
| advisory | gui | any | Setting size with a percentage component on a widget that is a direct child of an hbox or vbox, e.g. size = { 97% 72 } inside organization_custom_content | Remove the percentage size and rely on layout policies, or use non-percent fixed/min/max sizing when a fixed dimension is actually needed. For io_character_card in organization_custom_content, omit size/parentanchor and rely on the card type's built-in layoutpolicy_expanding. | Pattern is empty because static detection requires parsing GUI parent structure. Percent sizes are valid in some plain widget/fixedgrid contexts, but hbox/vbox children are arranged by the box layout and should be driven by layoutpolicy_* or stretch factors. |
| advisory | gui | any | Wrapping an organization_custom_content scrollarea in a child vbox and relying on autoresizescrollarea for dynamic overflow | Inject the scrollarea directly into organization_custom_content so it becomes a child of the vanilla inner vbox. Override margin_top/spacing on the block. Use a fixed viewport such as size = { 100% 430 } for bounded panels, or set the block layoutpolicy_vertical = expanding and the scrollarea using = layoutpolicy_expanding when the tab must claim the full IO pane from the common trailing expand spacer. Keep scrollwidget content with a natural/fixed vertical height (for example layoutpolicy_vertical = fixed plus ignoreinvisible = yes). Avoid autoresizescrollarea for tabs whose content can grow after state changes, because it can resize the viewport to the content and leave no useful overflow to scroll. | The vanilla custom content slot is already an inner vbox with margin_top = 5 followed by an expand spacer, and vanilla middle_kingdom injects children/properties directly into organization_custom_content. Adding another child vbox can make UI bounds look correct for the wrapper while the scrollarea sits lower inside it. A direct fixed viewport prevents autosize growth, but a full-pane custom tab also needs the injected block and scrollarea to be vertically expanding so the common trailing expand does not own the lower area. autoresizescrollarea is useful for dropdown-like natural-height widgets, but in this IO tab it grew with active exhibition content, removed overflow, and made the scrollarea stop scrolling. |
| advisory | gui | any | Using io_character_card outside a FilteredSortedList context without suppressing the inherited name sort highlights | Inside standalone io_character_card usages, add blockoverride "name_highlight" {} and blockoverride "character_entry_name_sort_by_highlight" {} unless the card is actually inside a sortable FilteredSortedList. | io_character_card uses character_entry, whose name highlight blocks call FilteredSortedList.IsKeyHoveredByWidgetName. Without a FilteredSortedList datacontext, the GUI data manager spams 'No context supplied' while the card is visible. |
| advisory | gui | any | A datamodel item block with more than one top-level child widget, e.g. item = { vbox = { ... } widget = { ... } } | Wrap all row content inside a single parent widget/vbox/hbox so the item block contains exactly one child. Put dividers or auxiliary widgets inside that parent. | GUI datamodel item descriptions can only contain a single child and no properties. Multiple top-level children log 'Malformed item desc' and can cause adjacent formatter noise while the list is rendered. |
| advisory | gui | any | RequirementsList with an empty blockoverride "block_title" { } to hide its title | Keep the nested title block and hide it explicitly: blockoverride "block_title" { block "block_title" { visible = no } } | An empty override removes RequirementsList's safe title replacement and exposes the underlying TooltipTableHeader textbox. Outside an actual tooltip, that header has no ExtraTooltipInfo context and continuously logs GetTintColor failures while the panel is hovered. |
| advisory | gui | any | Using TooltipTextBlock as a normal always-visible panel widget | Reserve TooltipTextBlock for real tooltip content (`tooltipwidget`, `ContextualTooltipType`, `AlertTooltipType`, etc.). In normal panels use text_single/text_multi or another non-tooltip text widget. | TooltipTextBlock inherits tooltip_text_block_template, whose font tint reads ExtraTooltipInfo.GetTintColor. Visible uses outside an actual tooltip context can continuously log FetchData failures while the panel is open. |
| advisory | gui | any | Wrapping tooltip rows that can grow in height in a fixed-size `widget` or an `expanding` child, e.g. `size = { W H }` around a `TooltipStringPairList` chain | Let the visible tooltip content establish its own height. Constrain width with `size = { W -1 }` or `minimumsize`, add `set_parent_size_to_minimum = yes` to the containing layout, and keep `ignoreinvisible = yes` so only visible rows contribute to the measured height. | A fixed-height tooltip row can keep the frame visually short while the modifier text spills below the border. The container must be height-auto if the modifier block can wrap to multiple lines. |
| lint | modifier | location | Direct numeric price in EU5 building definition, e.g. price = 200 (EU4 carry-over) | Use price = <named_constant> referencing a definition in common/prices/ (e.g. price = tv_academy_1_price with gold = 600). Capital buildings that scale with age use expensive = yes instead. | EU5 building gold cost uses named price constants from common/prices/ (e.g. price = expensive_estate_building at line 416 of estate_buildings.txt). Direct numeric literals like price = 200 are silently ignored. Named constants ARE valid. |
| lint | modifier | location | Boolean location modifier can_recruit_regiment_in_this_location assigned numeric 0/1 | Use can_recruit_regiment_in_this_location = yes or no. The modifier type is declared boolean=yes, and vanilla building modifiers use yes. | EU5 logs Malformed token near the numeric literal when a boolean modifier value is emitted as 1. Generated YAML data must preserve bools and render them as yes/no, not Python True/False or 1/0. |
| lint | script | any | Python-style boolean token True/False emitted into an EU5 common script | Render script booleans as yes or no. In Python generators, handle bool before int/float because bool is an int subclass. | YAML true/false loads as Python True/False; str(value) emits malformed tokens in Jomini scripts, as seen with global_peasants_migration_allowed = True. |
| advisory | encoding | any | Saving common/ scripts (static_modifiers, situations, scripted_effects, on_action, etc.) as plain UTF-8 without a byte-order mark | Save with UTF-8 BOM (first 3 bytes EF BB BF). In Python: write_bytes(b'\xef\xbb\xbf' + text.encode('utf-8')) or open(..., encoding='utf-8-sig'). Verify with `head -c 3 <file> | xxd` -> expect ef bb bf. | Engine retries without BOM but emits a warning per file per load. Localization YAMLs already require BOM; this rule extends the same requirement to common/ .txt scripts. Auto-generated files via scripts/gen_scaffold.py should always emit the BOM. |
| lint | localization | any | #Y[variable_expression]#! — color tag immediately followed by [ with no separator | Either omit the color tag and write [variable_expression] as plain text, OR add a space: #Y [variable_expression]#! — the space terminates the tag name so the parser reads #Y (tag) then ' value' (content). Note the leading space will be visible in the rendered string. | In EU5 Jomini localization, the color tag name is read from # until the first non-alphanumeric. When a [var] resolves to a number (e.g. 64) and is placed directly after #Y, the parser reads #Y64 as the tag name (unknown). The leading space in '#Y [var]' terminates the tag name at Y. |
| advisory | localization | any | custom_description { text = MY_LOC_KEY } where MY_LOC_KEY exists only in a localization YAML but not in common/trigger_localization/ | Create a trigger localization entry in src/in_game/common/trigger_localization/your_file.txt: MY_LOC_KEY = { global = MY_LOC_KEY }. The YAML key is still needed as the display text; the trigger_localization file is the bridge the engine uses at validation time. | EU5 engine validates custom_description text keys against common/trigger_localization/ at load. A YAML-only key passes validate.py but produces 'PostValidate of trigger custom_description returned false' and 'No trigger loc KEY' at game startup. |
| advisory | gui | any | Using HasVariable or is_set (script trigger syntax) inside a GUI visible expression to check whether a situation-scope variable exists | Use GetVariable('name').IsSet as the final accessor in a GUI visible expression: visible = "[SituationView.GetActiveSituation.GetSituation.MakeScope.GetVariable('name').IsSet]" | HasVariable is a script-side trigger and does not exist in the GUI expression language. GetVariable('name').IsSet is the GUI-native null guard for optional variables stored on situation/country scopes. |
| advisory | gui | any | Attempting to check equality of a CFixedPoint variable in a GUI visible expression without a typed function (e.g. assuming == works, or forgetting EqualTo_CFixedPoint exists) | Use EqualTo_CFixedPoint(expr, '(CFixedPoint)N.0') for equality; also available: LessThan_CFixedPoint, GreaterThan_CFixedPoint, LessThanOrEqualTo_CFixedPoint. Example: visible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_best_path').GetValue, '(CFixedPoint)0.0')]" | EqualTo_CFixedPoint and related typed comparison functions are confirmed to exist in EU5 GUI. The inline == operator does not work in visible expressions. The _scripted_widgets.info file also documents EqualTo_CFixedPoint as the canonical equality check. |
| needs_parser | script | any | limit = { var:some_var >= var:other_var } when some_var may not be set on the scope | Add has_variable = some_var to the limit block before the var: comparison: limit = { has_variable = some_var  var:some_var >= var:other_var } | EU5 does NOT default unset variables to 0 in trigger comparisons; var:X on the left side of >= throws a script error and aborts the if-block. set_variable = { value = var:X } with an unset var appears to be silent (no error), so only limit comparisons need the has_variable guard. |
| needs_parser | event | country | Event option effect sets variable X and then immediately calls a visible helper that compares var:X, assuming the option tooltip pre-evaluator sees the new value | Wrap the state-changing/application sequence in hidden_effect, or make the called helper guard has_variable before direct var:X comparisons. For optional option triggers, use var:X ?= N. | EU5 event option tooltip generation may pre-evaluate option effects without committing earlier set_variable writes. A later visible direct var:X comparison can fetch an unset variable repeatedly while the event is open. |
| advisory | event | any | Putting global scans, high-cardinality dispatch, broadcasts, construction cleanup, map rebuilds, or other heavy work in an event option hidden_effect and expecting tooltip hover not to evaluate it | Keep option hidden_effect blocks to light guards or scheduler calls. Run heavy hidden work from a hidden = yes event immediate block or another non-tooltip execution path. | hidden_effect suppresses visible tooltip output, but it does not isolate event-option code from hover evaluation. The wonder finalization hidden chain was expensive because it included per-wonder/per-style dispatch, final building construction, cleanup, world-news broadcast, post-finalization priority caching, and project-state clearing. |
| needs_parser | event | any | Event option or visible event helper computes a temporary variable and a later visible helper compares var:X or uses scope.var:X as an effect value | Do not rely on temporary variables written earlier in the same visible option/effect chain. Use optional checks directly against persistent state, branch over bounded values with literals, or wrap the state-changing chain in hidden_effect when the nested tooltip is not needed. | The event option tooltip pre-evaluator did not commit tv_wonder_final_building_extra_levels before the nested helper ran. The same failure reappeared when Prosperity M1 reinitialized wonder module/helper buildings from partially built wonders, and again when unit-completion hover rebuilt helper/module levels through temporary current/target vars. For bounded 0..6 helper/module sync, emit literal per-level building deltas directly from persistent state instead of temporary combinable/current/target variables. For rounded countdown displays, prefer script-value operators such as ceiling/floor over scratch check variables like tv_wonder_remaining_months_floor_check. |
| needs_parser | event | variable_map | A visible event option/helper computes a temporary variable-map key and immediately uses global_variable_map(...|var:X) to choose a dynamic building for construct_building or change_building_level_in_location | Do not infer that dynamic building effects are unsupported. For tooltip-visible building effects, use a map key that already exists before the event opens, or branch over bounded non-wonder dimensions such as module part or ceremony style and use direct maps keyed by persistent project state. Capture the mapped building type into a local variable before switching to the location scope. | The failed chain was set_variable/change_variable of tv_wonder_module_building_key followed by set_local_variable value = global_variable_map(tv_wonder_module_key_to_building_type|var:tv_wonder_module_building_key) and building_type = local_var:tv_wonder_module_building_type. The working chain branches on tv_wonder_last_completed_part, captures from tv_wonder_id_to_<part>_module_building_type keyed by var:tv_wonder_locked, and then runs construct_building/change_building_level_in_location with local_var:tv_wonder_module_building_type. This preserves variable_map/numeric-id architecture without generated per-wonder dispatch tables. |
| needs_parser | script | any | A visible generic action calls an effect that initializes variables and then compares them later in the same visible effect chain, assuming action tooltip pre-evaluation commits the earlier writes | Hide action widgets until their prerequisite state exists, re-check prerequisites inside the action effect, and make reusable effects safe under pre-evaluation by using optional variable reads (var:X ?= ...) or threshold-style comparisons instead of direct var:X comparisons against variables set earlier in the same chain. | EU5 generic action card and tooltip rendering can pre-evaluate the action effect without committing preceding set_variable writes. A helper that initializes construction variables and then immediately reads them with direct var:X comparisons can spam Failed to fetch variable / Invalid left side errors while the card is merely visible, hovered, or clicked. Even a display refresh that sets a variable to 0 before comparing it needs optional reads if it is reachable from a visible generic action. |
| advisory | script | any | A generic action effect wrapped in hidden_effect still initializes variables that a later helper compares during select_trigger hover pre-evaluation | Do not treat hidden_effect as a hover-evaluation barrier in generic actions. Guard the action effect on persistent state and make nested helper reads safe with optional variable links or persistent-state checks. | Hovering the Governor appointment selector still reached tv_governor_apply_modifiers_effect even though the appointment chain used hidden_effect. The pre-evaluator walked into the helper and needed optional reads for newly initialized task variables. |
| advisory | localization | any | MY_KEY = { global = MY_KEY } when the trigger using MY_KEY has custom_tooltip nested inside custom_description | Declare all four perspectives: MY_KEY = { none = MY_KEY  global = MY_KEY  first = MY_KEY  third = MY_KEY }. Use this form for every trigger_localization entry as a safe default. | EU5 engine requires the 'first' perspective when custom_description wraps a nested custom_tooltip. A global-only entry causes '(BUG: KEY missing perspective. Could not find parameters for PROMOUN first Negative required false)' at runtime. Adding all four perspectives is zero-cost and prevents this for any trigger regardless of nesting. |
| lint | modifier | character | static_modifier or character_modifier using artist_skill_modifier = X | Use artist_skill_level_gain = X instead (already_percent = yes, category = character) | artist_skill_modifier does not exist. The correct modifier type for boosting artist skill development is artist_skill_level_gain (declared as already_percent in modifier_type_definitions). |
| lint | enum | country | government = monarchy (EU4 trigger style) | government_type = government_type:monarchy  (and similarly for republic, theocracy, tribe, steppe_horde) | EU5 government type trigger is government_type = government_type:X; the EU4 'government = X' form does not exist |
| lint | script | any | multiply_variable = { name = X value = Y } or divide_variable = { name = X value = Y } | change_variable = { name = X multiply = Y } or change_variable = { name = X divide = Y }. Both accept var:Z references: multiply = var:Z, divide = var:Z | multiply_variable and divide_variable do not exist in EU5; all variable math uses change_variable with multiply/divide parameters |
| lint | script | any | change_variable = { name = X value = 1 } | Use a named arithmetic operator with change_variable, e.g. change_variable = { name = X add = 1 }. If you want to overwrite the variable, use set_variable = { name = X value = 1 }. | change_variable does not accept a value field. The engine logs 'Failed to read value for change_variable' when value is used there. value belongs to set_variable; change_variable uses add/subtract/multiply/divide. |
| lint | script | any | trigger_event = { id = X } or trigger_event = X inside scripted_effects or on_action | Use trigger_event_non_silently = { id = X } for popup events shown to the player, or trigger_event_silently = X for background events. Both accept simple form (= event_id) and object form (= { id = X days = Y }). | EU5 does not have a bare trigger_event effect. In scripted_effects and on_action files, the engine reports 'Unknown effect trigger_event'. The two valid variants are trigger_event_non_silently (shows popup) and trigger_event_silently (hidden/background). This distinction does not apply inside event option = { } blocks, which may use a different syntax. |
| lint | script | any | every_in_variable_list = { name = X ... } or remove_from_variable_list = { name = X target = Y } for persistent variable lists | every_in_list = { variable = X ... } to iterate; remove_list_variable = { name = X target = Y } to remove a specific entry. add_to_variable_list = { name = X target = Y } is still correct for adding. Temporary lists (add_to_list) use a different API: every_in_list = { list = X } and remove_from_list = X. | EU5 has two distinct list types: (1) Temporary lists created with add_to_list — iterated with every_in_list { list = X }, removed with remove_from_list = X. (2) Persistent variable lists created with add_to_variable_list — iterated with every_in_list { variable = X }, removed with remove_list_variable { name = X target = Y }. every_in_variable_list and remove_from_variable_list do not exist in EU5; the engine reports 'Unknown effect' for both. |
| lint | script | country | add_treasury = X to add or subtract gold from a country | Use add_gold = X (simple literal: add_gold = -200) or add_gold = { value = script_value multiply = N } (computed form). Trigger side: gold >= 200 (literal integer) is valid. | add_treasury does not exist in EU5. The effect is add_gold; it accepts a bare literal integer (positive or negative) or a block with value/multiply. Error: 'Unknown effect add_treasury'. |
| advisory | script | country | Hardcoding add_gold = -N for an event option that should scale with the country's economy | Use vanilla scripted effect change_gold_effect = { scale = -N } for dynamic country-event costs. The effect scales with capital_wealth and country_economical_base and clamps the result. | Vanilla event laws.0005 uses change_gold_effect = { scale = -3.5 } for the paid +10 research-progress option. Use fixed add_gold only when a flat cost is the design intent. |
| advisory | script | any | Defining a price in common/prices/ without a corresponding modifier_type in common/modifier_type_definitions/ | For every price named {name} in common/prices/, a modifier type named {name}_cost_modifier MUST exist in common/modifier_type_definitions/. EU5's price_database.cpp validates this at startup and logs 'Missing modifier type for price. {name}_cost_modifier'. The modifier type allows future modifiers/events to discount the price. | This is an engine-enforced convention, not optional. Even if no modifier currently applies the discount, the modifier_type definition must be present. The naming is exact: price 'tv_academy_1_price' requires modifier type 'tv_academy_1_price_cost_modifier'. These appear unused but are implicitly consumed by the price loading system. |
| needs_parser | script | any | Defining a direct effect block on a vanilla hardcoded on_action hook inside a mod file when the same hook already exists in the vanilla _hardcoded on_action table | Extend hardcoded vanilla hooks with on_actions = { tv_named_callback } delegation and put the TV logic in the named callback's own effect block. Do not add a second direct effect block to the hardcoded hook itself. | Hardcoded on_action hooks are singleton by key for top-level effect blocks. A mod file that adds another direct effect to the same hardcoded hook will not merge the bodies; Jomini keeps the most recent effect and silently drops the earlier one. Use an on_actions delegate to route TV logic into a separate named on_action. |
| advisory | script | any | ordered_in_global_list limit block that references situation:X.var:Y without a prior has_variable = Y guard when Y might not be set | Wrap the entire ordered_in_global_list block in: if = { limit = { has_variable = Y } ordered_in_global_list = { ... } }. The outer if is evaluated once on the situation scope, so it is cheap. The inner limit still includes the var: reference but now only runs when Y is known to be set. | ordered_in_global_list evaluates its limit block for EVERY entry in the pool. If the referenced situation variable (e.g. tv_rank_1_country) was never set (because the pool was empty at situation on_start), every pool entry triggers a script error, producing N errors on first load. Guard with has_variable on the outer if to skip the whole pass when the prerequisite rank is absent. |
| advisory | script | any | Cascading ordered_in_global_list ranks that never clear old rank variables and gate later ranks only with has_variable = previous_rank | Treat ranking output as a fresh snapshot: remove all rank_N variables before rebuilding, ensure every pool entry has numeric order_by variables, and guard rank N with global_variable_list_size value > N-1 before running ordered_in_global_list. | A stale previous rank can survive from an earlier rebuild. If the current pool has fewer eligible entries, the later ordered_in_global_list pass can run with no selected item; prev is none, set_variable value = prev logs 'Value of wrong type ... none', and order_by/var reads may log failed variable fetches. Clearing outputs and checking current pool size prevents the pass from running when no current candidate exists. |
| lint | script | any | can_start = { game_is_initialized = yes } on a situation intended to spawn at game start | Use can_start = { always = yes } for situations with monthly_spawn_chance_unique that should exist from turn 1. game_is_initialized evaluates to current_date > 1337.5.1 (strictly greater); EU5 starts on exactly 1337.5.x, so the first spawn attempt fails and the situation spawns one month late. | game_is_initialized = { current_date > 1337.5.1 }. The strict > operator means month-1 (1337.5.x) evaluates to FALSE. Situation spawns in month 2 and becomes visible to the player in month 3 due to UI propagation delay. For non-popup situations with on_start initialization, always = yes is safe because monthly_spawn_chance_unique prevents duplicate instances. |
| advisory | scope | any | every_X = { root = { change_variable = { name = Y ... } } } where Y was set_variable'd on `this` before the loop, called from a situation's on_monthly | Use set_local_variable / change_local_variable for the accumulator. local_variable persists across all scope switches. Inside every_X, access the current-scope value directly (no prev. needed): change_local_variable = { name = acc add = some_value }. Then use local_var:acc after the loop. | In a situation's on_monthly, root = the situation scope, not the iterating country. set_variable stores onto `this` (country); root = { change_variable } then targets the situation — the accumulation goes to the wrong scope and the variable on the country stays 0. fix: local_variable is scope-independent and thread-local to the effect call. |
| lint | scope | any | target_flag = selected_artist (or any custom name) in select_trigger for 3-step generic actions | Use the vanilla naming convention: target_flag = target for step 1, target_flag = target_1 for step 2, target_flag = target_2 for step 3. Always wrap effect block in if = { limit = { exists = scope:target } ... } guard. | Custom target_flag names (e.g. selected_artist) in multi-step generic actions cause 'Undefined event target' / 'scope returned unset scope' errors at runtime. Engine appears to only reliably resolve target, target_1, target_2 for scope: lookups in effects. Missing exists guard causes THIRD_MOVE_CHARACTER_EFFECT loc failures when AI path skips selections. |
| needs_parser | scope | any | A later select_trigger uses source = world while visible/enabled/effect-preview logic reads scope:target or another earlier target_flag | Do not use source = world on a selector that depends on earlier selections. Omit source, use an inherited/actor source, or build an interaction_source_list so the chooser retains the previous target flags. | Trade League embargo selected a market, then opened a country selector with source = world and filtered countries by any_owned_location = { market = scope:target }. The selector showed no countries and logged 'Asking for a flag that's not in the interaction target chooser specified'. Removing source = world kept the later selector in the same interaction-target chooser so scope:target remained available. |
| advisory | scope | any | international_organization:type_key used in the same effect call that creates the IO via create_international_organization | Use save_scope_as = my_io inside create_international_organization = { ... }, then reference scope:my_io. Put all post-creation IO operations (every_country loops, set_variable) inside the creation block. Save the calling country BEFORE the block with save_scope_as = tv_founder; use scope:tv_founder inside loops to reference the founder. | international_organization:X link cannot resolve an IO created in the same effect invocation. Only works for IOs created in a previous tick or persisted from prior game state. Pattern empty — detection requires full effect-context analysis. |
| lint | script | any | change_variable = { name = X add = prev.var:some_var } — reading a named variable from prev scope in a numeric value position | Use set_local_variable / change_local_variable to capture the numeric value before the scope switch, then use add = local_var:name inside the new scope. Example: set_local_variable = { name = tmp value = var:some_var }  then in the other scope: change_variable = { name = X add = local_var:tmp }. | prev.var:X is valid only as a scope reference (country, religion, etc.), not as a numeric value source. Using it in an add = position causes 'Variable not of the value scope type. Type: empty'. Built-in scripted values like prev.num_locations are valid inline; only prev.var:X is broken. |
| lint | script | any | var:X ?= { this >= prev.var:some_numeric_var } or var:X > prev.var:some_numeric_var inside a trigger/effect limit | Use root.var:some_numeric_var when root is verified to be the original numeric owner, or capture the value before an effect iterator with set_local_variable and compare/use local_var:name inside the switched scope. | Using prev.var:X as the RHS of a numeric comparison can log 'Invalid right side during comparison 'var'' and make the condition fail every tick. In Engineering Department construction this caused the monthly material check to fail and kept the UI stuck on the materials-insufficient pause state. |
| needs_parser | script | any | local_var:X ?= var:Y, local_var:X > var:Y, or similar dynamic numeric comparisons inside an effect/key iterator when var:Y is owned by the outer scope | Capture the outer-scope value before entering the iterator with set_local_variable, then compare local_var to local_var inside the iterator. For equality, prefer NOT < and NOT > against the captured local instead of ?= var:Y. | During Engineering Department priority-candidate unregister, a variable-map key iterator compared a map-captured local candidate id against the country-scoped current candidate id with local_var:X ?= var:Y. EU5 logged 'Invalid right side during comparison var'. Capturing the country value into a second local before the iterator and comparing locals avoids the RHS parser path. |
| advisory | scope | location | Inside a location-scoped block (`capital = {}`, `var:site ?= {}`, `every_location_in_province = {}`, etc.), reading a country-owned numeric as plain `var:X` | Plain `var:X` always reads the current scope's variable store. After switching into a location/province block, use `root.var:X` when root is the original country owner, or capture the number before the scope switch with `set_local_variable` and use `local_var:X` inside the nested scope. | A nested location/province scope does not inherit country `var:` reads. If the numeric lives on the country, plain `var:X` inside the location block fetches the location variable store instead and can log 'Failed to fetch variable' / unset-scope errors. |
| lint | script | any | random = { chance = X random_list = { ... } } — nesting random_list as a child of random | random_list = { (100-X) = {} X = { random_list = { ... } } }. Use the outer random_list to implement the probability gate and the inner random_list to pick the specific outcome. Both levels of random_list are confirmed valid EU5 syntax. | random_list as a direct child of random = { chance = X } is silently dropped by the EU5 engine — the 8% roll fires but the random_list produces no outcome. All intended events are skipped with no error in the log. Fix: restructure to nested random_list where the high-weight option is the no-event case. |
| advisory | script | any | Applying a computed bonus to an IO typed variable via change_variable inside on_action (e.g. every_international_organizations_member_of { change_variable { name = tv_arts_intl_influence add = local_var:X } }) to implement a monthly character contribution, or moving an existing visible IO variable's monthly gain from monthly_change into monthly_country_pulse/country effects | Put the contribution in the IO type's monthly_change scripted value using leader_country.var:X chained access: if = { limit = { international_organization_has_leader = yes  leader_country = { has_variable = X } }  add = { desc = KEY  value = leader_country.var:X } }. Store computed helper values on the country via on_action only for cached inputs or GUI display. If a visible IO variable stops advancing, fix the monthly_change scope/conditions or its cached inputs; do not migrate the source arithmetic out of monthly_change. | IO typed variables (in the variables = {} block of the IO type) have a monthly_change scripted value that drives the tooltip and automatic monthly breakdown. Contributions applied externally via on_action or country pulse change_variable are invisible to that breakdown and can make the UI lie even when the number changes. The leader_country.var:X chain is valid in scripted value context (same family as root.var:X); it is NOT valid in change_variable { add = } effect context (see change_variable_add_prev_var anti-pattern). Set leader_type = country and call set_leader_country in on_creation or the creation block. |
| advisory | scope | any | international_organization:type_name or is_member_of_international_organization = type_name for an IO type without unique = yes | For non-unique IOs, never add unique = yes. Use on_creation = { set_leader_country = scope:actor } in the IO type definition to set the leader on creation. To scope to an IO instance at runtime, use every_international_organization = { limit = { type = type_name } ... }. Do NOT use international_organization:type_name or is_member_of_international_organization = type_name — both require unique = yes and will error at runtime for non-unique types. | tv_arts_exhibition is a non-unique IO: each country pursuing cultural victory creates their own independent instance. unique = yes is FORBIDDEN on this IO. The engine errors 'Failed to find a valid event target link' and 'does not lead to a unique international organization' for both the scope link and is_member_of_international_organization trigger. Pattern empty — static detection would require cross-referencing IO type definitions. |
| lint | scope | character | is_courtier = yes in every_character limit to find a character dispatched via move_country in a generic_action | Use is_alive = yes + has_variable = <dispatch_tag> instead. The dispatch tag (e.g. tv_on_exhibition) set before move_country is sufficient to uniquely identify the character. | move_country called unilaterally from a generic_action (owncountry type) does NOT grant is_courtier = yes in the destination court. Only formal country_interaction invitations (e.g. invite_artist where the recipient accepts) produce a true courtier relationship. The result: every_character { is_courtier = yes } silently skips the dispatched character in both yearly and monthly pulses — timer never increments, no events fire. |
| needs_parser | scope | any | set_local_variable inside var:X ?= {} and then reading that local_var from prev = {} or after leaving the dynamic scope. Example: var:tv_chief_artist ?= { set_local_variable = { name = ca value = artist_skill } change_local_variable = { name = ca multiply = 0.002 }  prev = { set_variable = { name = tv_chief_artist_monthly_bonus value = local_var:ca } } } | Use chained scope access from the original scope instead of switching when possible. To read a character skill stored in a country variable: value = var:tv_chief_artist.artist_skill. If a temporary value is needed inside a later scope, set_local_variable before entering that scope and read local_var:X inside it; that direction is valid. | The unreliable direction is dynamic-scope local_var escaping outward through prev or later outer-scope reads. The opposite direction is valid: set_local_variable before a var:X ?= {} / location scope switch, then use local_var:X inside the nested scope. |
| advisory | script | any | add_country_to_international_organization = prev or set_leader_country = prev placed as direct (visible) children of create_international_organization = { ... } | Wrap add_country_to_international_organization and set_leader_country inside hidden_effect = { } within the create_international_organization block: create_international_organization = { type = ... hidden_effect = { add_country_to_international_organization = prev  set_leader_country = prev } } | The vanilla tooltip strings THIRD_ADD_TO_INTERNATIONAL_ORGANIZATION_EFFECT and SET_LEADER_EFFECT use INTERNATIONAL_ORGANIZATION.GetName / .GetLeaderTitle. When these effects are visible children of create_international_organization, the engine evaluates the tooltip before the IO is fully instantiated, causing 'Promote INTERNATIONAL_ORGANIZATION returned nullptr' errors. Wrapping in hidden_effect suppresses tooltip generation and eliminates the errors. The outer create_international_organization tooltip (CREATE_INTERNATIONAL_ORGANIZATION_EFFECT) still shows correctly. |
| advisory | scope | any | var:X ?= { trigger_event_non_silently = ... } or var:X ?= { change_variable = ... } inside a random_list weight branch that is itself inside another random_list or every_character | Navigate to the scope ONCE before the random_list using save_scope_as: var:tv_exhibition_home_country ?= { save_scope_as = touring_home }. Then use scope:touring_home = { ... } inside every branch. Guard the outer random_list with if = { limit = { exists = scope:touring_home } }. | var:X ?= {} is the optional-navigation operator: it silently skips the block if the variable is unset or the target doesn't exist. Inside deeply nested random_list branches (every_character → random_list { 8 = { random_list { weight = { ... } } } }) the optional scope switch silently fails — no engine error is logged, the block is skipped, and no event fires. Pre-saving the target as a named scope before the random_list and using scope:name = {} inside branches is the reliable pattern. |
| lint | gui | any | visible = "[InternationalOrganizationsView.GetInternationalOrganization.HasLeader]" — HasLeader does not exist on InternationalOrganization in the GUI data system | Use IsLeaderCountryInPlace instead: visible = "[InternationalOrganizationsView.GetInternationalOrganization.IsLeaderCountryInPlace]". To display the IO leader character's name, use datacontext = "[InternationalOrganizationsView.GetInternationalOrganization.GetLeaderCountry.GetGovernment.GetRuler]" then text = "[Character.GetShortNameWithNoTooltip]". | HasLeader is a method on Unit (for army/navy leaders), not on InternationalOrganization. Calling it on an IO scope produces 'Could not find data system function HasLeader' and a malformed token error. IsLeaderCountryInPlace is the equivalent check for IOs. |
| lint | event | any | flavor = flavor_neutral / flavor_good / flavor_bad inside an event block | Use outcome = neutral / good / bad / positive / negative instead. Valid values confirmed from vanilla: neutral, good, bad, positive, negative. | EU5 uses outcome = (neutral|good|bad|positive|negative); there is no 'flavor' key on events. Using flavor causes 'Unexpected token: flavor' parse errors and the event will be missing an outcome. |
| advisory | event | any | Two or more top-level desc = { ... } blocks on the same event — e.g. desc = { desc = key_a trigger = {...} } followed by desc = { desc = key_b trigger = {...} } | Use a single desc block with first_valid + triggered_desc children: desc = { first_valid = { triggered_desc = { trigger = { ... } desc = key_a } triggered_desc = { trigger = { ... } desc = key_b } } } | EU5 allows only one top-level desc block per event. A second desc block at the same level causes 'Scoped pointer has already been read' and 'There should be only one special desc' engine errors. All conditional descriptions must be nested inside a single desc = { first_valid = { ... } } block. |
| needs_parser | scope | any | scope:actor = { has_variable = X } followed by scope:actor = { var:X = { ... } } in a generic action allow/custom_tooltip/trigger, expecting has_variable to suppress the var:X read | Use the optional variable link for variable-dependent reads: var:X ?= { ... }, var:X ?= 1, or var:X ?= { this >= N }. For generic actions, move a prerequisite to potential only when the action should disappear entirely. Do not rely on has_variable, custom_tooltip order, or separate sibling scope blocks to prevent a direct var:X read. | EU5 generic action UI evaluation may fetch every trigger line needed for tooltips, including sibling scope blocks inside allow. A direct var:X = { ... } fetches X before the logical result of has_variable can protect it, producing 'Failed to fetch variable' and 'Event target link var returned an unset scope'. Optional ?= returns false when the variable/scope is absent and avoids the error. |
| advisory | gui | any | item = { widget = { datacontext = "[Scope.GetCharacter]" ... action_button_diamond = { parameter = { parameter_value = "[Character]" } } } } — rebinding datacontext to Character, then using [Character] in action_button_diamond parameter_value | MakeScope.GetList returns Scope wrappers. Do NOT rebind datacontext inside item's widget. Instead, use the full Scope.GetCharacter chain everywhere: raw_text = "[Scope.GetCharacter.GetNameToFit(...)]", parameter_value = "[Scope.GetCharacter]". action_button_diamond evaluates parameter_value in UIAction context, which does NOT inherit parent widget datacontext — so [Character] after datacontext rebind silently resolves to the outer context or null. | GetList always returns Scope wrappers regardless of stored type. action_button_diamond creates a UIAction object that evaluates parameter_value outside the widget's local datacontext chain. [Character] after datacontext = "[Scope.GetCharacter]" on the parent widget is NOT visible to the UIAction evaluator. Always use the explicit Scope.GetCharacter.xxx chain in every expression within the item, including parameter_value — never rely on datacontext inheritance through action_button_diamond. |
| advisory | modifier | any | INJECT:advance_name = { allow = { ... } } when the vanilla advance already has an allow block | Use REPLACE:advance_name = { <full vanilla block> allow = { OR = { <mod condition> <original condition> } } } to merge conditions. Use INJECT only for advances with no existing allow block. | EU5 advance definitions allow only ONE allow block. INJECT appends properties; a second allow block via INJECT is invalid. Must copy the full vanilla advance definition into a REPLACE block and merge the OR condition manually. |
| lint | scope | any | leader_country = scope:actor inside limit = {} inside any/every_international_organizations_member_of — non-optional = operator on leader_country | leader_country ?= scope:actor — use the optional scope operator so null leader_country returns false instead of erroring | EU5 tooltip evaluator does not short-circuit inside limit = {} blocks. any_international_organizations_member_of iterates ALL IOs the country is a member of (including vanilla coalitions, sects, unions) that have no leader_country. Using = on a null scope throws 'Event target link leader_country returned an invalid object'. Also: DA and Academy IOs had empty on_creation blocks that never called set_leader_country, making leader_country permanently null and breaking the leader block entirely. |
| lint | scope | country | limit = {} as a direct child of any_international_organizations_member_of in a scripted trigger | In trigger context, put iterator filters directly in the block: any_international_organizations_member_of = { international_organization_type = international_organization_type:<io_type> ... }. Reserve limit = {} for effect iterators such as every_international_organizations_member_of. | The trigger iterator treats direct children as trigger clauses; limit is not a trigger here and logs 'Unknown trigger type: limit'. |
| needs_parser | scope | country | Using any_international_organizations_member_of as an effect iterator in a scripted_effect body | Use every_international_organizations_member_of = { limit = { international_organization_type = international_organization_type:<io_type> ... } ... } when applying effects. Keep any_international_organizations_member_of only inside trigger contexts such as if/limit blocks. | any_international_organizations_member_of is trigger syntax. In effect context the engine logs Unknown effect any_international_organizations_member_of. |
| lint | scope | country | any_international_organizations_member_of directly under country_interaction potential | Wrap country-scoped checks in scope:actor = { ... } inside country_interaction potential. | country_interaction potential does not provide a country root for this iterator; direct usage logs inconsistent trigger scopes (invalid vs. country). Official defines document scope:actor as the acting country. |
| lint | script | any | Calling international_organization_chooses_new_leader anywhere in TV IO-related code (leader management actions, on_action, events, effects) | Never call international_organization_chooses_new_leader on TV IO types. The leader_country is locked to the founding country via on_creation and never reassigned. The great person character is tracked via independent country variables. This effect triggers the vanilla EU5 leader-election process, which violates the no-elections design. | international_organization_chooses_new_leader re-runs the leader selection process, which with leader_change_trigger_type = none still evaluates the leader = {} block and may reassign the engine-internal leader state in ways inconsistent with the design. The design deliberately omits this call to fully prevent any automatic leader change. |
| lint | scope | any | unique = yes on a TV IO type definition (tv_arts_exhibition, tv_diplomatic_alliance, tv_academy_of_sciences) | All three TV IOs are non-unique (unique = no). Never add unique = yes. Use every_international_organizations_member_of = { limit = { international_organization_type = international_organization_type:<io_type> } ... } to scope to them from country scope. | Adding unique = yes would collapse per-country IO instances into one shared instance, breaking the independent-per-country design. It also makes international_organization:type_name syntax appear to work, masking the underlying design violation. |
| lint | scope | any | international_organization:tv_arts_exhibition (or tv_diplomatic_alliance, tv_academy_of_sciences) scope link | Use every_international_organizations_member_of = { limit = { international_organization_type = international_organization_type:<io_type> } ... }. The international_organization:type_name syntax requires unique = yes and produces a runtime error for non-unique types. | All three TV IO types have unique = no. Using international_organization:X on non-unique types causes 'does not lead to a unique international organization' and the scope resolves to null, silently skipping all effects. |
| lint | scope | any | appointed_leader.adm, appointed_leader.dip, appointed_leader.artist_skill, etc. in TV IO monthly_change or monthly_effect blocks | Use leader_country.var:tv_xxx_leader_char.attribute instead. Example: value = leader_country.var:tv_academy_leader_char.adm. Always guard with international_organization_has_leader = yes and leader_country = { has_variable = tv_xxx_leader_char }. | appointed_leader accesses the leader via the vanilla IO leader mechanism, which in our design is decoupled from the great person character. The great person is stored as a country variable on leader_country; chained access leader_country.var:tv_xxx_leader_char.attribute is the correct pattern. |
| lint | modifier | character | value = character.artist_skill  multiply = 0.002 (assuming artist_skill is an integer 0-100 scale) | Use multiply = 0.1 (or other factor scaled to the 0-1 range). artist_skill in EU5 is a normalized float 0.0-1.0, not a raw integer. A skill-5 artist returns ~0.5, not 5. | artist_skill is a level-based attribute normalized to [0,1] by the engine when accessed as a scripted value. Design formulas written assuming 0-100 scale must divide the intended multiplier by 100. |
| advisory | modifier | character | Static modifier used with add_character_modifier but defined without game_data = { category = character }, e.g. my_modifier = { } | All static modifiers intended for use with add_character_modifier / has_character_modifier MUST include game_data = { category = character }. Example: my_modifier = { game_data = { category = character } [effects...] } | Without category = character, the engine does not recognize the modifier as character-scoped. add_character_modifier runs (tooltip shows the grant) but the modifier is not stored on the character — has_character_modifier returns false and no title prefix fires. Confirmed at runtime: tv_chief_artist_role_modifier = {} appeared in appoint tooltip but never on the character panel. |
| lint | modifier | country | Applying Governor's House War Tent tactics with add_character_modifier | Governor's House War Tent tactics intentionally use country-scoped modifiers as a fallback because the combat modifiers did not take effect from the general character in this system. Keep a code comment near the country modifier application explaining the approximation. | Vanilla character static modifiers can carry commander/army combat modifiers in general, but the Governor's House stored-general tactic application did not affect combat from the character. This feature is intentionally approximated as a country modifier. |
| lint | gui | character | Arts leader select_trigger using column.data = character_info | Use column.data = character_artist for artist-only character selectors so the list uses the artist ability view. | The character_artist attribute column swaps the character list's ability area and tooltip to artist skill. Generic character_info shows ruler-style ADM/DIP/MIL information and is the wrong view for selecting a Chief Artist. |
| advisory | gui | scripted_triggers | custom_description { text = KEY  has_variable = X  var:X >= N  has_variable = Y  var:Y >= M } — multiple independent conditions collapsed into one block | Use one custom_tooltip block per condition group at the top level of the scripted_trigger body (implicit AND). The extra_trigger_block YAML field must be a complete custom_tooltip { } block, not raw condition lines. | When multiple sub-conditions are collapsed into one custom_description or custom_tooltip, the player sees a single pass/fail indicator for all conditions combined. Separate custom_tooltip blocks each show their own pass/fail state, allowing the player to see which specific sub-condition is not yet met. |
| advisory | localization | character | Defining a second character_title_prefix block in a separate customizable_localization file | Copy/generate the full vanilla character_title.txt and insert custom title text entries into the original character_title_prefix block. | Top-level customizable localization keys are unique database entries, not additive merge blocks. A duplicate block is ignored, so custom character title prefixes never load. |
| advisory | gui | international_organization | Relying on InternationalOrganization.GetIcon for a mod-defined IO without providing a matching icon texture | Add a DDS at main_menu/gfx/interface/icons/international_organizations/<io_type>.dds. The filename should match the international_organization type key, e.g. tv_arts_exhibition.dds. | The shared IO UI calls InternationalOrganization.GetIcon. Vanilla IO icon files are named after IO type keys in gfx/interface/icons/international_organizations/. Mod IOs without matching files fall back to the generic IO icon. |
| advisory | localization | game_concepts | Adding localization keys game_concept_tv_x / game_concept_tv_x_desc and then using [tv_x|e] without defining tv_x in main_menu/common/game_concepts | Register every custom concept ID in src/main_menu/common/game_concepts/tv_game_concepts.txt with at least a texture block. Localization alone is not enough. | Unregistered custom concepts in square brackets are parsed as data-system functions. The engine logs 'Could not find data system function <concept>' and renders ERROR:<concept> in tooltip text. |
| advisory | gui | localization | Using dynamic concept-link helpers with key expressions that can be localized before the concept-link parser consumes them | Feed SelectGameConcept or [...|E] only registered raw concept ids. For generated dynamic routes, store a numeric id and build ids such as tv_wonder_display_<id>; use Localize(Concatenate('game_concept_', key)) only for intentionally plain text. | The failing test used a flag-derived wonder key that localized before link parsing. The bug is the localized key source, not numeric-id dynamic concept routing. |
| advisory | gui | variable_map | Using GetFlagName from a script variable or variable_map value as a raw script key for localization, modifier, texture, or effect-name concatenation | If the downstream GUI needs a stable script key, store a numeric id and generate static id branches, or use a typed datamodel object with a real GetKey accessor. Treat GetFlagName as display/localization-prone text. | Concept/localized flags can return their localized display name in GUI. Concatenating that result produced game_concept_法罗斯灯塔 and 法罗斯灯塔_level_3 instead of raw script keys. |
| needs_parser | scope | variable_map | Using a quoted variable_map/global_variable_map/local_variable_map lookup directly as an equality right-hand side, trigger comparison RHS, or effect parameter after scope switching | Use variable-map lookups primarily as left-side scope links. When the mapped value is needed as a RHS, first capture it with set_local_variable or set_variable in the map-owning scope, then use local_var:X or var:X. Prefer local_var for temporary values. | Quoted variable-map expressions are reliable for entering the mapped value scope on the left side. Runtime tests found direct RHS use unreliable for value extraction or comparisons, e.g. building_type = "variable_map(...)" inside a later location/building scope. Cache the mapped value before entering the later scope. |
| advisory | performance | variable_map | Calling a variable-map/global-map rebuild effect, including an if-needed rebuild router, from GUI refresh, country cache refresh, tooltip/projection, selection, monthly read, or other hot/read paths as a compatibility fallback | Build global variable-map indexes only at lifecycle points: startup, save-load initialization, data-change regeneration, or explicit initialization. If a read misses a map, fix the current lifecycle hook or the call order that reads before initialization; do not add if-needed routers, old-schema repair, or legacy state preservation. | Engineering Department briefly added tv_wonder_index_rebuild_global_maps_if_needed_effect with a map-version gate, then called it from cache/module refresh paths. That violated the no-compatibility policy, hid lifecycle/call-order bugs, and charged hot paths for defensive global-map checks. |
| needs_parser | scope | variable_map | Using root inside a variable-map key callback called from a generic-action effect to write back to the action actor/current country | Save the current owner scope before random_key_in_variable_map/every_key_in_variable_map, then write back through scope:<saved_owner>; preserve the variable-map key stream instead of replacing it with generated per-id branches. | Generic action selector hover can pre-evaluate action effects without a usable root event target. A proposal deck helper using random_key_in_variable_map -> root = { set_variable = { value = prev } } logged Event target link 'root' returned an unset scope and left no proposal slots. |
| needs_parser | scope | variable_map | Calling is_key_in_variable_map on the current every_key_in_variable_map/ordered_key_in_variable_map callback scope, especially with target = this | Save the map-owning scope before the key iterator, copy this into a local variable, and run is_key_in_variable_map inside scope:<saved_owner> with target = local_var:<key>. | In a key iterator callback the current scope may be the numeric key itself (for example 1.00), which does not support scope-owned variables. Membership checks for sibling country-scoped maps must execute from the saved country/map owner while the key is passed through a local variable. |
| advisory | scope | variable_map | Using an inflated ordered_key_in_variable_map max value such as 10000 to process every current key | Use every_key_in_variable_map with a found/stop flag when all keys may need inspection, or set ordered_key_in_variable_map max only to an exact known upper bound that cannot exceed the current map size. | EU5 logs a Script system error when ordered_key_in_variable_map max is larger than the current key list, even though it reports that it capped max at the list size. Do not use a huge max as a harmless 'iterate all' shortcut. |
| advisory | gui | image | Using CSS-like GUI image fittype = contain | Use a vanilla-supported EU5 fittype such as centercrop, fill, start, or end, or omit fittype when no fit transform is needed. | EU5 GUI fittype names are not CSS object-fit names. The location-window dynamic test icon used contain and failed static GUI conversion at startup. |
| lint | localization | gui | Engineering Department support building localization keys used as square-bracket game concept links | Use plain localization substitution such as $tv_wonder_labor_camp$ for building names, or register a real game concept before using [key|E]. | These support buildings have normal localization keys, not game_concept definitions. In GUI-bound localized text, [tv_wonder_labor_camp|E] is parsed as a data-system function and logs Could not find data system function. |
| advisory | localization | generic_actions | Using datacontext-dependent Country.MakeScope or container-dependent SCOPE.sCountry('actor') reads inside generic action title/description localization | Generic action description loc may be prefetched with no Country datacontext and no script-scope Container. Preserve player-country dynamic features by using a context-independent global binding such as Player.MakeScope, or move non-player-scope dynamic lines to a GUI widget/tooltip with an explicit datacontext. Do not replace the dynamic feature with static text without user approval. | action_button descriptions may be localized once through a contextless prefetch path and again through the real action hover. Country.MakeScope.GetVariable(...) and SCOPE.sCountry('actor').MakeScope.GetVariable(...) produced continuous PdxDataFetchLocalizedData errors even while the visible tooltip rendered the selected wonder correctly. |
| lint | gui | any | GetVariable('tv_governed_area').GetArea.GetName — navigating from a scope variable that stores an Area to call .GetName directly via .GetArea | GetVariable(...).GetArea is not a valid EU5 GUI accessor. To display an area name from a scope variable, store a location from the area as a separate character variable (e.g. tv_governed_area_capital), then use GetVariable('tv_governed_area_capital').GetLocation.GetArea.GetNameWithNoTooltip. Location.GetArea.GetName IS valid when the Location type is a proper GUI-layer typed object (e.g. from GetLocation on a typed view). | The accessor .GetArea exists on the Location GUI type but not on scope variable wrappers returned by GetVariable(). Chain: GetVariable('loc_var').GetLocation.GetArea.GetNameWithNoTooltip works because GetLocation returns a typed Location object. Pattern rule narrowed to only flag GetVariable(...).GetArea. to allow Location.GetArea. chains. |
| lint | event | character | kill_character = yes | Use kill_character = this when in character scope (e.g. inside var:name ?= { }), or kill_character = scope:char_ref from any scope. kill_character = yes is invalid — EU5 expects a character scope reference, not a boolean. | kill_character takes a character scope value, not a boolean. kill_character = yes causes PostValidate error: target scope type boolean, expected character. |
| lint | event | character | remove_trait = blind | remove_trait = trait:blind — always prefix the trait name with trait: for remove_trait and add_trait effects. Note: has_trait = blind (no prefix) is correct for trigger checks. | remove_trait effect requires trait: prefix (remove_trait = trait:blind). has_trait trigger does NOT use the prefix (has_trait = blind). Mixing them causes: remove_trait effect [ target: field not set ]. |
| advisory | localization | any | none_available_msg_key localization value missing @trigger_no! icon prefix | In the localization YAML, the VALUE of the none_available_msg_key string must begin with @trigger_no! — e.g. "@trigger_no! No characters available." The key name itself has no prefix requirement. | Engine logs: Key X doesn't exist or doesn't start with trigger_no icon. The @trigger_no! inline icon must be the first token in the localization value string. |
| lint | script | any | Placing character trait definitions in common/character_traits/ (e.g. src/in_game/common/character_traits/my_traits.txt) | Place trait files in common/traits/ — the correct EU5 directory: src/in_game/common/traits/my_traits.txt | EU5 loads character traits from common/traits/ not common/character_traits/. Files in the wrong directory are silently ignored and all has_trait checks against those trait IDs produce 'Invalid database object' errors at PostValidate. |
| advisory | script | location | construct_building = { ... cost_multiplier = N ... } without cost_multiplier_reason | When construct_building uses cost_multiplier, also provide cost_multiplier_reason = <loc key>. For event-funded free construction, vanilla commonly uses cost_multiplier_reason = "game_concept_event". | EU5 logs a script system error when construct_building has a cost multiplier but no reason key: 'No reason given for the cost multiplier in construct_building effect'. |
| advisory | modifier | country | Using capital_country_modifier for a building that can be finalized or created outside the capital, expecting country-wide effects everywhere | Keep local effects in building modifier/raw_modifier; apply national effects separately with scripted add_country_modifier effects, or require the building to be in the capital. | capital_country_modifier is explicitly applied to the country only if the building is built in the capital. Engineering Department wonders may be sited outside the capital, so their national/base/ceremony effects are permanent country modifiers and the final buildings carry local effects only. |
| advisory | localization | any | text = "LOC_KEY" in a GUI file when LOC_KEY is not defined in main_menu/localization | Define the key in every supported localization file, correct the key to an existing one, or use raw_text for literal display strings. | GUI text fields are localized. A typo such as TV_RM_TARGET_STEAM_ENGINE when only TV_RM_TARGET_IRON_WORKING exists logs 'Unlocalized text' at load. |
| advisory | script | country | Defining a generic action without listing it in common/generic_action_ai_lists | Add every generic action id to at least one generic_action_ai_lists file. Use the list potential to restrict evaluation to countries that can use that feature; player-only actions may still set ai_will_do = { add = -100 }. | Unlisted generic actions are put into the global AI list and the engine logs a performance warning: 'Action X is not explicitly listed in an ai list!'. |
| advisory | modifier | country | Defining a mod price key without a matching <price_key>_cost_modifier modifier type | For every mod-defined price key, add <price_key>_cost_modifier in main_menu/common/modifier_type_definitions with category = country, and localize the price key plus MODIFIER_TYPE_NAME/DESC entries. | EU5 automatically looks for a cost modifier type named <price_key>_cost_modifier for each price. If it is absent, the engine logs 'Missing modifier type for price'. |
| advisory | modifier | country | Defining a custom goods key without the matching auto-requested modifier type family | For every custom goods ID, add ban_exports_of_<good>, ban_imports_of_<good>, local_<good>_output_modifier, global_<good>_output_modifier, can_extract_<good>, <good>_impacts_inflation, <good>_used_for_minting, and global_<good>_pop_demand in main_menu/common/modifier_type_definitions, matching vanilla goods categories and boolean/percent flags. | EU5 automatically tries to use these modifier type names for every goods database entry. A custom goods key without the family logs 'Modifier type definition <name> must exist in DB'. |
| advisory | script | any | Defining a custom goods key with a vanilla goods color such as color = goods_masonry | Create a unique named color for the custom good in main_menu/common/named_colors, then reference it from the goods definition, e.g. color = goods_tv_wonder_materials. | EU5 logs 'Goods <custom_good> has same color as <vanilla_good>' when two goods resolve to the same named color/value. Custom goods should have their own goods_<id> color key. |
| advisory | modifier | any | Defining a custom modifier type without a matching common/modifier_icons entry | For every mod-defined modifier type, add a same-key entry in main_menu/common/modifier_icons with at least positive = "gfx/interface/icons/modifier_types/<vanilla_or_custom_icon>.dds". Reuse a close vanilla icon when no custom asset is needed. | EU5 loads modifier icon mappings separately from modifier_type_definitions. A valid custom modifier type can still log 'Missing Icon for Modifier : <name>' at startup until common/modifier_icons maps the key to a DDS. |
| advisory | script | country | Defining a generic action without a PERFORM_<action_id>_ACTION message type | Add a PERFORM_<action_id>_ACTION block to scripts/gen_messagetypes.py TV_ENTRIES, rerun scripts/gen_messagetypes.py, and define SETUP/LOG/MAP localization keys for supported languages. | EU5 expects a message type for generic action notifications. Missing entries log 'Failed to find message type: PERFORM_<action_id>_ACTION' at startup or action evaluation. |
| lint | localization | any | ROOT.MakeScope.GetVariable('var_name') inside event localization | Use ROOT.GetVariable('var_name') in event localization. Reserve Country.MakeScope.GetVariable(...) / Location.MakeScope.GetVariable(...) for GUI object contexts. | In event localization, ROOT is already a script scope. Adding MakeScope produces runtime data errors such as 'Could not find promote for MakeScope' and 'Failed converting statement'. |
| advisory | script | international_organization | Changing an IO type variable inside a scripted_effect with a bare change_variable, expecting callers to show the variable gain or loss | Wrap the IO-scope change_variable in custom_description = { text = <effect_loc_key> value = $value$ ... } and register <effect_loc_key> in common/effect_localization with global/first/third/past/negative perspectives. Add matching main_menu localization strings for those perspective keys. | Scripted effect calls can otherwise produce empty option/action tooltips even though the IO variable changes. Vanilla uses custom_description plus effect_localization for imperial, celestial, and papal authority so the tooltip shows the signed value while executing the real effect. |
| advisory | localization | any | Using [COUNTRY.GetName] in third/third_past effect localization strings for a custom_description shown from a generic action tooltip | Keep effect localization perspective strings self-contained, e.g. 'Gains $VALUE|+$ #Y $var$#!'; do not assume COUNTRY exists during action hover pre-evaluation. | Generic action hover pre-evaluation may select the third-person effect localization perspective without a COUNTRY promote target. [COUNTRY.GetName] then returns nullptr and logs data errors repeatedly while hovering. |
| advisory | script | location | Event-created artwork uses create_art with only artist/quality and no explicit type/key, expecting a reliable tooltip and artwork name | For event-created named artworks, provide an explicit work_of_art_type and key with matching main_menu localization. If the type depends on the artist, branch with visible if/else_if create_art blocks so the real effect tooltip renders. | tv_arts_exhibition.406 created a work in the capital but the option tooltip was empty and the resulting artwork had no name. Vanilla named artworks pair create_art with both type = work_of_art_type:<type> and key = <loc_key>. Visible if/else_if branches can render the real create_art tooltip while still selecting the appropriate art type by artist_type. |
| advisory | script | international_organization | Putting non-player-facing maintenance logic directly in an international_organization monthly_effect block | Wrap hidden IO maintenance in monthly_effect = { hidden_effect = { ... } }. Use visible monthly_effect children only when the effect text is meant to appear in the shared IO tooltip. | The shared IO tooltip can render visible monthly_effect children, exposing internal set_variable/custom_tooltip refresh logic. Vanilla HRE and Union wrap monthly maintenance in hidden_effect so the logic still runs but does not clutter player-facing IO tooltips. IO variable monthly_change remains the correct place for visible variable breakdowns. |
| lint | gui | international_organization | Trying to hide an IO parliament tab with blockoverride "organization_parliament_tab_visible" | Set has_parliament = no on the international_organization type. Keep law requires_vote behavior as a separate design decision. | The vanilla organization panel comments out the would-be parliament visible block and binds the tab directly to InternationalOrganizationsView.GetInternationalOrganization.HasParliament. There is no block to override. |
| advisory | script | international_organization | IO law votes routed through parliament without an implemented special status that has special_status_power | For uses_parliament_for_law_votes IOs, add a special status in common/international_organization_special_statuses, list it in special_statuses_implemented, give it special_status_power, define <status>_can_participate_in_parliament with category = internationalorganization, and enable that modifier in the parliament type. | The policy_vote resolution requires country_combined_special_status_power(scope:recipient) > 0 when uses_parliament_for_law_votes_trigger is active. Without a participating special status, the law debate can open but the IO parliament page has no voter group to show. |
| advisory | modifier | international_organization | Defining a custom IO special status without the matching auto-requested parliament modifier type pair | For every custom IO special status key that can be implemented on an international organization, define <status>_can_participate_in_parliament as boolean and <status>_agenda_impact as percent in main_menu/common/modifier_type_definitions with game_data category = internationalorganization. | EU5 automatically tries to resolve these modifier type names for custom IO special statuses; missing definitions produce startup DB assertions such as 'Modifier type definition <status>_can_participate_in_parliament must exist in DB'. |
| advisory | scope | international_organization | Using scope:recipient inside IO policy allow/on_activate/on_deactivate to read or mutate the IO | Use the current IO root directly in policy allow/on_activate/on_deactivate. Reserve scope:recipient for documented country-root policy AI math blocks such as wants_this_policy_bias and wants_propose_policy. | Generated Diplomatic Alliance laws wrapped IO variable checks and tier changes in scope:recipient in execution blocks. At runtime those blocks had no recipient event target, causing repeated 'Undefined event target recipient' and unset-scope errors. |
| needs_parser | scope | international_organization | Using scope:recipient inside IO policy AI math without first checking exists = scope:recipient | In wants_this_policy_bias, wants_propose_policy, wants_keep_policy, reasons_to_join, and diplomatic_capacity_cost, guard direct scope:recipient reads with exists = scope:recipient or use optional scope:recipient ?= blocks. | Although the law readme documents recipient = IO for policy AI math, the engine can pre-evaluate these maths without a recipient event target. Unguarded scope:recipient reads caused repeated 'Undefined event target recipient' and unset-scope errors in generated Diplomatic Alliance laws. |
| lint | scope | country | Comparing leader_country to this inside any_international_organizations_member_of in a generic_action_ai_list | The AI-list potential root is the evaluating country, while this becomes the IO inside the iterator. Compare with root instead: exists = leader_country; leader_country = root. | Inside the IO membership iterator, this is the international organization. leader_country = this compares a country to an international_organization and logs a type-mismatch error. |
| advisory | script | international_organization | Adding IO parliament issues for a special status without any valid parliament_agenda for that special status | Add at least one common/parliament_agendas entry with type = international_organization and special_status = <status>. Keep its potential/allow valid for the IO so the parliament button has a special-status agenda to resolve. | IO parliaments rely on special-status agendas as well as issues. Without a valid agenda for the participating special status, the UI can report that no special status wants to propose an issue even when parliament_issues exist. |
| advisory | script | international_organization | A single-member IO relies on a generic member special status to propose IO parliament issues | Do not rely on vanilla call_organization_parliament for single-member support-token meetings. The mechanic needs a different design before custom support meetings are enabled. | The vanilla call_organization_parliament picker builds its issue list through every_possible_parliament_issue and reports 'no special status wants to propose an issue' when it cannot surface one. propose_parliament_issue then initializes support from the issue special status and marks the proposer as voting yes. In a founder-only IO, self-backed support issues are brittle even with a leader status. |
| lint | scope | country | Reusable country milestone trigger uses root inside a nested market iterator | Use prev inside nested iterators so the target remains the currently checked country when the trigger is called from situation->every_country. | GUI ShowTriggerConditions(PlayerScope.Self) can pass because root is the player country, while the monthly situation checker calls the same trigger from a situation root. In any_market_with_merchants, prev points back to the iterated country. |
| advisory | gui | any | Putting text_single with multiline text inside an hbox elastic column whose width is unconstrained (for example size = { -1 H }) | Give the text area an explicit width or maximum width. For paragraph-like content, wrap it in a fixed-width widget/card and use text_multi with max_width and autoresize, as vanilla IO GUI does. | In Jomini GUI, multiline text can contribute its natural text width back into hbox layout. A right-side size = { -1 H } text column in the Engineering Department proposal row expanded to 446px and pushed its parent vbox from 470px to 548.3px even though the outer card bounds were correct. |
| lint | script | any | Using display/alias-like goods names such as candles or luxury_cloth in production method upkeep | Use real goods database IDs: beeswax for candle upkeep, fine_cloth for luxury cloth upkeep. | Production method input keys must be exact goods IDs. Invalid goods tokens inside common/production_methods can load as 'Unexpected token' errors, as seen with candles and luxury_cloth. |
| needs_parser | script | any | Production method base input/output values produce profit above 30% | Compute recipe value from goods default_market_price and keep base profit below 30%, usually by slightly increasing inputs or reducing output/default output price. | EU5 logs a startup error when a production_method's base profit exceeds 30%, e.g. tv_wonder_material_dispatch_point_production at 33.33%. A recipe worth 5 output needs input value greater than 3.8462 to be just under 30% profit. |
| lint | script | building | Using building levels multiplied by nominal employment size to determine how many workers a building actually employs | Scope into the building object with ordered_buildings_in_location/random_buildings_in_location and read building_employed_amount. If downstream variables are stored as people rather than pop-size units, multiply the value by 1000. | location_building_level reports completed levels/capacity, not current staffing. For labor-sensitive mechanics such as Engineering Department construction, underfilled labor camps must contribute only their actual employed laborers. |
| advisory | script | building | Custom generic-action building buttons rely only on can_build_building and do not repeat the building max-level / in-construction cap check | Add an explicit trigger shared by the action allow, select_trigger visible/enabled, and final effect guard. For per-country building caps, use total_building_levels_including_construction:<building> so queued levels count. For one-per-location selectors, also filter existing buildings and any_buildings_in_location with building_levels_under_construction >= 1. | Building max_levels can stop normal construction but a custom IO/generic-action button can still spend its scripted price unless its action and effect repeat the cap logic. Re-check inside the effect because generic action UI pre-evaluation and stale target lists can otherwise charge the player for a no-op. |
| needs_parser | script | building | A while loop checks location_building_level and then tries to drive construct_building instant = yes or change_building_level_in_location until the level catches up | Do not wait for location_building_level to change inside the same effect. Use fixed if / else_if branches or a bounded script-value delta so the effect runs once and never depends on a 0-day build task updating synchronously. | Wonder selection/finalization previously used while loops that assumed instant construction updated the location building level during the same effect. That can deadlock or spin forever because the engine only creates a queued task. Treat instant construction as asynchronous for script control flow. |
| advisory | script | building | Using building on_built as the only completion signal for a mechanic that also upgrades existing building levels | Use on_construction_ended to directly mark the active auxiliary-building ritual finished and run the normal ritual completion effect. Do not depend on on_built for upgrades, and do not require location_building_level to have updated inside the construction-ended hook. | Engineering Department style-2 ritual annex completion relied on on_built. Fresh annex construction completed, but expansion reused the same annex building as a higher level, so the ritual needed construction-ended direct completion. |
| advisory | scope | international_organization | Calling a country-scoped scripted_trigger from an IO variable monthly_change when that trigger depends on root, prev, caller-specific saved scopes, or country variables | Write the monthly_change condition in IO scope, or create a separate IO-scoped trigger. Use leader_country ?= { ... } for country state, compare IO variables directly against leader_country.var:X from the IO scope, and inline ownership/state checks whose prev/root chain must point at the leader country. | monthly_change evaluates from the international organization/variable context. A nested leader_country block does not make root inside a called scripted_trigger become the country, and caller-sensitive prev/root chains can differ from the same trigger's country-pulse behavior. In Engineering Department survey progress, reusing tv_wonder_survey_active_trigger inside the IO variable monthly_change silently prevented the visible IO progress variable from gaining monthly progress; inlining the active-survey conditions under leader_country preserved monthly_change as the tooltip-visible source. |
| lint | script | any | Filtering random_list branches with modifier = { factor = 0 ... } | Use trigger = { ... } inside each weighted random_list branch. Vanilla random_list branch eligibility uses trigger blocks; use NOT = { var:current ?= N } there when a reroll must exclude the current result. | Engineering Department estate-demand rerolls used modifier factor zero in random_list branches. A player could bribe a demand into the artwork-loss branch and repeated same-day rerolls kept resolving to that same visible demand. Replacing branch modifiers with trigger filters and excluding the current demand makes the random pool behave like vanilla and prevents visible no-op rerolls. |
| advisory | script | any | Using common/music_player_tracks as if it can load raw audio files or define country/event/war trigger conditions for automatic music playback | Register only Wwise event keys there, with optional composer/performer/soloist metadata and matching music_player localization. Use verified GUI/audio hooks such as Audio_PlayEvent(..., 'music_manager') or hardcoded on_actions for conditional music behavior; do not invent trigger fields inside music_player_tracks. | The official type note says new songs are added in Wwise and registered by event key. Vanilla entries contain no trigger/chance/category fields beyond metadata, and the in-game music player plays registered track objects via GUI bindings. EU5 does expose Audio_PlayEvent to send a Wwise event to music_manager from GUI, and on_war_declared exists as a script hook, but current references do not expose EU4-style songs.txt conditional rules or a normal effect that directly plays a named music-player track. |
| advisory | script | location | Applying vanilla promote_religion's full target condition to a regional priority heuristic without checking the mechanic's design intent | Use the full vanilla OR when mirroring cabinet-action province eligibility. For Governor's House autonomous conversion priority, use only the share of owned locations whose dominant_religion differs from the governing country's religion. | Vanilla promote_religion can still work on minority non-state-religion pops when local_religious_unity is below 100%. The Governor's House design intentionally ignores that cleanup case so regional governors only prioritize locations where the non-state religion is still dominant. |
| advisory | modifier | character | Putting country_cabinet_efficiency inside a character-scoped modifier to lower one cabinet member's efficiency | Use character_cabinet_efficiency for character modifiers. Reserve country_cabinet_efficiency for country modifiers. | country_cabinet_efficiency is a country modifier type and has no effect on a character modifier; the character-scoped equivalent is character_cabinet_efficiency. |
| advisory | gui | texture | Using texture = "[Concatenate('gfx/...', key, '.dds')]" to dynamically select a raw DDS path | Route dynamic images through a GUI function that returns a texture handle, such as GetConceptTexture(...). For arbitrary mod DDS assets, register image-only game concepts like tv_wonder_display_image_<id>, add matching game_concept_* and game_concept_*_desc localization, and build that concept id from a numeric slot id. | In the location-window test, GetConceptTexture(Concatenate(...)) rendered, but nested, suffix-only, and flat Concatenate expressions that returned raw DDS path strings did not render and produced no useful error log. Static texture paths are parsed by the GUI loader, while runtime texture expressions need a texture-like return value rather than a CString path. |
| advisory | gui | modifier | Calling ShowModifierEffect with modifier ids built only by GUI Concatenate(...) and never referenced from script effects | Keep the dynamic GUI id flow, but generate unreachable script references under if = { limit = { always = no } ... } using add_country_modifier/add_location_modifier for every possible modifier id. | The GUI can build ids such as tv_wonder_display_<id>_level_<level> at runtime, but engine/static lookup paths may not recognize modifier ids that only occur inside Concatenate. The unreachable effect gives the loader static modifier references without changing runtime state. |

## Valid Enum Values

| Enum | Valid Values | Notes |
| --- | --- | --- |
| location_rank | rural_settlement, town, city, megalopolis | Valid location rank type keys in EU5 1.x. EU4 names like village/hamlet/metropolis are invalid; use rural_settlement/town/city/megalopolis. |
| government_type | monarchy, republic, theocracy, tribe, steppe_horde | Government type IDs for trigger: government_type = government_type:X. Use exact IDs — not pluralized (monarchies/republics). steppe_horde is the horde subtype. |

## Quick Reference Rules

- **3-Step Rule**: Direct edit only if 100% certain → `reference_official_defines/` → `reference_game_files/` + `reference_mods/`
- **Mandatory Step 2/3** for: `blockoverride` names, `custom_tooltip` keys, `situation_card_common` structure, `location_rank:*`, modifier names, scripted triggers/effects, localization YAML encoding, GUI expressions
- **Verification format**: output `**Verification** — Step [2/3], Reference: file:line, Quote: "exact text"` before any code
- **Bug fix rule**: Replace syntax with verified form; do NOT remove the feature
- **Pre-test**: Run `python scripts/validate.py --changed` before launching game
- **Float precision**: EU5 truncates all floats at 5 decimal places; never emit 6+ dp in mod files

## Symbol Quick-Reference

> Read `data/index/<name>.txt` for the full list (one entry per line).  
> For icon availability, check `data/index/icons.txt` before using `@icon_name!` inline syntax.

| Category | Count | Index file | Notes |
| --- | --- | --- | --- |
| GUI Icons (`@xxx!`) | 351 | `data/index/icons.txt` | Use `@name!` in raw_text/text fields and YAML values |
| Scripted Triggers | 2695 | `data/index/scripted_triggers.txt` | Verify name before using in `trigger = { ... }` |
| Scripted Effects | 3914 | `data/index/scripted_effects.txt` | Verify name before using in `effect = { ... }` |
| Static Modifiers | 2329 | `data/index/static_modifiers.txt` | Modifier name whitelist; validate.py checks these |
| English Loc Keys | 11940 | `data/index/loc_keys_en.txt` | All defined EN keys; cross-check before adding duplicates |

## Codegen Script Map

| Script | Input(s) | Output(s) | When to run |
| --- | --- | --- | --- |
| `scripts/gen_brief.py` | anti_patterns.yaml + valid_enums.yaml + PROJECT_OVERVIEW.md + risk_cards/ | docs/knowledge/BRIEF.md | After editing any AI knowledge file |
| `scripts/gen_index.py` | reference_game_files + src/ | data/index/*.txt symbol indexes | Run by gen_brief.py automatically |
| `scripts/gen_scaffold.py` | --type argument | Scaffold .txt/.yml file in dir | When creating a new EU5 file |
| `scripts/ai_context.py` | --changed / --files | Compact AI task context + risk cards | At AI session start or before editing a task domain |
| `scripts/test_lint_rules.py` | anti_patterns.yaml + tests/fixtures/anti_patterns/ | Fixture test report for lint regexes | After adding/changing detectability: lint rules |
| `scripts/gen_victory.py` | data/victory_paths.yaml | 13 victory-path game files (see below) | After editing data/victory_paths.yaml |
| `scripts/in_game/common/customizable_localization/gen_character_title.py` | data/io_leaders.yaml | src/in_game/common/customizable_localization/character_title.txt | After editing IO leader title data |
| `scripts/validate.py` | src/ mod files + knowledge YAML | Console report (exit 0/1) | Before launching game; --changed for quick check |

## Auto-Generated Files (do not edit directly)

> These files have `# @AUTOGENERATED` on line 1.  
> `validate.py` warns if any of them appear in `git diff --cached`.

| File | Generator |
| --- | --- |
| `docs/knowledge/BRIEF.md` | `scripts/gen_brief.py` |
| `data/index/icons.txt` | `scripts/gen_index.py` |
| `data/index/scripted_triggers.txt` | `scripts/gen_index.py` |
| `data/index/scripted_effects.txt` | `scripts/gen_index.py` |
| `data/index/static_modifiers.txt` | `scripts/gen_index.py` |
| `data/index/loc_keys_en.txt` | `scripts/gen_index.py` |
| `src/in_game/common/scripted_triggers/towards_victory_triggers.txt` | `scripts/gen_victory.py` |
| `src/in_game/common/scripted_effects/towards_victory_effects.txt` | `scripts/gen_victory.py` |
| `src/in_game/common/static_modifiers/towards_victory_modifiers.txt` | `scripts/gen_victory.py` |
| `src/in_game/common/situations/towards_victory_situations.txt` | `scripts/gen_victory.py` |
| `src/in_game/common/on_action/towards_victory_yearly.txt` | `scripts/gen_victory.py` |
| `src/in_game/events/towards_victory_conquest_events.txt` | `scripts/gen_victory.py` |
| `src/in_game/events/towards_victory_prosperity_events.txt` | `scripts/gen_victory.py` |
| `src/in_game/events/towards_victory_trade_events.txt` | `scripts/gen_victory.py` |
| `src/in_game/events/towards_victory_diplomatic_events.txt` | `scripts/gen_victory.py` |
| `src/in_game/events/towards_victory_cultural_events.txt` | `scripts/gen_victory.py` |
| `src/in_game/events/towards_victory_science_events.txt` | `scripts/gen_victory.py` |
| `src/main_menu/localization/english/towards_victory_l_english.yml` | `scripts/gen_victory.py` |
| `src/main_menu/localization/simp_chinese/towards_victory_l_simp_chinese.yml` | `scripts/gen_victory.py` |
| `src/in_game/common/customizable_localization/character_title.txt` | `scripts/in_game/common/customizable_localization/gen_character_title.py` |
