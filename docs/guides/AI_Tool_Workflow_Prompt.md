# AI Tool Workflow Prompt (EU5)

Use the following prompt for AI coding tools in this project:

```text
You are an expert Europa Universalis 5 (EU5) modder. EU5 uses an updated Jomini engine. Do not assume EU4 syntax works.

### Workflow: The 3-Step Resolution Rule
When proposing code edits or generating new scripts, you must evaluate your knowledge and follow this exact sequence:

0. **Build task context**: before editing in Codex, Claude subagents, or any managed sandbox, run
   `C:\Users\Hades\anaconda3\envs\eu5\python.exe scripts\ai_context.py --changed` or
   `C:\Users\Hades\anaconda3\envs\eu5\python.exe scripts\ai_context.py --files <paths>` and read every listed risk card.
1. **Direct Edit**: If you are 100% certain about the EU5 syntax (e.g., standard Jomini logic), write the script directly.
2. **Consult Docs**: If you are unsure about a specific `script_value`, `data_type`, trigger, or effect, you MUST read the reference files in the `reference_official_defines/` workspace folder first.
3. **Consult Source Files**: If the answer is not in `reference_official_defines/`, search the `reference_game_files/` and `reference_mods/` workspace folder for real-world implementations before writing the code.

### Python Runner Policy

All project Python scripts must run inside the `eu5` environment. In Codex, Claude
subagents, or any managed sandbox, do **not** run or retry `conda run -n eu5`.
Use the `eu5` interpreter directly:

`C:\Users\Hades\anaconda3\envs\eu5\python.exe scripts\<script>.py ...`

In a normal user terminal, the canonical form remains
`conda run --no-capture-output -n eu5 python scripts/<script>.py ...`.

`conda run` may hang in managed sandboxes when the Anaconda base environment is
read-only, especially when `C:\Users\Hades\anaconda3\conda-meta\history` is not
writable. If the direct interpreter path is unavailable, activate through `cmd`:

`cmd /c "call C:\Users\Hades\anaconda3\Scripts\activate.bat eu5 && python scripts\<script>.py ..."`

This still uses `eu5`; do not use bare `python`. Treat any later `conda run`
example as normal-terminal documentation and translate it before executing in a
managed sandbox.

### Mandatory Reference Categories (Step 1 is FORBIDDEN)

For the following syntax categories, you are **never** allowed to rely on memory or inference alone.
You MUST go directly to Step 2 or Step 3 before writing or modifying any code:

| Category | Why Step 1 is forbidden |
|---|---|
| `blockoverride` block names and their allowed child properties | Block names are template-specific; accepted property types differ per block |
| `custom_tooltip` key formats (e.g. dotted suffixes like `.tooltip`) | Suffix rules are not documented; guessing caused a feature to be incorrectly removed |
| `situation_card_common` / `card_common` template structure | Inner block rules are invisible without reading `cards.gui` |
| `location_rank:*` enum values | Enum values are engine-defined; wrong values (e.g. `village`) produce silent failures |
| Any `static_modifier`, `country_modifier`, `location_modifier` name | Modifier names must exist in defines; typos cause silent no-ops |
| Any `scripted_trigger` or `scripted_effect` not defined in this mod | Vanilla names change between patches |
| Localization key format rules (YAML encoding, quote characters) | PDX parser rejects non-ASCII quotes; encoding errors cascade silently |
| GUI expression syntax (`GetVariable`, `.IsSet`, `MakeScope`, etc.) | Expression language is undocumented; wrong patterns produce no visible error |
| Any `variable_map` / `global_variable_map` / `local_variable_map` use | Direct RHS/effect-parameter use can silently fail; read the local Variable maps section and anti-pattern before changing map lookups |
| Any GUI icon display | Must check `font_icons.gui` for `@xxx!` inline texticon before using widget or custom approach |

### Declarative Verification Requirement

Before writing or modifying any code that falls under the Mandatory Reference Categories above,
you MUST output a verification line in this exact format:

> **Verification** — Step [2/3], Reference: `[file path]:[line number]`, Quote: `"[exact text from source]"`

This line must appear **before** the code block. No code may be written without it.

If you cannot locate a suitable reference, output:

> **Verification** — FAILED. Cannot verify `[syntax in question]`. Reporting to user before proceeding.

Then stop and ask the user for guidance. Do NOT guess.

### Constraints
- NEVER hallucinate or guess Paradox script syntax.
- If you cannot verify a command using the steps above, explicitly tell the user: "I cannot verify this syntax, please check the official wiki or logs."
- If a syntax pattern causes bugs, do NOT remove the feature as a first response. You MUST follow the 3-step rule in order (Direct Edit -> reference_official_defines/ -> reference_game_files/) and replace it with a verified working syntax.

### Unreleased Project: No Compatibility Policy

This mod has not shipped. There are no user save files, published mod versions, or public
APIs to preserve. Do not add old-save migrations, legacy alias variables, duplicate old/new
branches, wrapper launchers, defensive fallback paths, or "if old state exists" repair logic
unless the user explicitly names an external consumer that must keep working.
This is also a performance rule: compatibility probes, repair branches, and defensive
fallbacks add cost to runtime paths, and performance is a project priority.

When the architecture changes, update the canonical data, generators, callers, and docs to
one current shape, then delete stale paths. Reusing existing helpers is not a goal: if a
helper has the wrong scope, lifecycle, data model, or performance shape, split, replace, or
refactor it. Efficiency-improving refactors are desirable.

### Early Development: Ask-First Policy

This mod is in early development (v0.1.0). The design is still being refined. **When in doubt, ask the user before implementing.**

Ask the user before proceeding whenever:
- A design document leaves a specific threshold, weight, or value unspecified (e.g. "composite threshold 1" with no number given)
- Two or more valid EU5 approaches could implement the same mechanic and the choice has gameplay consequences
- A design decision requires assumptions about how the player interacts with the system (e.g. what happens if two victory paths reach Short-term Victory simultaneously)
- The scope of a requested change could be interpreted narrowly or broadly
- Any requirement in `docs/design/Towards_Victory_Design.md` is ambiguous or contradictory

Do NOT resolve ambiguity by picking the "most reasonable" interpretation and proceeding silently. The cost of asking once is always lower than implementing the wrong thing and rewriting it.
```

## Path Mapping In This Repository

- `docs/` -> project docs and technical notes
- `reference_official_defines/` -> official define/type reference files
- `reference_game_files/` -> vanilla script source files
- `reference_mods/` -> some representative community mods
- `docs/knowledge/risk_cards/` -> short, task-domain warning cards loaded by `scripts/ai_context.py`

## AI Context Bootstrap

Before any non-trivial edit, generate a compact task context:

```powershell
C:\Users\Hades\anaconda3\envs\eu5\python.exe scripts\ai_context.py --changed
```

or, when the target files are already known:

```powershell
C:\Users\Hades\anaconda3\envs\eu5\python.exe scripts\ai_context.py --files src/in_game/common/generic_actions/tv_govhouse_actions.txt
```

Normal user terminals may use the `conda run --no-capture-output -n eu5 python`
prefix instead. Managed sandboxes must keep the direct interpreter form.

The output tells the AI whether a file is generated, which risk cards apply, and which anti-pattern records are relevant. For `generic_actions`, the required card is `docs/knowledge/risk_cards/generic_actions.md`; for event files, `docs/knowledge/risk_cards/events.md` is required because option hover can pre-evaluate option effect chains, including `hidden_effect`; for IO definitions, IO laws, and country interactions that find or mutate TV IOs, `docs/knowledge/risk_cards/international_organizations.md` is required. Files containing `variable_map`, `global_variable_map`, or `local_variable_map` content are routed to the local Variable maps documentation and the `variable_map_scope_link_used_direct_rhs` anti-pattern.

## Resume / Handoff Discipline

When continuing from a handoff summary, compaction summary, or another agent's explicit progress report, treat that summary as the current task state.

- Trust "already read", "already verified", "already changed", and "remaining next steps" unless local evidence contradicts them.
- First actions should be limited to `git diff`, `git status`, reading named target files, or running the exact validation/check command listed as next.
- Do not rerun `ai_context.py`, reread broad docs, or re-verify references already named in the handoff unless the summary is inconsistent, files changed since the handoff, or a concrete validation/runtime error points there.
- Search narrowly. Broad `rg` over `reference_game_files`, `reference_mods`, or the whole repository requires a specific missing fact and stated pattern.
- If code was already modified before the handoff, run validation before opening a new investigation path. Let concrete errors guide further exploration.
- Before resuming substantial work, restate the continuation checkpoint: what is already done, the next blocking step, and the first local check.

## AI Knowledge Maintenance

AI agents are responsible for maintaining the workflow files. Humans provide requirements, testing, and feedback; do not wait for a human to update knowledge files.

When a new EU5 behavior or recurring AI failure is discovered:

- Add or update `docs/knowledge/anti_patterns.yaml`.
- Set `detectability` to `lint`, `needs_parser`, or `advisory`.
- Update the relevant `docs/knowledge/risk_cards/*.md` file when the issue belongs to an existing high-risk task domain.
- If a new high-risk domain needs a card, create it and register it in `scripts/ai_context.py` `DOMAIN_RULES`.
- If a `needs_parser` rule becomes reliably checkable, add the checker to `scripts/validate.py` and update the anti-pattern's `detectability`.
- If a `detectability: lint` regex or validator is added or changed, add or update fixtures under `tests/fixtures/anti_patterns/<rule_id>/` and run `scripts/test_lint_rules.py` through the Python Runner Policy.
- If validation reports a new warning, fix it unless the warning is intentionally accepted. Accepted warnings must be recorded in `data/validation_baseline.yaml` with a rationale.
- Update `PROJECT_OVERVIEW.md` when a workflow script or AI domain routing changes.
- Run `scripts/gen_brief.py` through the Python Runner Policy after changing knowledge files.

## Required Behavior For Bug Fixing

- This project is unreleased. Do not preserve old internal schemas, old save states, legacy
  wrappers, alias variables, or duplicate old/new code paths unless the user explicitly asks.
  Prefer one current architecture and remove stale implementation paths. No-compatibility is
  also a performance rule: avoid runtime probes, repair branches, and fallback work that only
  exists to support obsolete internal states.
- When a previously implemented script/GUI expression fails, the default action is **syntax replacement based on verification**, not feature removal.
- Do not convert a requested dynamic UI feature into static text merely to silence logs. Static fallback is a feature removal unless the user explicitly approves that tradeoff.
- Do not move visible IO typed-variable monthly arithmetic out of `variables = { <var> = { monthly_change = { ... } } }`. These blocks drive the IO variable UI and monthly tooltip breakdown. If such a variable stops advancing, fix the `monthly_change` scope/conditions or its cached country inputs; country pulses may compute helper caches or completion side effects, but must not become the source of truth for visible IO variable gain.
- When the current task is a refactor toward a specific architecture, preserve that architecture while fixing bugs. Do not replace `variable_map` / numeric-id-flow work with generated per-key branches or an older key-branch structure unless the user explicitly approves that rollback.
- Do not hide `variable_map` lifecycle or call-order bugs by adding map rebuilds to read paths. Global-map rebuilds belong at startup, save-load initialization, data-change regeneration, or explicit initialization; GUI refresh, cache refresh, tooltip/projection, selection, and monthly read paths must use already-built maps and fix the actual ordering/init bug when a map is missing. Do not add `*_if_needed` rebuild routers, old-schema repair, or legacy state preservation.
- Removal or fallback simplification is only allowed when:
  - the syntax cannot be verified in `reference_official_defines/`, `reference_game_files/` and `reference_mods/`, and
  - the tool explicitly reports this uncertainty to the user.

## GUI Icon Display Priority

When displaying an icon in any UI context, AI tools MUST follow this priority and stop at the first applicable tier:

| Tier | Method | When to use |
|---|---|---|
| 1 | `@icon_name!` inline syntax | Icon exists in `font_icons.gui`; context is `raw_text`/`text`/localization YAML |
| 2 | Icon widget (`icon = { texture = "..." }`) | Standalone widget needed, or icon absent from `font_icons.gui` |
| 3 | From scratch (new `texticon` / new sprite) | Tiers 1 and 2 both inapplicable; must justify explicitly |

Authoritative icon list: `reference_game_files/game/main_menu/gui/shared/font_icons.gui` (180+ entries)

## Documented Violations (Learning Record)

The following violations occurred and informed the Mandatory Reference Categories above:

| Date | Violation | Root cause | Correct behavior |
|---|---|---|---|
| 2026-07 | Governor's House creation logged `is_key_in_variable_map` errors for every region while refreshing display variables (`Scope: region ... This scope doesn't support variables`) | `every_in_list = { variable = tv_govhouse_region_list }` changed the current scope to a region, but the code read the country-owned `tv_govhouse_region_to_admin_location_map` from that region scope | Save the country/map owner before item iterators, copy the dynamic key to a local variable, and run both `is_key_in_variable_map` and the quoted `variable_map(...)` scope link inside `scope:<saved_owner>`. |
| 2026-07 | Trade Chain generated `tv_trade_chain_strength_display_0..20000` static display modifiers but no `STATIC_MODIFIER_NAME_tv_trade_chain_strength_display_*` localization, so runtime localization fell back to generated English names such as `STATIC MODIFIER NAME tv trade chain strength display 18980` | Treated `ShowModifierEffect` display-only static modifiers as if their effect contents were enough for GUI display, forgetting the modifier database still requests a static modifier name key | Generate `STATIC_MODIFIER_NAME_<id>` localization for every emitted static modifier in every supported language, including generated display-only families. `scripts/validate.py` now checks static modifier ids in both in-game and main-menu static modifier directories against main-menu localization. |
| 2026-07 | Academy Philosophy Debate generation placed top-level `*_defection_condition_trigger` blocks inside `common/scripted_effects`, causing trigger clauses such as `religious_unity`, `has_variable`, and `always` to load as `Unknown effect ...` | Treated helper trigger definitions as acceptable in the same generated file that calls them from effect `limit` blocks | Generate top-level `_trigger = { ... }` blocks only under `common/scripted_triggers`; keep `common/scripted_effects` top-level blocks as real scripted effects. `validate.py` now lints top-level `_trigger` definitions in scripted-effect paths. |
| 2026-07 | During the Academy research-target update, proposed collapsing target-specific event descriptions/options/effect dispatch into generic fallback behavior to reduce repetitive edits | Treated a data-list update as permission to downgrade established player-facing event structure | Preserve existing per-target event/GUI/localization structure when changing the data set. If the branch count is large, extend the generator/data source first or stop and ask before changing structure; never silently replace target-specific UX with generic fallbacks. |
| 2026-07 | Alhambra unique-ritual generated event options wrapped scripted-effect calls in `effect = { ... }`, producing `Unknown effect effect` for every option | Treated event option bodies like generic container blocks that need an `effect` child, instead of remembering that event options are already effect lists | Put option effect calls directly under `option = { ... }`, alongside `name` and optional `trigger`. Use `hidden_effect = { ... }` only to hide a chain from the tooltip; `scripts/validate.py` checks event option direct-child `effect` blocks. |
| 2026-07 | Claude/Codex subagents kept running `conda run -n eu5` and hanging before reaching validation or context output | The runner policy was added as an exception while earlier and later examples still began with `conda run`, and `.claude/settings.local.json` explicitly allowed that command family | Managed sandboxes must default to `C:\Users\Hades\anaconda3\envs\eu5\python.exe scripts\...`; reserve `conda run --no-capture-output -n eu5 python ...` for normal user terminals only, and do not whitelist `Bash(conda run *)` for subagents. |
| 2026-06 | Diplomatic Alliance row Subjugate button passed the member as `parameter_value = "[Country.Self]"` inside `left_action`; clicking silently spent no cohesion and created no subject | Confused the GUI object shape used for row comparisons with the script scope shape expected by a generic action's `scope:target` | For custom row action buttons that feed a generic action target, put the parameter on the button and pass `parameter_value = "[Country.MakeScope]"`. Keep `Country.Self` for GUI object comparisons such as `ObjectsEqual`. |
| 2026-06 | Diplomatic Alliance custom member list went blank and its subjugate button emitted GUI `FetchData failed` errors during fixes | The fix filtered `MemberTypeItem` by a localized special-status name, called `IsIOLeaderCountry(Country.Self)` from an IO member row, and then misread stale hot-reload action-button errors as a source problem | Keep the outer member group on `MemberTypeItem.IsAllMembers`; filter the country row by comparing `Country.Self` to `GetLeaderCountry.Self` with `ObjectsEqual`; avoid direct `enabled = "[UIAction.IsEnabled]"` in unverified contexts; after changing action widgets, restart or fully reload before drawing new conclusions from line-stable FetchData errors. |
| 2026-06 | Browsing Diplomatic Alliance laws logged `Event target link 'leader_country' returned an invalid object` from generated policy `on_activate` / `on_deactivate` lines | The law browser/policy tooltip evaluator walked activation/deactivation effect chains before a leader country was available, and the generator used direct `leader_country = { ... }` for mirrored country tier writes | Keep IO-root state changes direct, but enter mirrored leader-country side effects with `leader_country ?= { ... }` in IO policy activation/deactivation blocks. |
| 2026-06 | Diplomatic Alliance special statuses appeared only after starting a law vote | EU5 may already hold IO special-status state while the UI does not display it until the parliament/vote surface refreshes; the first proposed fix moved display repair into a monthly pulse, which would create avoidable recurring IO maintenance | Keep the implementation minimal: grant the leader's advocator status at creation and grant each joining country member status in `on_joined`. Do not add monthly special-status refreshes, full-member repair, or parliament-seat repair unless a separate runtime error proves they are needed. |
| 2026-06 | Diplomatic Alliance law vote reached `set_parliament_location effect [ target was null ]` when using `uses_parliament_for_law_votes` | The custom non-landed IO had no `parliament_seat` and no IO-owned locations, so vanilla `call_parliament_for_law_change` could not save `parliament_location` before activating the debate | For parliament-routed IO law votes, ensure the IO can resolve a meeting location. If the IO has no owned locations, set `parliament_seat` to a valid location such as the leader country's capital before policy votes can be proposed. |
| 2026-06 | Diplomatic Alliance creation logged `international_organization_type` scope errors from a helper called inside `create_international_organization` | A helper that assumed IO root was walked during generic-action pre-evaluation with country root from the creation block call stack | Keep IO-only initialization in the IO type's `on_creation`/`on_joined` hooks, or call it only after switching to a verified saved IO scope. Do not place helpers starting with IO-only triggers directly inside `create_international_organization` blocks. |
| 2026-06 | Diplomatic Alliance panel invitation logged `Value of wrong type ... Got none`, unset `tv_diplomatic_alliance_member_count`, and unset `var` scope errors before a country was selected | The country interaction `accept` block directly read an actor variable that was not initialized on alliance creation, and it also contained direct `scope:recipient` reads that can be evaluated before `select_trigger` sets recipient | Initialize actor-owned acceptance variables at the feature creation lifecycle point. In country interaction `accept` and target-mutating `effect` blocks, guard recipient-dependent reads or mutations with `exists = scope:recipient`; gate any still-nullable actor variable value read with `has_variable`. |
| 2026-07 | Loading `tv_trade_league_actions.txt` logged hundreds of `Unknown trigger type: save_scope_as` errors from the Trade Chain add-node action | `gen_tv_trade_league_actions.py` used `save_scope_as` inside `select_trigger` `visible`/`enabled` blocks and inside `if = { limit = { ... } }` distance-comparison bodies (trigger contexts); the engine's trigger parser does not recognize `save_scope_as` as a trigger type at all | Use `save_temporary_scope_as` for any scope save inside a trigger-evaluated block (select_trigger visible/enabled, allow/potential, limit bodies); reserve `save_scope_as` for saves written directly inside an effect body. |
| 2026-07 | Governor's House local-administration event option said "Build for 400 ducats" while the actual building was created by hidden `pay_price` plus free `construct_building` | Treated a building construction cost as option prose and hidden bookkeeping instead of letting the visible building effect drive the tooltip | Put real `construct_building` / `change_building_level_in_location` effects directly in visible event options when the player should see the construction and cost. The engine renders building price and cost modifiers automatically; avoid duplicated manual cost text except for intentionally nonstandard hidden funding flows. |
| 2026-06 | IO establishment called `location_and_owner_can_build = { building_type = building_type:tv_*_headquarters }`, causing `building_type:building_type:*` event-target link errors from vanilla `location_triggers.txt` | Passed a typed database link into a scripted trigger parameter whose template already prefixes it with `building_type:` | For `location_and_owner_can_build`, pass the bare building id: `location_and_owner_can_build = { building_type = tv_conquest_headquarters }`. Keep typed `building_type:<id>` for direct trigger/effect fields such as `has_building` and `construct_building`. |
| 2026-06 | IO establishment monthly AI progress called `location_and_owner_can_build` from a country pulse and spammed `Undefined event target 'actor'` / unset-scope errors from headquarters building `allow` blocks | Copied a generic-action/building eligibility flow into a monthly scripted effect and saved the country as `tv_io_establishment_ai_actor`, but the building checks still expected the literal `scope:actor` event target | Do not simulate simple generic-action AI from monthly/yearly pulses. Register the action in `generic_action_ai_lists` and give it `ai_tick`, `ai_tick_frequency`, and `ai_will_do`; only use execution pulses for requirement/cache updates or code that has its required event targets. |
| 2026-06 | IO establishment refresh checked `capital = { has_building = ... }` from country-scoped yearly/monthly logic and logged `Event target link 'capital' returned an invalid object` for countries without a valid capital | Treated the country `capital` link as always resolvable, then briefly tried sibling `exists = capital` as if trigger blocks short-circuit | Use `capital ?= { ... }` in trigger/limit checks. Only enter direct `capital = { ... }` in an effect body after an `if` limit using the optional link has passed. |
| 2026-06 | IO establishment localization used `[GetBuilding('tv_*_headquarters').GetName]`, causing GUI-bound localized text parsing to fail with `Failed to find type 'GetBuilding'` | Treated a script-like building accessor as a localization data function | Use localization helpers such as `[ShowBuildingTypeName('building_key')]` or `[ShowBuildingTypeNameWithNoTooltip('building_key')]` for building-type display names. |
| 2026-06 | Engineering Department wonder design risked using fixed-value population conversion/assimilation, local manpower, or local sailors modifiers as if values above `0.5` were ordinary percentage-scale bonuses | EU5 multiplies these fixed-value modifiers by 1000 in game, while the visually similar `*_modifier` variants are the percent modifiers | For wonder data, keep `global_pop_assimilation_speed`, `global_pop_conversion_speed`, `local_pop_assimilation_speed`, `local_pop_conversion_speed`, `local_manpower`, and `local_sailors` at or below `0.5`; use the matching `*_modifier` id for percentage bonuses. `validate.py` checks wonder paths. |
| 2026-07 | Diplomatic Victory milestone 4 granted `subject_loyalty = 0.10`, making the reward effectively a tiny fraction of a loyalty point | Treated `subject_loyalty` like a percent-style modifier because the modifier type displays decimals | `subject_loyalty` is loyalty-point scale. Vanilla advances use whole values such as `subject_loyalty = 5`; write `10` for +10 loyalty, not `0.10`. |
| 2026-06 | Engineering Department ritual tooltip effects used `add_country_modifier` with `tv_wonder_auto_unique_*_style_1_country_modifier`, producing `Invalid database object` script-system errors | Treated Country Auto modifiers from `common/auto_modifiers` as if they were static country modifiers accepted by `add_country_modifier` | Keep actual building-gated effects in Country Auto modifiers. For tooltip modifier lists, generate static country modifier mirrors and reference those static ids, or emit real effect/custom tooltip lines. Do not restore finalization-time static grants/removals. |
| 2026-06 | Engineering Department GUI put `parentanchor` on a progressbar that was a direct `hbox` child and put `margin_top` directly on Organization/Logistics progressbars, causing `Widget cannot have a position in a layout` and `Property 'margin_top' not handled` load errors | Treated progressbar as if it accepted the same layout/offset properties as text widgets and as if a box-layout child could position itself | In `hbox`/`vbox`, let the box layout own direct child placement. For progressbar vertical centering or offset, put a fixed-size wrapper widget in the layout and anchor the progressbar inside that wrapper; do not put `margin_top` on progressbar. |
| 2026-06 | Engineering Department saved priority proposal selection used `ordered_key_in_variable_map` with `max = 10000`, producing "Given max value was bigger than the list" script errors whenever fewer keys existed | Treated an inflated `max` as a harmless way to process all current variable-map keys | Use `every_key_in_variable_map` with a found/stop flag when the current key count is not known, or set `ordered_key_in_variable_map max` only to a value that cannot exceed the actual list size. |
| 2026-06 | Engineering Department proposal bribery pre-evaluation called `is_key_in_variable_map = { ... target = this }` inside an `ordered_key_in_variable_map` callback, producing "This scope doesn't support variables. Scope: 1.00/2.00/..." runtime errors | Treated the key callback's current scope as the original country/map owner; at runtime it could be the numeric key itself, which cannot host country-scoped variable maps | Save the map owner before the iterator, copy `this` into a local variable, and run sibling `is_key_in_variable_map` checks inside `scope:<saved_owner>` with `target = local_var:<key>`. |
| 2026-06 | Engineering Department wonder finalization generated event IDs such as `tv_engineering_department.50011` and world-news IDs such as `.60001`, producing invalid event ID and duplicate-ID runtime errors | Encoded both wonder id and ceremony style into the event numeric part, and used a `60000 + id` world-news range, missing EU5's `< 10000` event ID limit | Keep event numeric IDs below 10000. Use one event per wonder, dispatch the wonder type before firing the event, and branch only over the already-scoped ceremony style inside that per-wonder event. |
| 2026-06 | Engineering Department wonder inauguration options kept the heavy finalization chain inside the option's `hidden_effect`, causing stutter while merely hovering the option tooltip | Treated `hidden_effect` as if it were a performance boundary for event options; in practice the event-option tooltip evaluator can still walk hidden effect contents | Keep event-option hidden blocks light: guard state and schedule execution only. Move heavy hidden work such as per-wonder/per-style dispatch, construction cleanup, world-news broadcast, and cache/project-state refresh into a `hidden = yes` event `immediate` block or another non-tooltip execution path. |
| 2026-06 | Governor's House daily hidden-event test needed country logic to run once per day in `immediate` without a monthly pulse or tooltip-rendered option chain | Delayed `trigger_event_silently` was not yet documented as a reusable scheduler, and an unguarded seed would create duplicate recurring loops | Use `trigger_event_silently = { id = tv_namespace.900 days = 1 }` to seed a `hidden = yes` country event once from a lifecycle point. The event `immediate` should check the feature prerequisite and a persistent sentinel, run the daily work, then reschedule itself; teardown clears the sentinel so queued events stop naturally. |
| 2026-06 | Engineering Department global wonder maps gained `tv_wonder_index_rebuild_global_maps_if_needed_effect` with a version gate, then callers invoked it from cache/module refresh paths | Used a defensive compatibility router to mask missing lifecycle initialization, preserving obsolete state and shifting rebuild checks onto hot/read paths | Delete the router. Rebuild global wonder maps directly and only from the correct lifecycle hooks such as game start and save load, then fix any call-order bug that reads before initialization. |
| 2026-06 | Engineering Department module-completion events were at risk of being treated as if dynamic `building_type` effect tooltips could not render at all | Misread a same-visible-chain computed variable-map key failure (`tv_wonder_locked * 10 + part`) as a general limitation of dynamic building effects | Test the narrower hypothesis first. `construct_building` and `change_building_level_in_location` can render with `building_type = local_var:X` / `building = local_var:X` when `X` is captured from a direct map keyed by persistent state. For visible event options, avoid same-chain scratch composite keys; branch over bounded dimensions such as part/style and keep the wonder choice dynamic through `variable_map`. |
| 2026-06 | During Engineering Department survey-progress triage, proposed moving `tv_wonder_survey_progress` monthly gain from the IO variable `monthly_change` into country monthly survey effects | Treated `monthly_change` as only an execution location and forgot it is also the automatic IO variable UI and monthly tooltip breakdown source | Keep visible IO typed-variable gain in `monthly_change`. For stuck progress, repair the `monthly_change` scope/conditions or precomputed country inputs while preserving the tooltip-visible source. Country pulses may mirror helper values and run completion/event side effects only. |
| 2026-06 | Engineering Department ritual modifiers emitted boolean values as `can_recruit_regiment_in_this_location = 1` and later `global_peasants_migration_allowed = True`, causing startup to log malformed-token errors | Treated boolean modifiers as numeric/string values, and the generators did not render YAML bool values as EU5 `yes/no` | For modifier types declared `boolean=yes`, emit `yes` or `no`. In Python generators, handle `bool` before numeric paths because `bool` is an `int` subclass. |
| 2026-06 | Engineering Department style-2 wonder annex completion relied only on the annex building's `on_built`; expanding a wonder with an existing annex upgraded the same building level and did not complete the ritual | Treated `on_built` as a reliable signal for both first construction and later level upgrades | For mechanics that must complete after a building upgrade, use vanilla's `on_construction_ended` hook to directly mark the active auxiliary-building ritual finished and run the normal completion effect. Do not require `location_building_level` to update inside that hook. |
| 2026-05 | Added the Trade League IO with localized `io_opinion_tv_trade_league` but no matching `/biases/` entry, causing startup to log that the IO type needs an opinion of other members | Treated the `io_opinion_<io_type>` localization key as sufficient and forgot the separate bias definition that drives the automatic fellow-member opinion | For every custom IO type, add `in_game/common/biases/<io_type>_biases.txt` with `io_opinion_<io_type> = { value = <opinion> }` and keep matching localization in all supported languages. |
| 2026-05 | Wrapped the location-wonder tooltip rows in fixed-height GUI widgets, so the modifier text overflowed below the frame while the card still looked like a valid tooltip | Assumed the tooltip would naturally grow to fit a visible `TooltipStringPairList` chain even though the row and text column were still hard-sized | Give tooltip rows a height-auto container: use width-fixed `size = { W -1 }` / `minimumsize`, let the visible modifier block contribute its own height through `set_parent_size_to_minimum`, and keep `ignoreinvisible = yes` on the container. |
| 2026-06 | Location-window wonder ritual tooltip effects used `location.owner = { add_country_modifier = ... }` while GUI called them with `LocationView.GetLocation.MakeScope.Self`, causing repeated `Event target link 'location'` scope errors and `COUNTRY.GetName` tooltip data failures on hover | Treated `location.owner` as a universal way to reach the owner from any location-related context, missing that the GUI tooltip effect root was already the location and no separate `location` event target existed | In location-root GUI scripted-effect tooltips, use `owner ?= { ... }` for country effects. Reserve `location.owner` for contexts with a verified separate `location` event target, such as building `on_built`. |
| 2026-06 | Location-window wonder effect columns used `ShowModifierEffect(Concatenate(...))` for generated display modifier ids, so modifier ids that only existed in GUI string construction were not reliably recognized | Treated a runtime GUI string as equivalent to a static script reference | Keep the dynamic GUI route, but generate an unreachable script block guarded by `limit = { always = no }` that applies every possible display modifier with the correct `add_country_modifier` or `add_location_modifier` effect. |
| 2026-06 | Trade League commercial intelligence refreshed market merchant power by adding a new 2-month `tv_trade_intelligence_network` entry each month, so switching to permanent power would have left stale or duplicate values | Treated keyed merchant power as a fire-and-forget temporary effect instead of synchronized market/country state | Before reapplying intelligence power, remove only the `tv_trade_intelligence_network` key from previously tracked markets, then add `months = -1` for current members using the current floor strength; clear the same key when the Grand Merchant is invalid. |
| 2026-06 | Trade Chain bargaining-demand accumulation initially generated one export-route scan per goods id and per chain segment, creating a huge scripted-effect expansion even though the route scope already knows its goods | Treated goods as a generator index instead of using the goods scope exposed by the trade route | In `every_export` / `every_import`, save the route good with `traded_goods = { save_scope_as = ... }` and use that scope as a variable-map key. Keep per-good static dispatch only where the target effect needs a static database id, such as `add_temporary_demand type = demand:*`. |
| 2026-05 | Trade League virtual demand/production actions always granted amount 1 regardless of the selected monopoly percentage | Passed `scope:target_1` and later IO variables directly into `set_variable`, `add_temporary_demand scale`, and `add_goods_supply amount`, so the dynamic numeric read collapsed to a truthy value | Wrap dynamic reads in script-value blocks: `value = { value = scope:target_1 }`, `value = { value = scope:target_1 divide = cost }`, `scale = { value = scope:io.var:X }`, and `amount = { value = scope:io.var:X }`. |
| 2026-05 | Trade League IO started with the level-1 default policies already mapped into its `laws = {}` block, so the "lawless on creation" design was violated | Kept a generator that seeded initial policies from `data/trade_league_laws.yaml`, then treated that output as the intended starting state | Leave the Trade League IO definition without seeded laws at creation. Keep the law groups in `tv_trade_league_laws.txt`, but let policy votes enact them later instead of emitting initial mappings in `tv_trade_league.txt`. |
| 2026-05 | Trade League monopoly updater used `total_effective_goods_production_buildings(goods:<good>)` for world production and summed `traded_in_market:<good>` once per member-present market for IO trade | Confused producing building-level capacity and market-wide traded quantity with actual global goods output and member-owned trade routes | Use `produced_in_world:<good>` for global production. For IO member trade, iterate each member country's `every_trade`, filter `goods = goods:<good>`, and sum `trade_volume`. |
| 2026-05 | Trade League monopoly table headers used `raw_text = "@production_panel! $TV_TRADE_LEAGUE_WORLD_COLUMN$"` / `raw_text = "@trade! $TV_TRADE_LEAGUE_IO_COLUMN$"`, so the UI displayed the localization keys literally | Treated `raw_text` as if it performed `$KEY$` localization substitution | Use `text = "KEY"` for static localized labels. If an inline icon is needed, place `@icon!` inside the localization value, or split dynamic raw text and localized text into separate widgets. |
| 2026-05 | Trade League monopoly GUI called `GetGoodsIcon(...GetVariable('tv_trade_display_row_N_good').GetGoods)` and `.GetGoods.GetNameWithNoTooltip` from saved variables | Treated a saved GUI/script variable as a typed Goods object | GUI variables cannot be promoted to Goods with `.GetGoods`. Use typed datamodel accessors such as `Trade.GetGoods`/`GoodsMarketEntry.GetGoods`, or generate static icon/name branches keyed by the saved numeric goods id. |
| 2026-05 | CMF alert bridge called `OpenInternationalOrganizationView(GetPlayer.MakeScope.GetVariable('tv_io_chief_alert_open_requested').GetInternationalOrganization)` | Treated an IO saved in a script variable as if GUI `GetVariable` exposed `.GetInternationalOrganization` | `OpenInternationalOrganizationView` needs a typed `InternationalOrganization` object from a verified view/datamodel chain. Do not reconstruct it from `MakeScope.GetVariable`; if no typed object is available, make the alert direct the player to open the panel manually. |
| 2026-05 | Generated Trade League scripted effects used `any_international_organizations_member_of = { limit = { ... } ... }` as an effect iterator, causing `Unknown effect any_international_organizations_member_of` | Copied trigger-iterator syntax into effect bodies | In effect context use `every_international_organizations_member_of = { limit = { ... } ... }`. Keep `any_international_organizations_member_of` only inside trigger blocks such as `limit`. |
| 2026-05 | Engineering Department Organization/Logistics dispatch-point button called `SelectLocationToBuildDefault('tv_wonder_material_dispatch_point')`; clicking played the build audio but did not open the build-location interface | Passed a literal building key string where the GUI function expects a typed `BuildingType` object, and the following `PlayAudioEffect` made the click look partially successful | Pass `BuildingType` objects from GUI data contexts such as `BuildingItem.GetBuildingType`, or, from custom panels without a building-type data context, route the button through a `generic_action` with `looking_for_a = location` and a guarded `construct_building` effect. |
| 2026-05 | `tv_wonder_material_dispatch_point_production` logged `too high base profit 33.33%, should be no more than 30.00%` at startup | The recipe produced 5 value from 3.75 value of inputs, exceeding EU5's base-profit cap for production methods | Calculate recipe value from each good's `default_market_price` and keep production-method base profit below 30%; for a 5-value output, input value must be greater than 3.8462. |
| 2026-05 | Added `ignoreinvisible = yes` to a plain Engineering Department wonder-illustration `widget`, causing `Property 'ignoreinvisible' not handled` and `Error setting properties for '' (uberwidget)` at GUI load | Treated `ignoreinvisible` as a generic visibility helper instead of a layout-container property | Use `ignoreinvisible` on container layouts such as `hbox`/`vbox`, not on plain image/wrapper widgets. Hide the wrapper with `visible = ...` and give each image child its own guarded `visible` expression. |
| 2026-05 | Built the wonder-location preview as a conditional background-image column inside a plain widget/button that only had expanding layout policies, so the DDS previews and text row collapsed into a narrow block at the parent's left edge | Assumed a visible background widget and its enclosing button would stretch themselves just because the parent card had fixed size; the clickable wrapper and conditional branch still had no explicit bounds | Make the enclosing button fill the card, give the image column a fixed size, and size each visible background child with fixed pixels that match the content area while keeping the existing card/hbox/vbox structure. |
| 2026-05 | Engineering Department finalization event computed `tv_wonder_final_building_extra_levels`, then a nested visible helper compared and reused `prev.var:tv_wonder_final_building_extra_levels`, causing `Invalid left side during comparison 'var'`, unset variable fetches, and unset `var` scope errors while the inauguration option was evaluated | Assumed event option tooltip pre-evaluation would commit a temporary variable written earlier in the same visible effect chain | Re-check final persistent project state at the option effect boundary. For bounded derived values, branch directly from persistent state such as `tv_wonder_level` and pass literal effect values; do not pass same-chain temporary variables into visible helper comparisons or `value = scope.var:X`. |
| 2026-05 | Prosperity M1's first option reinitialized wonder module/helper buildings, computed temporary `*_combinable_levels` / `*_helper_extra_levels`, then immediately compared and reused them, logging unset-variable errors for partially built Bank of Saint George and Cahokia Mounds before the click resolved | Assumed event option pre-evaluation would commit temporary wonder-module variables even when the option only reached the helper through `hidden_effect` | For bounded wonder module-to-helper collapse, do not compute same-chain scratch variables. Emit one literal branch per level (1..6), apply fixed helper building deltas, and subtract fixed module deltas directly from building state. |
| 2026-05 | `tv_engineering_department.200` completed a wonder unit, then its option tooltip rebuilt helper/module buildings through temporary `*_helper_current_level` / `*_target_module_level` vars and rounded remaining months through `tv_wonder_remaining_months_floor_check`, producing unset-variable and invalid-comparison errors before the click resolved | Assumed event option hover would commit temporary helper/module/current-level and arithmetic scratch vars, even when the call chain ran under `hidden_effect` | For bounded helper/module rebuilds, drive each target level from persistent unit and wonder-level state with one literal branch per level. For pure rounded division, use verified script-value operators such as `ceiling = yes` instead of temp multiply/check variables. |
| 2026-05 | Would have treated `common/music_player_tracks` as a full scripted music-rule database for loading raw tracks and controlling playback by country/event/war conditions | Carried over expectations from older Paradox title music scripts without checking EU5's exposed type reference, then overcorrected by saying script could not influence music at all | EU5 text files register Wwise event keys plus metadata and localization. Current verified references expose no trigger/chance fields inside `music_player_tracks`, but GUI can call Wwise events through `Audio_PlayEvent(<event>, 'music_manager')`, and hardcoded hooks such as `on_war_declared` can react to gameplay state. Keep track registration, audio-manager events, and gameplay hooks conceptually separate. |
| 2026-05 | Generated Diplomatic Alliance IO policies used `scope:recipient` inside policy `allow`, `on_activate`, and `on_deactivate`, producing repeated runtime errors: `Undefined event target 'recipient'` and `Event target link 'scope' returned an unset scope` | Confused IO policy execution scope with IO policy AI math scope. The law readme documents `scope:recipient = IO` for `wants_this_policy_bias` / `wants_propose_policy`, but activation/deactivation run on the entity the policy applies to | In IO policy `allow` / `on_activate` / `on_deactivate`, read and mutate the current IO root directly (`var:tv_alliance_cohesion`, `change_variable`). Reserve `scope:recipient` for documented country-root AI math blocks such as `wants_this_policy_bias` and `wants_propose_policy`; use `leader_country ?= { ... }` for nullable leader mirrors. |
| 2026-05 | Generated Diplomatic Alliance IO policy AI math still used direct `scope:recipient` reads and continued logging `Undefined event target 'recipient'` / unset-scope errors during pre-evaluation | Treated documented AI math scopes as always populated; custom non-unique IO laws can have `wants_this_policy_bias` / `wants_propose_policy` evaluated before a recipient IO target is set | Keep the AI math behavior, but guard every direct recipient read with `exists = scope:recipient` in the same `limit` block, or use optional `scope:recipient ?= { ... }` for trigger-only checks. |
| 2026-06 | Governor's House subject-chain members sometimes showed extreme support for a law from `POLICY_MODIFIER_UTILITY` / "政策的实用性" | Generated leader-facing IO policy `country_modifier` blocks without a member filter; vanilla `policy_vote` automatically adds `scope:vote.modifier_utility(scope:actor)` for every voting member | For additive leader-only `country_modifier` effects, filter the member package with `potential_trigger`; keep the filter recipient-safe when the effect must also show in ordinary law browsing. |
| 2026-07 | Governor's House leader-only law effects showed "No Effects" during ordinary law browsing | The leader filter used `is_leader_of_international_organization = scope:recipient` directly; ordinary law-browser tooltip evaluation can lack a vote recipient event target | For visible additive leader-only IO law effects, use `country_modifier = { potential_trigger = { OR = { NOT = { exists = scope:recipient } is_leader_of_international_organization = scope:recipient } } ... }`. Use `leader_modifier` only when replacement semantics are intended. |
| 2026-05 | `tv_diplomatic_alliance_list` compared `leader_country = this` inside `any_international_organizations_member_of`, causing `country` vs `international_organization` comparison errors | In `generic_action_ai_lists`, root is the evaluating country, but inside the IO iterator `this` is the iterated international organization | For leader-only IO action lists, use `exists = leader_country` and `leader_country = root`. |
| 2026-05 | Reused `tv_wonder_has_materials_for_month_trigger` inside `tv_wonder_materials_stockpile.monthly_change`; construction progress advanced each month but the IO stockpile never paid the active construction cost | The trigger was written for country scope and compared against `root.var:tv_wonder_laborers`. Inside IO variable `monthly_change`, `root` remained the IO context even when the call was nested in `leader_country ?= { ... }`, so the condition failed and the subtract value stayed 0 | Do not call country-scoped triggers from IO `monthly_change` when they depend on `root.var`. Write IO-scoped conditions directly: check country state under `leader_country ?= { ... }`, then compare `var:io_stockpile >= leader_country.var:country_cost` from the IO scope. |
| 2026-06 | Engineering Department survey progress stayed at 0 even while the country monthly survey pulse considered the survey active | Reused `tv_wonder_survey_active_trigger` inside `tv_wonder_survey_progress.monthly_change`; that country-scoped helper depends on caller-sensitive state/ownership scope (`prev` chain), which is not equivalent inside IO variable evaluation | Keep `tv_wonder_survey_progress` gain in IO `monthly_change`, but inline the active-survey country checks under `leader_country ?= { ... }` there. Do not move visible IO variable gain to country pulse. |
| 2026-06 | Events fired directly from conditions checked by `country_monthly_pulse`, so their popups could resolve in the same pulse tick as the condition scan | Treated monthly pulse event dispatch as an immediate notification path instead of separating the monthly condition check from player-facing event execution | Add one day of delay to every monthly-country-pulse reachable event: `trigger_event_* = { id = X days = 1 }` for direct fires, or `delay = { days = 1 }` before native `events` / `random_events` entries. The setting lives in `data/pulse_registry.yaml` and `scripts/validate.py` enforces it by walking the monthly pulse callback graph. |
| 2026-06 | Engineering Department priority-candidate unregister compared `local_var:tv_wonder_priority_saved_candidate_wonder_id ?= var:tv_wonder_priority_candidate_wonder_id` inside a variable-map key iterator, logging `Invalid right side during comparison 'var'` whenever expansion refresh ran | Treated the outer country `var:` as a valid dynamic RHS after capturing the map value into a local | Capture the outer value before entering the iterator, then compare local to local. For dynamic numeric equality, use `NOT = { local_var:saved < local_var:target }` plus `NOT = { local_var:saved > local_var:target }` instead of `?= var:Y`. |
| 2026-05 | Kept generated per-good Trade League monopoly maintenance on the Trade League IO type's `monthly_effect`, and playtesting still had severe monthly stutter | Treated hidden IO monthly maintenance as cheap after moving the variables to leader country; this exposed that TV IO `monthly_effect` hooks are too expensive as a class | Do not define `monthly_effect` on TV IO types. Keep visible variable arithmetic in `monthly_change`; dispatch maintenance from `monthly_country_pulse` after cheap country/member/leader gates, then enter the matching IO scope only for the work that truly needs it. |
| 2026-05 | Compared Engineering Department IO material stockpile to country labor with `this >= prev.var:tv_wonder_laborers`, causing a monthly `Invalid right side during comparison 'var'` error and a permanent materials-insufficient pause | Treated `prev.var:X` as a numeric RHS after switching into IO/variable scope; the monthly logistics updater also computed dispatch-point income without adding it to the IO stockpile | Use `root.var:X` only when root is verified as the numeric owner, or capture values before effect iterators with `set_local_variable` and use `local_var:X` after switching scope. Ensure monthly income is applied before checking/consuming materials. |
| 2026-05 | Used `location_building_level(building_type:tv_wonder_labor_camp) * 1000` as the Engineering Department labor total | Treated completed building levels as current staffing; this ignored labor camps that were underfilled by the employment system | To read actual building staffing, enter the building object from the location with `ordered_buildings_in_location` / `random_buildings_in_location`, filter `building_type = building_type:<id>`, and read `prev.building_employed_amount` from the country scope. Multiply by 1000 only when the target variable is stored as people rather than pop-size units. |
| 2026-05 | Wonder selection/finalization used `while` loops that waited for `location_building_level` to change after `construct_building instant = yes`, so clicking "select construction site" could freeze immediately | Assumed instant construction synchronously changed the location's building level inside the same scripted effect; in practice the engine creates a 0-day construction task, so the loop condition never changes during that effect | Never use a `while` that expects `construct_building instant = yes` to update `location_building_level` immediately. Replace it with fixed threshold branches or one bounded `change_building_level_in_location` delta computed from persistent state. Keep old-save / repair reconstruction logic out of hot click paths such as wonder site selection. |
| 2026-05 | Let custom Engineering Department building buttons stay clickable after labor camps/depots/dispatch points had reached their caps | Relied on `can_build_building` and the building type's `max_levels`, but the custom generic action still charged its scripted price when the final `construct_building` became a no-op or targeted an already queued location | Put the same explicit cap trigger in the action `allow`, target `visible`/`enabled`, and effect guard. Use `total_building_levels_including_construction:<building>` for cap checks and `building_levels_under_construction >= 1` inside `any_buildings_in_location` for one-per-location selectors. |
| 2026-05 | Put Arts Exhibition Chief Artist header buttons in `blockoverride "country_header_extra"` and they did not appear in the standard IO header UI bounds | Treated the outer country header extra slot as a visible action row, while the actual standard IO header renders its lower information row through `abilities_widget` -> `ios_information_header` | For compact IO header readouts or action buttons, use `ios_information_header_content_extra_1/2/3` with `ios_header_content_extra_template`, or explicitly override `abilities_widget` while preserving `ios_information_header`. |
| 2026-05 | Put multiline proposal and estate-demand text in an `hbox` right-side elastic column with no width cap; the text looked like it overflowed a correct card | Assumed the parent `card_common`/outer widget width was the problem. UI bounds showed the outer widget was 470px but the inner `vbox` grew to 548.3px because the `text_single multiline=yes` column contributed natural text width back into layout | Do not place paragraph text in unconstrained `size = { -1 H }` hbox columns. Wrap it in a fixed-width widget/card and use `text_multi` with `max_width` and `autoresize`, or otherwise set explicit min/max width on the text area. |
| 2026-05 | Used `most_powerful_merchant = root` inside the Trade Victory milestone trigger that is checked from `tv_victory_situation`'s `every_country` monthly loop | Verified the trigger from a country-event example, but missed that reusable triggers inherit the caller's root; GUI `ShowTriggerConditions(PlayerScope.Self)` passed while the monthly checker had situation root | In nested iterators inside reusable country triggers called from situation/global loops, use `prev` or an explicit saved country scope to refer to the iterated country. |
| 2026-05 | Tried to make the founder-only Academy gain faction support through vanilla IO parliament issues | Assumed a leader special status plus agenda would make `call_organization_parliament` robust for a one-country IO | Do not rely on vanilla `call_organization_parliament` for founder-only support-token meetings. The issue picker can still report that no special status wants to propose an issue; redesign the mechanic before enabling custom support meetings. |
| 2026-05 | Added custom IO parliament issues for the Academy but no `parliament_agenda` for `tv_academy_member`, causing the meeting button to report that no special status wanted to propose an issue | Treated `parliament_issues` as the whole IO parliament flow | Add at least one `common/parliament_agendas` entry with `type = international_organization` and `special_status = <status>` for every special status expected to participate in custom IO parliament sessions. |
| 2026-06 | Diplomatic Alliance idle parliament meetings showed that no special status had issues to bring | Gave every custom idle issue a negative base `wants_this_parliament_issue_bias`, with only opinion bonuses able to raise it, so new or neutral alliances could have no positively desired issue | For vanilla `call_organization_parliament` idle sessions, keep at least one issue for the participating special status positively desired in ordinary states while preserving valid `potential`, `allow`, and `selectable_for`. |
| 2026-05 | Routed custom IO law changes through `uses_parliament_for_law_votes` without an implemented special status carrying voting power | Assumed `has_parliament`, `requires_vote = yes`, and `uses_parliament_for_law_votes = yes` were enough for the IO parliament UI | Add an IO special status, list it in `special_statuses_implemented`, give it `special_status_power`, define `<status>_can_participate_in_parliament` as an `internationalorganization` modifier type, and enable that modifier in the parliament type. Law vote support uses `country_combined_special_status_power(scope:recipient)`. |
| 2026-05 | Trade League custom special status `tv_trade_league_member_status` loaded without `tv_trade_league_member_status_can_participate_in_parliament` and `tv_trade_league_member_status_agenda_impact`, causing startup DB assertions | Treated the special-status parliament modifier pair as only needed for explicit parliament mechanics | For every custom IO special status implemented by an IO, define `<status>_can_participate_in_parliament` as boolean and `<status>_agenda_impact` as percent in `main_menu/common/modifier_type_definitions` with `category = internationalorganization`, plus localization. |
| 2026-05 | Tried to hide an IO parliament tab through `blockoverride "organization_parliament_tab_visible"` | Assumed the commented vanilla marker in `organization/common.gui` was an active override block | The IO parliament tab is directly visible when `InternationalOrganization.HasParliament` is true. Hide it by setting `has_parliament = no`; keep law `requires_vote` behavior as a separate design decision. |
| 2026-05 | Used `change_variable = { name = X value = 1 }` to increment a timer | Mixed up `set_variable`'s absolute assignment field with `change_variable`'s arithmetic fields | Use `change_variable = { name = X add = 1 }` for increments/decrements, or `set_variable = { name = X value = 1 }` for assignment. Runtime error: `Failed to read 'value' for 'change_variable'`. |
| 2026-05 | Changed a custom IO variable inside a reusable scripted effect with bare `change_variable`, leaving caller option/action tooltips empty | Assumed scripted effect calls would expose the nested IO variable change automatically | Wrap the IO-scope `change_variable` in `custom_description = { text = <effect_loc_key> value = $value$ ... }`, register `<effect_loc_key>` in `common/effect_localization` with positive/negative perspectives, and add matching localization strings. Vanilla uses this for imperial/celestial/papal authority helpers. |
| 2026-05 | Used `[COUNTRY.GetName]` in third-person effect localization for a `custom_description` shown in generic action tooltips | Assumed `COUNTRY` context always exists when effect localization chooses `third` or `third_past` perspective | Keep effect localization perspective strings self-contained. Generic action hover pre-evaluation may evaluate them before a `COUNTRY` promote target exists, causing repeated `COUNTRY.GetName` data errors while hovering. |
| 2026-05 | Left Engineering Department construction/selection action cards visible before survey completion; later, clicking "announce" after a completed survey reached `tv_wonder_update_construction_progress_display_effect` and logged `tv_wonder_remaining_progress` unset even though the real effect chain initialized it | Assumed generic action tooltip/pre-click evaluation commits earlier `set_variable` writes inside the same effect chain | Hide action widgets until their prerequisite state exists, re-check prerequisites inside the action effect, and make reusable effects safe under pre-evaluation with `var:X ?= ...` or threshold-style comparisons instead of direct reads of variables just initialized by the same chain. |
| 2026-05 | Trade League embargo's second selector used `source = world` and filtered countries with `any_owned_location = { market = scope:target }`, so the country chooser showed no available countries and logged `Asking for a flag that's not in the interaction target chooser specified` | A later selector was moved to a world-source chooser while still reading the earlier market target flag | Do not use `source = world` on a later `select_trigger` that reads an earlier target flag. Omit `source`, use a non-world source, or build an `interaction_source_list` so the chooser retains the earlier target. |
| 2026-06 | Added generic action `allow` triggers that scanned for "at least one selectable target" before opening a `select_trigger` chooser | Assumed an empty selector should be prevented in `allow`, even though the selector already handles empty candidate lists with `none_available_msg_key` | Do not duplicate target-availability scans in `allow`. Keep actor/state/resource prerequisites there, put target eligibility in `select_trigger` `visible`/`enabled`, provide `none_available_msg_key`, and guard the final effect with `exists = scope:target`. |
| 2026-06 | Victory Path headquarters build buttons populated their capital selector through `ai_interaction_source_list`, so player clicks always saw the no-location message | Treated the AI-only custom source list as if it also populated the human selector | Use `interaction_source_list` for shared player/AI selector candidates. Reserve `ai_interaction_source_list` for AI-only overrides after the player selector has its own valid source. |
| 2026-05 | Checked `location_rank` directly inside `any_neighbor_location` for Engineering Department survey site selection, causing repeated invalid-object errors while the picker was open | Assumed every neighbor yielded by `any_neighbor_location` has a rank, then assumed `trigger_if` with `is_land = yes` would stop the direct link from being fetched | In selector/tooltip contexts, use optional rank comparisons: `location_rank ?= location_rank:<rank>`. Do not rely on an outer guard to protect a direct `location_rank = ...` line. |
| 2026-05 | Created event artwork with `create_art = { artist = scope:X quality = { ... } }` only, leaving the option tooltip empty and the artwork unnamed | Assumed `create_art` would infer enough display data from the artist | For event-created named artworks, provide `type = work_of_art_type:<type>` and `key = <loc_key>` with main-menu localization. If the artwork type depends on `artist_type`, branch with visible `if`/`else_if` `create_art` blocks so the real effect tooltip renders. |
| 2026-06 | Left IO monthly maintenance effects under `monthly_effect` and tried to hide the body, causing tooltip noise and severe monthly performance risk | Assumed `hidden_effect` made IO monthly maintenance safe enough to keep | Do not use TV IO `monthly_effect` blocks at all. Keep intended visible IO variable breakdowns in `monthly_change`, and move maintenance/completion side effects to registered country pulses or explicit lifecycle hooks. |
| 2026-05 | Added TV logic directly under vanilla hardcoded on_action hooks like `on_winning_war`, `on_annex`, and `on_ruler_death`, and Jomini logged "There is more than one 'effect' defined using most recent" while silently dropping the earlier body | Assumed the hardcoded hook would merge separate mod files' direct `effect` blocks | Extend shared hooks with `on_actions = { tv_*_callback }` in the generated `tv_pulse_bridges.txt` file and keep the TV logic in the callback's own feature file. For vanilla singleton pulses, do not use vanilla-named files. |
| 2026-05 | Filtered Engineering Department `random_list` branches with `modifier = { factor = 0 ... }`, and estate-demand bribery could repeatedly stay on the artwork-loss demand | Treated EU4-style random-list weight modifiers as valid EU5 branch filtering; vanilla EU5 examples use `trigger = { ... }` inside weighted branches | Use `trigger = { ... }` for `random_list` branch eligibility. When a reroll should visibly refresh, also exclude the current result in the branch trigger, e.g. `NOT = { var:current_demand ?= N }`. |
| 2026-05 | Approved Governor's House tactics by setting `tv_govhouse_active_tactic` and immediately calling a visible helper that compared `var:tv_govhouse_active_tactic`, causing event tooltip pre-evaluation to spam unset-variable errors | Assumed event option tooltip pre-evaluation commits earlier `set_variable` writes before later helper comparisons | Put state-changing/application chains that depend on newly written variables inside `hidden_effect`, and make reusable helpers guard `has_variable` before direct `var:` comparisons. Use `var:X ?= N` for optional event option triggers. |
| 2026-05 | Governor's House appointment still spiked errors on hover even though the action used `hidden_effect` to call `tv_governor_apply_modifiers_effect` | Assumed `hidden_effect` itself would block generic-action select-trigger pre-evaluation from walking into the helper chain | Treat `hidden_effect` as execution-only, not as a hover-prevention fence. Guard the action on persistent state and make any helper that reads newly initialized variables safe with `has_variable` plus `var:X ?=` or a persistent-state fallback. |
| 2026-05 | Governor's House regional-governor dismissal button called `tv_governor_remove_effect` visibly, exposing variable/list cleanup and refresh logic to generic-action tooltip evaluation | Treated cleanup-only action effects as harmless because the final click executed correctly | Wrap cleanup-only generic-action effects in `hidden_effect = { ... }`. `scripts/validate.py` now fails registered hidden-only helpers such as `tv_governor_remove_effect` when they are called from a visible generic-action effect. |
| 2026-05 | Applied Governor's House War Tent tactics to the stored general character, but the combat bonuses did not take effect in play | Treated the stored-general character modifier path as equivalent to the active army commander path | War Tent tactics intentionally use country-scoped static modifiers as an approximation. Keep the explanatory comment near `add_country_modifier` and do not convert `tv_govhouse_tactic_*` back to `add_character_modifier` without a new runtime-verified active-commander path. |
| 2026-05 | Used `[ROOT.MakeScope.GetVariable('tv_local_exhibition_last_duration').GetValue\|0]` in country event localization | Treated event `ROOT` like a GUI object binding that needs `MakeScope` | In event localization, `ROOT` is already a script scope. Use `[ROOT.GetVariable('var_name').GetValue\|0]`; reserve `Country.MakeScope.GetVariable(...)` / `Location.MakeScope.GetVariable(...)` for GUI object contexts. Runtime error: `Could not find promote for 'MakeScope'`. |
| 2026-05 | Left the shared `situation_subheader_content` block active in a custom `situation_panel` that did not use subheader content | Assumed overriding main content was enough; the shared situation template still emits its default 45px subheader row | Add `blockoverride "situation_subheader_content" {}` near the top of custom situation panel files when no subheader is needed. Vanilla panels such as `colonial_revolution.gui` use this exact empty override. |
| 2026-05 | Used `text = "TV_RM_TARGET_STEAM_ENGINE"` in a GUI file without a matching localization key | Hard-coded an obsolete research-target key instead of deriving it from the current data/localization set | GUI `text = "KEY"` values are localized; verify the key exists in supported localization files, correct it to the data-backed key, or use `raw_text` only for literal strings. |
| 2026-05 | Weighted autonomous Governor's House religion conversion only from national spiritualist tendency | Treated conversion need as a pure country preference, then over-copied vanilla `promote_religion`'s minority-cleanup condition into a regional priority heuristic | For Governor's House conversion priority, count only owned locations whose dominant religion is not the governing country's religion, then use that share as the priority weight. |
| 2026-05 | Used `visible = "[And(a, b, c)]"` in GUI widget visibility expressions | Assumed GUI boolean helpers were variadic | GUI `And()` / `Or()` take exactly two operands. Use `And3()` / `Or3()` for exactly three operands, or nest binary helpers for larger expressions. Runtime error: `Function 'And' expected 2 arguments, got 3`. |
| 2026-05 | Put `limit = {}` directly inside `any_international_organizations_member_of` in a scripted trigger | Copied effect-iterator syntax into a trigger iterator | In trigger context, put filters directly inside the block: `any_international_organizations_member_of = { international_organization_type = international_organization_type:<io_type> ... }`. Runtime error: `Unknown trigger type: limit`. |
| 2026-05 | Put `any_international_organizations_member_of` directly under a `country_interaction` `potential` block | Assumed the top-level `potential` root was the acting country | Wrap country-scoped checks in `scope:actor = { ... }` inside country interaction `potential`. Runtime error: `Inconsistent trigger scopes (invalid vs. country)`. |
| 2026-05 | Put `position = { 50% 0 }` directly on overlay `icon` widgets and every arrow rendered at the left edge | Assumed percent positions behave the same on icons as on vanilla widget wrappers | For fixed-width icon overlays, use pixel positions. If percent placement is needed, position a `widget` wrapper with percentages and put the icon inside it. |
| 2026-05 | Added `tv_appoint_governor` and `tv_remove_governor` generic actions but did not list them in any `generic_action_ai_lists` file | Assumed `ai_will_do` and high `ai_tick_frequency` were sufficient for player-facing actions | Every generic action must be explicitly listed in at least one AI list. Use list `potential` to limit evaluation and keep player-only actions listed with restrictive AI behavior. |
| 2026-05 | Added persistent local exhibition generic actions but did not add their `PERFORM_<action>_ACTION` message types | Added action localization keys but forgot the separate message type database in `main_menu/gui/messagetypes.txt` | Add each new generic action to `scripts/gen_messagetypes.py`, rerun it, and keep SETUP/LOG/MAP localization keys in every supported language. |
| 2026-05 | Added mod-defined price keys for local exhibition costs without matching `<price_key>_cost_modifier` modifier type definitions | Assumed defining `common/prices` was enough for the price database | Every mod-defined price key needs a matching cost modifier type in `main_menu/common/modifier_type_definitions` plus price and modifier-type localization. |
| 2026-05 | Added custom goods `tv_wonder_materials` without the matching modifier type family (`ban_exports_of_*`, `ban_imports_of_*`, `local_*_output_modifier`, `global_*_output_modifier`, `can_extract_*`, `*_impacts_inflation`, `*_used_for_minting`, `global_*_pop_demand`) | Assumed the goods database alone was enough for EU5 to accept the commodity | Every custom goods ID needs the full vanilla-style modifier family in `main_menu/common/modifier_type_definitions`, or the engine logs missing modifier type errors at startup. |
| 2026-05 | Reused `color = goods_masonry` for custom good `tv_wonder_materials`, causing startup to log `Goods tv_wonder_materials has same color as masonry` | Treated a vanilla goods color key as a harmless visual alias for a masonry-like custom good | Give every custom good its own `goods_<custom_id>` named color in `main_menu/common/named_colors`, then reference that unique key from the goods definition. |
| 2026-05 | Added custom modifier types for prices, custom goods, and IO special statuses without `common/modifier_icons` mappings, causing startup `Missing Icon for Modifier` logs | Treated `modifier_type_definitions` and existing DDS files as enough for modifier UI icons | Add same-key entries under `main_menu/common/modifier_icons/*.txt`; each entry should map `positive` and optionally `negative` to a vanilla or custom DDS path. |
| 2026-05 | Used `construct_building = { ... cost_multiplier = 0 instant = yes }` without `cost_multiplier_reason` | Assumed a zero multiplier did not need a cost reason because the building was event-created | Any `construct_building` with `cost_multiplier` must also include `cost_multiplier_reason = <loc key>`. For event-funded construction, vanilla uses `cost_multiplier_reason = "game_concept_event"`. |
| 2026-05 | Left mod-defined IOs without matching `international_organizations/<io_type>.dds` textures | Assumed custom organization panels could provide all visible IO icons | Shared IO UI uses `InternationalOrganization.GetIcon`; provide `main_menu/gfx/interface/icons/international_organizations/<io_type>.dds` matching the IO type key, or shared title/list/tooltips fall back to the generic IO icon. |
| 2026-05 | Added custom `game_concept_tv_*` localization and used `[tv_*\|e]` without registering the concept ID | Assumed `game_concept_*` localization alone creates a concept | Define every custom concept in `src/main_menu/common/game_concepts/tv_game_concepts.txt` with at least a `texture`, or reuse an existing registered concept. Otherwise square-bracket links are parsed as data-system functions and render `ERROR:<concept>`. |
| 2026-05 | Used `[tv_wonder_labor_camp\|E]`, `[tv_wonder_material_depot\|E]`, and `[tv_wonder_material_dispatch_point\|E]` in GUI-bound help text | Treated ordinary building localization keys as if they were registered game concepts | Use `$key$` for plain localization substitution, or register a real game concept before using `[key\|E]`. GUI localized text otherwise logs `Could not find data system function '<key>'`. |
| 2026-06 | Used `building_type = "variable_map(tv_wonder_probe_helper_by_wonder_id\|local_var:probe_key)"` inside a later building/location filter | Treated a quoted variable-map scope link as a general RHS value expression | Use `variable_map(...)` primarily on the left side to enter the mapped scope. If the mapped value must be compared or passed as a RHS, first capture it in the map-owning scope with `set_local_variable` or `set_variable`, then use `local_var:X` / `var:X`; prefer `local_var` for temporary bridge values. |
| 2026-06 | Replaced `random_key_in_variable_map` proposal selection with generated `random_list` branches while fixing a `root` unset runtime error | Fixed the symptom by undoing the requested variable_map/id-flow refactor shape | Keep the variable-map key stream. In generic-action pre-evaluation paths, save the current country/actor before the key callback, then write back through that named scope instead of `root`. |
| 2026-06 | Used `Country.MakeScope.GetVariable(...)`, then `SCOPE.sCountry('actor').MakeScope.GetVariable(...)`, inside a generic action description localization key | Treated action description localization like a GUI/action-scope text surface | Generic action title/description loc can be fetched without GUI `Country` and without a script-scope Container even when the real hover renders correctly. Preserve player-country dynamic text with context-independent `Player.MakeScope`; use an explicitly scoped GUI route only when the text needs non-player scopes. Runtime errors included `wanted context of type 'Country'` and `wanted context of type 'Container'`. |
| 2026-06 | Used `SelectGameConcept` with a dynamic wonder key from a variable map / `GetFlagName` in `location_window.gui` | Assumed a flag-derived value would remain the raw script id | The failing value had already localized before concept-link parsing, producing `[localized_name\|e]` and `Could not find data system function`. Keep dynamic clickable concept routes, but feed them numeric-id-built registered ids such as `tv_wonder_display_<id>`; reserve `Localize(Concatenate('game_concept_', key))` for intentionally plain text. |
| 2026-06 | Used `GetFlagName` from a variable-map flag value as a raw wonder key in GUI | Assumed `flag:tv_wonder_unique_pharos_lighthouse` would round-trip to the script id string | `GetFlagName` can return localized display text, e.g. `法罗斯灯塔`. Store numeric ids and generate static id branches when GUI must build localization, modifier, texture, or scripted-effect keys. |
| 2026-06 | Used CSS-like `fittype = contain` for a GUI image | Assumed GUI image fit values followed web/CSS naming | EU5 accepts vanilla GUI fit names such as `centercrop`, `fill`, `start`, and `end`; `contain` logs `Unknown fit type 'contain'` at startup. |
| 2026-06 | Used `texture = "[Concatenate('gfx/...', key, '.dds')]"` to dynamically choose a DDS path | Assumed a GUI `texture` expression could return a CString path like a static `texture = "gfx/...dds"` property | Runtime texture expressions need a texture-like return value. `GetConceptTexture(Concatenate(...))` renders, while raw DDS path strings built by `Concatenate` stay blank. Route arbitrary dynamic DDS assets through image-only game concepts keyed by numeric ids. |
| 2026-05 | Left `STATIC_MODIFIER_NAME_tv_wonder_*_level_*` in the hand-written Engineering Department localization files after the wonder-mechanics generators started emitting the same keys | Split ownership of one localization key family between manual and generated YAML files, so the same language pack loaded duplicate definitions | Keep each localization key defined in exactly one file per language. When a generator takes over a key family, delete the stale manual copies and let `scripts/validate.py` fail duplicate localization keys before launch. |
| 2026-06 | Generated `MAPMODE_TV_WONDER` localization with a real physical newline inside the quoted value, so the continuation text was parsed as a new key and logged `Invalid character` plus `Missing colon` | Treated YAML multiline text as acceptable in EU5 localization output | Keep each localization key/value on one physical line. Emit intentional line breaks as `\n` inside the quoted value; `scripts/validate.py` now catches malformed localization physical lines. |
| 2026-05 | Used `multiply_variable = { name = X value = Y }` to scale a variable in-place | Assumed EU4-style multiply_variable exists in EU5 | Use `change_variable = { name = X multiply = Y }`. EU5 has no multiply_variable or divide_variable — all variable math uses change_variable with multiply/divide keys. |
| 2026-05 | Divided Governor's House average location `prosperity` by 100 before negating it for autonomous recovery weight, making recovery too small to beat other task priorities | Treated `prosperity` reads as 0..100 percent points instead of signed fractional values | Use raw fractional prosperity. For recovery priorities, negate the average directly and clamp below 0, e.g. `max(0, -average_prosperity)`. |
| 2026-05 | Attempted `GetVariable('tv_governed_area').GetArea.GetName` to display an area name stored in a character variable | Assumed `.GetArea` exists as a GUI accessor symmetrically with `.GetCharacter` | `.GetArea` does not exist in EU5 GUI. Pre-compute numeric display values as character variables and read them via `GetVariable('name').GetValue\|0`. Area names stored as scope variables cannot be displayed in GUI. |
| 2026-05 | Used standalone `io_character_card` widgets without overriding inherited sort-highlight blocks | Missed that `io_character_card` reuses `character_entry`, whose name highlight widgets call `FilteredSortedList.IsKeyHoveredByWidgetName` | For `io_character_card` outside a sortable `FilteredSortedList`, add `blockoverride "name_highlight" {}` and `blockoverride "character_entry_name_sort_by_highlight" {}`. Vanilla `middle_kingdom.gui` documents this as blocking error log spam. |
| 2026-05 | Put two top-level children inside a datamodel `item = { ... }` (`vbox` row plus sibling divider `widget`) | Treated an item block like a normal layout container | A datamodel `item` description must contain exactly one child and no properties. Wrap the row and divider inside one parent `vbox`/`widget`. Runtime error: `Malformed item desc`, and it can coincide with text formatter noise while the list renders. |
| 2026-05 | Used `blockoverride "block_title" {}` inside an inline `RequirementsList` | Tried to hide the title by emptying the block, which removed the safe nested title override and exposed `TooltipTableHeader` without `ExtraTooltipInfo` | Hide the title explicitly with `blockoverride "block_title" { block "block_title" { visible = no } }`; empty overrides can trigger continuous `ExtraTooltipInfo.GetTintColor` errors while hovering the panel. |
| 2026-05 | Used `TooltipTextBlock` directly inside the always-visible location wonder card | Treated a tooltip-only widget as normal panel content; its template reads `ExtraTooltipInfo.GetTintColor`, so unique-wonder provinces with a visible level-0 fallback spammed `FetchData failed` while the location window stayed open | Reserve `TooltipTextBlock` for real tooltip content and use `text_single`/`text_multi` for always-visible panel text. |
| 2026-03 | Removed `custom_tooltip` from event options | Guessed dotted key format was invalid; skipped Steps 2/3 | Read `reference_game_files/`; `ali_qushji_settles.tooltip` confirms dotted keys are valid |
| 2026-03 | Used `location_rank:village` | Guessed enum value; did not check defines | Read `reference_game_files/game/in_game/common/location_ranks/00_default.txt`; valid values are `rural_settlement`, `town`, `city`, `megalopolis` |
| 2026-03 | Placed child `text_single` inside `blockoverride "common_header_text"` | Guessed block accepted child widgets; skipped reading `cards.gui` | Read `cards.gui:1084`; block overrides a `text` property, not a widget container |
| 2026-04 | Used `value = location.local_*` inside a location-scope script value | Assumed `location.` prefix was always required for location variables | `location.` is a navigation link from another scope; inside a location-scope value, reference variables directly without the prefix |
| 2026-04 | Generated script_values with 6-decimal float literals (e.g. `0.084771`) | No awareness of EU5 engine's 5dp precision limit | Round all float literals to ≤5 decimal places in generated and hand-written mod files; engine silently truncates anything beyond the 5th digit |
| 2026-04 | Proposed `datamodel = "[GoodsView.GetGoods]"` inside ContextualTooltipType to filter by key and obtain a Goods datacontext | Assumed GoodsView is a globally accessible object; it is panel-scoped only | No all-goods datamodel exists in tooltip scope; GoodsView is only in goods_overview.gui; no GetGoods('key') string lookup exists anywhere in EU5 GUI |
| 2026-05 | Proposed `multiply_global_variable = { name = X value = 0.95 }` to scale a global variable in-place | Assumed EU5 has a multiply variant analogous to `change_global_variable` | No `multiply_global_variable` effect exists; use hardcoded `set_global_variable` values per case (idempotent) or compute in a local_variable first |
| 2026-05 | Generated `limit = { capital = { prosperity >= var:dm_prosperity_target } }` inside `dm_tick_all_missions` | Assumed `var:X` always reads from country scope; inside `capital = {}` the scope is location, so `var:X` reads the location variable store (wrong value) | Use the literal integer from the spec (e.g. `prosperity >= 75`) instead of `var:` inside location-scope blocks; set `dm_prosperity_target` for GUI display only |
| 2026-05 | Engineering Department finalization applied `years = var:tv_wonder_final_completion_local_years` and `change_prosperity = var:tv_wonder_final_completion_prosperity` inside `var:tv_wonder_site ?= { ... }`, causing unset-variable errors after the final wonder event | Assumed plain `var:X` inside the saved site/location block would still read the country preview variables | After switching into location/province scope, plain `var:X` reads that nested variable store. Use `root.var:X` for country-owned numeric inputs such as `change_prosperity` or `add_location_modifier years`, or capture the value before the scope switch. |
| 2026-05 | Wrote 30 events as `tv.conquest.1 = { ... }` etc. under `namespace = tv` | Assumed event IDs accept arbitrary multi-dot keys; engine actually splits on the first dot only | Event ID must be `<namespace>.<integer>`; split into 6 files (one namespace per category, IDs `tv_conquest.1..5`, etc.). Loc-key dotted suffixes (`tv_conquest.1.t`) are still valid because they aren't event IDs themselves |
| 2026-05 | Set `parentanchor = vcenter` on widgets that were direct children of an `hbox` (36 sites in tv_victory_situation.gui) | Did not internalize that hbox/vbox auto-arrange children on the cross-axis, so child anchors are rejected | Remove `parentanchor` from any widget directly inside `hbox = { ... }` or `vbox = { ... }`; keep it on children of plain `widget`/`window`/etc. |
| 2026-05 | Set `size = { 97% 72 }` on `io_character_card` widgets directly under `organization_custom_content` | Treated percentage sizing as independent of the parent layout; hbox/vbox children cannot use percentage `size` components | Remove percentage `size` from hbox/vbox children and drive them with layout policies, stretch factors, or non-percent fixed/min/max sizing. Vanilla `io_character_card` already uses `layoutpolicy_expanding`. |
| 2026-05 | Put a root expanding `scrollarea` directly in `organization_custom_content`, then alternated between a collapsed tab, an oversized viewport that did not scroll, a visible 5px top gap, an unsupported `margin_top` on `scrollarea`, and a wrapper `vbox` whose child scrollarea sat lower inside it | The vanilla custom-content slot is already an inner vbox with `margin_top = 5` followed by an `expand` spacer; wrapping another vbox hides that structure, and `autoresizescrollarea` can grow with active content so there is no overflow left to scroll | Inject the `scrollarea` directly into `organization_custom_content`, override the block's `margin_top`/`spacing`, give the scrollarea a fixed viewport (`size = { 100% N }` or equivalent fixed min/max), and make the `scrollwidget` content establish natural height with `layoutpolicy_vertical = fixed`/`ignoreinvisible = yes`. Avoid `autoresizescrollarea` for dynamic IO tabs that need scrolling after state changes. |
| 2026-05 | Saved `towards_victory_location_modifiers.txt` and `towards_victory_situations.txt` without UTF-8 BOM | Assumed BOM was only required for localization YAML; engine actually requires it on common/ .txt scripts as well | Always save common/ scripts with UTF-8 BOM (`utf-8-sig`); validate with `head -c 3 <file> \| xxd` -> `ef bb bf` |
| 2026-05 | Stored progress_pct as `score ÷ threshold` (e.g. `160 ÷ 2000`) expecting a float [0,1]; GUI progressbar showed 0% for all paths except science | `change_variable { divide }` is FLOOR integer division — `160÷2000=0`; progressbar default max=1, so integer 1 = 100% and integer 0 = 0% | Use integer scale [0,100]: divide by `threshold÷100` when threshold is divisible by 100, otherwise `multiply=100; divide=threshold`. Always set `max = 100` explicitly on the progressbar widget. |
| 2026-05 | Used `#Y[Country.MakeScope.GetVariable(...).GetValue\|0]#!` to gold-highlight an inline scripted variable in a localization string | Assumed `#Y` is a self-contained tag; when the variable resolves to a digit (e.g. 64), the engine reads `#Y64` as the tag name — `Unknown formatting tag 'y64!'` | Use plain `[variable_expression]` without color, OR add a space separator: `#Y [variable_expression]#!` (leading space becomes visible content). Never put `[` directly after a color tag. |
| 2026-05 | Proposed `INJECT:printing_press_advance = { allow = { has_variable = tv_advance_printing_press_unlocked } }` to gate a vanilla advance | Assumed INJECT could add a second allow block; vanilla advances support only ONE allow block | When vanilla advance already has an allow block, use `REPLACE:advance_name = { <full vanilla properties> allow = { OR = { <mod condition> <original condition> } } }`. Use INJECT only when the advance has no existing allow block. |
| 2026-05 | Used `text = TV_CONQUEST_M1_TRIGGER_DESC` in `custom_description` trigger blocks with only a YAML localization entry | Assumed a YAML key suffices for `custom_description`'s `text`; engine actually validates against `common/trigger_localization/` at load | Create an entry in `src/in_game/common/trigger_localization/`: `KEY = { global = KEY }`. The YAML key remains; trigger_localization is the bridge the engine requires. Error: `'No trigger loc KEY'` at startup. |
| 2026-05 | Wrote trigger_localization entries as `KEY = { global = KEY }` only, for milestones whose trigger has `custom_tooltip` nested inside `custom_description` | Assumed `global` alone was sufficient; engine also requires `first` (and `none`, `third`) perspectives when nested `custom_tooltip` is present | Use all four perspectives: `KEY = { none = KEY  global = KEY  first = KEY  third = KEY }`. Use this form for every trigger_localization entry as a safe default. Error: `'(BUG: KEY missing perspective. Could not find parameters for PROMOUN first Negative required false)'` at runtime. |
| 2026-05 | Used `value = leader_country.var:tv_arts_exhibition_leader_char.artist_skill  multiply = 0.002` assuming `artist_skill` was an integer 0–100 | Design doc assumed raw integer skill; engine actually normalizes `artist_skill` to a 0.0–1.0 float before exposing it to scripted values | Treat `artist_skill` as a 0–1 float in all scripted value math. Scale multipliers accordingly: the old design-intent `skill × 0.002` (for 0-100) becomes `skill × 0.2` (for 0-1). Then balance as needed. |
| 2026-05 | Would have used `artist_skill_modifier` as a character modifier key for boosting artist skill | Guessed modifier name by analogy; `artist_skill_modifier` does not exist in modifier_type_definitions | Step 3 verified: use `artist_skill_level_gain = X` (`already_percent = yes`). Source: `00_modifier_types.txt`, `03_artist.txt`. |
| 2026-05 | Proposed `multiply_variable = { name = X value = Y }` and `divide_variable = { name = X value = Y }` | Guessed EU5 has separate multiply/divide variable commands by analogy | Neither command exists; use `change_variable = { name = X multiply = Y }` / `divide = Y`. Both accept `var:Z` references. Verified in `cmm_core_slider_setting_effects.txt:223`. |
| 2026-05 | Would have written `government = monarchy` in a trigger | EU4 carry-over; `government` trigger does not exist in EU5 | Correct syntax: `government_type = government_type:monarchy`. Government type IDs: `monarchy`, `republic`, `theocracy`, `tribe`, `steppe_horde`. Source: `00_default.txt`. |
| 2026-05 | Used `add_treasury = -200` to deduct gold in a generic action effect | EU4 carry-over; `add_treasury` does not exist in EU5 | Use `add_gold = -200` (literal) or `add_gold = { value = script_value multiply = N }`. Trigger: `gold >= 200` (literal integer) is valid. Error: `Unknown effect add_treasury`. |
| 2026-05 | Would have used a flat `add_gold = -N` cost for a vanilla-style event option that should scale by country economy | Did not check vanilla event cost helpers | Use `change_gold_effect = { scale = -N }` for dynamic country-event costs; it scales with `capital_wealth` and `country_economical_base`. Source: `country_gold_effects.txt`; example: `laws.0005` paid research option uses `scale = -3.5`. |
| 2026-05 | Used `trigger_event = { id = X }` in scripted_effects / on_action files | Assumed `trigger_event` is the universal event-fire effect; it does not exist outside event `option = { }` blocks | Use `trigger_event_non_silently = { id = X }` (shows popup to player) or `trigger_event_silently = X` (background). Engine error: `jomini_effect.cpp:556: Unknown effect trigger_event`. Source: `on_action_effects.txt:39`, `character_death_pulses.txt:57`. |
| 2026-05 | Used `datacontext = "[InternationalOrganizationsView.GetPlayer]"` inside `action_button_diamond` in IO organization panels; used `Country.MakeScope.GetVariable(...)` in visible expressions | Assumed `datacontext` was needed to give `Country` a country scope for variable lookup; did not check vanilla IO GUI patterns | Vanilla never sets `datacontext` on `action_button_diamond` in org panels — doing so breaks tooltip generation entirely (empty tooltip, button permanently grayed). Use `Player.MakeScope.GetVariable(...).IsSet` in visible expressions; `Player` is a global GUI binding that never requires a datacontext. Source: `defensive_league.gui:128`; `Player.MakeScope` verified at `religious_doctrine.gui:1831`. |
| 2026-05 | Used `can_start = { game_is_initialized = yes }` on `tv_victory_situation` | `game_is_initialized` = `current_date > 1337.5.1` (strict); EU5 starts on exactly 1337.5.x so the month-1 check fails; situation spawns in month 2 and becomes visible in month 3 | Use `can_start = { always = yes }` for permanent, non-popup situations with `monthly_spawn_chance_unique`. Source: `game_triggers.txt:58-60`. |
| 2026-05 | `ordered_in_global_list` ranks 2-5 in `tv_update_leaderboard_effect` accessed `situation:X.var:tv_rank_N_country` in `limit` without `has_variable` guards | pool was empty at `on_start`, rank-1 body never ran, `tv_rank_1_country` was unset; every pool entry in ranks 2-5 triggered a script error | Wrap each rank 2-5 pass in `if = { limit = { has_variable = tv_rank_{N-1}_country } ... }` so unset prerequisite ranks are skipped entirely. |
| 2026-05 | Rebuilt leaderboard ranks without clearing old `tv_rank_N_country` variables, then used `has_variable = previous_rank` as the only later-rank gate | stale rank variables survived after the current pool shrank; a later `ordered_in_global_list` pass could select no item, making `prev = none` and producing wrong-type/failed-var errors | Clear all rank variables at the start of each rebuild, ensure pool entries have numeric order_by variables, and guard rank N with `global_variable_list_size value > N-1`. |
| 2026-05 | Used `has_variable = tv_conquest_general_char` in `tv_govhouse_actions` allow, then read `var:tv_conquest_general_char = { is_alive = yes }` in a sibling `scope:actor` block | Assumed sibling trigger blocks in generic action `allow` would short-circuit; the UI evaluator still fetched the direct `var:` link and spammed errors when no Grand General existed | Use optional variable links for nullable variable reads: `var:X ?= { ... }`, `var:X ?= 1`, or `var:X ?= { this >= N }`. Move conditions to `potential` only when hiding the action is intended. |
| 2026-05 | Added a separate `tv_character_titles.txt` containing another `character_title_prefix = { ... }` block | Assumed customizable localization blocks merge additively across files; engine treats the top-level key as unique and ignores duplicates | Generate a full `character_title.txt` from vanilla and insert mod title entries into the original `character_title_prefix` block. Runtime log: `Duplicated key character_title_prefix will not be created`. Source: `character_title.txt:1`. |
| 2026-05 | Added `unique = yes` to `tv_arts_exhibition` IO type and used `international_organization:tv_arts_exhibition` + `is_member_of_international_organization = tv_arts_exhibition` in scripts | Assumed non-unique IO can be referenced by type name as a scope link; `tv_arts_exhibition` must NOT be unique because each country creates its own independent IO instance | NEVER add `unique = yes` to `tv_arts_exhibition`. Use `on_creation = { set_leader_country = scope:actor }` in the IO type for leader init. Use `every_international_organization = { limit = { type = X } }` to scope at runtime. Errors: `'key X does not lead to a unique international organization'`, `'Failed to find a valid event target link'`. |
| 2026-05 | Applied Chief Artist bonus to IO typed variable via `change_variable` in `monthly_country_pulse` on_action; bonus never appeared in the IO variable tooltip | IO typed-variable `monthly_change` is a scripted value that drives the tooltip; external `change_variable` calls are invisible to it | Put the contribution in `monthly_change` using `leader_country.var:X` chain (`value = leader_country.var:tv_chief_artist_monthly_bonus`). Store the computed value on the country in on_action for GUI display only. Set `leader_type = country` and call `set_leader_country` in `on_creation`/creation block. Source: `red_turban_rebellions.txt:3371` (root.var:X), `christian_tenets.txt:359` (leader_country.modifier:X). |
| 2026-05 | Used `var:tv_chief_artist ?= { set_local_variable { name = ca value = artist_skill } ... prev = { set_variable { value = local_var:ca } } }` to read a character's skill and store it on the country | A local variable set inside `var:X ?= {}` was read outward through `prev = {}`; that escape direction is unreliable and the value remains 0. This does not forbid setting a local variable before a scope switch and reading `local_var:X` inside the nested scope. | Use chained access from country scope: `set_variable = { name = tv_chief_artist_monthly_bonus  value = { value = var:tv_chief_artist.artist_skill  multiply = 0.002 } }`. Check liveness in the limit: `var:tv_chief_artist = { is_alive = yes }`. Source: `invite_artist.txt:129` (`add = scope:target.artist_skill`). |
