# @Generated by scripts_court_positions/in_game/common/character_interactions/gen_tv_court_positions_character_interactions.py
#   Data:    data/court_positions.yaml
#   Regen:   python scripts_court_positions/in_game/common/character_interactions/gen_tv_court_positions_character_interactions.py
# Do not edit directly - modify the data file and re-run the generator.

tv_court_appoint_physician_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "tv_court_select_physician"
		none_available_msg_key = "tv_court_no_physician_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { hidden_effect = { tv_court_positions_appoint_physician_direct_effect = yes } }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_tutor_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "tv_court_select_tutor"
		none_available_msg_key = "tv_court_no_tutor_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { hidden_effect = { tv_court_positions_appoint_tutor_direct_effect = yes } }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_steward_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "tv_court_select_steward"
		none_available_msg_key = "tv_court_no_steward_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { hidden_effect = { tv_court_positions_appoint_steward_direct_effect = yes } }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_architect_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "tv_court_select_architect"
		none_available_msg_key = "tv_court_no_architect_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { hidden_effect = { tv_court_positions_appoint_architect_direct_effect = yes } }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_cultural_emissary_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "tv_court_select_cultural_emissary"
		none_available_msg_key = "tv_court_no_cultural_emissary_available"
		column = { data = character_artist }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = yes
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { hidden_effect = { tv_court_positions_appoint_cultural_emissary_direct_effect = yes } }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_chronicler_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "tv_court_select_chronicler"
		none_available_msg_key = "tv_court_no_chronicler_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { hidden_effect = { tv_court_positions_appoint_chronicler_direct_effect = yes } }
		}
	}
	ai_will_do = { add = -100 }
}
