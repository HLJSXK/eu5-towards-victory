# @Generated by scripts_court_positions/in_game/common/character_interactions/gen_tv_court_positions_character_interactions.py
#   Data:    data/court_positions.yaml
#   Regen:   conda run --no-capture-output -n eu5 python scripts_court_positions/in_game/common/character_interactions/gen_tv_court_positions_character_interactions.py
# Do not edit directly — modify the data file and re-run the generator.

tv_court_appoint_physician_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_physician_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_tutor_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_tutor_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_steward_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_steward_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_architect_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_architect_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_cultural_emissary_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
			is_alive = yes
			is_adult = yes
			is_artist = yes
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_cultural_emissary_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_chronicler_character = {
	sound = UI_action_religion_generic
	message = yes
	on_own_nation = yes
	ai_tick = never
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_chronicler_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}
