# @Generated by scripts_court_positions/in_game/common/generic_actions/gen_tv_court_positions_actions.py
#   Data:    data/court_positions.yaml
#   Regen:   conda run --no-capture-output -n eu5 python scripts_court_positions/in_game/common/generic_actions/gen_tv_court_positions_actions.py
# Do not edit directly — modify the data file and re-run the generator.

tv_court_appoint_physician = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_court_select_physician"
		none_available_msg_key = "tv_court_no_physician_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = { tv_court_positions_appoint_physician_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_physician_character = {
	type = character
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
		is_alive = yes
		is_adult = yes
		is_artist = no
		is_ruler = no
		is_heir = no
		is_consort = no
		in_cabinet = no
		is_general = no
		is_admiral = no
		is_explorer = no
		is_loyal = yes
		NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_physician_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_dismiss_physician = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { has_variable = tv_court_physician_character } }
	effect = {
		scope:actor = { hidden_effect = { tv_court_positions_remove_physician_effect = yes } }
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_tutor = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_court_select_tutor"
		none_available_msg_key = "tv_court_no_tutor_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = { tv_court_positions_appoint_tutor_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_tutor_character = {
	type = character
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
		is_alive = yes
		is_adult = yes
		is_artist = no
		is_ruler = no
		is_heir = no
		is_consort = no
		in_cabinet = no
		is_general = no
		is_admiral = no
		is_explorer = no
		is_loyal = yes
		NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_tutor_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_dismiss_tutor = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { has_variable = tv_court_tutor_character } }
	effect = {
		scope:actor = { hidden_effect = { tv_court_positions_remove_tutor_effect = yes } }
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_steward = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_court_select_steward"
		none_available_msg_key = "tv_court_no_steward_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = { tv_court_positions_appoint_steward_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_steward_character = {
	type = character
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
		is_alive = yes
		is_adult = yes
		is_artist = no
		is_ruler = no
		is_heir = no
		is_consort = no
		in_cabinet = no
		is_general = no
		is_admiral = no
		is_explorer = no
		is_loyal = yes
		NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_steward_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_dismiss_steward = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { has_variable = tv_court_steward_character } }
	effect = {
		scope:actor = { hidden_effect = { tv_court_positions_remove_steward_effect = yes } }
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_architect = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_court_select_architect"
		none_available_msg_key = "tv_court_no_architect_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = { tv_court_positions_appoint_architect_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_architect_character = {
	type = character
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
		is_alive = yes
		is_adult = yes
		is_artist = no
		is_ruler = no
		is_heir = no
		is_consort = no
		in_cabinet = no
		is_general = no
		is_admiral = no
		is_explorer = no
		is_loyal = yes
		NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_architect_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_dismiss_architect = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { has_variable = tv_court_architect_character } }
	effect = {
		scope:actor = { hidden_effect = { tv_court_positions_remove_architect_effect = yes } }
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_cultural_emissary = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_court_select_cultural_emissary"
		none_available_msg_key = "tv_court_no_cultural_emissary_available"
		column = { data = character_artist }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = yes
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = { tv_court_positions_appoint_cultural_emissary_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_cultural_emissary_character = {
	type = character
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
		is_alive = yes
		is_adult = yes
		is_artist = yes
		is_ruler = no
		is_heir = no
		is_consort = no
		in_cabinet = no
		is_general = no
		is_admiral = no
		is_explorer = no
		is_loyal = yes
		NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_cultural_emissary_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_dismiss_cultural_emissary = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { has_variable = tv_court_cultural_emissary_character } }
	effect = {
		scope:actor = { hidden_effect = { tv_court_positions_remove_cultural_emissary_effect = yes } }
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_chronicler = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { always = yes } }
	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_court_select_chronicler"
		none_available_msg_key = "tv_court_no_chronicler_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
			in_cabinet = no
			is_general = no
			is_admiral = no
			is_explorer = no
			is_loyal = yes
			NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = { tv_court_positions_appoint_chronicler_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_appoint_chronicler_character = {
	type = character
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = {
		scope:recipient = {
		is_alive = yes
		is_adult = yes
		is_artist = no
		is_ruler = no
		is_heir = no
		is_consort = no
		in_cabinet = no
		is_general = no
		is_admiral = no
		is_explorer = no
		is_loyal = yes
		NOT = { has_character_modifier = busy_modifier }
		}
	}
	effect = {
		if = {
			limit = { exists = scope:recipient }
			scope:actor = { tv_court_positions_appoint_chronicler_direct_effect = yes }
		}
	}
	ai_will_do = { add = -100 }
}

tv_court_dismiss_chronicler = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999
	potential = { scope:actor = { has_variable = tv_court_positions_initialized } }
	allow = { scope:actor = { has_variable = tv_court_chronicler_character } }
	effect = {
		scope:actor = { hidden_effect = { tv_court_positions_remove_chronicler_effect = yes } }
	}
	ai_will_do = { add = -100 }
}
