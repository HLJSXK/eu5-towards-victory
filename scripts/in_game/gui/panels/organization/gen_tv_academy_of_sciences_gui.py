"""
Generate src/in_game/gui/panels/organization/tv_academy_of_sciences.gui from data/locked_advances.yaml.

Only the research target display entries (the EqualTo_CFixedPoint text_single widgets)
are generated from data. The surrounding static GUI code (stage cards, custom tabs,
action cards, IO header overrides) is stored verbatim as template prefix/suffix strings in
this file.
"""

import sys
import argparse
from pathlib import Path

if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")

import yaml

REPO_ROOT = Path(__file__).resolve().parents[5]
DATA_FILE = REPO_ROOT / "data" / "locked_advances.yaml"
DEBATE_DATA_FILE = REPO_ROOT / "data" / "philosophy_debates.yaml"
OUT_FILE = REPO_ROOT / "src" / "in_game" / "gui" / "panels" / "organization" / "tv_academy_of_sciences.gui"

T = "\t"
LOCAL_DEBATE_SEAT_SIZE = 68
WORLD_DEBATE_SEAT_SIZE = 36
STANCE_COLOR_TEXTURES = {
    1: "color_light_green_texture",
    2: "color_red_texture",
    3: "color_yellow_texture",
}
STANCE_TOOLTIP_KEYS = {
    1: "TV_ACADEMY_DEBATE_STANCE_SUPPORT",
    2: "TV_ACADEMY_DEBATE_STANCE_OPPOSE",
    3: "TV_ACADEMY_DEBATE_STANCE_NEUTRAL",
}

# ─────────────────────────────────────────────────────────────────────────────
# Static template parts — do not move the generated block markers
# ─────────────────────────────────────────────────────────────────────────────

GUI_PREFIX = """\
# @Generated by scripts/in_game/gui/panels/organization/gen_tv_academy_of_sciences_gui.py
#   Data:    data/locked_advances.yaml  (fields: gui_value, id) + data/philosophy_debates.yaml
#   Regen:   conda run --no-capture-output -n eu5 python scripts/in_game/gui/panels/organization/gen_tv_academy_of_sciences_gui.py
# Do not edit directly — modify the data file and re-run the generator.
# Towards Victory — Academy of Sciences IO Panel
# Provides the interactive research mechanism UI: research target selection,
# preparation/concentrated research stage display, and action buttons.
# The Chief Scientist (IO leader character) is managed from the IO header.
#
# Verification — Step 3, Reference: autocephalous_patriarchate.gui:57,60
#   Quote: "visible = "[InternationalOrganizationsView.GetInternationalOrganization.MakeScope.GetVariable('patriarch').IsSet]""
#   Quote: "datacontext = "[InternationalOrganizationsView.GetInternationalOrganization.MakeScope.GetVariable('patriarch').GetCharacter]""
# Verification — Step 3, Reference: country_header.gui:34,67
#   Quote: block "country_header_character_visible" / block "country_header_character_context"

organization_panel = {

\tblockoverride "organization_overview_tab_visible" {}

\tblockoverride "organization_panel_overview_visible" {
\t\tvisible = "[InternationalOrganizationsView.Vars.NotExistOrHasValue( 'organizations', 'overview' )]"
\t}

\tblockoverride "organization_members_tab_down" {
\t\tdown = "[InternationalOrganizationsView.Vars.HasValue( 'organizations', 'members' )]"
\t}

\tblockoverride "organization_panel_members_visible" {
\t\tvisible = "[InternationalOrganizationsView.Vars.HasValue( 'organizations', 'members' )]"
\t}

\tblockoverride "organization_resolutions_tab_visible" {
\t\tvisible = yes
\t}
\tblockoverride "organization_resolutions_tab_text" {
\t\ttext = "TV_ACADEMY_PHILOSOPHY_TAB"
\t}
\tblockoverride "organization_resolutions_tooltip" {
\t\ttooltip = "TV_ACADEMY_PHILOSOPHY_TAB_TOOLTIP"
\t}

\tblockoverride "scroll_overview_empty_visible" { visible = no }

\tblockoverride "country_header_character_visible" {
\t\tvisible = yes
\t}
\tblockoverride "country_header_religion" {}
\tblockoverride "extra_titles" {}
\tblockoverride "organization_panel_extra_item" {
\t\twidget = {
\t\t\tsize = { 260 120 }
\t\t\tparentanchor = top|right
\t\t\twidgetanchor = top|right
\t\t\tposition = { -12 6 }
\t\t\tallow_outside = yes

\t\t\tvbox = {
\t\t\t\tlayoutpolicy_horizontal = fixed
\t\t\t\tlayoutpolicy_vertical = fixed
\t\t\t\tminimumsize = { 260 120 }
\t\t\t\tmaximumsize = { 260 120 }
\t\t\t\tusing = bg_paper_card_situations

\t\t\t\twidget = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tsize = {-1 42}
\t\t\t\t\tusing = bg_geodiplo_situation_header_secondary

\t\t\t\t\thbox = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tspacing = 4
\t\t\t\t\t\texpand = {}
\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\traw_text = "@adm!"
\t\t\t\t\t\t\tsize = { 30 30 }
\t\t\t\t\t\t\tfontsize = 20
\t\t\t\t\t\t\talign = center|nobaseline
\t\t\t\t\t\t}
\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_INSPIRATION_TITLE"
\t\t\t\t\t\t\tfontsize = 14
\t\t\t\t\t\t\talign = center|nobaseline
\t\t\t\t\t\t}
\t\t\t\t\t\texpand = {}
\t\t\t\t\t}
\t\t\t\t}

\t\t\t\tvbox = {
\t\t\t\t\tsize = { 236 72 }
\t\t\t\t\tlayoutpolicy_horizontal = fixed
\t\t\t\t\tlayoutpolicy_vertical = fixed
\t\t\t\t\tmargin = { 10 4 }
\t\t\t\t\tignoreinvisible = yes
\t\t\t\t\tspacing = 8

\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[Not(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet)]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_NO_CHIEF"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[And(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet, EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0'))]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_CONCENTRATED_RESEARCH"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[And3(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet, Not(EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0')), InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').IsSet)]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_TREATMENT"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[And3(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet, Not(Or(EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0'), InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').IsSet)), InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet)]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_MEDITATION"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[And(And3(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet, EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_philosophy_phase').GetValue, '(CFixedPoint)1.0'), EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_debate_current_node_type').GetValue, '(CFixedPoint)1.0')), Not(Or3(EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0'), InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').IsSet, InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet)))]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_LOCAL_DEBATE"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[And3(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet, EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_debate_current_node_type').GetValue, '(CFixedPoint)2.0'), Not(Or3(EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0'), InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').IsSet, InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet)))]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_WORLD_DEBATE"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[And3(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet, EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_debate_current_node_type').GetValue, '(CFixedPoint)0.0'), Not(Or3(EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0'), InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').IsSet, InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet)))]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_HISTORY"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tvisible = "[And(And3(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet, EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_debate_current_node_type').GetValue, '(CFixedPoint)1.0'), Not(Or3(EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0'), InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').IsSet, InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet))), Not(EqualTo_CFixedPoint(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_philosophy_phase').GetValue, '(CFixedPoint)1.0')))]"
\t\t\t\t\t\tsize = { 236 34 }
\t\t\t\t\t\tmax_width = 236
\t\t\t\t\t\ttext = "TV_ACADEMY_HEADER_ACTIVITY_INSPIRATION"
\t\t\t\t\t\tfontsize = 13
\t\t\t\t\t\talign = nobaseline|left
\t\t\t\t\t}

\t\t\t\t\thbox = {
\t\t\t\t\t\tlayoutpolicy_horizontal = fixed
\t\t\t\t\t\tsize = { 236 22 }
\t\t\t\t\t\tspacing = 6
\t\t\t\t\t\twidget = {
\t\t\t\t\t\t\tsize = { 168 20 }
\t\t\t\t\t\t\tprogressbar = {
\t\t\t\t\t\t\t\tsize = { 168 14 }
\t\t\t\t\t\t\t\tparentanchor = vcenter
\t\t\t\t\t\t\t\twidgetanchor = vcenter
\t\t\t\t\t\t\t\tusing = progress_bar_blue_alt
\t\t\t\t\t\t\t\tmin = 0
\t\t\t\t\t\t\t\tmax = 100
\t\t\t\t\t\t\t\tvalue = "[InternationalOrganizationsView.GetInternationalOrganization.MakeScope.GetVariable('tv_academy_philosophy_inspiration').GetValue]"
\t\t\t\t\t\t\t\ttooltip = "TV_ACADEMY_PHILOSOPHY_CONCEPT_PROGRESS_TT"
\t\t\t\t\t\t\t}
\t\t\t\t\t\t}
\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\traw_text = "[InternationalOrganizationsView.GetInternationalOrganization.GetVariableText('tv_academy_philosophy_inspiration')]"
\t\t\t\t\t\t\tsize = { 62 20 }
\t\t\t\t\t\t\tmax_width = 62
\t\t\t\t\t\t\tfontsize = 12
\t\t\t\t\t\t\ttooltip = "TV_ACADEMY_PHILOSOPHY_CONCEPT_PROGRESS_TT"
\t\t\t\t\t\t\talign = nobaseline|right
\t\t\t\t\t\t}
\t\t\t\t\t}
\t\t\t\t}
\t\t\t\texpand = {}
\t\t\t}
\t\t}
\t}
\tblockoverride "country_header_names_visible" {
\t\tvisible = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet]"
\t}
\tblockoverride "country_header_character_name" {
\t\tdatacontext = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').GetCharacter]"
\t\tmargin = { 2 0 }
\t\tcharacter_label_name = {
\t\t\tmargin_left = 0
\t\t\tmargin_right = 10
\t\t\tblockoverride "character_name_text" {
\t\t\t\traw_text = "[Character.GetNameToFit('(int32)40', '(bool)yes')]"
\t\t\t}
\t\t}
\t}
\tblockoverride "character_widget" {
\t\twidget = {
\t\t\tposition = { 0 -30 }
\t\t\tallow_outside = yes
\t\t\tblock "portrait_io_widget" {
\t\t\t\tsize = { 250 200 }
\t\t\t}

\t\t\tblock "character_portrait_anchor" {
\t\t\t\tparentanchor = bottom
\t\t\t}

\t\t\tportrait_standard_full_body_center_button = {
\t\t\t\tvisible = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet]"
\t\t\t\tblock "character_right_visible" {
\t\t\t\t\tvisible = yes
\t\t\t\t}
\t\t\t\tparentanchor = bottom

\t\t\t\tsize = { 250 200 }
\t\t\t\tdatacontext = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').GetCharacter]"
\t\t\t}

\t\t\taction_button = {
\t\t\t\tvisible = "[Not(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet)]"
\t\t\t\tsize = { 250 200 }
\t\t\t\tparentanchor = center
\t\t\t\twidgetanchor = center
\t\t\t\tposition = { 0 0 }
\t\t\t\talpha = 0
\t\t\t\talwaystransparent = no
\t\t\t\tusing = action_button_common_template
\t\t\t\ttitle = "tv_appoint_academy_leader"
\t\t\t\tdescription = "tv_appoint_academy_leader_desc"
\t\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\t\tleft_action = { action_name = "tv_appoint_academy_leader" }
\t\t\t}

\t\t\taction_button = {
\t\t\t\tvisible = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet]"
\t\t\t\tsize = { 78 26 }
\t\t\t\tparentanchor = top|right
\t\t\t\twidgetanchor = top|right
\t\t\t\tposition = { 0 20 }
\t\t\t\tusing = button_regular_texture_alt_yellow
\t\t\t\tusing = action_button_common_template
\t\t\t\tusing = button_common_textobj_template
\t\t\t\tfontsize = 13
\t\t\t\ttitle = "tv_change_academy_leader"
\t\t\t\tdescription = "tv_change_academy_leader_desc"
\t\t\t\ttext = "TV_ACADEMY_CHANGE_LEADER_SHORT"
\t\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\t\tleft_action = { action_name = "tv_change_academy_leader" }
\t\t\t}
\t\t\taction_button = {
\t\t\t\tvisible = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_academy_leader_char').IsSet]"
\t\t\t\tsize = { 78 26 }
\t\t\t\tparentanchor = top|right
\t\t\t\twidgetanchor = top|right
\t\t\t\tposition = { 0 49 }
\t\t\t\tusing = button_regular_texture_alt_red
\t\t\t\tusing = action_button_common_template
\t\t\t\tusing = button_common_textobj_template
\t\t\t\tfontsize = 13
\t\t\t\ttitle = "tv_remove_academy_leader"
\t\t\t\tdescription = "tv_remove_academy_leader_desc"
\t\t\t\ttext = "TV_ACADEMY_REMOVE_LEADER_SHORT"
\t\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\t\tleft_action = { action_name = "tv_remove_academy_leader" }
\t\t\t}
\t\t}
\t}

\tblockoverride "io_main_actions_icon" {
\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t}

\tblockoverride "organization_main_actions_extra_visible" {
\t\tvisible = yes
\t}

\tblockoverride "organization_overview_list_custom_top_extra" {
\t\tvbox = {
\t\t\tmargin = { 10 0 }
\t\t\tlayoutpolicy_horizontal = expanding
\t\t\tlayoutpolicy_vertical = expanding
\t\t\tignoreinvisible = yes
\t\t\tdatacontext = "[InternationalOrganizationsView.GetPlayer]"
\t\t\tspacing = 8

\t\t\tcard_common = {
\t\t\t\tmaximumsize = { 500 -1 }
\t\t\t\tvisible = "[Not(Country.MakeScope.GetVariable('tv_research_target').IsSet)]"

\t\t\t\tblockoverride "common_header_icon_texture" {
\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t}

\t\t\t\tblockoverride "common_header_text" {
\t\t\t\t\ttext = "TV_RM_STATUS_CARD_TITLE"
\t\t\t\t}

\t\t\t\tblockoverride "common_bottom_content" {
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tautoresize = yes
\t\t\t\t\t\tmax_width = 440
\t\t\t\t\t\ttext = "TV_RM_NO_TARGET_HINT"
\t\t\t\t\t\talign = center
\t\t\t\t\t\tmargin = { 4 8 }
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}

\t\t\tcard_common = {
\t\t\t\tmaximumsize = { 500 -1 }
\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_research_target').IsSet]"

\t\t\t\tblockoverride "common_header_icon_texture" {
\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t}

\t\t\t\tblockoverride "common_header_text" {
\t\t\t\t\ttext = "TV_RM_TARGET_CARD_TITLE"
\t\t\t\t}

\t\t\t\tblockoverride "common_bottom_content" {
\t\t\t\t\thbox = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tmargin = { 4 8 }
\t\t\t\t\t\texpand = {}
"""

# Generated block inserted here (one text_single entry per advance)

GUI_SUFFIX = """\
\t\t\t\t\t\texpand = {}
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}

\t\t\tcard_common = {
\t\t\t\tmaximumsize = { 500 -1 }
\t\t\t\tvisible = "[And(Country.MakeScope.GetVariable('tv_research_target').IsSet, Not(EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_d_ready').GetValue, '(CFixedPoint)1.0')))]"

\t\t\t\tblockoverride "common_header_icon_texture" {
\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t}

\t\t\t\tblockoverride "common_header_text" {
\t\t\t\t\ttext = "TV_RM_PREPARATION_CARD_TITLE"
\t\t\t\t}

\t\t\t\tblockoverride "common_bottom_content" {

\t\t# ── Research Status Overview ────────────────────────────────────────────────────────────────────
\t\tvbox = {
\t\t\tlayoutpolicy_horizontal = expanding
\t\t\tlayoutpolicy_vertical = fixed
\t\t\tignoreinvisible = yes
\t\t\tspacing = 4
\t\t\tmargin = { 4 6 }

\t\t\t\t# ── Subprocess A: Interest Collection ─────────────────────────────────────────────────────
\t\t\t\thbox = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tspacing = 4
\t\t\t\t\ttext_single = {
\t\t\t\t\t\ttext = "TV_ACADEMY_SUBPROCESS_A_LABEL"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\texpand = {}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\traw_text = "[Country.MakeScope.GetVariable('tv_research_interest').GetValue|0]/5"
\t\t\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_research_interest').IsSet]"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_a').GetValue, '(CFixedPoint)0.0')]"
\t\t\t\t\t\ttext = "TV_RM_STATUS_NOT_STARTED"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_a').GetValue, '(CFixedPoint)1.0')]"
\t\t\t\t\t\ttext = "TV_RM_STATUS_IN_PROGRESS"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_a').GetValue, '(CFixedPoint)2.0')]"
\t\t\t\t\t\ttext = "TV_RM_STATUS_COMPLETE"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t}
\t\t\t\tprogressbar = {
\t\t\t\t\tsize = { 440 16 }
\t\t\t\t\tusing = progress_bar_blue_alt
\t\t\t\t\tmin = 0
\t\t\t\t\tmax = 5
\t\t\t\t\tvalue = "[Country.MakeScope.GetVariable('tv_research_interest').GetValue]"
\t\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_rm_subprocess_a').IsSet]"
\t\t\t\t}

\t\t\t\t# ── Subprocess B: Technology Prerequisites ────────────────────────────────────────────────────
\t\t\t\thbox = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tspacing = 4
\t\t\t\t\ttext_single = {
\t\t\t\t\t\ttext = "TV_ACADEMY_SUBPROCESS_B_LABEL"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\texpand = {}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[Not(EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_b').GetValue, '(CFixedPoint)2.0'))]"
\t\t\t\t\t\ttext = "TV_RM_STATUS_NOT_STARTED"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_b').GetValue, '(CFixedPoint)2.0')]"
\t\t\t\t\t\ttext = "TV_RM_STATUS_COMPLETE"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t}
\t\t\t\tprogressbar = {
\t\t\t\t\tsize = { 440 16 }
\t\t\t\t\tusing = progress_bar_blue_alt
\t\t\t\t\tmin = 0
\t\t\t\t\tmax = 2
\t\t\t\t\tvalue = "[Country.MakeScope.GetVariable('tv_rm_subprocess_b').GetValue]"
\t\t\t\t}

\t\t\t\ttext_single = {
\t\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_research_target').IsSet]"
\t\t\t\t\ttext = "TV_RM_SUBPROCESS_B_REQUIREMENTS_LABEL"
\t\t\t\t\talign = nobaseline
\t\t\t\t}
\t\t\t\tRequirementsList = {
\t\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_research_target').IsSet]"
\t\t\t\t\ttextcontext = "[ShowTriggerConditions('tv_research_check_b_trigger', PlayerScope.Self)]"
\t\t\t\t\tblockoverride "block_title" {
\t\t\t\t\t\tblock "block_title" {
\t\t\t\t\t\t\tvisible = no
\t\t\t\t\t\t}
\t\t\t\t\t}
\t\t\t\t\tblockoverride "requirementslist_datamodel_is_empty" {
\t\t\t\t\t\tvisible = no
\t\t\t\t\t}
\t\t\t\t}

\t\t\t\t# ── Subprocess C: Literacy Foundation ─────────────────────────────────────────────────────
\t\t\t\thbox = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tspacing = 4
\t\t\t\t\ttext_single = {
\t\t\t\t\t\ttext = "TV_ACADEMY_SUBPROCESS_C_LABEL"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\texpand = {}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_c').GetValue, '(CFixedPoint)0.0')]"
\t\t\t\t\t\ttext = "TV_RM_STATUS_NOT_STARTED"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_c').GetValue, '(CFixedPoint)1.0')]"
\t\t\t\t\t\traw_text = "[InternationalOrganizationsView.GetInternationalOrganization.GetVariableText('tv_research_c_progress')]"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_c').GetValue, '(CFixedPoint)2.0')]"
\t\t\t\t\t\ttext = "TV_RM_STATUS_COMPLETE"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t}
\t\t\t\tprogressbar = {
\t\t\t\t\tsize = { 440 16 }
\t\t\t\t\tusing = progress_bar_blue_alt
\t\t\t\t\tmin = 0
\t\t\t\t\tmax = 100
\t\t\t\t\tvalue = "[InternationalOrganizationsView.GetInternationalOrganization.MakeScope.GetVariable('tv_research_c_progress').GetValue]"
\t\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_rm_subprocess_c').IsSet]"
\t\t\t\t}
\t\t}
\t\t\t\t}
\t\t\t}

\t\t\tcard_common = {
\t\t\t\tmaximumsize = { 500 -1 }
\t\t\t\tvisible = "[And(Country.MakeScope.GetVariable('tv_research_target').IsSet, EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_d_ready').GetValue, '(CFixedPoint)1.0'))]"

\t\t\t\tblockoverride "common_header_icon_texture" {
\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t}

\t\t\t\tblockoverride "common_header_text" {
\t\t\t\t\ttext = "TV_RM_PREPARATION_CARD_TITLE"
\t\t\t\t}

\t\t\t\tblockoverride "common_bottom_content" {
\t\t\t\t\ttext_multi = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tautoresize = yes
\t\t\t\t\t\tmax_width = 440
\t\t\t\t\t\ttext = "TV_RM_PREPARATION_COMPLETE_INFO"
\t\t\t\t\t\talign = center
\t\t\t\t\t\tmargin = { 4 8 }
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}

\t\t\tcard_common = {
\t\t\t\tmaximumsize = { 500 -1 }
\t\t\t\tvisible = "[And(Country.MakeScope.GetVariable('tv_research_target').IsSet, EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_rm_subprocess_d_ready').GetValue, '(CFixedPoint)1.0'))]"

\t\t\t\tblockoverride "common_header_icon_texture" {
\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t}

\t\t\t\tblockoverride "common_header_text" {
\t\t\t\t\ttext = "TV_RM_CONCENTRATED_RESEARCH_CARD_TITLE"
\t\t\t\t}

\t\t\t\tblockoverride "common_bottom_content" {
\t\t\t\t\tvbox = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tlayoutpolicy_vertical = fixed
\t\t\t\t\t\tspacing = 4
\t\t\t\t\t\tmargin = { 4 6 }

\t\t\t\t\t\thbox = {
\t\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\t\tspacing = 4
\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\ttext = "TV_ACADEMY_SUBPROCESS_D_LABEL"
\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t}
\t\t\t\t\t\t\texpand = {}
\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\tvisible = "[Not(EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0'))]"
\t\t\t\t\t\t\t\ttext = "TV_RM_STATUS_READY"
\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t}
\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\tvisible = "[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_research_phase').GetValue, '(CFixedPoint)2.0')]"
\t\t\t\t\t\t\t\traw_text = "[InternationalOrganizationsView.GetInternationalOrganization.GetVariableText('tv_research_d_progress')]"
\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t}
\t\t\t\t\t\t}
\t\t\t\t\t\tprogressbar = {
\t\t\t\t\t\t\tsize = { 440 16 }
\t\t\t\t\t\t\tusing = progress_bar_blue_alt
\t\t\t\t\t\t\tmin = 0
\t\t\t\t\t\t\tmax = 100
\t\t\t\t\t\t\tvalue = "[InternationalOrganizationsView.GetInternationalOrganization.MakeScope.GetVariable('tv_research_d_progress').GetValue]"
\t\t\t\t\t\t}
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}
\t\t}
\t}

\tblockoverride "organization_main_actions_extra" {

\t\t# ── Action Buttons ────────────────────────────────────────────────────────────────────────────────────

\t\t# Request Research Target
\t\taction_button_diamond = {
\t\t\tsize = { 470 30 }
\t\t\ttext = "TV_RM_REQUEST_TARGET_BTN"
\t\t\ttitle = "TV_RM_REQUEST_TARGET_BTN"
\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\tleft_action = {
\t\t\t\taction_name = "tv_request_research_target"
\t\t\t}
\t\t}

\t\t# Abandon Target (visible when target is set)
\t\taction_button_diamond = {
\t\t\tsize = { 470 30 }
\t\t\ttext = "TV_RM_ABANDON_TARGET_BTN"
\t\t\ttitle = "TV_RM_ABANDON_TARGET_BTN"
\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\tleft_action = {
\t\t\t\taction_name = "tv_abandon_research_target"
\t\t\t}
\t\t}

\t\t# Begin Concentrated Research (visible when A+B+C complete and D not started)
\t\taction_button_diamond = {
\t\t\tsize = { 470 30 }
\t\t\ttext = "TV_RM_START_D_BTN"
\t\t\ttitle = "TV_RM_START_D_BTN"
\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\tleft_action = {
\t\t\t\taction_name = "tv_start_concentrated_research"
\t\t\t}
\t\t}

\t\t# Pause Concentrated Research (visible when D is in progress)
\t\taction_button_diamond = {
\t\t\tsize = { 470 30 }
\t\t\ttext = "TV_RM_PAUSE_D_BTN"
\t\t\ttitle = "TV_RM_PAUSE_D_BTN"
\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\tleft_action = {
\t\t\t\taction_name = "tv_pause_concentrated_research"
\t\t\t}
\t\t}

\t}

\t# -- Philosophy tab: reuse the vanilla resolutions slot
\tblockoverride "organization_resolutions_content" {}

\t# -- Medicine tab: enable the custom tab slot -------------------------------
\tblockoverride "organization_custom_tab_visible" {
\t\tvisible = yes
\t}
\tblockoverride "organization_custom_tab_text" {
\t\ttext = "TV_MED_TAB"
\t}
\tblockoverride "organization_custom_tab_tooltip" {
\t\ttooltip = "TV_MED_TAB_TOOLTIP"
\t}

\tblockoverride "organization_custom_content_visible" {
\t\tvisible = "[InternationalOrganizationsView.Vars.HasValue( 'organizations', 'custom' )]"
\t}

\tblockoverride "organization_custom_content" {
\t\tvbox = {
\t\t\tmargin = { 10 0 }
\t\t\tlayoutpolicy_horizontal = expanding
\t\t\tspacing = 10

\t\t\tvbox = {
\t\t\t\tvisible = "[InternationalOrganizationsView.Vars.HasValue( 'organizations', 'custom' )]"
\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\tspacing = 10

\t\t\tcard_common = {
\t\t\t\tmaximumsize = { 500 -1 }

\t\t\t\tblockoverride "common_header_icon_texture" {
\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t}

\t\t\t\tblockoverride "common_header_text" {
\t\t\t\t\ttext = "TV_MED_TREATMENT_CARD_TITLE"
\t\t\t\t}

\t\t\t\tblockoverride "common_bottom_content" {
\t\t\t\t\tvbox = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tspacing = 6

\t\t# ── Patient character card (visible when treatment is active) ──────────
\t\tio_character_card = {
\t\t\tvisible = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').IsSet]"
\t\t\tdatacontext = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_med_target_char').GetCharacter]"
\t\t\tusing = layoutpolicy_expanding
\t\t\tblockoverride "name_highlight" {}
\t\t\tblockoverride "character_entry_name_sort_by_highlight" {}
\t\t}

\t\t# ── Data block: patient + rates display ───────────────────────────────
\t\twidget = {
\t\t\tsize = { 470 140 }
\t\t\tdatacontext = "[InternationalOrganizationsView.GetPlayer]"

\t\t\tvbox = {
\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\tspacing = 4
\t\t\t\tmargin = { 4 6 }

\t\t\t\t# ── Current patient ─────────────────────────────────────────────────
\t\t\t\thbox = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tspacing = 4
\t\t\t\t\ttext_single = {
\t\t\t\t\t\ttext = "TV_MED_PATIENT_LABEL"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\texpand = {}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[Not(Country.MakeScope.GetVariable('tv_med_target_char').IsSet)]"
\t\t\t\t\t\ttext = "TV_MED_NO_PATIENT"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_med_target_char').IsSet]"
\t\t\t\t\t\tdatacontext = "[Country.MakeScope.GetVariable('tv_med_target_char').GetCharacter]"
\t\t\t\t\t\ttext = "[Character.GetShortNameWithNoTooltip]"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t}

\t\t\t\t# ── Progress bar ────────────────────────────────────────────────────
\t\t\t\tprogressbar = {
\t\t\t\t\tsize = { 440 16 }
\t\t\t\t\tusing = progress_bar_green_alt
\t\t\t\t\tmin = 0
\t\t\t\t\tmax = 100
\t\t\t\t\tvalue = "[Country.MakeScope.GetVariable('tv_med_progress').GetValue]"
\t\t\t\t\tvisible = "[Country.MakeScope.GetVariable('tv_med_target_char').IsSet]"
\t\t\t\t}

\t\t\t\t# ── Monthly progress gain (adm x 0.1%) ──────────────────────────────
\t\t\t\thbox = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tspacing = 4
\t\t\t\t\ttext_single = {
\t\t\t\t\t\ttext = "TV_MED_MONTHLY_GAIN_LABEL"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\texpand = {}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\traw_text = "[Country.MakeScope.GetVariable('tv_med_disp_adm').GetValue|0]x0.1%"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t}

\t\t\t\t# ── Discovery event chance (dip x 0.2%) ─────────────────────────────
\t\t\t\thbox = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tspacing = 4
\t\t\t\t\ttext_single = {
\t\t\t\t\t\ttext = "TV_MED_DISCOVERY_CHANCE_LABEL"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\texpand = {}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\traw_text = "[Country.MakeScope.GetVariable('tv_med_disp_dip').GetValue|0]x0.2%"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t}

\t\t\t\t# ── Accident chance (5% - mil x 0.05%) ──────────────────────────────
\t\t\t\thbox = {
\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\tspacing = 4
\t\t\t\t\ttext_single = {
\t\t\t\t\t\ttext = "TV_MED_ACCIDENT_CHANCE_LABEL"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t\texpand = {}
\t\t\t\t\ttext_single = {
\t\t\t\t\t\traw_text = "5%-[Country.MakeScope.GetVariable('tv_med_disp_mil').GetValue|0]x0.05%"
\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}
\t\t}

\t\t\t\t\t}
\t\t\t\t}
\t\t\t}

\t\t\tcard_common = {
\t\t\t\tmaximumsize = { 500 -1 }

\t\t\t\tblockoverride "common_header_icon_texture" {
\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t}

\t\t\t\tblockoverride "common_header_text" {
\t\t\t\t\ttext = "TV_MED_MEDITATION_CARD_TITLE"
\t\t\t\t}

\t\t\t\tblockoverride "common_bottom_content" {
\t\t\t\t\tvbox = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tspacing = 6

\t\t\t\t\t\ttext_multi = {
\t\t\t\t\t\t\tvisible = "[Not(InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet)]"
\t\t\t\t\t\t\tsize = { 470 -1 }
\t\t\t\t\t\t\tautoresize = yes
\t\t\t\t\t\t\tmax_width = 460
\t\t\t\t\t\t\ttext = "TV_MED_MEDITATION_NO_TARGET_DESC"
\t\t\t\t\t\t}

\t\t\t\t\t\tio_character_card = {
\t\t\t\t\t\t\tvisible = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet]"
\t\t\t\t\t\t\tdatacontext = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').GetCharacter]"
\t\t\t\t\t\t\tusing = layoutpolicy_expanding
\t\t\t\t\t\t\tblockoverride "name_highlight" {}
\t\t\t\t\t\t\tblockoverride "character_entry_name_sort_by_highlight" {}
\t\t\t\t\t\t}

\t\t\t\t\t\twidget = {
\t\t\t\t\t\t\tvisible = "[InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('tv_meditation_target_char').IsSet]"
\t\t\t\t\t\t\tsize = { 470 88 }
\t\t\t\t\t\t\tdatacontext = "[InternationalOrganizationsView.GetPlayer]"

\t\t\t\t\t\t\tvbox = {
\t\t\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\t\t\tspacing = 4
\t\t\t\t\t\t\t\tmargin = { 4 6 }

\t\t\t\t\t\t\t\tprogressbar = {
\t\t\t\t\t\t\t\t\tsize = { 440 16 }
\t\t\t\t\t\t\t\t\tusing = progress_bar_green_alt
\t\t\t\t\t\t\t\t\tmin = 0
\t\t\t\t\t\t\t\t\tmax = 100
\t\t\t\t\t\t\t\t\tvalue = "[Country.MakeScope.GetVariable('tv_meditation_progress').GetValue]"
\t\t\t\t\t\t\t\t}

\t\t\t\t\t\t\t\thbox = {
\t\t\t\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\t\t\t\tspacing = 4
\t\t\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\t\t\ttext = "TV_MED_MEDITATION_PROGRESS_LABEL"
\t\t\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t\t\t}
\t\t\t\t\t\t\t\t\texpand = {}
\t\t\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\t\t\traw_text = "[Country.MakeScope.GetVariable('tv_meditation_progress').GetValue|0]%"
\t\t\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t\t\t}
\t\t\t\t\t\t\t\t}

\t\t\t\t\t\t\t\thbox = {
\t\t\t\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\t\t\t\tspacing = 4
\t\t\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\t\t\ttext = "TV_MED_MONTHLY_GAIN_LABEL"
\t\t\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t\t\t}
\t\t\t\t\t\t\t\t\texpand = {}
\t\t\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\t\t\traw_text = "[Country.MakeScope.GetVariable('tv_med_disp_adm').GetValue|0]x0.1%"
\t\t\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t\t\t}
\t\t\t\t\t\t\t\t}

\t\t\t\t\t\t\t\thbox = {
\t\t\t\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\t\t\t\tspacing = 4
\t\t\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\t\t\ttext = "TV_MED_MEDITATION_EVENT_CHANCE_LABEL"
\t\t\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t\t\t}
\t\t\t\t\t\t\t\t\texpand = {}
\t\t\t\t\t\t\t\t\ttext_single = {
\t\t\t\t\t\t\t\t\t\traw_text = "10%"
\t\t\t\t\t\t\t\t\t\talign = nobaseline
\t\t\t\t\t\t\t\t\t}
\t\t\t\t\t\t\t\t}
\t\t\t\t\t\t\t}
\t\t\t\t\t\t}
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}

\t\t\tcard_actions = {
\t\t\t\tblockoverride "card_header_text" {
\t\t\t\t\ttext = "MAIN_ACTIONS"
\t\t\t\t}
\t\t\t\tblockoverride "card_header_icon" {
\t\t\t\t\tblock "io_main_actions_icon" {
\t\t\t\t\t\ttexture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"
\t\t\t\t\t}
\t\t\t\t}

\t\t\t\tblockoverride "actions_override" {
\t\t\t\t\tvbox = {
\t\t\t\t\t\tlayoutpolicy_horizontal = expanding
\t\t\t\t\t\tspacing = 2

\t\t\t\t\t\t# Treat Character button
\t\t\t\t\t\taction_button_diamond = {
\t\t\t\t\t\t\tsize = { 470 30 }
\t\t\t\t\t\t\ttext = "tv_treat_character"
\t\t\t\t\t\t\ttitle = "tv_treat_character"
\t\t\t\t\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\t\t\t\t\tleft_action = {
\t\t\t\t\t\t\t\taction_name = "tv_treat_character"
\t\t\t\t\t\t\t}
\t\t\t\t\t\t}

\t\t\t\t\t\t# Start Meditation button
\t\t\t\t\t\taction_button_diamond = {
\t\t\t\t\t\t\tsize = { 470 30 }
\t\t\t\t\t\t\ttext = "tv_start_meditation"
\t\t\t\t\t\t\ttitle = "tv_start_meditation"
\t\t\t\t\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\t\t\t\t\tleft_action = {
\t\t\t\t\t\t\t\taction_name = "tv_start_meditation"
\t\t\t\t\t\t\t}
\t\t\t\t\t\t}

\t\t\t\t\t\t# Stop Meditation button
\t\t\t\t\t\taction_button_diamond = {
\t\t\t\t\t\t\tsize = { 470 30 }
\t\t\t\t\t\t\ttext = "tv_stop_meditation"
\t\t\t\t\t\t\ttitle = "tv_stop_meditation"
\t\t\t\t\t\t\tactor = "[InternationalOrganizationsView.GetPlayer]"
\t\t\t\t\t\t\tleft_action = {
\t\t\t\t\t\t\t\taction_name = "tv_stop_meditation"
\t\t\t\t\t\t\t}
\t\t\t\t\t\t}
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}
\t\t\t}
\t\t}
\t}
}
"""


def gen_target_entry(advance: dict) -> str:
    gui_value = advance["gui_value"]
    loc_key = f"TV_RM_TARGET_{advance['id'].upper()}"
    tt_key = f"{loc_key}_TT"
    float_str = f"{gui_value}.0"
    return (
        T*6 + "text_single = {\n"
        + T*7 + "size = { 440 26 }\n"
        + T*7 + f"visible = \"[EqualTo_CFixedPoint(Country.MakeScope.GetVariable('tv_research_target').GetValue, '(CFixedPoint){float_str}')]\"\n"
        + T*7 + f"text = \"{loc_key}\"\n"
        + T*7 + "default_format = \"#explanation_link\"\n"
        + T*7 + f"tooltip = \"{tt_key}\"\n"
        + T*7 + "align = center|nobaseline\n"
        + T*6 + "}"
    )


def player_var(name: str) -> str:
    return f"InternationalOrganizationsView.GetPlayer.MakeScope.GetVariable('{name}')"


def io_var(name: str) -> str:
    return f"InternationalOrganizationsView.GetInternationalOrganization.MakeScope.GetVariable('{name}')"


def fixed_eq(var_expr: str, value: int) -> str:
    return f"EqualTo_CFixedPoint({var_expr}.GetValue, '(CFixedPoint){value}.0')"


def player_var_eq(name: str, value: int) -> str:
    return fixed_eq(player_var(name), value)


def emit(lines: list[str], level: int, text: str = "") -> None:
    lines.append((T * level + text) if text else "")


PHILOSOPHY_ISSUES = [
    (1, "TV_ACADEMY_PHILOSOPHY_NAME_MERITOCRACY"),
    (2, "TV_ACADEMY_PHILOSOPHY_NAME_RENAISSANCE"),
    (3, "TV_ACADEMY_PHILOSOPHY_NAME_BANKING"),
    (4, "TV_ACADEMY_PHILOSOPHY_NAME_NEW_WORLD"),
    (5, "TV_ACADEMY_PHILOSOPHY_NAME_PRINTING_PRESS"),
    (6, "TV_ACADEMY_PHILOSOPHY_NAME_CONFESSIONALISM"),
    (7, "TV_ACADEMY_PHILOSOPHY_NAME_GLOBAL_TRADE"),
    (8, "TV_ACADEMY_PHILOSOPHY_NAME_MANUFACTORIES"),
    (9, "TV_ACADEMY_PHILOSOPHY_NAME_SCIENTIFIC_REVOLUTION"),
]

NODE_TYPE_LABELS = [
    (0, "TV_ACADEMY_DEBATE_NODE_TYPE_HISTORY"),
    (1, "TV_ACADEMY_DEBATE_NODE_TYPE_LOCAL"),
    (2, "TV_ACADEMY_DEBATE_NODE_TYPE_WORLD"),
]

NODE_TYPE_ICONS = [
    (0, "@time!", "TV_ACADEMY_DEBATE_NODE_HISTORY_TT"),
    (1, "@location!", "TV_ACADEMY_DEBATE_NODE_LOCAL_TT"),
    (2, "@diplomacy!", "TV_ACADEMY_DEBATE_NODE_WORLD_TT"),
]

CURRENT_NODE_LINE_KEYS = [
    (0, "TV_ACADEMY_DEBATE_CURRENT_NODE_LINE_HISTORY"),
    (1, "TV_ACADEMY_DEBATE_CURRENT_NODE_LINE_LOCAL"),
    (2, "TV_ACADEMY_DEBATE_CURRENT_NODE_LINE_WORLD"),
]

DEBATE_GROUPS: list[dict] = []


def current_node_type_eq(value: int) -> str:
    return player_var_eq("tv_academy_debate_current_node_type", value)


def debate_active_expr() -> str:
    return player_var_eq("tv_academy_philosophy_phase", 1)


def append_node_type_text(lines: list[str], level: int, var_name: str, font_size: int = 15) -> None:
    for value, loc_key in NODE_TYPE_LABELS:
        emit(lines, level, "text_single = {")
        emit(lines, level + 1, f'visible = "[{player_var_eq(var_name, value)}]"')
        emit(lines, level + 1, f'text = "{loc_key}"')
        emit(lines, level + 1, f"fontsize = {font_size}")
        emit(lines, level + 1, "align = center|nobaseline")
        emit(lines, level, "}")


def append_current_philosophy_name(lines: list[str], level: int, width: int, font_size: int = 16) -> None:
    emit(lines, level, "text_multi = {")
    emit(lines, level + 1, f'visible = "[Not({debate_active_expr()})]"')
    emit(lines, level + 1, f"size = {{ {width} 48 }}")
    emit(lines, level + 1, f"max_width = {width}")
    emit(lines, level + 1, 'text = "TV_ACADEMY_PHILOSOPHY_RECESS"')
    emit(lines, level + 1, f"fontsize = {font_size}")
    emit(lines, level + 1, "align = center|nobaseline")
    emit(lines, level, "}")
    emit(lines, level, "text_multi = {")
    emit(lines, level + 1, f'visible = "[And({debate_active_expr()}, Not({player_var("tv_academy_philosophy_current")}.IsSet))]"')
    emit(lines, level + 1, f"size = {{ {width} 48 }}")
    emit(lines, level + 1, f"max_width = {width}")
    emit(lines, level + 1, 'text = "TV_ACADEMY_PHILOSOPHY_RECESS"')
    emit(lines, level + 1, f"fontsize = {font_size}")
    emit(lines, level + 1, "align = center|nobaseline")
    emit(lines, level, "}")
    for value, loc_key in PHILOSOPHY_ISSUES:
        emit(lines, level, "text_multi = {")
        emit(lines, level + 1, f'visible = "[And({debate_active_expr()}, {player_var_eq("tv_academy_philosophy_current", value)})]"')
        emit(lines, level + 1, f"size = {{ {width} 56 }}")
        emit(lines, level + 1, f"max_width = {width}")
        emit(lines, level + 1, f'text = "{loc_key}"')
        emit(lines, level + 1, f"fontsize = {font_size}")
        emit(lines, level + 1, "align = center|nobaseline")
        emit(lines, level, "}")


def append_debate_node_icon(lines: list[str], level: int, slot: int) -> None:
    node_var = f"tv_academy_debate_node_{slot}_type"
    emit(lines, level, "widget = {")
    emit(lines, level + 1, "size = { 42 42 }")
    emit(lines, level + 1, "using = bg_circle_piechart")
    for value, icon, tooltip in NODE_TYPE_ICONS:
        emit(lines, level + 1, "text_single = {")
        emit(lines, level + 2, f'visible = "[{player_var_eq(node_var, value)}]"')
        emit(lines, level + 2, "parentanchor = center")
        emit(lines, level + 2, "size = { 100% 100% }")
        emit(lines, level + 2, f'raw_text = "{icon}"')
        emit(lines, level + 2, "fontsize = 21")
        emit(lines, level + 2, "autoresize = no")
        emit(lines, level + 2, f'tooltip = "{tooltip}"')
        emit(lines, level + 2, "align = center|nobaseline")
        emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_node_year(lines: list[str], level: int, slot: int) -> None:
    emit(lines, level, "text_single = {")
    emit(lines, level + 1, "size = { 42 22 }")
    emit(lines, level + 1, f'raw_text = "[{player_var(f"tv_academy_debate_node_{slot}_year")}.GetValue|0]"')
    emit(lines, level + 1, "fontsize = 13")
    emit(lines, level + 1, "autoresize = no")
    emit(lines, level + 1, "align = center|nobaseline")
    emit(lines, level, "}")


def append_debate_progress_footer(
    lines: list[str],
    level: int,
    tooltip: str,
    value_expr: str,
    label_expr: str,
    using: str = "progress_bar_blue_alt",
) -> None:
    emit(lines, level, "hbox = {")
    emit(lines, level + 1, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 1, "size = { 462 24 }")
    emit(lines, level + 1, "spacing = 8")
    emit(lines, level + 1, "widget = {")
    emit(lines, level + 2, "size = { 380 22 }")
    emit(lines, level + 2, "progressbar = {")
    emit(lines, level + 3, "size = { 380 16 }")
    emit(lines, level + 3, "parentanchor = vcenter")
    emit(lines, level + 3, "widgetanchor = vcenter")
    emit(lines, level + 3, f"using = {using}")
    emit(lines, level + 3, "min = 0")
    emit(lines, level + 3, "max = 100")
    emit(lines, level + 3, f'value = "{value_expr}"')
    emit(lines, level + 3, f'tooltip = "{tooltip}"')
    emit(lines, level + 2, "}")
    emit(lines, level + 1, "}")
    emit(lines, level + 1, "text_single = {")
    emit(lines, level + 2, "size = { 74 22 }")
    emit(lines, level + 2, f'raw_text = "{label_expr}"')
    emit(lines, level + 2, "fontsize = 13")
    emit(lines, level + 2, f'tooltip = "{tooltip}"')
    emit(lines, level + 2, "align = right|nobaseline")
    emit(lines, level + 1, "}")
    emit(lines, level, "}")


def debate_seat_var(seat: int, suffix: str) -> str:
    return f"tv_academy_debate_seat_{seat}_{suffix}"


def world_debate_seat_var(seat: int, suffix: str) -> str:
    return f"tv_academy_world_debate_seat_{seat}_{suffix}"


def append_stance_circle(lines: list[str], level: int, visible_expr: str, texture: str, tooltip_key: str) -> None:
    emit(lines, level, "icon = {")
    emit(lines, level + 1, f'visible = "{visible_expr}"')
    emit(lines, level + 1, "parentanchor = center")
    emit(lines, level + 1, "size = { 100% 100% }")
    emit(lines, level + 1, 'texture = "gfx/interface/component_tiles/hud_corners/circle_progress_bg.dds"')
    emit(lines, level + 1, "modify_texture = {")
    emit(lines, level + 2, f"using = {texture}")
    emit(lines, level + 1, "}")
    emit(lines, level + 1, "alpha = 0.88")
    emit(lines, level + 1, f'tooltip = "{tooltip_key}"')
    emit(lines, level, "}")


def append_local_debate_seat(lines: list[str], level: int, x: int, y: int, seat: int | None, crown: bool) -> None:
    emit(lines, level, "widget = {")
    emit(lines, level + 1, f"position = {{ {x} {y} }}")
    emit(lines, level + 1, f"size = {{ {LOCAL_DEBATE_SEAT_SIZE} {LOCAL_DEBATE_SEAT_SIZE} }}")
    emit(lines, level + 1, "using = bg_circle_piechart_big")
    emit(lines, level + 1, 'tooltip = "TV_ACADEMY_DEBATE_LOCAL_EMPTY_SEAT_TT"')
    if crown:
        append_stance_circle(
            lines,
            level + 1,
            f"[{debate_active_expr()}]",
            STANCE_COLOR_TEXTURES[1],
            STANCE_TOOLTIP_KEYS[1],
        )
        emit(lines, level + 1, "text_single = {")
        emit(lines, level + 2, f'visible = "[{debate_active_expr()}]"')
        emit(lines, level + 2, "parentanchor = center")
        emit(lines, level + 2, "size = { 100% 100% }")
        emit(lines, level + 2, 'raw_text = "@crown_estate!"')
        emit(lines, level + 2, "fontsize = 32")
        emit(lines, level + 2, 'tooltip = "TV_ACADEMY_DEBATE_LOCAL_CROWN_SEAT_TT"')
        emit(lines, level + 2, "align = center|nobaseline")
        emit(lines, level + 1, "}")
    else:
        assert seat is not None
        group_var = player_var(debate_seat_var(seat, "group"))
        stance_var = player_var(debate_seat_var(seat, "stance"))
        seat_set = f"{group_var}.IsSet"
        for stance, texture in STANCE_COLOR_TEXTURES.items():
            append_stance_circle(
                lines,
                level + 1,
                f"[And({seat_set}, {fixed_eq(stance_var, stance)})]",
                texture,
                STANCE_TOOLTIP_KEYS[stance],
            )
        for group in DEBATE_GROUPS:
            emit(lines, level + 1, "text_single = {")
            emit(lines, level + 2, f'visible = "[{fixed_eq(group_var, int(group["id"]))}]"')
            emit(lines, level + 2, "parentanchor = center")
            emit(lines, level + 2, "size = { 100% 100% }")
            emit(lines, level + 2, f'raw_text = "{group["icon"]}"')
            emit(lines, level + 2, "fontsize = 30")
            emit(lines, level + 2, f'tooltip = "TV_ACADEMY_DEBATE_GROUP_{group["key"].upper()}_TT"')
            emit(lines, level + 2, "align = center|nobaseline")
            emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_round_table(lines: list[str], level: int, visible_expr: str, table_text_key: str | None, history: bool = False) -> None:
    emit(lines, level, "widget = {")
    emit(lines, level + 1, f'visible = "{visible_expr}"')
    emit(lines, level + 1, "size = { 470 404 }")
    emit(lines, level + 1, "allow_outside = yes")
    emit(lines, level + 1, "widget = {")
    emit(lines, level + 2, "size = { 220 220 }")
    emit(lines, level + 2, "position = { 125 54 }")
    emit(lines, level + 2, "using = bg_circle_piechart")
    emit(lines, level + 2, "widget = {")
    emit(lines, level + 3, "parentanchor = center")
    emit(lines, level + 3, "widgetanchor = center")
    emit(lines, level + 3, "size = { 160 90 }")
    if history:
        emit(lines, level + 3, "text_multi = {")
        emit(lines, level + 4, "size = { 160 90 }")
        emit(lines, level + 4, "max_width = 160")
        emit(lines, level + 4, 'text = "TV_ACADEMY_DEBATE_HISTORY_TABLE_TEXT"')
        emit(lines, level + 4, "fontsize = 16")
        emit(lines, level + 4, "align = center|nobaseline")
        emit(lines, level + 3, "}")
    elif table_text_key:
        emit(lines, level + 3, "text_multi = {")
        emit(lines, level + 4, "size = { 160 90 }")
        emit(lines, level + 4, "max_width = 160")
        emit(lines, level + 4, f'text = "{table_text_key}"')
        emit(lines, level + 4, "fontsize = 16")
        emit(lines, level + 4, "align = center|nobaseline")
        emit(lines, level + 3, "}")
    else:
        append_current_philosophy_name(lines, level + 3, 160, 16)
    emit(lines, level + 2, "}")
    emit(lines, level + 1, "}")
    if not history:
        for x, y, crown, seat in [
            (201, 258, True, None),
            (201, 8, False, 1),
            (78, 78, False, 2),
            (324, 78, False, 3),
            (78, 208, False, 4),
            (324, 208, False, 5),
        ]:
            append_local_debate_seat(lines, level + 1, x, y, seat, crown)
        emit(lines, level + 1, "vbox = {")
        emit(lines, level + 2, "position = { 4 342 }")
        emit(lines, level + 2, "size = { 462 56 }")
        emit(lines, level + 2, "spacing = 4")
        local_balance = io_var("tv_academy_debate_local_balance")
        debate_position = io_var("tv_academy_philosophy_debate_position")
        append_debate_progress_footer(
            lines,
            level + 2,
            "TV_ACADEMY_DEBATE_LOCAL_BALANCE_TT",
            f"[{local_balance}.GetValue]",
            f"[{local_balance}.GetValue|0]%",
            "progress_bar_green_red_alt",
        )
        append_debate_progress_footer(
            lines,
            level + 2,
            "TV_ACADEMY_DEBATE_LOCAL_PROGRESS_TT",
            f"[{debate_position}.GetValue]",
            f"[{debate_position}.GetValue|0]%",
        )
        emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_world_seat_tint(lines: list[str], level: int, seat_set: str, stance_var: str, stance: int, texture: str) -> None:
    emit(lines, level, "widget = {")
    emit(lines, level + 1, f'visible = "[And({seat_set}, {fixed_eq(stance_var, stance)})]"')
    emit(lines, level + 1, "size = { 100% 100% }")
    emit(lines, level + 1, "icon = {")
    emit(lines, level + 2, "parentanchor = center")
    emit(lines, level + 2, "size = { 100% 100% }")
    emit(lines, level + 2, 'texture = "gfx/interface/component_tiles/hud_corners/circle_progress_bg.dds"')
    emit(lines, level + 2, "modify_texture = {")
    emit(lines, level + 3, f"using = {texture}")
    emit(lines, level + 2, "}")
    emit(lines, level + 2, "alpha = 0.88")
    emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_world_seat(lines: list[str], level: int, seat: int) -> None:
    country_var = player_var(world_debate_seat_var(seat, "country"))
    stance_var = player_var(world_debate_seat_var(seat, "stance"))
    seat_set = f"{country_var}.IsSet"
    emit(lines, level, "widget = {")
    emit(lines, level + 1, f"size = {{ {WORLD_DEBATE_SEAT_SIZE} {WORLD_DEBATE_SEAT_SIZE} }}")
    emit(lines, level + 1, "using = bg_circle_piechart_big")
    emit(lines, level + 1, 'tooltip = "TV_ACADEMY_WORLD_DEBATE_EMPTY_SEAT_TT"')
    append_world_seat_tint(lines, level + 1, seat_set, stance_var, 1, "color_light_green_texture")
    append_world_seat_tint(lines, level + 1, seat_set, stance_var, 2, "color_red_texture")
    append_world_seat_tint(lines, level + 1, seat_set, stance_var, 3, "color_yellow_texture")
    emit(lines, level + 1, "widget = {")
    emit(lines, level + 2, f'visible = "[{seat_set}]"')
    emit(lines, level + 2, "parentanchor = center")
    emit(lines, level + 2, "widgetanchor = center")
    emit(lines, level + 2, "size = { 32 20 }")
    emit(lines, level + 2, f'datacontext = "[{country_var}.GetCountry]"')
    emit(lines, level + 2, 'tooltip = "TV_ACADEMY_WORLD_DEBATE_SEAT_TT"')
    emit(lines, level + 2, "country_flag_small_plus = { size = { 32 20 } }")
    emit(lines, level + 1, "}")
    emit(lines, level + 1, "text_single = {")
    emit(lines, level + 2, f'visible = "[Not({seat_set})]"')
    emit(lines, level + 2, "parentanchor = center")
    emit(lines, level + 2, "size = { 100% 100% }")
    emit(lines, level + 2, 'raw_text = "@diplomacy!"')
    emit(lines, level + 2, "fontsize = 15")
    emit(lines, level + 2, "align = center|nobaseline")
    emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_world_table(lines: list[str], level: int) -> None:
    strength_var = player_var("tv_academy_world_debate_strength")
    progress_var = player_var("tv_academy_world_debate_progress")
    emit(lines, level, "widget = {")
    emit(lines, level + 1, f'visible = "[{current_node_type_eq(2)}]"')
    emit(lines, level + 1, "size = { 470 328 }")
    emit(lines, level + 1, "vbox = {")
    emit(lines, level + 2, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 2, "size = { 462 320 }")
    emit(lines, level + 2, "margin = { 4 6 }")
    emit(lines, level + 2, "spacing = 4")
    seat = 1
    for _ in range(5):
        emit(lines, level + 2, "hbox = {")
        emit(lines, level + 3, "layoutpolicy_horizontal = fixed")
        emit(lines, level + 3, "size = { 462 36 }")
        emit(lines, level + 3, "spacing = 4")
        emit(lines, level + 3, "expand = {}")
        for _ in range(10):
            append_world_seat(lines, level + 3, seat)
            seat += 1
        emit(lines, level + 3, "expand = {}")
        emit(lines, level + 2, "}")
    emit(lines, level + 2, "expand = {}")
    append_debate_progress_footer(
        lines,
        level + 2,
        "TV_ACADEMY_WORLD_DEBATE_STRENGTH_TT",
        f"[{strength_var}.GetValue]",
        f"[{strength_var}.GetValue|0]%",
    )
    append_debate_progress_footer(
        lines,
        level + 2,
        "TV_ACADEMY_WORLD_DEBATE_PROGRESS_TT",
        f"[{progress_var}.GetValue]",
        f"[{progress_var}.GetValue|0]%",
    )
    emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_current_node_line(lines: list[str], level: int) -> None:
    for value, loc_key in CURRENT_NODE_LINE_KEYS:
        emit(lines, level, "text_single = {")
        emit(lines, level + 1, f'visible = "[{current_node_type_eq(value)}]"')
        emit(lines, level + 1, "size = { 462 28 }")
        emit(lines, level + 1, f'text = "{loc_key}"')
        emit(lines, level + 1, "fontsize = 16")
        emit(lines, level + 1, "align = center|nobaseline")
        emit(lines, level, "}")


def append_current_node_card(lines: list[str], level: int) -> None:
    emit(lines, level, "widget = {")
    emit(lines, level + 1, "size = { 500 54 }")
    emit(lines, level + 1, "using = bg_paper_card_situations")
    emit(lines, level + 1, "vbox = {")
    emit(lines, level + 2, "parentanchor = center")
    emit(lines, level + 2, "widgetanchor = center")
    emit(lines, level + 2, "size = { 462 28 }")
    emit(lines, level + 2, "ignoreinvisible = yes")
    append_current_node_line(lines, level + 2)
    emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_timeline_card(lines: list[str], level: int) -> None:
    debate_position = io_var("tv_academy_philosophy_debate_position")
    emit(lines, level, "card_common = {")
    emit(lines, level + 1, "maximumsize = { 500 -1 }")
    emit(lines, level + 1, 'blockoverride "common_header_icon_texture" {')
    emit(lines, level + 2, 'texture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"')
    emit(lines, level + 1, "}")
    emit(lines, level + 1, 'blockoverride "common_header_text" {')
    emit(lines, level + 2, 'text = "TV_ACADEMY_DEBATE_TIMELINE_CARD_TITLE"')
    emit(lines, level + 1, "}")
    emit(lines, level + 1, 'blockoverride "common_bottom_content" {')
    emit(lines, level + 2, "vbox = {")
    emit(lines, level + 3, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 3, "layoutpolicy_vertical = fixed")
    emit(lines, level + 3, "size = { 470 66 }")
    emit(lines, level + 3, "spacing = 0")
    emit(lines, level + 3, "widget = {")
    emit(lines, level + 4, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 4, "layoutpolicy_vertical = fixed")
    emit(lines, level + 4, "size = { 470 42 }")
    emit(lines, level + 4, "hbox = {")
    emit(lines, level + 5, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 5, "layoutpolicy_vertical = fixed")
    emit(lines, level + 5, "size = { 470 42 }")
    emit(lines, level + 5, "spacing = 0")
    emit(lines, level + 5, "widget = { size = { 21 42 } }")
    emit(lines, level + 5, "widget = {")
    emit(lines, level + 6, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 6, "layoutpolicy_vertical = fixed")
    emit(lines, level + 6, "size = { 428 42 }")
    emit(lines, level + 6, "progressbar = {")
    emit(lines, level + 7, "size = { 428 14 }")
    emit(lines, level + 7, "parentanchor = vcenter")
    emit(lines, level + 7, "widgetanchor = vcenter")
    emit(lines, level + 7, "using = progress_bar_blue_alt")
    emit(lines, level + 7, "min = 0")
    emit(lines, level + 7, "max = 100")
    emit(lines, level + 7, f'value = "[{debate_position}.GetValue]"')
    emit(lines, level + 6, "}")
    emit(lines, level + 5, "}")
    emit(lines, level + 5, "widget = { size = { 21 42 } }")
    emit(lines, level + 4, "}")
    emit(lines, level + 4, "hbox = {")
    emit(lines, level + 5, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 5, "layoutpolicy_vertical = fixed")
    emit(lines, level + 5, "size = { 470 42 }")
    emit(lines, level + 5, "spacing = 65")
    for slot in range(1, 6):
        append_debate_node_icon(lines, level + 5, slot)
    emit(lines, level + 4, "}")
    emit(lines, level + 3, "}")
    emit(lines, level + 3, "widget = {")
    emit(lines, level + 4, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 4, "layoutpolicy_vertical = fixed")
    emit(lines, level + 4, "size = { 470 24 }")
    emit(lines, level + 4, "hbox = {")
    emit(lines, level + 5, "size = { 470 24 }")
    emit(lines, level + 5, "layoutpolicy_horizontal = fixed")
    emit(lines, level + 5, "layoutpolicy_vertical = fixed")
    emit(lines, level + 5, "spacing = 65")
    for slot in range(1, 6):
        append_node_year(lines, level + 5, slot)
    emit(lines, level + 4, "}")
    emit(lines, level + 3, "}")
    emit(lines, level + 2, "}")
    emit(lines, level + 1, "}")
    emit(lines, level, "}")


def append_current_issue_card(lines: list[str], level: int) -> None:
    emit(lines, level, "card_common = {")
    emit(lines, level + 1, "maximumsize = { 500 -1 }")
    emit(lines, level + 1, 'blockoverride "common_header_icon_texture" {')
    emit(lines, level + 2, 'texture = "gfx/interface/icons/flat_icons/tabicons/advances.dds"')
    emit(lines, level + 1, "}")
    emit(lines, level + 1, 'blockoverride "common_header_text" {')
    emit(lines, level + 2, 'text = "TV_ACADEMY_DEBATE_CURRENT_ISSUE_CARD_TITLE"')
    emit(lines, level + 1, "}")
    emit(lines, level + 1, 'blockoverride "common_bottom_content" {')
    emit(lines, level + 2, "widget = {")
    emit(lines, level + 3, "size = { 470 412 }")
    append_round_table(lines, level + 3, f"[{current_node_type_eq(0)}]", "TV_ACADEMY_DEBATE_HISTORY_TABLE_TEXT", history=True)
    append_round_table(lines, level + 3, f"[{current_node_type_eq(1)}]", None, history=False)
    append_world_table(lines, level + 3)
    emit(lines, level + 2, "}")
    emit(lines, level + 1, "}")
    emit(lines, level, "}")


def gen_philosophy_block() -> str:
    lines: list[str] = []

    emit(lines, 1, 'blockoverride "organization_resolutions_content" {')
    emit(lines, 2, "margin = { 0 0 }")
    emit(lines, 2, "layoutpolicy_vertical = expanding")
    emit(lines, 2, "scrollarea = {")
    emit(lines, 3, "using = layoutpolicy_expanding")
    emit(lines, 3, "scrollbarpolicy_horizontal = always_off")
    emit(lines, 3, "scrollbar_vertical = {")
    emit(lines, 4, "using = Scrollbar_Vertical")
    emit(lines, 3, "}")
    emit(lines, 3, "scrollwidget = {")
    emit(lines, 4, "vbox = {")
    emit(lines, 5, "margin = { 10 0 }")
    emit(lines, 5, "layoutpolicy_horizontal = expanding")
    emit(lines, 5, "layoutpolicy_vertical = expanding")
    emit(lines, 5, "ignoreinvisible = yes")
    emit(lines, 5, "spacing = 10")

    append_current_node_card(lines, 5)
    append_timeline_card(lines, 5)
    append_current_issue_card(lines, 5)

    emit(lines, 5, "expand = {}")
    emit(lines, 4, "}")
    emit(lines, 3, "}")
    emit(lines, 2, "}")
    emit(lines, 1, "}")
    return "\n".join(lines)


def _extract_block_body(block: str) -> str:
    open_brace = block.index("{")
    close_brace = block.rfind("\n\t}")
    if close_brace == -1:
        raise ValueError("Could not find block closing brace")
    return block[open_brace + 1 : close_brace].strip("\n")


def _indent_block(text: str) -> str:
    return "\n".join((T + line) if line else line for line in text.splitlines())


def _append_actions_to_research_block(research_block: str, actions_block: str) -> str:
    actions_body = _indent_block(_extract_block_body(actions_block))
    insert_at = "\n\t\t}\n\t}"
    if not research_block.endswith(insert_at):
        raise ValueError("Research block did not end with expected vbox/blockoverride closing marker")
    prefix = research_block[: -len(insert_at)]
    return (
        prefix
        + "\n\n"
        + T * 3
        + "# Action Buttons\n"
        + actions_body
        + "\n"
        + T * 3
        + "expand = {}"
        + insert_at
    )


def swap_academy_tabs(content: str) -> str:
    """Move Philosophy to overview and Concentrated Research to resolutions."""
    overview_marker = T + 'blockoverride "organization_overview_list_custom_top_extra" {'
    actions_marker = T + 'blockoverride "organization_main_actions_extra" {'
    philosophy_comment = T + "# -- Philosophy tab: reuse the vanilla resolutions slot"
    philosophy_marker = T + 'blockoverride "organization_resolutions_content" {'
    medicine_marker = T + "# -- Medicine tab: enable the custom tab slot"

    overview_start = content.index(overview_marker)
    actions_start = content.index(actions_marker)
    philosophy_comment_start = content.index(philosophy_comment)
    philosophy_start = content.index(philosophy_marker, philosophy_comment_start)
    medicine_start = content.index(medicine_marker)

    research_block = content[overview_start:actions_start].rstrip()
    actions_block = content[actions_start:philosophy_comment_start].rstrip()
    philosophy_block = gen_philosophy_block().rstrip()

    philosophy_overview = philosophy_block.replace(
        'blockoverride "organization_resolutions_content"',
        'blockoverride "organization_overview_list_custom_top_extra"',
        1,
    )
    concentrated_research = _append_actions_to_research_block(
        research_block.replace(
            'blockoverride "organization_overview_list_custom_top_extra"',
            'blockoverride "organization_resolutions_content"',
            1,
        ),
        actions_block,
    )

    header = content[:overview_start].replace(
        T + 'blockoverride "organization_main_actions_extra_visible" {\n'
        + T * 2
        + "visible = yes\n"
        + T
        + "}",
        T + 'blockoverride "organization_main_actions_extra_visible" {\n'
        + T * 2
        + "visible = no\n"
        + T
        + "}",
        1,
    )
    header = header.replace('text = "TV_ACADEMY_PHILOSOPHY_TAB"', 'text = "TV_RM_CONCENTRATED_RESEARCH_TAB"', 1)
    header = header.replace(
        'tooltip = "TV_ACADEMY_PHILOSOPHY_TAB_TOOLTIP"',
        'tooltip = "TV_RM_CONCENTRATED_RESEARCH_TAB_TOOLTIP"',
        1,
    )

    return (
        header
        + T
        + "# -- Philosophy tab: use the vanilla overview slot -----------------------\n"
        + philosophy_overview
        + "\n\n"
        + T
        + 'blockoverride "organization_main_actions_extra" {}\n\n'
        + T
        + "# -- Concentrated Research tab: use the vanilla resolutions slot ------------\n"
        + concentrated_research
        + "\n\n"
        + content[medicine_start:]
    )


def generate(data: dict, debate_data: dict) -> str:
    global DEBATE_GROUPS
    DEBATE_GROUPS = sorted(debate_data["groups"], key=lambda group: int(group["id"]))
    advances = data["locked_advances"]
    entries = "\n".join(gen_target_entry(a) for a in advances)
    return swap_academy_tabs(GUI_PREFIX + entries + "\n" + GUI_SUFFIX)


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--dry", action="store_true", help="Print to stdout instead of writing")
    args = parser.parse_args()

    with open(DATA_FILE, encoding="utf-8") as f:
        data = yaml.safe_load(f)
    with open(DEBATE_DATA_FILE, encoding="utf-8") as f:
        debate_data = yaml.safe_load(f)

    content = generate(data, debate_data)

    if args.dry:
        print(content)
    else:
        OUT_FILE.write_text(content, encoding="utf-8-sig")
        print(f"Written: {OUT_FILE.relative_to(REPO_ROOT)}")


if __name__ == "__main__":
    main()
