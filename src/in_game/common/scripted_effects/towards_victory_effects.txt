# Towards Victory — Scripted effect definitions
# Generated by scripts/gen_victory_scaffold.py

# ═══════════════════════════════════════════════════════════════════════════
# SECTION 1: TOP-LEVEL DISPATCHER
# ═══════════════════════════════════════════════════════════════════════════

tv_check_all_milestones_effect = {
	tv_check_conquest_milestones_effect = yes
	tv_check_prosperity_milestones_effect = yes
	tv_check_trade_milestones_effect = yes
	tv_check_diplomatic_milestones_effect = yes
	tv_check_cultural_milestones_effect = yes
	tv_check_science_milestones_effect = yes
}

# ═══════════════════════════════════════════════════════════════════════════
# SECTION 1.5: CONQUEST SCORE UPDATER
# Computes tv_conquest_score = num_locations + 0.5 * subject_or_below locations.
# Verification — Step 3, Reference: country_effects.txt:2519, Quote: "root = { remove_reform = prev }"
# Verification — Step 3, Reference: qa_debug.txt:2351, Quote: "change_variable = { name = qa_counter add = var:qa_amount_of_special_units }"
# ═══════════════════════════════════════════════════════════════════════════

tv_update_conquest_score_effect = {
	set_variable = { name = tv_conquest_score value = num_locations }
	set_variable = { name = tv_conquest_subj_temp value = 0 }
	every_subject_or_below = {
		root = {
			change_variable = { name = tv_conquest_subj_temp add = prev.num_locations }
		}
	}
	change_variable = { name = tv_conquest_subj_temp divide = 2 }
	change_variable = { name = tv_conquest_score add = var:tv_conquest_subj_temp }
}

# ═══════════════════════════════════════════════════════════════════════════
# SECTION 1.6: PROSPERITY SCORE UPDATER
# Computes tv_prosperity_score = total_population + Σ(prosperity per owned location).
# Called YEARLY only (expensive every_owned_location iteration).
# Milestone checker reads the cached tv_prosperity_score — never calls this directly.
# Verification — Step 3, Reference: country_effects.txt:1809, Quote: "value = total_population"
# Verification — Step 3, Reference: country_effects.txt:2519, Quote: "root = { change_variable = { add = prev"
# ═══════════════════════════════════════════════════════════════════════════

tv_update_prosperity_score_effect = {
	set_variable = { name = tv_prosperity_score value = total_population }
	set_variable = { name = tv_prosperity_temp value = 0 }
	every_owned_location = {
		root = {
			change_variable = { name = tv_prosperity_temp add = prev.prosperity }
		}
	}
	change_variable = { name = tv_prosperity_score add = var:tv_prosperity_temp }
}

# ═══════════════════════════════════════════════════════════════════════════
# SECTION 1.7: TRADE SCORE UPDATER
# Computes tv_trade_score = monthly_trade_income.
# Verification — Step 3, Reference: events/economy/trade.txt:185, Quote: "monthly_trade_income > { value = monthly_income_trade_and_tax multiply = 0.33 }"
# ═══════════════════════════════════════════════════════════════════════════

tv_update_trade_score_effect = {
	set_variable = { name = tv_trade_score value = monthly_trade_income }
}

# ═══════════════════════════════════════════════════════════════════════════
# SECTION 2: PER-PATH MILESTONE CHECKERS
# Guards via var:tv_*_milestone < N prevent re-triggering after grant.
# ═══════════════════════════════════════════════════════════════════════════

# ── Conquest Victory ──────────────────────────────────────────────────────
tv_check_conquest_milestones_effect = {
	tv_update_conquest_score_effect = yes
	# Initialise on first call (guards against missing variable)
	if = {
		limit = { NOT = { has_variable = tv_conquest_milestone } }
		set_variable = { name = tv_conquest_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 1
			tv_conquest_milestone_1 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 1 }
		trigger_event_non_silently = tv.conquest.1
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 2
			tv_conquest_milestone_2 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 2 }
		trigger_event_non_silently = tv.conquest.2
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 3
			tv_conquest_milestone_3 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 3 }
		trigger_event_non_silently = tv.conquest.3
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 4
			tv_conquest_milestone_4 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 4 }
		trigger_event_non_silently = tv.conquest.4
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 5
			tv_conquest_milestone_5 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 5 }
		trigger_event_non_silently = tv.conquest.5
	}
}

# ── Prosperity Victory ──────────────────────────────────────────────────────
tv_check_prosperity_milestones_effect = {
	# Initialise on first call (guards against missing variable)
	if = {
		limit = { NOT = { has_variable = tv_prosperity_milestone } }
		set_variable = { name = tv_prosperity_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 1
			tv_prosperity_milestone_1 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 1 }
		trigger_event_non_silently = tv.prosperity.1
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 2
			tv_prosperity_milestone_2 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 2 }
		trigger_event_non_silently = tv.prosperity.2
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 3
			tv_prosperity_milestone_3 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 3 }
		trigger_event_non_silently = tv.prosperity.3
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 4
			tv_prosperity_milestone_4 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 4 }
		trigger_event_non_silently = tv.prosperity.4
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 5
			tv_prosperity_milestone_5 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 5 }
		trigger_event_non_silently = tv.prosperity.5
	}
}

# ── Trade Victory ──────────────────────────────────────────────────────
tv_check_trade_milestones_effect = {
	tv_update_trade_score_effect = yes
	# Initialise on first call (guards against missing variable)
	if = {
		limit = { NOT = { has_variable = tv_trade_milestone } }
		set_variable = { name = tv_trade_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_trade_milestone < 1
			tv_trade_milestone_1 = yes
		}
		set_variable = { name = tv_trade_milestone value = 1 }
		trigger_event_non_silently = tv.trade.1
	}
	if = {
		limit = {
			var:tv_trade_milestone < 2
			tv_trade_milestone_2 = yes
		}
		set_variable = { name = tv_trade_milestone value = 2 }
		trigger_event_non_silently = tv.trade.2
	}
	if = {
		limit = {
			var:tv_trade_milestone < 3
			tv_trade_milestone_3 = yes
		}
		set_variable = { name = tv_trade_milestone value = 3 }
		trigger_event_non_silently = tv.trade.3
	}
	if = {
		limit = {
			var:tv_trade_milestone < 4
			tv_trade_milestone_4 = yes
		}
		set_variable = { name = tv_trade_milestone value = 4 }
		trigger_event_non_silently = tv.trade.4
	}
	if = {
		limit = {
			var:tv_trade_milestone < 5
			tv_trade_milestone_5 = yes
		}
		set_variable = { name = tv_trade_milestone value = 5 }
		trigger_event_non_silently = tv.trade.5
	}
}

# ── Diplomatic Victory ──────────────────────────────────────────────────────
tv_check_diplomatic_milestones_effect = {
	# Initialise on first call (guards against missing variable)
	if = {
		limit = { NOT = { has_variable = tv_diplomatic_milestone } }
		set_variable = { name = tv_diplomatic_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 1
			tv_diplomatic_milestone_1 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 1 }
		trigger_event_non_silently = tv.diplomatic.1
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 2
			tv_diplomatic_milestone_2 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 2 }
		trigger_event_non_silently = tv.diplomatic.2
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 3
			tv_diplomatic_milestone_3 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 3 }
		trigger_event_non_silently = tv.diplomatic.3
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 4
			tv_diplomatic_milestone_4 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 4 }
		trigger_event_non_silently = tv.diplomatic.4
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 5
			tv_diplomatic_milestone_5 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 5 }
		trigger_event_non_silently = tv.diplomatic.5
	}
}

# ── Cultural Victory ──────────────────────────────────────────────────────
tv_check_cultural_milestones_effect = {
	# Initialise on first call (guards against missing variable)
	if = {
		limit = { NOT = { has_variable = tv_cultural_milestone } }
		set_variable = { name = tv_cultural_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 1
			tv_cultural_milestone_1 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 1 }
		trigger_event_non_silently = tv.cultural.1
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 2
			tv_cultural_milestone_2 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 2 }
		trigger_event_non_silently = tv.cultural.2
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 3
			tv_cultural_milestone_3 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 3 }
		trigger_event_non_silently = tv.cultural.3
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 4
			tv_cultural_milestone_4 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 4 }
		trigger_event_non_silently = tv.cultural.4
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 5
			tv_cultural_milestone_5 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 5 }
		trigger_event_non_silently = tv.cultural.5
	}
}

# ── Scientific Victory ──────────────────────────────────────────────────────
tv_check_science_milestones_effect = {
	# Initialise on first call (guards against missing variable)
	if = {
		limit = { NOT = { has_variable = tv_science_milestone } }
		set_variable = { name = tv_science_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_science_milestone < 1
			tv_science_milestone_1 = yes
		}
		set_variable = { name = tv_science_milestone value = 1 }
		trigger_event_non_silently = tv.science.1
	}
	if = {
		limit = {
			var:tv_science_milestone < 2
			tv_science_milestone_2 = yes
		}
		set_variable = { name = tv_science_milestone value = 2 }
		trigger_event_non_silently = tv.science.2
	}
	if = {
		limit = {
			var:tv_science_milestone < 3
			tv_science_milestone_3 = yes
		}
		set_variable = { name = tv_science_milestone value = 3 }
		trigger_event_non_silently = tv.science.3
	}
	if = {
		limit = {
			var:tv_science_milestone < 4
			tv_science_milestone_4 = yes
		}
		set_variable = { name = tv_science_milestone value = 4 }
		trigger_event_non_silently = tv.science.4
	}
	if = {
		limit = {
			var:tv_science_milestone < 5
			tv_science_milestone_5 = yes
		}
		set_variable = { name = tv_science_milestone value = 5 }
		trigger_event_non_silently = tv.science.5
	}
}

# ═══════════════════════════════════════════════════════════════════════════
# SECTION 3: PROGRESS PERCENTAGE UPDATER
# Stores tv_*_progress_pct in [0.0, 1.0] for each path (used by GUI bars).
# Called monthly for players, yearly for AI — must be cheap to evaluate.
# ═══════════════════════════════════════════════════════════════════════════

tv_update_all_progress_pct_effect = {
	# ── Conquest Victory ────────────────────────────────────────────────────
	if = {
		limit = { has_variable = tv_conquest_score }
		set_variable = { name = tv_conquest_progress_pct value = var:tv_conquest_score }
		change_variable = { name = tv_conquest_progress_pct divide = 1600 }
		if = { limit = { var:tv_conquest_progress_pct > 1 } set_variable = { name = tv_conquest_progress_pct value = 1 } }
	}

	# ── Prosperity Victory ────────────────────────────────────────────────────
	if = {
		limit = { has_variable = tv_prosperity_score }
		set_variable = { name = tv_prosperity_progress_pct value = var:tv_prosperity_score }
		change_variable = { name = tv_prosperity_progress_pct divide = 25000 }
		if = { limit = { var:tv_prosperity_progress_pct > 1 } set_variable = { name = tv_prosperity_progress_pct value = 1 } }
	}

	# ── Trade Victory ────────────────────────────────────────────────────
	if = {
		limit = { has_variable = tv_trade_score }
		set_variable = { name = tv_trade_progress_pct value = var:tv_trade_score }
		change_variable = { name = tv_trade_progress_pct divide = 120 }
		if = { limit = { var:tv_trade_progress_pct > 1 } set_variable = { name = tv_trade_progress_pct value = 1 } }
	}

	# ── Diplomatic Victory ────────────────────────────────────────────────────
	# TODO: compute 0.0-1.0 progress fraction for diplomatic path
	# Formula: tv_diplomatic_progress_pct = raw_metric / v2_threshold
	# V2 threshold hint: tv_diplomatic_victory_points >= 580
	# Pattern:
	#   set_variable = { name = tv_diplomatic_progress_pct value = <raw_metric_accessor> }
	#   change_variable = { name = tv_diplomatic_progress_pct divide = <v2_threshold_literal> }
	#   if = { limit = { var:tv_diplomatic_progress_pct > 1 } set_variable = { name = tv_diplomatic_progress_pct value = 1 } }

	# ── Cultural Victory ────────────────────────────────────────────────────
	# TODO: compute 0.0-1.0 progress fraction for cultural path
	# Formula: tv_cultural_progress_pct = raw_metric / v2_threshold
	# V2 threshold hint: tv_cultural_influence_points >= 580
	# Pattern:
	#   set_variable = { name = tv_cultural_progress_pct value = <raw_metric_accessor> }
	#   change_variable = { name = tv_cultural_progress_pct divide = <v2_threshold_literal> }
	#   if = { limit = { var:tv_cultural_progress_pct > 1 } set_variable = { name = tv_cultural_progress_pct value = 1 } }

	# ── Scientific Victory ────────────────────────────────────────────────────
	# TODO: compute 0.0-1.0 progress fraction for science path
	# Formula: tv_science_progress_pct = raw_metric / v2_threshold
	# V2 threshold hint: tv_science_score — Age 5 (steam/industrial) threshold
	# Pattern:
	#   set_variable = { name = tv_science_progress_pct value = <raw_metric_accessor> }
	#   change_variable = { name = tv_science_progress_pct divide = <v2_threshold_literal> }
	#   if = { limit = { var:tv_science_progress_pct > 1 } set_variable = { name = tv_science_progress_pct value = 1 } }

}

# ═══════════════════════════════════════════════════════════════════════════
# SECTION 4: MILESTONE REWARD EFFECTS
# Called from events tv.<path>.<n> after the milestone is first reached.
# Each effect applies the permanent static modifier for that milestone.
# ═══════════════════════════════════════════════════════════════════════════

# Conquest Victory
tv_grant_conquest_milestone_1 = {
	add_country_modifier = { modifier = tv_conquest_m1_bonus days = -1 }
}

tv_grant_conquest_milestone_2 = {
	add_country_modifier = { modifier = tv_conquest_m2_bonus days = -1 }
}

tv_grant_conquest_milestone_3 = {
	add_country_modifier = { modifier = tv_conquest_m3_bonus days = -1 }
}

tv_grant_conquest_milestone_4 = {
	add_country_modifier = { modifier = tv_conquest_m4_bonus days = -1 }
}

tv_grant_conquest_milestone_5 = {
	add_country_modifier = { modifier = tv_conquest_m5_bonus days = -1 }
}


# Prosperity Victory
tv_grant_prosperity_milestone_1 = {
	add_country_modifier = { modifier = tv_prosperity_m1_bonus days = -1 }
}

tv_grant_prosperity_milestone_2 = {
	add_country_modifier = { modifier = tv_prosperity_m2_bonus days = -1 }
}

tv_grant_prosperity_milestone_3 = {
	add_country_modifier = { modifier = tv_prosperity_m3_bonus days = -1 }
}

tv_grant_prosperity_milestone_4 = {
	add_country_modifier = { modifier = tv_prosperity_m4_bonus days = -1 }
}

tv_grant_prosperity_milestone_5 = {
	add_country_modifier = { modifier = tv_prosperity_m5_bonus days = -1 }
}


# Trade Victory
tv_grant_trade_milestone_1 = {
	add_country_modifier = { modifier = tv_trade_m1_bonus days = -1 }
}

tv_grant_trade_milestone_2 = {
	add_country_modifier = { modifier = tv_trade_m2_bonus days = -1 }
}

tv_grant_trade_milestone_3 = {
	add_country_modifier = { modifier = tv_trade_m3_bonus days = -1 }
}

tv_grant_trade_milestone_4 = {
	add_country_modifier = { modifier = tv_trade_m4_bonus days = -1 }
}

tv_grant_trade_milestone_5 = {
	add_country_modifier = { modifier = tv_trade_m5_bonus days = -1 }
}


# Diplomatic Victory
tv_grant_diplomatic_milestone_1 = {
	# TODO: Diplomatic Victory — Milestone 1 reward
	# Design intent: diplomatic reputation, opinion bonus
	# add_country_modifier = { name = tv_diplomatic_m1_bonus }
}

tv_grant_diplomatic_milestone_2 = {
	# TODO: Diplomatic Victory — Milestone 2 reward
	# Design intent: alliance reliability, defensive bonuses
	# add_country_modifier = { name = tv_diplomatic_m2_bonus }
}

tv_grant_diplomatic_milestone_3 = {
	# TODO: Diplomatic Victory — Short-term Victory reward
	# Design intent: diplomatic range, claim fabrication speed
	# add_country_modifier = { name = tv_diplomatic_m3_bonus }
}

tv_grant_diplomatic_milestone_4 = {
	# TODO: Diplomatic Victory — Milestone 4 reward
	# Design intent: diplomatic immunity, vassal bonuses
	# add_country_modifier = { name = tv_diplomatic_m4_bonus }
}

tv_grant_diplomatic_milestone_5 = {
	# TODO: Diplomatic Victory — Long-term Victory reward
	# Design intent: permanent casus belli defense, prestige
	# add_country_modifier = { name = tv_diplomatic_m5_bonus }
}


# Cultural Victory
tv_grant_cultural_milestone_1 = {
	# TODO: Cultural Victory — Milestone 1 reward
	# Design intent: court attraction, ideas cost reduction
	# add_country_modifier = { name = tv_cultural_m1_bonus }
}

tv_grant_cultural_milestone_2 = {
	# TODO: Cultural Victory — Milestone 2 reward
	# Design intent: diplomat effectiveness, cultural prestige
	# add_country_modifier = { name = tv_cultural_m2_bonus }
}

tv_grant_cultural_milestone_3 = {
	# TODO: Cultural Victory — Short-term Victory reward
	# Design intent: artifact find chance, court income
	# add_country_modifier = { name = tv_cultural_m3_bonus }
}

tv_grant_cultural_milestone_4 = {
	# TODO: Cultural Victory — Milestone 4 reward
	# Design intent: cultural spread multiplier, missionary strength
	# add_country_modifier = { name = tv_cultural_m4_bonus }
}

tv_grant_cultural_milestone_5 = {
	# TODO: Cultural Victory — Long-term Victory reward
	# Design intent: permanent cultural spread multiplier, prestige cap
	# add_country_modifier = { name = tv_cultural_m5_bonus }
}


# Scientific Victory
tv_grant_science_milestone_1 = {
	# TODO: Scientific Victory — Milestone 1 reward
	# Design intent: research speed bonus
	# add_country_modifier = { name = tv_science_m1_bonus }
}

tv_grant_science_milestone_2 = {
	# TODO: Scientific Victory — Milestone 2 reward
	# Design intent: technology spread, military effectiveness
	# add_country_modifier = { name = tv_science_m2_bonus }
}

tv_grant_science_milestone_3 = {
	# TODO: Scientific Victory — Short-term Victory reward
	# Design intent: institution spread speed, educated pop growth
	# add_country_modifier = { name = tv_science_m3_bonus }
}

tv_grant_science_milestone_4 = {
	# TODO: Scientific Victory — Milestone 4 reward
	# Design intent: scientific institution bonus, production bonus
	# add_country_modifier = { name = tv_science_m4_bonus }
}

tv_grant_science_milestone_5 = {
	# TODO: Scientific Victory — Long-term Victory reward
	# Design intent: permanent production bonus, army modernization
	# add_country_modifier = { name = tv_science_m5_bonus }
}

