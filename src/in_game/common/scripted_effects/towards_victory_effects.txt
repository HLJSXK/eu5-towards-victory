# @Generated by scripts/gen_victory.py
#   Data:    data/victory_paths.yaml
#   Regen:   conda run --no-capture-output -n eu5 python scripts/gen_victory.py
# Do not edit directly — modify the data file and re-run the generator.


# ═══════════════════════════════════════════════════════════════════════
# SECTION 1: TOP-LEVEL DISPATCHER
# ═══════════════════════════════════════════════════════════════════════

tv_check_all_milestones_effect = {
	tv_check_conquest_milestones_effect = yes
	tv_check_prosperity_milestones_effect = yes
	tv_check_trade_milestones_effect = yes
	tv_check_diplomatic_milestones_effect = yes
	tv_check_cultural_milestones_effect = yes
	tv_check_science_milestones_effect = yes
}

# ═══════════════════════════════════════════════════════════════════════
# SECTION: CONQUEST SCORE UPDATER
# ═══════════════════════════════════════════════════════════════════════

tv_update_conquest_score_effect = {
	set_variable = { name = tv_conquest_score value = num_locations }
	set_local_variable = { name = tv_conquest_subj_temp value = 0 }
	every_subject_or_below = {
		change_local_variable = { name = tv_conquest_subj_temp add = { value = num_locations  multiply = 0.5 } }
	}
	change_variable = { name = tv_conquest_score add = local_var:tv_conquest_subj_temp }
}

# ═══════════════════════════════════════════════════════════════════════
# SECTION: PROSPERITY SCORE UPDATER
# ═══════════════════════════════════════════════════════════════════════

tv_update_prosperity_score_effect = {
	# 1. 计数领地
	set_local_variable = { name = tv_num_locs value = 0 }
	every_owned_location = {
		change_local_variable = { name = tv_num_locs add = 1 }
	}
	# 2. 累加总量：total_pop + Σdev + tax_base
	set_variable = { name = tv_raw_score value = total_population }
	set_local_variable = { name = tv_sum_dev value = 0 }
	every_owned_location = {
		change_local_variable = { name = tv_sum_dev add = development }
	}
	change_variable = { name = tv_raw_score add = local_var:tv_sum_dev }
	change_variable = { name = tv_raw_score add = country_tax_base }
	# 3. base_score = raw × 50 / (N + 50)  递减规模惩罚
	set_variable = { name = tv_denom value = local_var:tv_num_locs }
	change_variable = { name = tv_denom add = 50 }
	change_variable = { name = tv_raw_score multiply = 50 }
	change_variable = { name = tv_raw_score divide = var:tv_denom }
	set_variable = { name = tv_prosperity_score value = var:tv_raw_score }
	# 4. 稳定度乘数：× (100 + stability) / 100
	set_variable = { name = tv_stab_factor value = stability }
	change_variable = { name = tv_stab_factor add = 100 }
	change_variable = { name = tv_prosperity_score multiply = var:tv_stab_factor }
	change_variable = { name = tv_prosperity_score divide = 100 }
	# 5. 政体权力乘数（君主=合法性，共和=传统，等）
	if = {
		limit = { government_type = government_type:monarchy }
		set_variable = { name = tv_gov_power value = legitimacy }
		change_variable = { name = tv_prosperity_score multiply = var:tv_gov_power }
		change_variable = { name = tv_prosperity_score divide = 100 }
	}
	else_if = {
		limit = { government_type = government_type:republic }
		set_variable = { name = tv_gov_power value = republican_tradition }
		change_variable = { name = tv_prosperity_score multiply = var:tv_gov_power }
		change_variable = { name = tv_prosperity_score divide = 100 }
	}
	else_if = {
		limit = { government_type = government_type:theocracy }
		set_variable = { name = tv_gov_power value = devotion }
		change_variable = { name = tv_prosperity_score multiply = var:tv_gov_power }
		change_variable = { name = tv_prosperity_score divide = 100 }
	}
	else_if = {
		limit = { government_type = government_type:tribe }
		set_variable = { name = tv_gov_power value = tribal_cohesion }
		change_variable = { name = tv_prosperity_score multiply = var:tv_gov_power }
		change_variable = { name = tv_prosperity_score divide = 100 }
	}
	else_if = {
		limit = { government_type = government_type:steppe_horde }
		set_variable = { name = tv_gov_power value = horde_unity }
		change_variable = { name = tv_prosperity_score multiply = var:tv_gov_power }
		change_variable = { name = tv_prosperity_score divide = 100 }
	}
}

# ═══════════════════════════════════════════════════════════════════════
# SECTION: TRADE SCORE UPDATER
# ═══════════════════════════════════════════════════════════════════════

tv_update_trade_score_effect = {
	set_variable = { name = tv_trade_score value = monthly_trade_income }
}

# ═══════════════════════════════════════════════════════════════════════
# SECTION: SCIENCE SCORE UPDATER
# ═══════════════════════════════════════════════════════════════════════

tv_update_science_score_effect = {
	set_variable = { name = tv_science_score value = num_of_advances_researched }
}

# ═══════════════════════════════════════════════════════════════════════
# SECTION 2: PER-PATH MILESTONE CHECKERS
# Guards via var:tv_*_milestone < N prevent re-triggering after grant.
# ═══════════════════════════════════════════════════════════════════════

# ── Conquest Victory ──────────────────────────────────────────────────
tv_check_conquest_milestones_effect = {
	tv_update_conquest_score_effect = yes
	# Initialise on first call (guards against missing variable)
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_VARIABLE_NOT_SET_TT
				NOT = { has_variable = tv_conquest_milestone }
			}
		}
		set_variable = { name = tv_conquest_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 1
			tv_conquest_milestone_1 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 1 }
		trigger_event_non_silently = tv_conquest.1
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 2
			tv_conquest_milestone_2 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 2 }
		trigger_event_non_silently = tv_conquest.2
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 3
			tv_conquest_milestone_3 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 3 }
		trigger_event_non_silently = tv_conquest.3
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 4
			tv_conquest_milestone_4 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 4 }
		trigger_event_non_silently = tv_conquest.4
	}
	if = {
		limit = {
			var:tv_conquest_milestone < 5
			tv_conquest_milestone_5 = yes
		}
		set_variable = { name = tv_conquest_milestone value = 5 }
		trigger_event_non_silently = tv_conquest.5
	}
}

# ── Prosperity Victory ──────────────────────────────────────────────────
tv_check_prosperity_milestones_effect = {
	tv_update_prosperity_score_effect = yes
	# Initialise on first call (guards against missing variable)
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_VARIABLE_NOT_SET_TT
				NOT = { has_variable = tv_prosperity_milestone }
			}
		}
		set_variable = { name = tv_prosperity_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 1
			tv_prosperity_milestone_1 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 1 }
		trigger_event_non_silently = tv_prosperity.1
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 2
			tv_prosperity_milestone_2 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 2 }
		trigger_event_non_silently = tv_prosperity.2
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 3
			tv_prosperity_milestone_3 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 3 }
		trigger_event_non_silently = tv_prosperity.3
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 4
			tv_prosperity_milestone_4 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 4 }
		trigger_event_non_silently = tv_prosperity.4
	}
	if = {
		limit = {
			var:tv_prosperity_milestone < 5
			tv_prosperity_milestone_5 = yes
		}
		set_variable = { name = tv_prosperity_milestone value = 5 }
		trigger_event_non_silently = tv_prosperity.5
	}
}

# ── Trade Victory ──────────────────────────────────────────────────
tv_check_trade_milestones_effect = {
	tv_update_trade_score_effect = yes
	# Initialise on first call (guards against missing variable)
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_VARIABLE_NOT_SET_TT
				NOT = { has_variable = tv_trade_milestone }
			}
		}
		set_variable = { name = tv_trade_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_trade_milestone < 1
			tv_trade_milestone_1 = yes
		}
		set_variable = { name = tv_trade_milestone value = 1 }
		trigger_event_non_silently = tv_trade.1
	}
	if = {
		limit = {
			var:tv_trade_milestone < 2
			tv_trade_milestone_2 = yes
		}
		set_variable = { name = tv_trade_milestone value = 2 }
		trigger_event_non_silently = tv_trade.2
	}
	if = {
		limit = {
			var:tv_trade_milestone < 3
			tv_trade_milestone_3 = yes
		}
		set_variable = { name = tv_trade_milestone value = 3 }
		trigger_event_non_silently = tv_trade.3
	}
	if = {
		limit = {
			var:tv_trade_milestone < 4
			tv_trade_milestone_4 = yes
		}
		set_variable = { name = tv_trade_milestone value = 4 }
		trigger_event_non_silently = tv_trade.4
	}
	if = {
		limit = {
			var:tv_trade_milestone < 5
			tv_trade_milestone_5 = yes
		}
		set_variable = { name = tv_trade_milestone value = 5 }
		trigger_event_non_silently = tv_trade.5
	}
}

# ── Diplomatic Victory ──────────────────────────────────────────────────
tv_check_diplomatic_milestones_effect = {
	# Initialise score variable on first call
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_VARIABLE_NOT_SET_TT
				NOT = { has_variable = tv_diplomatic_victory_points }
			}
		}
		set_variable = { name = tv_diplomatic_victory_points value = 0 }
	}
	# Initialise on first call (guards against missing variable)
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_VARIABLE_NOT_SET_TT
				NOT = { has_variable = tv_diplomatic_milestone }
			}
		}
		set_variable = { name = tv_diplomatic_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 1
			tv_diplomatic_milestone_1 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 1 }
		trigger_event_non_silently = tv_diplomatic.1
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 2
			tv_diplomatic_milestone_2 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 2 }
		trigger_event_non_silently = tv_diplomatic.2
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 3
			tv_diplomatic_milestone_3 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 3 }
		trigger_event_non_silently = tv_diplomatic.3
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 4
			tv_diplomatic_milestone_4 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 4 }
		trigger_event_non_silently = tv_diplomatic.4
	}
	if = {
		limit = {
			var:tv_diplomatic_milestone < 5
			tv_diplomatic_milestone_5 = yes
		}
		set_variable = { name = tv_diplomatic_milestone value = 5 }
		trigger_event_non_silently = tv_diplomatic.5
	}
}

# ── Cultural Victory ──────────────────────────────────────────────────
tv_check_cultural_milestones_effect = {
	# Initialise on first call (guards against missing variable)
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_VARIABLE_NOT_SET_TT
				NOT = { has_variable = tv_cultural_milestone }
			}
		}
		set_variable = { name = tv_cultural_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 1
			tv_cultural_milestone_1 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 1 }
		trigger_event_non_silently = tv_cultural.1
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 2
			tv_cultural_milestone_2 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 2 }
		trigger_event_non_silently = tv_cultural.2
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 3
			tv_cultural_milestone_3 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 3 }
		trigger_event_non_silently = tv_cultural.3
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 4
			tv_cultural_milestone_4 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 4 }
		trigger_event_non_silently = tv_cultural.4
	}
	if = {
		limit = {
			var:tv_cultural_milestone < 5
			tv_cultural_milestone_5 = yes
		}
		set_variable = { name = tv_cultural_milestone value = 5 }
		trigger_event_non_silently = tv_cultural.5
	}
}

# ── Science Victory ──────────────────────────────────────────────────
tv_check_science_milestones_effect = {
	tv_update_science_score_effect = yes
	# Initialise on first call (guards against missing variable)
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_VARIABLE_NOT_SET_TT
				NOT = { has_variable = tv_science_milestone }
			}
		}
		set_variable = { name = tv_science_milestone value = 0 }
	}
	if = {
		limit = {
			var:tv_science_milestone < 1
			tv_science_milestone_1 = yes
		}
		set_variable = { name = tv_science_milestone value = 1 }
		trigger_event_non_silently = tv_science.1
	}
	if = {
		limit = {
			var:tv_science_milestone < 2
			tv_science_milestone_2 = yes
		}
		set_variable = { name = tv_science_milestone value = 2 }
		trigger_event_non_silently = tv_science.2
	}
	if = {
		limit = {
			var:tv_science_milestone < 3
			tv_science_milestone_3 = yes
		}
		set_variable = { name = tv_science_milestone value = 3 }
		trigger_event_non_silently = tv_science.3
	}
	if = {
		limit = {
			var:tv_science_milestone < 4
			tv_science_milestone_4 = yes
		}
		set_variable = { name = tv_science_milestone value = 4 }
		trigger_event_non_silently = tv_science.4
	}
	if = {
		limit = {
			var:tv_science_milestone < 5
			tv_science_milestone_5 = yes
		}
		set_variable = { name = tv_science_milestone value = 5 }
		trigger_event_non_silently = tv_science.5
	}
}

# ═══════════════════════════════════════════════════════════════════════
# SECTION 3: PROGRESS PERCENTAGE UPDATER
# Stores tv_*_progress_pct in [0, 100] for each path (used by GUI bars).
# ═══════════════════════════════════════════════════════════════════════

tv_update_all_progress_pct_effect = {
	# ── Conquest Victory ─────────────────────────────────────────────────────
	# piecewise: thresholds 200/400/600/1200/2500 → each milestone = +20%
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_CONQUEST_SCORE_TT
				has_variable = tv_conquest_score
			}
		}
		# Seg 1: 0–200 → 0–20
		set_variable = { name = tv_conquest_progress_pct value = var:tv_conquest_score }
		change_variable = { name = tv_conquest_progress_pct multiply = 20 }
		change_variable = { name = tv_conquest_progress_pct divide = 200 }
		# Seg 2: 200–400 → 20–40
		if = {
			limit = { var:tv_conquest_score >= 200 }
			set_variable = { name = tv_conquest_progress_pct value = var:tv_conquest_score }
			change_variable = { name = tv_conquest_progress_pct subtract = 200 }
			change_variable = { name = tv_conquest_progress_pct multiply = 20 }
			change_variable = { name = tv_conquest_progress_pct divide = 200 }
			change_variable = { name = tv_conquest_progress_pct add = 20 }
		}
		# Seg 3: 400–600 → 40–60
		if = {
			limit = { var:tv_conquest_score >= 400 }
			set_variable = { name = tv_conquest_progress_pct value = var:tv_conquest_score }
			change_variable = { name = tv_conquest_progress_pct subtract = 400 }
			change_variable = { name = tv_conquest_progress_pct multiply = 20 }
			change_variable = { name = tv_conquest_progress_pct divide = 200 }
			change_variable = { name = tv_conquest_progress_pct add = 40 }
		}
		# Seg 4: 600–1200 → 60–80
		if = {
			limit = { var:tv_conquest_score >= 600 }
			set_variable = { name = tv_conquest_progress_pct value = var:tv_conquest_score }
			change_variable = { name = tv_conquest_progress_pct subtract = 600 }
			change_variable = { name = tv_conquest_progress_pct multiply = 20 }
			change_variable = { name = tv_conquest_progress_pct divide = 600 }
			change_variable = { name = tv_conquest_progress_pct add = 60 }
		}
		# Seg 5: 1200–2500 → 80–100
		if = {
			limit = { var:tv_conquest_score >= 1200 }
			set_variable = { name = tv_conquest_progress_pct value = var:tv_conquest_score }
			change_variable = { name = tv_conquest_progress_pct subtract = 1200 }
			change_variable = { name = tv_conquest_progress_pct multiply = 20 }
			change_variable = { name = tv_conquest_progress_pct divide = 1300 }
			change_variable = { name = tv_conquest_progress_pct add = 80 }
		}
		if = { limit = { var:tv_conquest_progress_pct > 100 } set_variable = { name = tv_conquest_progress_pct value = 100 } }
	}

	# ── Prosperity Victory ────────────────────────────────────────────────────
	# piecewise: thresholds 2000/3000/5000/7000/11000 → each milestone = +20%
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_PROSPERITY_SCORE_TT
				has_variable = tv_prosperity_score
			}
		}
		# Seg 1: 0–2000 → 0–20
		set_variable = { name = tv_prosperity_progress_pct value = var:tv_prosperity_score }
		change_variable = { name = tv_prosperity_progress_pct multiply = 20 }
		change_variable = { name = tv_prosperity_progress_pct divide = 2000 }
		# Seg 2: 2000–3000 → 20–40
		if = {
			limit = { var:tv_prosperity_score >= 2000 }
			set_variable = { name = tv_prosperity_progress_pct value = var:tv_prosperity_score }
			change_variable = { name = tv_prosperity_progress_pct subtract = 2000 }
			change_variable = { name = tv_prosperity_progress_pct multiply = 20 }
			change_variable = { name = tv_prosperity_progress_pct divide = 1000 }
			change_variable = { name = tv_prosperity_progress_pct add = 20 }
		}
		# Seg 3: 3000–5000 → 40–60
		if = {
			limit = { var:tv_prosperity_score >= 3000 }
			set_variable = { name = tv_prosperity_progress_pct value = var:tv_prosperity_score }
			change_variable = { name = tv_prosperity_progress_pct subtract = 3000 }
			change_variable = { name = tv_prosperity_progress_pct multiply = 20 }
			change_variable = { name = tv_prosperity_progress_pct divide = 2000 }
			change_variable = { name = tv_prosperity_progress_pct add = 40 }
		}
		# Seg 4: 5000–7000 → 60–80
		if = {
			limit = { var:tv_prosperity_score >= 5000 }
			set_variable = { name = tv_prosperity_progress_pct value = var:tv_prosperity_score }
			change_variable = { name = tv_prosperity_progress_pct subtract = 5000 }
			change_variable = { name = tv_prosperity_progress_pct multiply = 20 }
			change_variable = { name = tv_prosperity_progress_pct divide = 2000 }
			change_variable = { name = tv_prosperity_progress_pct add = 60 }
		}
		# Seg 5: 7000–11000 → 80–100
		if = {
			limit = { var:tv_prosperity_score >= 7000 }
			set_variable = { name = tv_prosperity_progress_pct value = var:tv_prosperity_score }
			change_variable = { name = tv_prosperity_progress_pct subtract = 7000 }
			change_variable = { name = tv_prosperity_progress_pct multiply = 20 }
			change_variable = { name = tv_prosperity_progress_pct divide = 4000 }
			change_variable = { name = tv_prosperity_progress_pct add = 80 }
		}
		if = { limit = { var:tv_prosperity_progress_pct > 100 } set_variable = { name = tv_prosperity_progress_pct value = 100 } }
	}

	# ── Trade Victory ─────────────────────────────────────────────────────────
	# piecewise: thresholds 100/200/400/1000/2000 → each milestone = +20%
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_TRADE_SCORE_TT
				has_variable = tv_trade_score
			}
		}
		# Seg 1: 0–100 → 0–20
		set_variable = { name = tv_trade_progress_pct value = var:tv_trade_score }
		change_variable = { name = tv_trade_progress_pct multiply = 20 }
		change_variable = { name = tv_trade_progress_pct divide = 100 }
		# Seg 2: 100–200 → 20–40
		if = {
			limit = { var:tv_trade_score >= 100 }
			set_variable = { name = tv_trade_progress_pct value = var:tv_trade_score }
			change_variable = { name = tv_trade_progress_pct subtract = 100 }
			change_variable = { name = tv_trade_progress_pct multiply = 20 }
			change_variable = { name = tv_trade_progress_pct divide = 100 }
			change_variable = { name = tv_trade_progress_pct add = 20 }
		}
		# Seg 3: 200–400 → 40–60
		if = {
			limit = { var:tv_trade_score >= 200 }
			set_variable = { name = tv_trade_progress_pct value = var:tv_trade_score }
			change_variable = { name = tv_trade_progress_pct subtract = 200 }
			change_variable = { name = tv_trade_progress_pct multiply = 20 }
			change_variable = { name = tv_trade_progress_pct divide = 200 }
			change_variable = { name = tv_trade_progress_pct add = 40 }
		}
		# Seg 4: 400–1000 → 60–80
		if = {
			limit = { var:tv_trade_score >= 400 }
			set_variable = { name = tv_trade_progress_pct value = var:tv_trade_score }
			change_variable = { name = tv_trade_progress_pct subtract = 400 }
			change_variable = { name = tv_trade_progress_pct multiply = 20 }
			change_variable = { name = tv_trade_progress_pct divide = 600 }
			change_variable = { name = tv_trade_progress_pct add = 60 }
		}
		# Seg 5: 1000–2000 → 80–100
		if = {
			limit = { var:tv_trade_score >= 1000 }
			set_variable = { name = tv_trade_progress_pct value = var:tv_trade_score }
			change_variable = { name = tv_trade_progress_pct subtract = 1000 }
			change_variable = { name = tv_trade_progress_pct multiply = 20 }
			change_variable = { name = tv_trade_progress_pct divide = 1000 }
			change_variable = { name = tv_trade_progress_pct add = 80 }
		}
		if = { limit = { var:tv_trade_progress_pct > 100 } set_variable = { name = tv_trade_progress_pct value = 100 } }
	}

	# ── Diplomatic Victory ────────────────────────────────────────────────────
	# piecewise: thresholds 1/50/500/2500/15000 → each milestone = +20%
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_DIPLOMATIC_VICTORY_POINTS_TT
				has_variable = tv_diplomatic_victory_points
			}
		}
		# Seg 1: 0-1 → 0-20
		set_variable = { name = tv_diplomatic_progress_pct value = var:tv_diplomatic_victory_points }
		change_variable = { name = tv_diplomatic_progress_pct multiply = 20 }
		# Seg 2: 1-50 → 20-40
		if = {
			limit = { var:tv_diplomatic_victory_points >= 1 }
			set_variable = { name = tv_diplomatic_progress_pct value = var:tv_diplomatic_victory_points }
			change_variable = { name = tv_diplomatic_progress_pct subtract = 1 }
			change_variable = { name = tv_diplomatic_progress_pct multiply = 20 }
			change_variable = { name = tv_diplomatic_progress_pct divide = 49 }
			change_variable = { name = tv_diplomatic_progress_pct add = 20 }
		}
		# Seg 3: 50-500 → 40-60
		if = {
			limit = { var:tv_diplomatic_victory_points >= 50 }
			set_variable = { name = tv_diplomatic_progress_pct value = var:tv_diplomatic_victory_points }
			change_variable = { name = tv_diplomatic_progress_pct subtract = 50 }
			change_variable = { name = tv_diplomatic_progress_pct multiply = 20 }
			change_variable = { name = tv_diplomatic_progress_pct divide = 450 }
			change_variable = { name = tv_diplomatic_progress_pct add = 40 }
		}
		# Seg 4: 500-2500 → 60-80
		if = {
			limit = { var:tv_diplomatic_victory_points >= 500 }
			set_variable = { name = tv_diplomatic_progress_pct value = var:tv_diplomatic_victory_points }
			change_variable = { name = tv_diplomatic_progress_pct subtract = 500 }
			change_variable = { name = tv_diplomatic_progress_pct multiply = 20 }
			change_variable = { name = tv_diplomatic_progress_pct divide = 2000 }
			change_variable = { name = tv_diplomatic_progress_pct add = 60 }
		}
		# Seg 5: 2500-15000 → 80-100
		if = {
			limit = { var:tv_diplomatic_victory_points >= 2500 }
			set_variable = { name = tv_diplomatic_progress_pct value = var:tv_diplomatic_victory_points }
			change_variable = { name = tv_diplomatic_progress_pct subtract = 2500 }
			change_variable = { name = tv_diplomatic_progress_pct multiply = 20 }
			change_variable = { name = tv_diplomatic_progress_pct divide = 12500 }
			change_variable = { name = tv_diplomatic_progress_pct add = 80 }
		}
		if = { limit = { var:tv_diplomatic_progress_pct > 100 } set_variable = { name = tv_diplomatic_progress_pct value = 100 } }
	}

	# ── Cultural Victory ──────────────────────────────────────────────────────
	# piecewise: thresholds 50/200/400/800/1600 → each milestone = +20%
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_CULTURAL_INFLUENCE_POINTS_TT
				has_variable = tv_cultural_influence_points
			}
		}
		# Seg 1: 0–50 → 0–20
		set_variable = { name = tv_cultural_progress_pct value = var:tv_cultural_influence_points }
		change_variable = { name = tv_cultural_progress_pct multiply = 20 }
		change_variable = { name = tv_cultural_progress_pct divide = 50 }
		# Seg 2: 50–200 → 20–40
		if = {
			limit = { var:tv_cultural_influence_points >= 50 }
			set_variable = { name = tv_cultural_progress_pct value = var:tv_cultural_influence_points }
			change_variable = { name = tv_cultural_progress_pct subtract = 50 }
			change_variable = { name = tv_cultural_progress_pct multiply = 20 }
			change_variable = { name = tv_cultural_progress_pct divide = 150 }
			change_variable = { name = tv_cultural_progress_pct add = 20 }
		}
		# Seg 3: 200–400 → 40–60
		if = {
			limit = { var:tv_cultural_influence_points >= 200 }
			set_variable = { name = tv_cultural_progress_pct value = var:tv_cultural_influence_points }
			change_variable = { name = tv_cultural_progress_pct subtract = 200 }
			change_variable = { name = tv_cultural_progress_pct multiply = 20 }
			change_variable = { name = tv_cultural_progress_pct divide = 200 }
			change_variable = { name = tv_cultural_progress_pct add = 40 }
		}
		# Seg 4: 400–800 → 60–80
		if = {
			limit = { var:tv_cultural_influence_points >= 400 }
			set_variable = { name = tv_cultural_progress_pct value = var:tv_cultural_influence_points }
			change_variable = { name = tv_cultural_progress_pct subtract = 400 }
			change_variable = { name = tv_cultural_progress_pct multiply = 20 }
			change_variable = { name = tv_cultural_progress_pct divide = 400 }
			change_variable = { name = tv_cultural_progress_pct add = 60 }
		}
		# Seg 5: 800–1600 → 80–100
		if = {
			limit = { var:tv_cultural_influence_points >= 800 }
			set_variable = { name = tv_cultural_progress_pct value = var:tv_cultural_influence_points }
			change_variable = { name = tv_cultural_progress_pct subtract = 800 }
			change_variable = { name = tv_cultural_progress_pct multiply = 20 }
			change_variable = { name = tv_cultural_progress_pct divide = 800 }
			change_variable = { name = tv_cultural_progress_pct add = 80 }
		}
		if = { limit = { var:tv_cultural_progress_pct > 100 } set_variable = { name = tv_cultural_progress_pct value = 100 } }
	}

	# ── Scientific Victory ────────────────────────────────────────────────────
	# piecewise: thresholds 100/150/200/300/400 → each milestone = +20%
	if = {
		limit = {
			custom_tooltip = {
				text = TV_HAS_SCIENCE_SCORE_TT
				has_variable = tv_science_score
			}
		}
		# Seg 1: 0–100 → 0–20
		set_variable = { name = tv_science_progress_pct value = var:tv_science_score }
		change_variable = { name = tv_science_progress_pct multiply = 20 }
		change_variable = { name = tv_science_progress_pct divide = 100 }
		# Seg 2: 100–150 → 20–40
		if = {
			limit = { var:tv_science_score >= 100 }
			set_variable = { name = tv_science_progress_pct value = var:tv_science_score }
			change_variable = { name = tv_science_progress_pct subtract = 100 }
			change_variable = { name = tv_science_progress_pct multiply = 20 }
			change_variable = { name = tv_science_progress_pct divide = 50 }
			change_variable = { name = tv_science_progress_pct add = 20 }
		}
		# Seg 3: 150–200 → 40–60
		if = {
			limit = { var:tv_science_score >= 150 }
			set_variable = { name = tv_science_progress_pct value = var:tv_science_score }
			change_variable = { name = tv_science_progress_pct subtract = 150 }
			change_variable = { name = tv_science_progress_pct multiply = 20 }
			change_variable = { name = tv_science_progress_pct divide = 50 }
			change_variable = { name = tv_science_progress_pct add = 40 }
		}
		# Seg 4: 200–300 → 60–80
		if = {
			limit = { var:tv_science_score >= 200 }
			set_variable = { name = tv_science_progress_pct value = var:tv_science_score }
			change_variable = { name = tv_science_progress_pct subtract = 200 }
			change_variable = { name = tv_science_progress_pct multiply = 20 }
			change_variable = { name = tv_science_progress_pct divide = 100 }
			change_variable = { name = tv_science_progress_pct add = 60 }
		}
		# Seg 5: 300–400 → 80–100
		if = {
			limit = { var:tv_science_score >= 300 }
			set_variable = { name = tv_science_progress_pct value = var:tv_science_score }
			change_variable = { name = tv_science_progress_pct subtract = 300 }
			change_variable = { name = tv_science_progress_pct multiply = 20 }
			change_variable = { name = tv_science_progress_pct divide = 100 }
			change_variable = { name = tv_science_progress_pct add = 80 }
		}
		if = { limit = { var:tv_science_progress_pct > 100 } set_variable = { name = tv_science_progress_pct value = 100 } }
	}

}

# ═══════════════════════════════════════════════════════════════════════
# SECTION 4: MILESTONE REWARD EFFECTS
# Called from events tv.<path>.<n> after the milestone is first reached.
# ═══════════════════════════════════════════════════════════════════════

# Conquest Victory
tv_grant_conquest_milestone_1 = {
	add_country_modifier = { modifier = tv_conquest_m1_bonus days = -1 }
	custom_tooltip = { text = tv_conquest_m1_govhouse_tt }
	hidden_effect = { tv_govhouse_create_effect = yes }
}

tv_grant_conquest_milestone_2 = {
	add_country_modifier = { modifier = tv_conquest_m2_bonus days = -1 }
}

tv_grant_conquest_milestone_3 = {
	add_country_modifier = { modifier = tv_conquest_m3_bonus days = -1 }
}

tv_grant_conquest_milestone_4 = {
	add_country_modifier = { modifier = tv_conquest_m4_bonus days = -1 }
}

tv_grant_conquest_milestone_5 = {
	add_country_modifier = { modifier = tv_conquest_m5_bonus days = -1 }
}


# Prosperity Victory
tv_grant_prosperity_milestone_1 = {
	add_country_modifier = { modifier = tv_prosperity_m1_bonus days = -1 }
	custom_tooltip = { text = tv_prosperity_m1_engineering_department_grant_tt }
	hidden_effect = { tv_engineering_department_create_effect = yes }
}

tv_grant_prosperity_milestone_2 = {
	add_country_modifier = { modifier = tv_prosperity_m2_bonus days = -1 }
}

tv_grant_prosperity_milestone_3 = {
	add_country_modifier = { modifier = tv_prosperity_m3_bonus days = -1 }
}

tv_grant_prosperity_milestone_4 = {
	add_country_modifier = { modifier = tv_prosperity_m4_bonus days = -1 }
}

tv_grant_prosperity_milestone_5 = {
	add_country_modifier = { modifier = tv_prosperity_m5_bonus days = -1 }
}


# Trade Victory
tv_grant_trade_milestone_1 = {
	add_country_modifier = { modifier = tv_trade_m1_bonus days = -1 }
}

tv_grant_trade_milestone_2 = {
	add_country_modifier = { modifier = tv_trade_m2_bonus days = -1 }
}

tv_grant_trade_milestone_3 = {
	add_country_modifier = { modifier = tv_trade_m3_bonus days = -1 }
}

tv_grant_trade_milestone_4 = {
	add_country_modifier = { modifier = tv_trade_m4_bonus days = -1 }
}

tv_grant_trade_milestone_5 = {
	add_country_modifier = { modifier = tv_trade_m5_bonus days = -1 }
}


# Diplomatic Victory
tv_grant_diplomatic_milestone_1 = {
	add_country_modifier = { modifier = tv_diplomatic_m1_bonus days = -1 }
	if = {
	    limit = {
	        custom_tooltip = {
	            text = TV_HAS_VARIABLE_NOT_SET_TT
	            NOT = { has_variable = tv_diplomatic_victory_points }
	        }
	    }
	    set_variable = { name = tv_diplomatic_victory_points value = 1 }
	}
	else_if = {
	    limit = { var:tv_diplomatic_victory_points < 1 }
	    set_variable = { name = tv_diplomatic_victory_points value = 1 }
	}
}

tv_grant_diplomatic_milestone_2 = {
	add_country_modifier = { modifier = tv_diplomatic_m2_bonus days = -1 }
}

tv_grant_diplomatic_milestone_3 = {
	add_country_modifier = { modifier = tv_diplomatic_m3_bonus days = -1 }
}

tv_grant_diplomatic_milestone_4 = {
	add_country_modifier = { modifier = tv_diplomatic_m4_bonus days = -1 }
}

tv_grant_diplomatic_milestone_5 = {
	add_country_modifier = { modifier = tv_diplomatic_m5_bonus days = -1 }
}


# Cultural Victory
tv_grant_cultural_milestone_1 = {
	add_country_modifier = { modifier = tv_cultural_m1_bonus days = -1 }
	custom_tooltip = { text = tv_cultural_m1_io_grant_tt }
	hidden_effect = {
	    set_variable = { name = tv_arts_exhibition_member value = 1 }
	    create_international_organization = {
	        type = international_organization_type:tv_arts_exhibition
	        add_country_to_international_organization = prev
	        set_leader_country = prev
	    }
	}
}

tv_grant_cultural_milestone_2 = {
	add_country_modifier = { modifier = tv_cultural_m2_bonus days = -1 }
}

tv_grant_cultural_milestone_3 = {
	add_country_modifier = { modifier = tv_cultural_m3_bonus days = -1 }
}

tv_grant_cultural_milestone_4 = {
	add_country_modifier = { modifier = tv_cultural_m4_bonus days = -1 }
}

tv_grant_cultural_milestone_5 = {
	add_country_modifier = { modifier = tv_cultural_m5_bonus days = -1 }
}


# Science Victory
tv_grant_science_milestone_1 = {
	add_country_modifier = { modifier = tv_science_m1_bonus days = -1 }
	custom_tooltip = { text = tv_science_m1_io_grant_tt }
	hidden_effect = {
	    if = {
	        limit = {
	        	custom_tooltip = {
	        		text = TV_HAS_VARIABLE_NOT_SET_TT
	        		NOT = { has_variable = tv_academy_io_member }
	        	}
	        }
	        set_variable = { name = tv_academy_io_member value = 1 }
	        set_variable = { name = tv_research_phase value = 0 }
	        set_variable = { name = tv_research_interest value = 0 }
	        set_variable = { name = tv_research_b_done value = 0 }
	        set_variable = { name = tv_rm_subprocess_a value = 0 }
	        set_variable = { name = tv_rm_subprocess_b value = 0 }
	        set_variable = { name = tv_rm_subprocess_c value = 0 }
	        set_variable = { name = tv_rm_subprocess_d_ready value = 0 }
	        set_variable = { name = tv_med_progress  value = 0 }
	        set_variable = { name = tv_med_disp_adm  value = 0 }
	        set_variable = { name = tv_med_disp_dip  value = 0 }
	        set_variable = { name = tv_med_disp_mil  value = 0 }
	        create_international_organization = {
	            type = international_organization_type:tv_academy_of_sciences
	            hidden_effect = {
	                add_country_to_international_organization = prev
	                set_leader_country = prev
	            }
	        }
	    }
	}
}

tv_grant_science_milestone_2 = {
	add_country_modifier = { modifier = tv_science_m2_bonus days = -1 }
}

tv_grant_science_milestone_3 = {
	add_country_modifier = { modifier = tv_science_m3_bonus days = -1 }
}

tv_grant_science_milestone_4 = {
	add_country_modifier = { modifier = tv_science_m4_bonus days = -1 }
}

tv_grant_science_milestone_5 = {
	add_country_modifier = { modifier = tv_science_m5_bonus days = -1 }
}

