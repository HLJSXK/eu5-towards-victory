# @Generated by scripts/in_game/common/generic_actions/gen_tv_victory_path_task_actions.py
#   Data:    data/victory_path_tasks.yaml
#   Regen:   conda run --no-capture-output -n eu5 python scripts/in_game/common/generic_actions/gen_tv_victory_path_task_actions.py
# Do not edit directly - modify the data/generator and re-run it.
# Player-only task claim and right-click refresh actions.


tv_victory_claim_conquest_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_conquest_victory_enabled var:tv_conquest_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_conquest_task_slot_1_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.1 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_conquest_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_conquest_victory_enabled var:tv_conquest_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.2 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_conquest_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_conquest_victory_enabled var:tv_conquest_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_conquest_task_slot_2_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.3 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_conquest_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_conquest_victory_enabled var:tv_conquest_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.4 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_conquest_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_conquest_victory_enabled var:tv_conquest_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_conquest_task_slot_3_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.5 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_conquest_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_conquest_victory_enabled var:tv_conquest_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.6 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_prosperity_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_prosperity_victory_enabled var:tv_prosperity_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_prosperity_task_slot_1_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.7 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_prosperity_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_prosperity_victory_enabled var:tv_prosperity_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.8 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_prosperity_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_prosperity_victory_enabled var:tv_prosperity_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_prosperity_task_slot_2_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.9 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_prosperity_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_prosperity_victory_enabled var:tv_prosperity_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.10 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_prosperity_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_prosperity_victory_enabled var:tv_prosperity_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_prosperity_task_slot_3_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.11 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_prosperity_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_prosperity_victory_enabled var:tv_prosperity_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.12 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_trade_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_trade_victory_enabled var:tv_trade_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_trade_task_slot_1_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.13 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_trade_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_trade_victory_enabled var:tv_trade_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.14 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_trade_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_trade_victory_enabled var:tv_trade_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_trade_task_slot_2_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.15 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_trade_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_trade_victory_enabled var:tv_trade_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.16 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_trade_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_trade_victory_enabled var:tv_trade_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_trade_task_slot_3_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.17 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_trade_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_trade_victory_enabled var:tv_trade_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.18 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_diplomatic_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_diplomatic_victory_enabled var:tv_diplomatic_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_diplomatic_task_slot_1_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.19 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_diplomatic_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_diplomatic_victory_enabled var:tv_diplomatic_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.20 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_diplomatic_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_diplomatic_victory_enabled var:tv_diplomatic_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_diplomatic_task_slot_2_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.21 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_diplomatic_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_diplomatic_victory_enabled var:tv_diplomatic_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.22 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_diplomatic_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_diplomatic_victory_enabled var:tv_diplomatic_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_diplomatic_task_slot_3_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.23 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_diplomatic_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_diplomatic_victory_enabled var:tv_diplomatic_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.24 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_cultural_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_cultural_victory_enabled var:tv_cultural_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_cultural_task_slot_1_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.25 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_cultural_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_cultural_victory_enabled var:tv_cultural_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.26 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_cultural_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_cultural_victory_enabled var:tv_cultural_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_cultural_task_slot_2_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.27 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_cultural_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_cultural_victory_enabled var:tv_cultural_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.28 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_cultural_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_cultural_victory_enabled var:tv_cultural_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_cultural_task_slot_3_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.29 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_cultural_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_cultural_victory_enabled var:tv_cultural_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.30 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_science_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_science_victory_enabled var:tv_science_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_science_task_slot_1_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.31 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_science_task_slot_1 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_science_victory_enabled var:tv_science_task_slot_1_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.32 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_science_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_science_victory_enabled var:tv_science_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_science_task_slot_2_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.33 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_science_task_slot_2 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_science_victory_enabled var:tv_science_task_slot_2_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.34 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_claim_science_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_science_victory_enabled var:tv_science_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { has_variable = tv_science_task_slot_3_complete } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.35 }
			}
		}
	}
	ai_will_do = { add = -100 }
}

tv_victory_refresh_science_task_slot_3 = {
	type = owncountry
	show_message = no
	ai_tick = never
	automation_tick = never
	potential = { scope:actor = { is_human = yes has_variable = tv_science_victory_enabled var:tv_science_task_slot_3_id ?= { this > 0 } } }
	allow = { scope:actor = { prestige >= 10 } }
	effect = {
		scope:actor = {
			hidden_effect = {
				trigger_event_silently = { id = tv_victory_task_action.36 }
			}
		}
	}
	ai_will_do = { add = -100 }
}
