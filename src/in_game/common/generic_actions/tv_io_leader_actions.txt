# @Generated by scripts/in_game/common/generic_actions/gen_tv_io_leader_actions.py
#   Data:    data/io_leaders.yaml
#   Regen:   conda run --no-capture-output -n eu5 python scripts/in_game/common/generic_actions/gen_tv_io_leader_actions.py
# Do not edit directly — modify the data file and re-run the generator.

# ═════════════════════════════════════════════════════════════════════════════
# Arts Exhibition (non-unique IO)
# ═════════════════════════════════════════════════════════════════════════════

tv_appoint_arts_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ARTS_EXHIBITION_MEMBER_TT
			scope:actor = { has_variable = tv_arts_exhibition_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_arts_leader"
		none_available_msg_key = "tv_no_arts_leader_available"
		column = { data = character_artist }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = yes
			custom_tooltip = {
			text = TV_HAS_VARIABLE_NOT_SET_TT
			NOT = { has_variable = tv_on_exhibition }
			}
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_arts_exhibition_leader_char value = scope:target }
			}
			scope:target = {
				add_character_modifier = { modifier = tv_chief_artist_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}

tv_remove_arts_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ARTS_EXHIBITION_MEMBER_TT
			scope:actor = { has_variable = tv_arts_exhibition_member }
		}
	}

	allow = { always = yes }

	effect = {
		scope:actor = {
			if = {
				limit = {
					custom_tooltip = {
						text = TV_HAS_ARTS_EXHIBITION_LEADER_CHAR_TT
						has_variable = tv_arts_exhibition_leader_char
					}
				}
				var:tv_arts_exhibition_leader_char ?= {
					remove_character_modifier = tv_chief_artist_role_modifier
				}
			}
			remove_variable = tv_arts_exhibition_leader_char
		}
	}

	ai_will_do = { add = -100 }
}

tv_change_arts_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ARTS_EXHIBITION_MEMBER_TT
			scope:actor = { has_variable = tv_arts_exhibition_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_arts_leader"
		none_available_msg_key = "tv_no_arts_leader_available"
		column = { data = character_artist }
		visible = {
			is_alive = yes
			is_adult = yes
			is_artist = yes
			custom_tooltip = {
			text = TV_HAS_VARIABLE_NOT_SET_TT
			NOT = { has_variable = tv_on_exhibition }
			}
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		scope:actor = {
			if = {
				limit = {
					custom_tooltip = {
						text = TV_HAS_ARTS_EXHIBITION_LEADER_CHAR_TT
						has_variable = tv_arts_exhibition_leader_char
					}
				}
				var:tv_arts_exhibition_leader_char ?= {
					remove_character_modifier = tv_chief_artist_role_modifier
				}
			}
		}
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_arts_exhibition_leader_char value = scope:target }
			}
			scope:target = {
				add_character_modifier = { modifier = tv_chief_artist_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}

# ═════════════════════════════════════════════════════════════════════════════
# Diplomatic Alliance (non-unique IO)
# ═════════════════════════════════════════════════════════════════════════════

tv_appoint_alliance_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_DIPLOMATIC_ALLIANCE_MEMBER_TT
			scope:actor = { has_variable = tv_diplomatic_alliance_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_alliance_leader"
		none_available_msg_key = "tv_no_alliance_leader_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_diplomatic_alliance_leader_char value = scope:target }
			}
		}
	}

	ai_will_do = { add = -100 }
}

tv_remove_alliance_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_DIPLOMATIC_ALLIANCE_MEMBER_TT
			scope:actor = { has_variable = tv_diplomatic_alliance_member }
		}
	}

	allow = { always = yes }

	effect = {
		scope:actor = {
			remove_variable = tv_diplomatic_alliance_leader_char
		}
	}

	ai_will_do = { add = -100 }
}

tv_change_alliance_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_DIPLOMATIC_ALLIANCE_MEMBER_TT
			scope:actor = { has_variable = tv_diplomatic_alliance_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_alliance_leader"
		none_available_msg_key = "tv_no_alliance_leader_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_diplomatic_alliance_leader_char value = scope:target }
			}
		}
	}

	ai_will_do = { add = -100 }
}

# ═════════════════════════════════════════════════════════════════════════════
# Academy of Sciences (non-unique IO)
# ═════════════════════════════════════════════════════════════════════════════

tv_appoint_academy_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ACADEMY_IO_MEMBER_TT
			scope:actor = { has_variable = tv_academy_io_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_academy_leader"
		none_available_msg_key = "tv_no_academy_leader_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			NOT = { is_artist = yes }
			OR = {
			is_female = no
			scope:actor = {
			any_international_organizations_member_of = {
			international_organization_type = international_organization_type:tv_academy_of_sciences
			leader_country ?= scope:actor
			international_organization_has_policy = policy:tv_academy_leader_gender_unrestricted_policy
			}
			}
			}
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_academy_leader_char value = scope:target }
			}
			scope:target = {
				add_character_modifier = { modifier = tv_chief_scientist_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}

tv_remove_academy_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ACADEMY_IO_MEMBER_TT
			scope:actor = { has_variable = tv_academy_io_member }
		}
	}

	allow = { always = yes }

	effect = {
		scope:actor = {
			if = {
				limit = {
					custom_tooltip = {
						text = TV_HAS_ACADEMY_LEADER_CHAR_TT
						has_variable = tv_academy_leader_char
					}
				}
				var:tv_academy_leader_char ?= {
					remove_character_modifier = tv_chief_scientist_role_modifier
				}
			}
			remove_variable = tv_academy_leader_char
		}
	}

	ai_will_do = { add = -100 }
}

tv_change_academy_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_ACADEMY_IO_MEMBER_TT
			scope:actor = { has_variable = tv_academy_io_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_academy_leader"
		none_available_msg_key = "tv_no_academy_leader_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			NOT = { is_artist = yes }
			OR = {
			is_female = no
			scope:actor = {
			any_international_organizations_member_of = {
			international_organization_type = international_organization_type:tv_academy_of_sciences
			leader_country ?= scope:actor
			international_organization_has_policy = policy:tv_academy_leader_gender_unrestricted_policy
			}
			}
			}
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		scope:actor = {
			if = {
				limit = {
					custom_tooltip = {
						text = TV_HAS_ACADEMY_LEADER_CHAR_TT
						has_variable = tv_academy_leader_char
					}
				}
				var:tv_academy_leader_char ?= {
					remove_character_modifier = tv_chief_scientist_role_modifier
				}
			}
		}
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_academy_leader_char value = scope:target }
			}
			scope:target = {
				add_character_modifier = { modifier = tv_chief_scientist_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}

# ═════════════════════════════════════════════════════════════════════════════
# Governor's House - Grand General (non-unique IO)
# ═════════════════════════════════════════════════════════════════════════════

tv_appoint_conquest_general_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_GOVHOUSE_MEMBER_TT
			scope:actor = { has_variable = tv_govhouse_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_grand_general"
		none_available_msg_key = "tv_no_grand_general_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_female = no
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_conquest_general_char value = scope:target }
				tv_govhouse_reset_power_consolidation_effect = yes
			}
			scope:target = {
				add_character_modifier = { modifier = tv_grand_general_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}

tv_remove_conquest_general_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_GOVHOUSE_MEMBER_TT
			scope:actor = { has_variable = tv_govhouse_member }
		}
	}

	allow = { always = yes }

	effect = {
		scope:actor = {
			if = {
				limit = {
					custom_tooltip = {
						text = TV_HAS_CONQUEST_GENERAL_CHAR_TT
						has_variable = tv_conquest_general_char
					}
				}
				var:tv_conquest_general_char ?= {
					remove_character_modifier = tv_grand_general_role_modifier
				}
			}
			tv_training_clear_effect = yes
			tv_govhouse_clear_war_tent_effect = yes
			tv_govhouse_pay_grand_general_replacement_cost_effect = yes
			tv_govhouse_reset_power_consolidation_effect = yes
			remove_variable = tv_conquest_general_char
		}
	}

	ai_will_do = { add = -100 }
}

tv_change_conquest_general_leader = {
	type = owncountry
	sound = UI_action_religion_generic
	ai_tick = monthly
	ai_tick_frequency = 99999

	potential = {
		custom_tooltip = {
			text = TV_HAS_GOVHOUSE_MEMBER_TT
			scope:actor = { has_variable = tv_govhouse_member }
		}
	}

	allow = { always = yes }

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = target
		name = "tv_select_grand_general"
		none_available_msg_key = "tv_no_grand_general_available"
		column = { data = character_info }
		visible = {
			is_alive = yes
			is_adult = yes
			is_female = no
			is_artist = no
			is_ruler = no
			is_heir = no
			is_consort = no
		}
	}

	effect = {
		scope:actor = {
			if = {
				limit = {
					custom_tooltip = {
						text = TV_HAS_CONQUEST_GENERAL_CHAR_TT
						has_variable = tv_conquest_general_char
					}
				}
				var:tv_conquest_general_char ?= {
					remove_character_modifier = tv_grand_general_role_modifier
				}
			}
			tv_training_clear_effect = yes
			tv_govhouse_clear_war_tent_effect = yes
			tv_govhouse_pay_grand_general_replacement_cost_effect = yes
			tv_govhouse_reset_power_consolidation_effect = yes
		}
		if = {
			limit = { exists = scope:target }
			scope:actor = {
				set_variable = { name = tv_conquest_general_char value = scope:target }
			}
			scope:target = {
				add_character_modifier = { modifier = tv_grand_general_role_modifier years = -1 mode = add_and_extend }
			}
		}
	}

	ai_will_do = { add = -100 }
}
