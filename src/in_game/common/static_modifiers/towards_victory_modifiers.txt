# Towards Victory — Static modifier definitions (permanent milestone rewards)
# Generated by scripts/gen_victory_scaffold.py
# All 30 modifiers are stubs. Fill in actual modifier effects path by path.
#
# Before adding modifier effect keys, verify names against:
#   reference_game_files/game/main_menu/common/modifier_type_definitions/
# Country-scoped modifiers only. Location-scoped modifiers require TRY_REPLACE
# in src/main_menu/common/static_modifiers/ per EU5 engine requirements.

# ──────────────────────────────────────────────────────────────────────
# CONQUEST VICTORY MODIFIERS
# ──────────────────────────────────────────────────────────────────────

tv_conquest_m1_bonus = {
	# TODO: Conquest Victory — Milestone 1 reward (~1 Advance equivalent)
	# Design intent: combat capability (manpower_recovery_speed, land_morale)
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_conquest_m2_bonus = {
	# TODO: Conquest Victory — Milestone 2 reward (~1 Advance equivalent)
	# Design intent: logistics (supply, attrition reduction)
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_conquest_m3_bonus = {
	# TODO: Conquest Victory — Short-term Victory reward (~2 Advance equivalent)
	# Design intent: governance (admin efficiency, unrest reduction)
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_conquest_m4_bonus = {
	# TODO: Conquest Victory — Milestone 4 reward (~1 Advance equivalent)
	# Design intent: administration at scale (autonomy reduction, corruption)
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_conquest_m5_bonus = {
	# TODO: Conquest Victory — Long-term Victory reward (~2 Advance equivalent)
	# Design intent: grand legacy (prestige cap, manpower cap)
	# Verify modifier key names in modifier_type_definitions/ before adding values
}


# ──────────────────────────────────────────────────────────────────────
# PROSPERITY VICTORY MODIFIERS
# ──────────────────────────────────────────────────────────────────────

tv_prosperity_m1_bonus = {
	# TODO: Prosperity Victory — Milestone 1 reward (~1 Advance equivalent)
	# Design intent: domestic production bonus, tax income
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_prosperity_m2_bonus = {
	# TODO: Prosperity Victory — Milestone 2 reward (~1 Advance equivalent)
	# Design intent: pop growth, building cost reduction
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_prosperity_m3_bonus = {
	# TODO: Prosperity Victory — Short-term Victory reward (~2 Advance equivalent)
	# Design intent: development cost reduction
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_prosperity_m4_bonus = {
	# TODO: Prosperity Victory — Milestone 4 reward (~1 Advance equivalent)
	# Design intent: construction speed, educated pop bonus
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_prosperity_m5_bonus = {
	# TODO: Prosperity Victory — Long-term Victory reward (~2 Advance equivalent)
	# Design intent: permanent construction speed, population cap bonus
	# Verify modifier key names in modifier_type_definitions/ before adding values
}


# ──────────────────────────────────────────────────────────────────────
# TRADE VICTORY MODIFIERS
# ──────────────────────────────────────────────────────────────────────

tv_trade_m1_bonus = {
	# TODO: Trade Victory — Milestone 1 reward (~1 Advance equivalent)
	# Design intent: merchant power, trade node influence
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_trade_m2_bonus = {
	# TODO: Trade Victory — Milestone 2 reward (~1 Advance equivalent)
	# Design intent: light ships, merchant count
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_trade_m3_bonus = {
	# TODO: Trade Victory — Short-term Victory reward (~2 Advance equivalent)
	# Design intent: trade efficiency
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_trade_m4_bonus = {
	# TODO: Trade Victory — Milestone 4 reward (~1 Advance equivalent)
	# Design intent: trade steering bonus
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_trade_m5_bonus = {
	# TODO: Trade Victory — Long-term Victory reward (~2 Advance equivalent)
	# Design intent: permanent trade income multiplier, merchant count
	# Verify modifier key names in modifier_type_definitions/ before adding values
}


# ──────────────────────────────────────────────────────────────────────
# DIPLOMATIC VICTORY MODIFIERS
# ──────────────────────────────────────────────────────────────────────

tv_diplomatic_m1_bonus = {
	# TODO: Diplomatic Victory — Milestone 1 reward (~1 Advance equivalent)
	# Design intent: diplomatic reputation, opinion bonus
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_diplomatic_m2_bonus = {
	# TODO: Diplomatic Victory — Milestone 2 reward (~1 Advance equivalent)
	# Design intent: alliance reliability, defensive bonuses
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_diplomatic_m3_bonus = {
	# TODO: Diplomatic Victory — Short-term Victory reward (~2 Advance equivalent)
	# Design intent: diplomatic range, claim fabrication speed
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_diplomatic_m4_bonus = {
	# TODO: Diplomatic Victory — Milestone 4 reward (~1 Advance equivalent)
	# Design intent: diplomatic immunity, vassal bonuses
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_diplomatic_m5_bonus = {
	# TODO: Diplomatic Victory — Long-term Victory reward (~2 Advance equivalent)
	# Design intent: permanent casus belli defense, prestige
	# Verify modifier key names in modifier_type_definitions/ before adding values
}


# ──────────────────────────────────────────────────────────────────────
# CULTURAL VICTORY MODIFIERS
# ──────────────────────────────────────────────────────────────────────

tv_cultural_m1_bonus = {
	# TODO: Cultural Victory — Milestone 1 reward (~1 Advance equivalent)
	# Design intent: court attraction, ideas cost reduction
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_cultural_m2_bonus = {
	# TODO: Cultural Victory — Milestone 2 reward (~1 Advance equivalent)
	# Design intent: diplomat effectiveness, cultural prestige
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_cultural_m3_bonus = {
	# TODO: Cultural Victory — Short-term Victory reward (~2 Advance equivalent)
	# Design intent: artifact find chance, court income
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_cultural_m4_bonus = {
	# TODO: Cultural Victory — Milestone 4 reward (~1 Advance equivalent)
	# Design intent: cultural spread multiplier, missionary strength
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_cultural_m5_bonus = {
	# TODO: Cultural Victory — Long-term Victory reward (~2 Advance equivalent)
	# Design intent: permanent cultural spread multiplier, prestige cap
	# Verify modifier key names in modifier_type_definitions/ before adding values
}


# ──────────────────────────────────────────────────────────────────────
# SCIENTIFIC VICTORY MODIFIERS
# ──────────────────────────────────────────────────────────────────────

tv_science_m1_bonus = {
	# TODO: Scientific Victory — Milestone 1 reward (~1 Advance equivalent)
	# Design intent: research speed bonus
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_science_m2_bonus = {
	# TODO: Scientific Victory — Milestone 2 reward (~1 Advance equivalent)
	# Design intent: technology spread, military effectiveness
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_science_m3_bonus = {
	# TODO: Scientific Victory — Short-term Victory reward (~2 Advance equivalent)
	# Design intent: institution spread speed, educated pop growth
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_science_m4_bonus = {
	# TODO: Scientific Victory — Milestone 4 reward (~1 Advance equivalent)
	# Design intent: scientific institution bonus, production bonus
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

tv_science_m5_bonus = {
	# TODO: Scientific Victory — Long-term Victory reward (~2 Advance equivalent)
	# Design intent: permanent production bonus, army modernization
	# Verify modifier key names in modifier_type_definitions/ before adding values
}

