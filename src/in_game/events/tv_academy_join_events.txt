# Towards Victory — Academy of Sciences join events
# Fired by tv_academy_of_sciences_1 on_built to enrol the country in its own IO instance.
# Guard: trigger block prevents duplicate joins via tv_academy_io_member flag.
#
# Verification — Step 3, Reference: tv_arts_exhibition_events.txt:1-27 (event structure)
# Verification — Step 3, Reference: towards_victory_effects.txt:830-835
#   Quote: "set_variable = { name = tv_arts_exhibition_member value = 1 }
#           create_international_organization = { type = ... add_country_to_international_organization = prev  set_leader_country = prev }"

namespace = tv_academy

# ── Academy of Sciences Founded ───────────────────────────────────────────────
# Fires when Phase I building completes; country joins its own IO instance.
tv_academy.1 = {
	type = country_event
	title = tv_academy.1.t
	desc = tv_academy.1.d
	outcome = good

	trigger = {
		NOT = { has_variable = tv_academy_io_member }
	}

	option = {
		name = tv_academy.1.a
		set_variable = { name = tv_academy_io_member value = 1 }
		create_international_organization = {
			type = international_organization_type:tv_academy_of_sciences
			add_country_to_international_organization = prev
			set_leader_country = prev
		}
	}
}
