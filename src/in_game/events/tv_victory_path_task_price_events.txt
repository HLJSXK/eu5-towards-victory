# @Generated by scripts/in_game/events/gen_tv_victory_path_task_price_events.py
#   Data:    data/victory_path_tasks.yaml + reference_game_files/.../prices.txt
#   Regen:   conda run --no-capture-output -n eu5 python scripts/in_game/events/gen_tv_victory_path_task_price_events.py
# Do not edit directly - modify the data/generator and re-run it.
# Minimal same-ID vanilla price event overrides with task callbacks.

namespace = prices

REPLACE:prices.1 = {
	type = country_event
	fire_only_once = yes
	title = prices.1.title
	desc = prices.1.desc
	outcome = neutral
	trigger = {
		capital.market ?= {
			location.continent = continent:europe
			has_merchant = root
			most_powerful_merchant = root
		}
		any_market_with_merchants = {
			location.region = region:guinea_region
			has_merchant = root
			most_powerful_merchant = root
		}
		any_market_with_merchants = {
			location.continent = continent:america
			has_merchant = root
			most_powerful_merchant = root
		}
	}

	immediate = {
		tv_victory_task_prices_1_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_slaves_goods.dds"

	option = {
		name = prices.1.a
		add_prestige = prestige_extreme_bonus

		every_market_with_merchants = {
			limit = {
				location.continent = continent:europe
			}
			add_temporary_demand = { type = demand:triangle_trade_europe months = 240 }
		}

		every_market_with_merchants = {
			limit = {
				location.continent = continent:america
			}
			add_temporary_demand = { type = demand:triangle_trade_america months = 240 }
		}

		every_market_with_merchants = {
			limit = {
				location.region = region:guinea_region
			}
			add_temporary_demand = { type = demand:triangle_trade_africa months = 240 }
		}

	}
}

REPLACE:prices.2 = {
	type = country_event
	title = prices.2.title
	desc = prices.2.desc
	outcome = neutral

	immediate = {
		tv_victory_task_prices_2_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_porcelain.dds"

	fire_only_once = yes

	trigger = {
		capital = {
			continent = continent:europe
		}
		any_market_with_merchants = {
			location.region = region:south_china_region
			most_powerful_merchant = root
		}
	}

	option = {
		name = prices.2.a

		every_market_with_merchants = {
			limit = {
				location.continent = continent:europe
			}
			add_temporary_demand = { type = demand:porcelain_from_china months = 600 }
		}

		change_gold_effect = { scale = 3.00 }
		add_prestige = prestige_extreme_bonus
	}
}

REPLACE:prices.5 = {
	type = country_event

	title = prices.5.title
	desc = prices.5.desc
	outcome = neutral
	trigger = {
		current_year > 1500
		religion.group = religion_group:christian
		capital = {
			continent = continent:europe
		}
		capital.market = {
			NOT = { has_temporary_demand = demand:coffee_boom_happened }
			is_traded_in_market = goods:coffee
		}

	}

	immediate = {
		tv_victory_task_prices_5_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_coffee.dds"

	option = {
		name = prices.5.a
		capital.market = {
			add_temporary_demand = { type = demand:coffee_boom_happened months = 120 scale_with_pop = 0.01 }
		}
	}
}

REPLACE:prices.8 = {
	type = country_event
	fire_only_once = yes
	title = prices.8.title
	desc = prices.8.desc
	outcome = neutral

	immediate = {
		tv_victory_task_prices_8_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_pepper.dds"

	trigger = {

		num_of_ports >= 10
		capital = {
			continent = continent:europe
			market = {
				any_import = {
					goods = goods:pepper
					from_market.location.sub_continent = sub_continent:south_east_asia
				}
			}
		}
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_pepper.dds"

	option = {
		name = prices.8.a
		every_market_with_merchants = {
			limit = {
				location.continent = continent:europe
			}

			add_temporary_demand = { type = demand:spice_trade months = 240 scale_with_pop = 0.01 }
		}
		add_prestige = prestige_extreme_bonus
	}
}

REPLACE:prices.18 = {
	type = country_event
	title = prices.18.title
	desc = prices.18.desc
	outcome = neutral
	trigger = {
		has_markets = yes
		capital = {
			continent = continent:europe
			market = {
				NOT = { has_temporary_demand = demand:felt_hats }
			}
		}
		current_year > 1600
	}

	immediate = {
		tv_victory_task_prices_18_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_fur.dds"

	option = {
		name = prices.18.a
		capital.market= {
			add_temporary_demand = { type = demand:felt_hats months = 240 scale_with_pop = 0.01 }
		}
	}
}

REPLACE:prices.19 = {
	type = country_event
	title = prices.19.title
	desc = prices.19.desc
	outcome = neutral

	trigger = {
		has_markets = yes
		capital = {
			market = {
				NOT = { has_temporary_demand = demand:professional_navies }
			}
		}
		current_year > 1600

		num_of_ports > 10
	}

	immediate = {
		tv_victory_task_prices_19_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_naval_supplies.dds"

	option = {
		name = prices.19.a
		capital.market= {
			add_temporary_demand = { type = demand:professional_navies months = 240 scale_with_pop = 0.01 }
		}
	}
}

REPLACE:prices.20 = {
	type = country_event
	title = prices.20.title
	desc = prices.20.desc
	outcome = neutral

	trigger = {
		has_markets = yes
		has_advance = artillery_institution_advance
		current_age = age_4_reformation

		capital.market = {
			produced_in_market:cannons > 1.5
			NOT = { has_temporary_demand = demand:bronze_cannons }
		}
	}

	immediate = {
		tv_victory_task_prices_20_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_copper.dds"

	option = {
		name = prices.20.a
		capital.market= {
			add_temporary_demand = { type = demand:bronze_cannons months = 240 scale_with_pop = 0.01 }
		}

	}
}

REPLACE:prices.29 = {
	type = country_event
	title = prices.29.title
	desc = prices.29.desc
	outcome = neutral

	trigger = {
		has_markets = yes
		capital_in_old_world_trigger = yes

		capital.market = {
			is_traded_in_market = goods:cocoa
			NOT = { has_temporary_demand = demand:hot_chocolate }
		}
	}

	immediate = {
		tv_victory_task_prices_29_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_cocoa.dds"

	option = {
		name = prices.29.a
		capital.market= {
			add_temporary_demand = { type = demand:hot_chocolate months = 240 scale_with_pop = 0.01 }
		}
	}
}

REPLACE:prices.30 = {
	type = country_event
	title = prices.30.title
	desc = prices.30.desc
	outcome = neutral

	trigger = {
		has_markets = yes
		capital_in_old_world_trigger = yes
		OR = {
			religion.group = religion_group:christian
			religion.group = religion_group:muslim
		}

		capital.market = {
			is_traded_in_market = goods:tobacco
			NOT = { has_temporary_demand = demand:popularization_of_tobacco }
		}
	}

	immediate = {
		tv_victory_task_prices_30_callback_effect = yes
	}

	image = "gfx/interface/icons/trade_goods/illustrations/icon_goods_tobacco.dds"

	option = {
		name = prices.30.a
		capital.market= {
			add_temporary_demand = { type = demand:popularization_of_tobacco months = 240 scale_with_pop = 0.01 }
		}
	}
}
