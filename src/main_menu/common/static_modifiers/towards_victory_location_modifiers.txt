# Location-scoped static modifiers for Towards Victory
# These modifiers require game_data = { category = location } to scope correctly.
# Apply via add_location_modifier in location scope; NOT add_modifier.

tv_exhibition_success_location_modifier = {
	local_production_efficiency = -0.2
	local_migration_attraction = 2
	local_unrest = -0.2
	game_data = {
		category = location
	}
}
